# 06. 설계 결정 기록 (Decision Log)

> **SKN 3차 단위 프로젝트** · 전 워크스트림 공통 참조 · 담당 **팀장**
> 상위 문서: `00_기획-요구사항분석.md` · 판단 기준: `05_설계원칙-코드와LLM의분업.md`
>
> 최종 갱신: **2026-08-04** · 기록 **86건** (D-01~D-22 · D-29~D-92 · 결번 D-23~D-28)
>
> 📌 날짜는 **한국 시각(KST)** 기준이다. 2026-08-01 정정 —
> 이전 판은 UTC로 찍혀 전 항목이 하루씩 앞당겨져 있었다 (7-27은 대화가 없던 날이다).
>
> **작성자: 오한빈 (팀장)**
>
> ### 이 기록의 성격
>
> **팀장이 직접 결정하고 작업한 내용을 그 자리에서 기록한 문서다.** 회의록도, 사후
> 정리본도 아니다 — 결정을 내린 시점에 맥락·대안·트레이드오프를 함께 적었고,
> 그래서 *"그때 무엇을 몰랐는가"* 까지 남아 있다.
>
> **팀원의 기여가 있는 항목은 그 자리에 별도로 표시했다.** 예 —
> D-73(라벨을 모델도 아는가 · 이서은) · D-91(④ 판정의 바닥 · 오한빈·이서은 합의) ·
> D-92(다이어리 하루 1건 · lgj 머지로 유입).
> 표시가 없는 항목은 팀장이 단독으로 판단한 것이다.
>
> 되돌린 결정(D-87)과 코드가 기록을 앞지른 결정(D-90)도 지우지 않고 남긴다 —
> **폐기한 시도도 기록이고, 그것을 근거로 삼는 다른 항목이 있다.**

---

## 0. 이 문서의 목적

> **설계의 매 순간마다 "왜 이렇게 설계했는가"에 명확한 이유를 댈 수 있어야 한다.**

"왜"에는 두 종류가 있다.

| 종류 | 질문 | 답하는 곳 |
|---|---|---|
| **존재의 이유** | 이 조각이 왜 필요한가 — 앞 조각의 어떤 한계를 푸는가 | **05** 정지 판정 |
| **선택의 이유** | 여러 대안 중 왜 이것을 골랐는가 | **본 문서** |

### 0.1 기록 형식

```
- 상태 / 맥락 / 대안 / 결정 / 트레이드오프 / 영향
```

**트레이드오프 문장이 핵심이다.**

> `[문제]를 풀기 위해 [선택]을 택했고, 그 덕분에 [얻은 것] 대신, [감수한 것]을 떠안았다.`

감수한 것을 적지 않은 "왜"는 자기 합리화이고, 적으면 설계 판단이 된다.
심사·리뷰에서 *"단점은 없나요?"* 라는 질문에 이미 답이 준비되어 있게 된다.

### 0.2 기록 규칙

- ✅ 기록한다 — 되돌리기 어려운 결정, **대안이 실제로 있었던** 결정, 나중에 질문받을 결정
- ❌ 기록하지 않는다 — 자명한 것, 언제든 바꿀 수 있는 것(변수명, 사소한 라이브러리 선택)
- 결정이 뒤집히면 **삭제하지 않고 `폐기` 또는 `대체됨` 상태로 남기고 새 번호를 부여**한다.
  **뒤집힌 이력 자체가 근거다.**
- 상태: `확정` / `제안` / `조건부` / `폐기` / `대체됨(→ D-xx)`
- **결번 D-23~D-28** — 문서 5분할(D-22) 직후 번호를 앞당겨 붙이면서 비었다.
  D-29의 비고가 참조하는 `D-28(사실 추출 + 출처 표기)` 은 **D-37로 대체**되었다.

### 0.3 색인

| # | 결정 | 영역 | 상태 |
|---|---|---|---|
| D-01 | 도메인 — 반려동물 헬스케어 & 응급 대응 | 도메인 | 확정 |
| D-02 | 브이로그 다이어리로 확장 | 도메인 | 확정 |
| D-03 | 대상 종 — 개·고양이·앵무새 3종 | 도메인 | 확정 |
| D-04 | RAG와 파인튜닝의 역할 분리 | 구조 | 확정 |
| D-05 | **멀티태스크 구성 5종** (번역 ⑥ 미채택) | 구조 | 확정 |
| D-42 | **베이스 sLLM — Qwen3-4B QLoRA** | 구조 | 확정 |
| D-06 | **ReAct·MCP·멀티에이전트 미채택** | 구조 | 확정 |
| D-07 | 오케스트레이션 — LangGraph | 구조 | 확정 |
| D-08 | LangChain 선택적 채택 | 구조 | 확정 |
| D-09 | **트리아지 하향 금지 게이트** | 안전 | 확정 (7-31 종별 분기 개정) |
| D-10 | 종 미확인 시 답변 금지 | 안전 | 확정 |
| D-11 | 진단 금지 — 트리아지 포지셔닝 | 안전 | 확정 |
| D-39 | **트리아지 등급 — 데이터 도출 4단계 + 급여 축** | 안전 | 확정 |
| D-12 | 평가 목표 재정의 — 문서 충실도 | 평가 | 확정 |
| D-13 | 과소평가율 최우선 지표 | 평가 | 확정 |
| D-14 | 물질(항목) 단위 청킹 | 데이터 | 확정 |
| D-15 | **표형 자료 문장화(verbalization)** | 데이터 | 확정 |
| D-16 | 정형 수치는 계산 노드, 벡터DB 미적재 | 데이터 | 확정 |
| D-17 | 체중 `weight_g` 단일 단위 | 데이터 | 확정 |
| D-18 | 내부 문서 — 가상 프로필·기록 생성 | 데이터 | 확정 |
| D-19 | cross-lingual — 다국어 임베딩 (전제 변경으로 확정) | 데이터 | 확정 |
| D-20 | 증분 인덱싱 + 코퍼스 버전 태깅 | 데이터 | 확정 |
| D-29 | **작업용 보관 / 제출용 배포 분리** | 데이터 | 확정 |
| D-30 | **스냅샷 품질 등급 · `요약` 인용 금지** | 데이터 | 확정 |
| D-31 | 접근 차단 사유별 4분류 처리 | 데이터 | 확정 |
| D-32 | **비용·허가 발생 자료 배제 (재현성 기준)** | 데이터 | 확정 |
| D-33 | **AI 학습 금지 조항 자료 절대 배제** | 데이터 | 확정 |
| D-34 | 데이터베이스권 — 대량 DB 전량 복제 금지 | 데이터 | 확정 |
| D-35 | **수집 전 게이트 — 차단 4 + 조건 2** | 데이터 | 확정 (7-31 2차 개정) |
| D-37 | **G2는 복제를, G1a는 용도를 규율한다** | 데이터 | 확정 |
| D-38 | **문장화는 코드가 한다 — 문장화도 검증 대상** | 데이터 | 확정 |
| D-36 | **개인정보는 LLM에 닿기 전에 코드가 벗긴다** | 개인정보 | 확정 |
| D-43 | **사진 업로드를 만든다 — 대신 업로드 경로에 관문을 둔다** | 개인정보 | 확정 |
| D-21 | UI는 가드레일 뒤 / 대형 LLM 베이스라인 우선 | 운영 | 확정 |
| D-22 | 문서 5분할 + 단일 출처 원칙 | 운영 | 확정 |
| D-40 | **배달 계층 — 화면이 아니라 계약을 먼저 고정한다** | 운영 | 확정 |
| D-41 | **저장소 구조 — 파라미터를 코드에서 빼고 의존성을 쪼갠다** | 운영 | 확정 |
| D-44 | **벡터DB — Chroma 확정 (pgvector 우선 검토를 뒤집는다)** | 운영 | 확정 |
| D-45 | **경로①을 끄고, 자격 목록을 원장에서 유도한다** | 데이터 | 확정 |
| D-46 | **유사도 임계값은 거절을 만들지 못한다 — 거절은 노드가 책임진다** | 안전 | 확정 |
| D-47 | **외국 핫라인 번호는 생성 단계에서 차단한다** | 안전 | 확정 |
| D-48 | **팀원 코드는 계약·주입·설정 구조 안으로 흡수한 뒤 병합한다** | 운영 | 확정 |
| D-49 | **증상 질의는 검색으로 원인을 찾지 않는다 — 되묻기로 보낸다** | 안전 | 확정 |
| D-50 | **`rule_level` 은 판정이 아니라 바닥이다 — 역치 매핑과 출처 상충 처리** | 안전 | 확정 |
| D-51 | **식물 부위 단위(`g leaves/kg`)는 체중당 용량으로 환산하지 않는다** | 안전 | 확정 |
| D-52 | **반려동물 프로필은 자동 주입이 아니라 확인을 받는다 — 판정 조회는 ORM** | 데이터 | 확정 |
| D-53 | **벡터DB는 원본이 아니라 인덱스다 — 캐시 금지 목록** | 구조 | 확정 |
| D-54 | **안전 강제는 노드가 아니라 주입·계약에 둔다** | 안전 | 확정 |
| D-55 | **역치의 기준이 "음식"인지 "독소"인지 구분한다** | 안전 | 확정 |
| D-56 | **빈 필터는 "필터 없음"이 아니라 "0건"이다** | 안전 | 확정 |
| D-57 | **틀린 동작을 고정한 테스트는 함께 뒤집고 이유를 남긴다** | 운영 | 확정 |
| D-58 | **검사기는 그 계층이 통제하는 것만 판정한다** | 운영 | 확정 |
| D-59 | **물질 동정은 생성이 아니라 폐쇄 목록에서의 선택이다** | 안전 | 확정 |

---

## 1. 도메인

### D-01 · 도메인 — 반려동물 헬스케어 & 응급 대응

- **상태**: 확정 (2026-07-30)
- **맥락**: 과제 요건이 "내외부 문서 기반 QA"이고, 팀 기준으로 *"전문 지식이 없어 정보를 찾기 힘든 일상 영역"* 을 지향
- **대안**: (a) 근로계약서 검토 (b) 임대차 계약 검토 (c) 전자제품 A/S (d) 헬스장·학원 환불 (e) **반려동물 헬스케어**
- **결정**: (e). 환각 당위성·페인 포인트·분류 체계·신선함·시연 임팩트에서 우위이며, **개인정보 리스크가 낮아 개인 기록 축적 확장이 가능한 유일한 후보**
- **트레이드오프**:
  > 정보 비대칭이 큰 일상 영역을 다루려는 문제를 풀기 위해 반려동물 헬스케어를 택했고,
  > 그 덕분에 시연 임팩트와 확장성을 얻은 대신,
  > **정답 검증이 어렵고(수의학 지식 부재) 범위가 쉽게 폭증하는** 부담을 떠안았다.
- **영향**: 00 §3 · `도메인검토-반려동물-헬스케어.md`
- **연관**: D-12(정답 검증 우회) · D-21(범위 관리)

### D-02 · 브이로그 다이어리로 확장

- **상태**: 확정 (2026-07-30)
- **맥락**: 원안(중독 응급 QA)은 **"무엇이 내부 문서인가"** 에 답하지 못했고, 요약 태스크의 필연성도 약했다
- **대안**: (a) 동물병원 원내 프로토콜을 가상 생성해 내부 문서로 (b) 반려동물 프로필만 내부 데이터로 (c) **일상 기록 다이어리를 내부 문서로**
- **결정**: (c). 개인 기록이 곧 내부 문서가 되어 *"내 문서 × 공적 기준 → 판정"* 구조가 성립하고, **기간 요약 리포트**라는 킬러 유스케이스가 생긴다
- **트레이드오프**:
  > 내부 문서 부재와 요약 필연성 부족이라는 문제를 풀기 위해 다이어리 확장을 택했고,
  > 그 덕분에 두 약점을 동시에 해소하고 RAG 필연성까지 강화한 대신,
  > **다이어리 CRUD라는 RAG와 무관한 구현 부담**과 범위 폭증 리스크를 떠안았다.
- **영향**: 00 §3.1 · 01 §13 · 02 §5
- **연관**: D-21(다이어리 최소 구현)

### D-03 · 대상 종 — 개·고양이·앵무새 3종

- **상태**: 확정 (2026-07-30)
- **맥락**: 종 범위를 정해야 데이터 수집·분류 체계·골든 셋이 확정된다
- **대안**: (a) 개만 (b) 개·고양이 (c) **개·고양이·앵무새**
- **결정**: (c). 앵무새는 **진료 병원이 희소하고 한국어 자료가 빈약해 정보 비대칭이 가장 크며**, 피식동물 특성상 증상을 은폐하므로 **일일 기록 기반 조기 탐지의 가치가 극대화**된다 → D-02의 당위성을 완성
- **트레이드오프**:
  > 다이어리 확장의 당위성을 강화하려는 문제를 풀기 위해 조류를 포함했고,
  > 그 덕분에 정보 비대칭이 가장 큰 대상을 커버하고 종 분류 태스크의 난이도를 높인 대신,
  > **종별 데이터 불균형과 포유류↔조류 오적용이라는 새 리스크**를 떠안았다.
- **영향**: 00 §3.1 · 01 §7 · 03 §4.1 · 04 §2.3
- **연관**: D-10(종 미확인 시 답변 금지) · D-17(단위 통일)

---

## 2. 구조

### D-04 · RAG와 파인튜닝의 역할 분리

- **상태**: 확정 (2026-07-30)
- **맥락**: 과제가 RAG와 멀티태스크 파인튜닝을 **동시에** 요구한다. 둘을 어떻게 한 시스템에 담을 것인가
- **대안**: (a) 파인튜닝으로 도메인 지식 주입 + RAG 보조 (b) **RAG가 지식, sLLM은 보조 태스크** (c) 둘을 별개 산출물로 병렬 제출
- **결정**: (b). *"RAG가 근거를 가져오고, 파인튜닝한 sLLM이 그 근거가 지켜졌는지 검증한다"*
- **트레이드오프**:
  > 두 요건이 따로 노는 문제를 풀기 위해 역할 분리 구도를 택했고,
  > 그 덕분에 두 요건이 하나의 목표(환각 방지)로 묶이고 **파인튜닝 실패 시에도 폴백으로 시스템이 동작**하는 대신,
  > sLLM이 최종 답변을 생성하지 않아 **"파인튜닝 효과"를 보여주려면 별도 비교 실험이 필요한** 부담을 떠안았다.
- **영향**: 00 §2 · 03 §1 · 04 §5

### D-05 · 멀티태스크 구성 5종 (+번역 조건부)

- **상태**: 확정 (2026-07-30) / **⑥ 번역은 채택하지 않는다** — 2026-08-01, D-19 후속으로 닫힘.
  **멀티태스크 5종 확정**
- **맥락**: 공지는 "분류·요약·번역"을 예시로 들었을 뿐 태스크 구성은 선택 가능
- **대안**: (a) 공지 예시 그대로 분류·요약·번역 (b) **파이프라인이 실제로 필요로 하는 태스크로 재구성**
- **결정**: (b) — ① 분류 ② 슬롯 추출 ③ 압축 요약 ④ **근거 검증** ⑤ 평이화. 번역은 조건부 ⑥
- **근거**: ④가 과제 목표 1번(환각 방지)을 파인튜닝 모델이 직접 담당하게 만든다. 출력 길이가 다른 태스크를 의도적으로 섞어 **태스크 간섭이라는 실제 논점**을 확보
- **트레이드오프**:
  > 억지로 채운 태스크는 "왜 하는가"에 답할 수 없다는 문제를 풀기 위해 파이프라인 수요 기반 재구성을 택했고,
  > 그 덕분에 각 태스크의 존재 이유를 ablation으로 증명할 수 있게 된 대신,
  > **공지 예시와 다른 구성을 설명해야 하는** 부담과 태스크 5종의 학습 부하를 떠안았다.
- **영향**: 03 §2 · 04 §3 · 04 E4

### D-06 · ReAct · MCP · 멀티에이전트 미채택

- **상태**: 확정 (2026-07-30)
- **맥락**: 에이전트 스택을 어디까지 붙일 것인가 — 정지 판정
- **대안**: (a) 전부 채택 (b) 도구 루프만 (c) **세 가지 모두 미채택**
- **결정**: (c)
  - **ReAct** — 계산 노드는 있으나 **LLM이 도구를 선택할 필요가 없다.** intent가 정해지면 어떤 계산이 필요한지 코드가 안다
  - **MCP** — 도구 재사용·표준화 수요 없음. 외부에 도구를 노출할 계획 없음
  - **멀티에이전트** — God Object 아님. 도구 5~7개+ 아님
- **트레이드오프**:
  > 한계가 드러나지도 않은 조각을 미리 붙이면 복잡도만 오른다는 문제를 풀기 위해 세 조각을 배제했고,
  > 그 덕분에 과설계를 피하고 디버깅 가능한 구조를 유지한 대신,
  > **"요즘 기술을 안 썼다"는 인상을 줄 수 있는** 위험과 매 단계 필요성을 판단해야 하는 부담을 떠안았다.
- **비고**: 후자의 위험은 **미채택 근거를 문서로 남김으로써** 상쇄한다. "안 썼다"가 아니라 "판정해서 뺐다"
- **영향**: 02 §1.1 · 05 §2

### D-07 · 오케스트레이션 — LangGraph

- **상태**: 확정 (2026-07-30)
- **맥락**: 되묻기 순환과 근거 검증 실패 시 재검색이 있어 흐름이 선형이 아니다
- **대안**: (a) LCEL 선형 체인 (b) 직접 구현한 상태 머신 (c) **LangGraph**
- **결정**: (c). 조건부 분기·순환·상태 공유를 명시적 그래프로 표현할 수 있고, 흐름이 코드에 드러난다
- **트레이드오프**:
  > 되묻기·재검색 순환을 선형 체인으로 표현할 수 없다는 문제를 풀기 위해 LangGraph를 택했고,
  > 그 덕분에 흐름의 예측 가능성과 디버깅 용이성을 얻은 대신,
  > **프레임워크 학습 비용과 상태 스키마 유지보수** 부담을 떠안았다.
- **영향**: 02 §6 · 05 §2

### D-08 · LangChain 선택적 채택

- **상태**: 확정 (2026-07-30)
- **맥락**: 과제가 "LangChain을 활용한 연동"을 요구하나, 팀은 전면 의존을 피하고자 한다
- **대안**: (a) 전면 채택 (b) 완전 배제 (c) **계층별 선택적 채택**
- **결정**: (c) — 문서 규격·LangGraph·`@tool`·LangSmith는 채택, **체인·리트리버 추상화는 직접 구현**
- **트레이드오프**:
  > 요건 충족과 검색 로직 제어권 확보가 충돌하는 문제를 풀기 위해 계층별 선택 채택을 택했고,
  > 그 덕분에 요건을 충족하면서 핵심 로직의 통제권을 유지한 대신,
  > **직접 구현한 부분의 유지보수 책임**과 활용 범위를 설명해야 하는 부담을 떠안았다.
- **영향**: 02 §10

### D-42 · 베이스 sLLM — Qwen3-4B QLoRA

- **상태**: 확정 (2026-07-31)
- **맥락**: D-05가 태스크 5종을 정했으나 **어떤 모델에 얹을지가 비어 있었고**,
  이것이 학습 데이터 형식·프롬프트 템플릿·평가 비교군을 전부 막고 있었다.
  제약은 세 가지였다 — ① 한국어 성능 ② 상업적으로 안전한 라이선스
  ③ **팀이 실제로 굴릴 수 있는 VRAM**.
- **대안**: (a) Qwen3-1.7B (b) **Qwen3-4B** (c) Qwen3-8B 이상
- **결정**: (b) Qwen3-4B, QLoRA 4bit(NF4) + LoRA r=16.
  00 §5에 적은 *"1~4B급"* 의 상한이며, 4bit로 16GB VRAM에 들어간다.

  **1.7B를 택하지 않은 이유가 태스크 구성에 있다.** 이 프로젝트의 핵심 태스크는
  ④ 근거 검증이고(D-05), 이것은 라벨 분류가 아니라 **문장별 판단**이다.
  출력 길이가 서로 다른 5종을 한 어댑터에 얹으면 간섭이 생기는데(04 E4),
  용량이 작을수록 그 간섭이 성능 붕괴로 나타난다. 8B는 QLoRA로도 학습 시간이
  팀 일정에 맞지 않는다.

  **학습 스택은 HF PEFT + TRL**로 고정한다. Unsloth가 더 빠르지만 래퍼 계층이
  하나 더 생기고, 산출물 ③·④에서 *"무엇을 어떻게 학습했는가"* 를 설명할 때
  표준 스택 쪽이 검증하기 쉽다.

- **04 비교군과의 대응** — 같은 `LLMClient` 프로토콜 뒤에 셋을 둔다.

  | 비교군 | 구현 | 의미 |
  |---|---|---|
  | **A** | `APIClient` (대형 LLM) | 상한선이자 **폴백 경로** (D-21) |
  | **C** | `LocalQwenClient` (어댑터 O) | 파인튜닝 효과 |
  | **D** | `LocalQwenClient` (어댑터 X) | 베이스 대비 증분 |

  C와 D가 **같은 클래스**여야 조건이 동일해진다. 어댑터 유무만 다르게 둔다.

- **트레이드오프**:
  > 태스크 5종을 한 모델에 얹으면서 팀 GPU 안에 들어가야 하는 문제를 풀기 위해
  > Qwen3-4B QLoRA를 택했고,
  > 그 덕분에 **④ 근거 검증에 필요한 용량을 확보하면서 16GB에서 학습 가능**해진 대신,
  > **1.7B 대비 실험 반복 속도가 느려지고** 4bit 양자화로 인한 성능 손실을
  > 비교군에서 분리해내기 어려워지는 부담을 떠안았다.
- **비고**: `configs/default.yaml` 의 `model.revision` 을 **학습 착수 시 반드시 채운다.**
  모델도 코퍼스처럼 버전이 있고(D-20), 비워두면 재현이 깨진다.
  또한 평가 시 `do_sample=False` 를 강제한다 — 샘플링이 섞이면 04 §8이 성립하지 않는다.
- **미결**: 4bit 손실 분리를 위한 bf16 대조군을 돌릴지 (GPU 여유에 따라 판단)
- **후속 (2026-08-01)**: **`transformers` 하한이 이 결정에 종속된다.**

  `constraints.txt` 가 `transformers==4.48.0` 을 박고 있었는데, **Qwen3 아키텍처 지원은
  4.51 부터**다. 그대로 두면 학습 착수 당일 모델 로드 시점에야 발견됐을 문제다.
  `4.51.3` 으로 올리고, `pyproject` 하한도 `>=4.51` 로 함께 올렸다.

  연쇄로 두 개가 더 움직였다 — `chromadb 0.5.23` 이 `tokenizers<=0.20.3` 을 요구해
  충돌하므로 `0.6.3` 으로, `langchain-community 0.3.14` 가 py3.11 에서 `numpy<2` 를
  요구하므로 `0.3.24` 로 올렸다(`langchain-core` 는 그에 딸려 `0.3.63`).

  **이 조합은 Python 3.11 에서 실제 설치·테스트·Chroma 왕복까지 확인했다.**

  > **왜 CI 가 못 잡았나** — `test` 잡이 `.[api,dev]` 만 설치해서 `rag` 그룹이
  > 한 번도 해결된 적이 없었다. `rag-deps` 잡을 추가해 설치 해결과
  > **Qwen3 지원 여부를 CI 가 직접 검사**하게 했다. 하한이 다시 내려가면 CI 가 막는다.
- **영향**: 00 §5 · 03 §2·§4 · 04 §5 · `constraints.txt` · `.github/workflows/ci.yml` · D-04 · D-05 · D-21 · D-41

---

## 3. 안전

### D-09 · 트리아지 하향 금지 게이트 🔒

- **상태**: 확정 (2026-07-30)
- **맥락**: 트리아지 등급을 LLM이 판정하면 **과소평가(under-triage) 시 생명 위험**이 발생한다
- **대안**: (a) LLM 단독 판정 (b) 규칙 테이블 단독 (c) **하이브리드 + 하향 금지 게이트**
- **결정**: (c). 규칙 테이블이 1차 판정(`물질 × 종 × 체중당 섭취량`), LLM은 규칙 미적용 케이스만.
  최종 등급 = **위험도 기준 `max(rule, llm)`** — **LLM은 등급을 낮출 수 없다**
- **트레이드오프**:
  > LLM 판정이 과소평가를 낼 수 있다는 문제를 풀기 위해 하향 금지 게이트를 택했고,
  > 그 덕분에 under-triage를 **지표가 아니라 구조로** 억제하게 된 대신,
  > **과대평가(over-triage) 증가**와 규칙 테이블 유지보수 부담을 떠안았다.
- **비고**: 축①의 *"LLM 출력은 반드시 검증해서 받는다"* 가 가장 구체적으로 구현된 지점.
  트레이드오프는 실험 **E6**으로 정량화한다

#### 개정 — 종별 분기 (2026-07-31, D-39 도출 후)

초판은 규칙 테이블을 **`물질 × 종 × 체중당 섭취량`** 으로 잡았다.
**코퍼스 실측 결과 조류에는 체중당 섭취량이 0건이라 이 형태가 성립하지 않는다.**

| 종 | 규칙 테이블 입력 | 근거 |
|---|---|---|
| **개·고양이** | `물질 × 체중당 섭취량` **정량** | 주목 2.3 g/kg · Allium 체중 0.5% · 백합 1–2장 · 3시간 골든타임 |
| **조류** | `물질 × 증상` **정성** | 체중당 용량 **0건** · 호흡수 기준 **0건** · 체온 기준 **0건**.<br>유일한 수치 트리거는 출혈 5–10분(S-072) 1건 |

> **조류 질의에 체중·섭취량 수치를 요구하는 슬롯·프롬프트를 두면 반드시 환각이 발생한다.**
> 근거가 없는 값을 LLM이 지어내기 때문이다. 조류는 **질적 조건**
> (*"acting ill"* · *"not resolving"* · *"continues to vomit"*)과 증상 목록으로만 판정한다.

**등급 값은 D-39를 따른다** — `EMERGENCY(4) > CALL_NOW(3) > VISIT_SOON(2) > MONITOR(1)`.
**숫자가 클수록 위험**하므로 `max()`가 그대로 성립한다.

#### 후속 (2026-08-01) — **"0건" 전제는 깨졌다. 그래도 결정은 유지한다**

팀원이 추가 수집한 조류 학술 문헌 3건(S-070 전문 재수집 · S-093 · S-094)을 추출하면서
**조류 체중당 수치 11건**이 나왔다. 개정판이 근거로 삼은 *"체중당 용량 0건"* 은
**더 이상 사실이 아니다.**

| 성격 | 건수 | 예 |
|---|---|---|
| **중독 임계치** | **2** | `F-070-034` DMSA **≥80 mg/kg 치사** · `F-070-009` 집참새 **≥1500 mg/kg NaCl** |
| 치료 용량 | 9 | 활성탄 1–3 g/kg · CaEDTA 35–40 mg/kg IM · 비타민 K1 2.2 mg/kg IM 등 |

**그럼에도 종별 분기는 그대로 둔다.** 나온 2건이 규칙 테이블에 못 들어가기 때문이다.

- `F-070-034` 는 **킬레이션 약물(DMSA)의 과량 투여** 치사량이다. 보호자가 먹이는 물질이 아니다
- `F-070-009` 는 **집참새(house sparrow)** 실험값이다. 앵무류가 아니고, 조류는 종 간
  체중이 10 g~1 kg으로 100배 차이라 **다른 종의 체중당 값을 옮겨 쓸 수 없다**
- 치료 용량 9건은 **수의사가 투여하는 양**이다. 이걸 섭취 역치로 오인하면 정반대 오답이 된다

> **바뀐 것은 근거의 형태이지 결론이 아니다.**
> 초판 근거 *"코퍼스에 0건"* → 개정 근거 **"보호자 질의에 쓸 수 있는 조류 체중당 역치가 0건"**.
> 문헌에는 있고, 우리 시스템이 쓸 수 있는 것은 없다.

- **`check_facts.py` 의 `species=bird` + `dose` → ERROR 규칙은 유지한다.**
  추출 시 위 11건 전부 `dose` 를 비우고 `note` 에 원문 위치와 값을 남겼다.
  **검사기가 막았기 때문에 사람이 판단할 기회가 생겼다** — 규칙이 의도대로 동작한 사례다.
- **다시 열 조건**: 앵무류(psittacine) 대상의 **경구 섭취** 체중당 역치가 나오면 그때 재검토한다.
  그 전까지 조류는 정성 판정이다.
- **미결**: `F-070-034`·`F-070-009` 를 `threshold_type=기타` 로 행에 남길지,
  `note` 로만 둘지 — 지금은 후자다

- **영향**: 02 §7.2 · 04 §4.1 · 04 E6 · 05 §5 · `scripts/check_facts.py` · **D-39**

### D-10 · 종 미확인 시 답변 금지

- **상태**: 확정 (2026-07-30)
- **맥락**: 같은 물질도 종에 따라 독성이 크게 다르며, **포유류 기준의 조류 적용은 치명적**이다
- **대안**: (a) 종 미상 시 일반적 답변 제공 (b) 개 기준을 기본값으로 (c) **답변하지 않고 되묻기**
- **결정**: (c). 슬롯 추출 단계에서 `species`를 필수로 두고, 확인되지 않으면 검색 자체를 수행하지 않는다
- **트레이드오프**:
  > 포유류↔조류 오적용 위험을 없애려는 문제를 풀기 위해 종 미확인 시 답변 금지를 택했고,
  > 그 덕분에 가장 치명적인 오류 유형을 원천 차단한 대신,
  > **되묻기가 늘어 UX 마찰이 증가**하는 것을 감수했다.
- **영향**: 01 §11 · 02 §8.1 · 02 §9 · 03 §2.2

### D-11 · 진단 금지 — 트리아지 포지셔닝

- **상태**: 확정 (2026-07-30)
- **맥락**: 수의학 진료 행위와의 경계, 그리고 오답 시 법적·윤리적 리스크
- **대안**: (a) 증상 기반 질환 추정 제공 (b) **트리아지(응급도 분류)와 문서 근거 제시만**
- **결정**: (b). *"진단하지 않는다. 검증된 문서의 근거를 제시하고 응급도만 분류한다."*
  모든 응답에 고지 문구를 노출하고, 판정 기본값을 안전 쪽으로 편향
- **트레이드오프**:
  > 진료 행위 경계와 오답 책임 문제를 풀기 위해 트리아지로 범위를 좁혔고,
  > 그 덕분에 리스크를 관리하면서 **평가 목표를 문서 충실도로 정렬**할 수 있게 된 대신,
  > **"그래서 무슨 병인가"에 답하지 못하는** 기능적 한계를 감수했다.
- **영향**: 00 §3.2 · 02 §9 · 04 §1.1
- **연관**: D-12 · D-39

---

### D-39 · 트리아지 등급 — 데이터에서 도출한 4단계 + 별도 급여 축 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: D-09가 하향 금지 게이트를 정했으나 **등급 체계 자체가 미정**이었고, 이것이
  규칙 테이블·골든셋·04 지표를 전부 막고 있었다. 그리고 **D-38에서 "템플릿은 발명하지 않고
  데이터에서 도출한다"고 정해놓고 등급만 위에서 내려주는 것은 앞뒤가 맞지 않았다.**
- **대안**: (a) 통상적 트리아지 4단계(응급/조기내원/관찰/정상)를 차용
  (b) **살아있는 코퍼스 42건에서 실제 등급·행동 표현을 추출해 도출**
- **결정**: (b).

  **먼저 밝혀진 사실 — 코퍼스에 "긴급도 N단계"를 선언한 소스는 0개다.**

  > ⚠️ **초기 판단 정정.** 그동안 *"조류 등급의 유일한 근거는 Lafeber 3단계(S-005)"* 로 여러 번
  > 기술했으나, 원문을 재확인한 결과 이는 **급여 허용 등급이지 진료 긴급도 등급이 아니다.**
  > S-005 전문에 *"수의사에게 데려가라"* 는 지시가 **한 줄도 없다.**

  대신 **행동 지시어(action language)** 를 모으니 4단계로 갈렸다.

  | 값 | 정수 | 배지 | 사용자 문장 | 근거 (verbatim) | 근거 수 |
  |---|---|---|---|---|---|
  | `EMERGENCY` | **4** | 응급 | 지금 바로 동물병원으로 가세요 | *"**Don't call.** Take your pet straight to the nearest ER"* (S-038) | 18 |
  | `CALL_NOW` | **3** | 전화 | 지금 수의사에게 전화해 상태를 알리세요 | *"call your veterinarian to **discuss whether your pet needs to be seen**"* (S-086) | 10 |
  | `VISIT_SOON` | **2** | 내원 | 오늘 중 진료를 받으세요 | *"**Contact your veterinarian if** repetitive behavior occurs daily or worsens"* (S-080) | 11 |
  | `MONITOR` | **1** | 관찰 | 집에서 지켜보고, 아래 증상이 나타나면 연락하세요 | *"**Monitor condition. If condition is not resolving** seek veterinary care"* (S-092) | 11 |

  **`EMERGENCY`와 `CALL_NOW`를 병합할 수 없는 이유가 데이터에 있다** — S-038이
  *"Don't call"* 로 **전화를 명시적으로 금지**하고 이송을 지시한다. S-030도 전화번호(L2)와
  *"go immediately to the nearest emergency veterinary hospital"* (L1)을 나란히 배치한다.

  **`MONITOR`에는 상승 조건을 반드시 함께 표시한다.** 데이터의 이 단계는 "그냥 지켜봐라"가
  아니라 *"if not resolving"* 이라는 **조건부 상승**이 본질이다. 조건 없이 "관찰"만 띄우면
  **그 자체가 과소평가**다.

- **정수 방향**: **숫자가 클수록 위험**하다. D-09의 `max(rule, llm)`이 그대로 성립한다.
  (초안의 `L1=최고위험` 표기는 `min()`을 요구해 코드에서 혼동을 낳으므로 폐기)

- **축이 둘이다 — 섞지 않는다**

  ```
  축 A · 사고 대응 긴급도   EMERGENCY > CALL_NOW > VISIT_SOON > MONITOR
  축 B · 급여 가부          NEVER > CAUTION > SAFE
  ```

  *"포도는 개에게 금지"*(축 B)와 *"포도를 먹었으니 EMERGENCY"*(축 A)는 다른 판정이다.
  **D-38 템플릿의 `{risk_level}`은 축 B**이며, 축 A는 질의 시점에 `물질 + 증상 + (종별) 용량`으로 계산한다.

  | 축 B 값 | 표시 | 근거 |
  |---|---|---|
  | `NEVER` | 급여 금지 | *"should **never be fed** … under any circumstances"* |
  | `CAUTION` | 조건부 | *"**not recommended** but sometimes fed without incident"* / *"with caution"* |
  | `SAFE` | 안전 | *"safe for dogs **in moderation**"* |

  > **조류의 축 B는 2단계(`NEVER`/`CAUTION`)로 축약한다.** S-005와 S-091이 동일 항목
  > (Peanuts · Grit · Mushrooms)을 서로 다른 티어에 배정하고 있어, 3단계를 그대로 노출하면
  > **출처에 없는 정밀도를 주장**하게 된다.

- **종별 판정 근거의 비대칭** — 행동 단계 수는 같고, 판정 재료가 다르다

  | | 지지 단계 | 수치 트리거 |
  |---|---|---|
  | 개 | 4단계 완전. 유일하게 긴급도 라벨 보유(S-080) | 호흡수 40–60/분 · 주목 2.3 g/kg · 간식 10% |
  | 고양이 | **자체 자료는 2단계뿐**(`CALL_NOW`·`VISIT_SOON`) | Allium 체중 0.5% · 3시간 골든타임 |
  | 앵무새 | **4단계 지지** — S-092 단일 문서 안에서 대조 | **출혈 5–10분 1건이 전부** |

  > **고양이는 `mammal/`·`all/` 문서와 반드시 함께 인덱싱**해야 4단계가 성립한다.
  > 조류는 단계 수가 아니라 **판정 근거**가 빈약하다 (D-09 개정 참조).

- **트레이드오프**:
  > 등급 체계를 임의로 정하면 출처에 없는 정밀도를 주장하게 되는 문제를 풀기 위해
  > **코퍼스의 행동 지시어에서 도출**하는 방식을 택했고,
  > 그 덕분에 **모든 등급에 verbatim 근거가 붙고 종별 한계까지 드러난** 대신,
  > **통상적 트리아지 체계와 다른 구성**(정상 단계 없음)을 설명해야 하는 부담과
  > 코퍼스가 바뀌면 등급 근거를 재점검해야 하는 비용을 떠안았다.
- **비고**: **"정상 / 조치 불필요" 단계를 두지 않았다.** 사고 발생 후
  *"아무것도 하지 않아도 된다"* 는 지시가 코퍼스에 **0건**이기 때문이다.
  안전 진술은 전부 축 B(급여 가부)에 속한다.
  또한 `위험`·`중증` 같은 **의학적 중증도 어휘를 쓰지 않는다** — D-11 진단 금지.
  코퍼스의 소스들도 등급 이름 없이 **행동만 지시**하고 있었다
- **미결**: 사용자 노출 배지 표현은 팀 회의에서 확정. **내부 값은 본 결정으로 확정**
- **영향**: D-09 · D-11 · D-38 · 02 §7.2 · 04 §2.5·§4 · 01 §2.2

---

## 4. 평가

### D-12 · 평가 목표 재정의 — 문서 충실도

- **상태**: 확정 (2026-07-30)
- **맥락**: 팀에 수의학 전문 지식이 없어 **의학적 정답을 만들 수 없다**. 임상 판단은 개체차에 좌우된다
- **대안**: (a) 수의사 자문을 받아 임상 정답 구축 (b) 공개 사례 기반 정답 (c) **문서 충실도로 목표 재정의**
- **결정**: (c). *"의학적으로 옳은가"가 아니라 **"출처 문서에 명시된 기준을 정확히 인용했는가"***
- **근거**: 회피가 아니라 **RAG 평가의 정석(Faithfulness)** 이며, D-11 포지셔닝과 일치한다
- **트레이드오프**:
  > 팀이 의학적 정답을 만들 수 없다는 문제를 풀기 위해 평가 목표를 문서 충실도로 재정의했고,
  > 그 덕분에 **검증 가능한 골든 셋을 구축할 수 있게 된** 대신,
  > **"의학적으로 옳은 시스템"임은 주장할 수 없다**는 한계를 감수했다.
- **영향**: 04 §1.1
- **연관**: D-11

### D-13 · 과소평가율 최우선 지표

- **상태**: 확정 (2026-07-30)
- **맥락**: 트리아지 오류는 두 방향이지만 **결과의 무게가 다르다**
- **대안**: (a) 전체 정확도 단일 지표 (b) 정확도 + 오류 유형별 분석 (c) **과소평가율을 최우선 지표로 선언**
- **결정**: (c). *"일반 정확도가 높아도 과소평가율이 높으면 실패한 시스템으로 간주한다."*
  과대평가율도 함께 보고하되, **상충 시 과소평가 억제를 우선**
- **트레이드오프**:
  > 오류 방향에 따라 결과의 무게가 다르다는 문제를 풀기 위해 과소평가율을 최우선 지표로 삼았고,
  > 그 덕분에 안전 설계를 의식한 평가 체계를 갖춘 대신,
  > **전체 정확도가 낮아 보일 수 있는** 것과 불필요한 내원 권고 증가를 감수했다.
- **영향**: 04 §4.1 · 04 E6
- **연관**: D-09

---

## 5. 데이터

### D-14 · 물질(항목) 단위 청킹

- **상태**: 확정 (2026-07-30)
- **맥락**: 독성 자료에서 **임계 용량과 증상이 분리되면 치명적**이다
- **대안**: (a) 고정 길이 500자 (b) 문서 소제목 단위 (c) **물질(항목) 단위**
- **결정**: (c). 물질 하나가 의미 완결 단위이며, (a)를 **비교군으로 실험**하여 근거를 제시한다
- **트레이드오프**:
  > 임계 용량과 증상이 분리될 위험을 없애려는 문제를 풀기 위해 물질 단위 청킹을 택했고,
  > 그 덕분에 검색 단위와 의미 단위를 일치시킨 대신,
  > **자료마다 항목 경계를 추출하는 파싱 로직**을 개별 구현해야 하는 부담을 떠안았다.
- **영향**: 01 §10 · 04 E1

### D-15 · 표형 자료 문장화 (verbalization)

- **상태**: 확정 (2026-07-30)
- **맥락**: ASPCA 식물 DB 등 표·리스트형 자료를 원본 그대로 임베딩하면 통짜 청크가 되어 개별 물질 검색이 실패한다. 게다가 자료의 90%가 영문이다
- **대안**: (a) 원본 그대로 RAG (b) 정형 DB로만 적재하고 벡터 검색 제외 (c) **정형 레코드 + 문장화 청크 병행**
- **결정**: (c). CSV 한 행을 템플릿으로 **한국어 자연어 문장으로 렌더링**하여 임베딩하고, 정형 레코드와 `substance_id`로 연결
- **근거**: 한 처리로 세 문제가 동시에 해결된다 — 물질 단위 검색 가능, **cross-lingual 문제 상당 부분 해소**, 메타데이터 필터 작동
- **트레이드오프**:
  > 표형 자료가 검색되지 않고 영문이 한국어 질의와 매칭되지 않는 문제를 풀기 위해 문장화를 택했고,
  > 그 덕분에 검색 품질과 언어 정합성을 동시에 확보한 대신,
  > **템플릿 설계·유지보수 비용**과 원문과 렌더링본이 이원화되는 관리 부담을 떠안았다.
- **비고**: 원문 인용(`quote`)을 반드시 보존하여 근거 제시 시 사용한다
- **영향**: 01 §9~11

### D-16 · 정형 수치는 계산 노드, 벡터DB 미적재

- **상태**: 확정 (2026-07-30)
- **맥락**: 한국표준사료성분표·FEDIAF·AAFCO 등은 수치 기준표다
- **대안**: (a) 전부 벡터DB에 적재 (b) **관계형 테이블로 적재하고 계산 노드가 조회**
- **결정**: (b). **수치를 벡터 검색으로 찾는 것은 안티패턴**이다
- **트레이드오프**:
  > 정형 수치를 유사도로 찾으면 부정확하다는 문제를 풀기 위해 계산 노드로 분리했고,
  > 그 덕분에 정확성과 재현성을 확보하고 축①(결정론은 코드로)에 정렬한 대신,
  > **RAG가 아닌 컴포넌트가 늘어나 "RAG 비중"이 줄어 보이는** 것을 감수했다.
- **영향**: 01 §12 · 02 §7

### D-17 · 체중 `weight_g` 단일 단위

- **상태**: 확정 (2026-07-30)
- **맥락**: 조류는 수십~수백 g, 개는 수 kg~수십 kg로 스케일 차이가 크다
- **대안**: (a) 종별로 kg/g 혼용 (b) **전 구간 g 단일 단위**, 표시 시에만 환산
- **결정**: (b)
- **트레이드오프**:
  > 단위 혼용이 곧 용량 계산 오류로 직결되는 문제를 풀기 위해 g 단일 단위를 택했고,
  > 그 덕분에 계산 오류 경로를 하나 제거한 대신,
  > **개의 체중이 30000g처럼 직관적이지 않게 표기**되어 표시 계층에서 환산이 필요해졌다.
- **영향**: 01 §11 · 03 §4.2
- **연관**: D-03

#### D-17 후속 (2026-08-01) — **개정.** 체중은 kg, 섭취량은 g

**이 결정은 코드에서 한 번도 지켜진 적이 없다.** PR#3 검수에서 확인했다.

```
app/session.py:35      SLOTS = (..., "weight_kg", "amount_g")
app/contracts.py:47    weight_kg: float | None
graph/state.py:23      weight_kg: float
app/models.py (신규)    weight_kg: Mapped[float | None]
```

넷 다 `weight_kg` 다. 이근준 팀원이 어긴 것이 아니라 **원래 어긋나 있었고,
이번에 DB 스키마까지 그 방향으로 굳는 순간**이었다.

그런데 다시 보니 **어긋난 쪽이 맞았다.**

> 규칙 테이블(`정량임계치.csv`)의 단위가 `mg/kg`·`g/kg` 즉 **체중당**이다.
> 체중을 g 로 들고 있으면 계산 노드가 매번 1000으로 나눠야 한다.
> **나눗셈을 한 번 빠뜨리는 것이 곧 1000배 오판이다** — 원래 막으려던 바로 그 사고다.

- **개정**: 체중 `weight_kg` (kg) · 섭취량 `amount_g` (g). 필드명이 단위를 말한다
- **단위 없는 이름을 쓰지 않는다** — `weight` 나 `amount` 는 금지다.
  D-17 원안이 막으려던 것은 kg/g 혼용이 아니라 **어느 쪽인지 모르는 상태**였다
- **트레이드오프**: 두 단위가 공존하므로 변환 지점이 남는다. 대신
  **각 값이 쓰이는 곳의 단위와 이름이 일치**해 변환 자체가 드물어진다
- **영향**: `app/contracts.py` · `app/models.py` · `app/session.py` · `graph/state.py` ·
  `compute/rules.py` · 01 §11 · 03 §4.2

### D-18 · 내부 문서 — 가상 프로필·기록 생성

- **상태**: 확정 (2026-07-30)
- **맥락**: 실제 사용자 기록을 확보할 수 없고, 확보한다면 개인정보 문제가 발생한다
- **대안**: (a) 실제 사용자 모집 (b) 공개 커뮤니티 게시글 수집 (c) **가상 프로필 3~5건 + 누적 기록 생성**
- **결정**: (c). 종을 개·고양이·앵무새로 분산하고, **의도적 시나리오**(정상 대조군·급여 위험·점진적 변화·조류 은폐형·판단 불가)를 심는다
- **트레이드오프**:
  > 내부 데이터를 확보할 수 없다는 문제를 풀기 위해 가상 데이터 생성을 택했고,
  > 그 덕분에 **정답을 아는 상태로 데이터를 설계**하여 골든 셋 구축이 쉬워진 대신,
  > **실사용 데이터의 노이즈·다양성을 재현하지 못하는** 한계를 감수했다.
- **영향**: 01 §13 · 04 §2.1

### D-19 · cross-lingual — 다국어 임베딩 기본안

- **상태**: **확정** (2026-08-01) — 전제가 바뀌어 조건부를 닫는다. 아래 후속 참조
- **맥락**: 외부 자료의 약 90%가 영문인데 사용자는 한국어로 질의한다
- **대안**: (a) 사전 번역 인덱싱 (b) **다국어 임베딩 cross-lingual 검색** (c) 질의 번역
- **결정**: (b)를 기본안으로 두고 (a)와 비교(E2). D-15의 문장화가 한국어로 이뤄지므로 부담이 일부 완화된다
- **연동**: (a)를 채택하면 **번역 태스크 ⑥이 함께 채택**된다 (D-05)
- **트레이드오프**:
  > 언어 불일치로 검색이 실패하는 문제를 풀기 위해 다국어 임베딩을 기본안으로 택했고,
  > 그 덕분에 추가 번역 비용 없이 대응 가능해진 대신,
  > **임베딩 모델의 cross-lingual 성능에 전적으로 의존**하게 되어 검증 실험이 필수가 되었다.

#### 후속 (2026-08-01) — **전제가 사라져 조건부를 닫는다**

이 결정은 *"영문 원문이 벡터DB에 들어간다"* 를 전제로 썼다. **그 전제가 세 번 바뀌었다.**

| | 무엇이 바뀌었나 |
|---|---|
| **D-37** | 복제 허락이 없어 **원문을 넣지 못한다.** 사실만 뽑아 재구성한다 |
| **D-38** | 그 재구성을 **코드가 한국어로** 한다 |
| **D-45** | 복제 허락이 있는 자료까지 **원문 적재를 껐다** |

실제 적재된 433청크가 이렇다.

```
개에게 양파·리크·샬롯·마늘·차이브(알리움류)는 급여 금지로 분류된다. (출처: WSAVA, S-053)
앵무새에서 PTFE(테플론) 과열 흄은 전화 상황이다. 확인할 증상은 개구호흡, 꼬리 흔들림…
```

**코퍼스가 한국어이고 질의도 한국어다.** 원 맥락의 *"자료 90%가 영문"* 은 **수집 시점의 사실**이지
**적재 시점의 사실이 아니다.** 이 문서가 *"문장화로 부담이 일부 완화된다"* 고 적었던 것은
실제로는 "일부"가 아니라 대부분이었다.

- **남은 다국어 요소는 둘뿐이다** — `scientific_name` 을 원문 그대로 둔 것(*Lilium* ·
  *Persea americana* · *Pasteurella multocida*)과, 팀원이 중국어·일본어 자료를 추가할 가능성.
  둘 다 다국어 임베딩이 있으면 무료로 처리되므로 **`bge-m3` 를 그대로 쓴다.**
- **바꾸지 않는 이유**: 한국어 성능도 준수하고, 마감 3일을 남기고 임베딩을 갈면
  **검색 결과가 통째로 바뀌어 골든셋 기준이 흔들린다.** 근거 없는 교체는 하지 않는다.
- **E2 실험(사전 번역 vs cross-lingual)은 무의미해졌다.** 번역할 원문이 벡터DB에 없다.
  04에서 E2를 폐기하고 사유를 남긴다.
- **번역 태스크 ⑥은 채택하지 않는다.** D-05의 조건부 ⑥은 D-19의 (a)안에 딸려 있었고,
  (a)가 성립하지 않으므로 함께 닫힌다. **멀티태스크는 5종으로 확정**이다.

> **전제가 바뀌면 결정도 다시 봐야 한다.** 조건부로 떠 있는 항목은 조건이 사라진 줄 모르고
> 계속 매달려 있기 쉽다 — 이 항목은 "실험 미실시"로 심사에서 지적받을 수 있었다.

- **영향**: 01 §8 · 02 §8.2 · 03 §2.3 · 04 E2 · D-05 · D-37 · D-38 · D-45

### D-20 · 증분 인덱싱 + 코퍼스 버전 태깅

- **상태**: 확정 (2026-07-30)
- **맥락**: 데이터를 지속적으로 보충할 예정이다
- **대안**: (a) 매번 전체 재구축 (b) **`source_id` 기준 upsert + 전체 재구축도 가능**
- **결정**: (b). 그리고 **평가 결과에 코퍼스 버전을 함께 기록**한다
- **트레이드오프**:
  > 자료가 계속 늘어나 재구축 비용이 커지고 평가 결과 비교가 불가능해지는 문제를 풀기 위해
  > 증분 인덱싱과 버전 태깅을 택했고,
  > 그 덕분에 **"성능이 오른 게 모델 때문인지 데이터 때문인지"를 구분**할 수 있게 된 대신,
  > 초기 파이프라인 설계 복잡도와 버전 관리 부담을 떠안았다.
- **영향**: 01 §2 · 04 §8

---

### D-29 · 작업용 보관과 제출용 배포의 분리 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: 활용 가능 판정 78건 중 원문 보관은 8건뿐이었고, 나머지는 URL만 있었다.
  웹 자료는 개정·삭제되면 인용 근거가 사라진다 — 이미 WPT 403, ASPCA 정책 페이지 500을 겪었다.
  그런데 전량을 확보해 제출물에 담으면 이번에는 저작권 문제가 생긴다.
  **"보관해야 한다"와 "배포하면 안 된다"가 동시에 참**인 상황이다.
- **대안**:
  (a) 확보하지 않는다 — 재현성 포기
  (b) 확보하고 전량 제출한다 — 저작권 위반
  (c) **확보하되 보관과 배포를 분리한다**
- **결정**: (c). 두 개의 아카이브를 만든다.
  - `data_work_*.zip` — 전량. **팀 내부 한정, 외부 배포 금지.** 검증·재현·인덱싱용
  - `data_submit_*.zip` — 오픈 라이선스·정부 간행물·팀 내부 산출물만.
    나머지는 원문 대신 `SOURCES_CITED.csv`(출처·URL·접근일·인용 사실)로 대체
- **트레이드오프**:
  > 웹 자료가 사라지면 근거를 잃는 문제와 원문을 제출물에 담으면 저작권을 침해하는 문제를 **동시에** 풀기 위해
  > 보관·배포 이원화를 택했고,
  > 그 덕분에 **재현성과 저작권 준수를 둘 다 확보**한 대신,
  > **아카이브를 두 벌 관리하고 파일마다 배포 가부를 판정하는** 부담을 떠안았다.
- **비고**: D-28(사실 추출 + 출처 표기)의 물리적 구현이다.
  원문을 옮기지 않고 **사실을 옮기고 출처를 밝힌다**는 원칙을 폴더 구조로 강제한다
- **영향**: 01 §2 · `data/README.md` §2 · 04 §8

---

### D-30 · 스냅샷 품질 등급과 `요약` 등급 인용 금지 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: 확보한 스냅샷 54건은 원문 바이너리가 아니라 **텍스트 추출본**이다.
  추출기가 축약한 경우가 7건 있었고, 목록이 중간에서 절단된 경우가 4건 있었다.
  **축약된 문장을 원문으로 오인하면 그 자체가 환각의 출처가 된다** — 이 프로젝트가
  막으려는 실패를 데이터 단계에서 그대로 재현하는 셈이다
- **대안**: (a) 전부 동등하게 적재 (b) **품질 5등급을 판정하고 등급별로 적재 가부를 가른다**
- **결정**: (b). `완전`(41) 적재 / `부분`(4) 재수집 후 / **`요약`(7)·`빈약`(1)·`부적합`(1)은 인용 근거로 사용 금지**.
  판정은 `SNAPSHOT_MANIFEST.csv`의 `quality` 컬럼에 기록한다
- **트레이드오프**:
  > 추출 손실을 모른 채 적재하면 환각의 근거가 되는 문제를 풀기 위해
  > 품질 등급제를 택했고,
  > 그 덕분에 **인용 근거의 신뢰도를 파일 단위로 통제**하게 된 대신,
  > **활용 가능한 자료가 54건에서 41건으로 줄고**, 신규 스냅샷마다 판정 공정을 떠안았다
- **비고**: 개별 경고는 `notes`에 기록한다. 특히 **S-053**(WSAVA 개 간식 가이드)은
  체중별 칼로리 표에 추출 오류가 의심되어 파일 내부에 주석으로 표시했다 — 해당 수치 적재 금지
- **영향**: 01 §2 · `data/README.md` §4 · 04 §8

---

### D-31 · 접근 차단은 사유별로 다르게 처리한다

- **상태**: 확정 (2026-07-31)
- **맥락**: 스냅샷 확보에서 15건이 막혔는데, "막혔다"를 한 덩어리로 다루면
  **뚫어도 되는 것과 뚫으면 안 되는 것을 구분하지 못한다.** 실제로 근거가 서로 다르다
- **결정**: 차단 사유를 4분류하고 각각 다르게 처리한다

  | 사유 | 성격 | 처리 |
  |---|---|---|
  | **기술 오류** (404 · URL 결손 · JS 렌더링) | 아무 제한도 아님 | 주소 정정 후 그대로 수집 |
  | **봇 차단** (403 · CAPTCHA) | 자동화 거부. 사람은 대상 아님 | **사람이 받아 파일로 전달**. 링크 재fetch 아님 |
  | **robots.txt** | RFC 9309 — **크롤러 대상 권고**. *"not a form of access authorization"* | **사람이 받아 파일로 전달만.** 링크를 받아 재fetch하면 사람을 한 번 경유했을 뿐 여전히 자동 수집 |
  | **로그인·구독** | 접근 자체가 계약 대상 | **대체 출처로 교체.** D-32 참조 |

- **트레이드오프**:
  > "막혔다"를 한 덩어리로 다루면 뚫어도 되는 건 포기하고 뚫으면 안 되는 건 우회하게 되는 문제를 풀기 위해
  > 사유별 4분류를 택했고,
  > 그 덕분에 **각 건에 대해 근거 있는 판단**을 하게 된 대신,
  > 차단될 때마다 사유를 확인하는 공정을 떠안았다.
- **비고**: **robots.txt를 통과해도 끝이 아니다.** 이용약관과 저작권은 별개 층으로 남는다.
  robots는 "수집해도 되는가", 약관·저작권은 "확보한 것을 쓸 수 있는가"를 규율한다
- **영향**: 01c §6.5 · `data/README.md` §2

---

### D-32 · 비용·허가가 발생하는 자료는 재현성 요건에서 배제한다 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: 구독료(VIN 월 $69)나 재수록 허가료(Elsevier·Wiley·Springer → CCC RightsLink)가
  발생하는 자료가 코퍼스 후보에 섞여 있다. 이를 **저작권 리스크로만 보면 핵심을 놓친다**
- **대안**: (a) 리스크를 감수하고 활용 (b) 허가·구독을 확보 (c) **재현성 요건으로 배제**
- **결정**: (c). 판단 근거를 **저작권이 아니라 04 §8 재현성**에 둔다.

  > 구독이 있어야 열리는 자료는 **채점자와 다른 팀원이 검증할 수 없다.**
  > 프로젝트가 끝나 구독이 끊기면 근거 자체가 사라진다.
  > 법적 판단 이전에 **재현성 요건에서 이미 탈락**이다.

- **트레이드오프**:
  > 유료 자료의 저작권 리스크를 사안별로 따지느라 판단이 지연되는 문제를 풀기 위해
  > 재현성 기준의 일괄 배제를 택했고,
  > 그 덕분에 **법률 판단 없이 명확하게 선을 긋게 된** 대신,
  > **VIN 등 임상 품질이 높은 자료를 포기**하는 것을 감수했다.
- **비고**: 단 **재수록 허가료는 우리 설계가 이미 회피하고 있다.**
  Elsevier 자신이 명시한다 — *"Creating an original figure or table from data or factual
  information that was not previously in figure or table format typically does not require permission."*
  **D-28(사실 추출 + 문장화)이 정확히 그 경로다.** 원문 표를 옮기면 허가 대상, 사실을 뽑아 우리 문장으로 쓰면 대상 아님.
  한국 저작권법으로도 같은 결론 — 제2조("표현한" 창작물) · 제7조5호 · 제28조(인용) + 제37조(출처명시) · 제35조의5(공정이용)
- **영향**: 01b · 04 §8 · `data/SOURCES_CITED.csv`

---

### D-33 · AI 학습 금지 조항이 있는 자료는 절대 배제한다 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: **Clinician's Brief 이용약관에서 다음을 확인했다.**

  > *"You are prohibited from using any Educational Concepts Content to **train any type of
  > artificial intelligence model**."*

  우리가 이미 **원문 PDF를 확보해 `raw/`에 보관 중이던 S-061**(Feline Compulsive Disorder)이
  바로 이 매체다. 확장 제안①(행동 이상 관찰 지표)의 근거로 쓰려던 자료다
- **대안**: (a) RAG는 학습이 아니라고 해석 (b) **조항이 있으면 무조건 배제**
- **결정**: (b). **해석의 여지를 두지 않는다.**
  RAG 적재가 "train"에 해당하는지는 다툴 여지가 있으나, **다툴 여지가 있다는 것 자체가 배제 사유**다.
  학습 프로젝트에서 그 리스크를 질 이유가 없다
- **조치**: S-061을 `usability = D 제외`로 재판정하고 `raw/`·아카이브에서 제거.
  행동 이상 지표는 **S-079·S-080(VCA·petMD 강박행동)**으로 대체하고, 고양이 정형행동은 신규 수집 대상으로 넘긴다
- **트레이드오프**:
  > 약관 해석의 회색지대에서 판단이 흔들리는 문제를 풀기 위해
  > 무조건 배제를 택했고,
  > 그 덕분에 **판단 비용 없이 리스크를 0으로** 만든 대신,
  > **확보 완료된 Tier 1급 자료 1건을 버리고** 고양이 정형행동 근거 공백을 떠안았다.
- **비고**: 앞으로 **신규 자료의 약관에서 "artificial intelligence" · "machine learning" · "train"을
  먼저 검색**한다. 라이선스 확인 절차의 첫 단계로 둔다
- **영향**: 01b · 01c §3 · `자료수집원장.xlsx`

---

### D-34 · 데이터베이스제작자 권리 — 대량 DB는 전량 복제하지 않는다

- **상태**: 확정 (2026-07-31)
- **맥락**: "사실은 저작물이 아니다"(D-32)가 **대량 DB에는 그대로 적용되지 않는다.**
  한국 저작권법 **제93조(데이터베이스제작자의 권리)** 는 사실 데이터라도
  **DB의 "전부 또는 상당한 부분"의 복제·전송에 별도 배타적 권리**를 인정한다.
  우리 코퍼스에는 ASPCA 594학명(S-031)·PPH 328행(S-026)이 있다
- **대안**: (a) 사실이니 전량 적재 (b) **항목 단위 사실 추출 + 자체 재구성**
- **결정**: (b). DB를 **통째로 옮기지 않는다.** 질의에 필요한 **항목 단위로 사실을 추출**하고,
  스키마·분류·문장화는 우리가 새로 만든다 (D-14 물질 단위 청킹 · D-15 문장화와 일치)
- **트레이드오프**:
  > 사실 비보호 원칙만 믿고 DB를 통째로 적재하면 제93조에 걸리는 문제를 풀기 위해
  > 항목 단위 추출·재구성을 택했고,
  > 그 덕분에 **DB권 리스크를 피하면서 청킹 설계와도 일치**하게 된 대신,
  > **원본 DB를 그대로 쓰는 편의**를 포기했다.
- **비고**: 이미 채택한 D-14·D-15가 우연히 이 요건을 만족시키고 있었다.
  **설계 근거를 하나 더 얻은 셈**이다
- **영향**: 01 §10 · D-14 · D-15

---

### D-35 · 검증을 수집 뒤가 아니라 앞에 놓는다 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: 지금까지는 **다 모으고 나서 검증**했다(01b). 그 결과 S-061은
  팀원이 찾아서 받고, 전달하고, 내가 파싱해서 "잡지 레이아웃이라 영역 분리 필요"까지 분석한 뒤에 버려졌다.
  **약관에서 단어 하나 검색하는 데 1분이면 되는 일이었다**
- **대안**: (a) 수집 후 일괄 검증 유지 (b) **수집 착수 전 게이트 통과 의무화**
- **결정**: (b). 자료를 확보하기 **전에** 게이트를 통과시킨다.
  단 항목을 **차단 3 + 조건 2의 2층 구조**로 나눈다 (개정 근거는 아래 §개정 이력)

  **1층 · 차단 게이트** — 통과/중단을 가른다

  | # | 확인 | 걸리면 |
  |---|---|---|
  | **G1a** | **목적 금지** — 두 묶음을 검색한다.<br>① `artificial intelligence` · `machine learning` · `train` · `data mining` · `repurpose`<br>② **AI를 언급하지 않고 막는 표현** — `information retrieval system` · `storage and retrieval` · `stored, processed` · `electronic systems` · `store` · `derivative works` | ⛔ **무조건 중단.** 어떻게 취득했든 **용도**가 금지된 것이라 피할 수 없다 (D-33) |
  | **G1b** | **수단 금지** — `robot` · `spider` · `crawler` · `scrape` | 크롤링 금지일 뿐 **열람 금지가 아니다.** 수동 취득으로 전환하면 해소된다 |
  | **G2** | **개인·비상업 복제를 허용하는가** | 허용 문언이 없으면 ⛔ **중단.** 불명확(허락도 금지도 없음)은 **작업용 한정**으로 착수 |
  | **G3** | 접근에 구독·로그인이 필요한가 | ⛔ **중단** — 재현성 탈락 (D-32) |

  **2층 · 사용 조건** — 통과한 뒤 "어떻게 쓸지"를 정한다

  | # | 확인 | 조치 |
  |---|---|---|
  | **G4** | 대량 DB인가 | 전량 복제 대신 **항목 단위 추출 + 자체 재구성** (D-34) |
  | **G5** | 개인정보가 포함되는가 | 코드로 제거 후 사용. 제거 불가면 중단 (D-36) |

- **트레이드오프**:
  > 수집·전달·분석을 다 끝낸 뒤 버리게 되는 매몰 비용 문제를 풀기 위해
  > 수집 전 게이트를 택했고,
  > 그 덕분에 **버릴 자료에 시간을 쓰지 않게 된** 대신,
  > **자료 1건마다 착수 전 확인 절차**를 떠안았다.
- **비고**: **불명확을 "배제"가 아니라 "사용 범위 축소"로 처리한다**는 점이 핵심이다.
  그래야 Lafeber(조류 3단계 등급의 유일한 출처)처럼 조건이 불분명하지만 대체 불가능한 자료를
  잃지 않는다. D-29의 보관/배포 분리가 이 완충을 만들어준다

#### 개정 이력 — 2026-07-31, 초판을 12개 도메인 38건에 적용한 결과

초판은 5단계를 **한 층으로 나열**했다. 보유 자료에 소급 적용해보니 두 가지가 드러났다.

**① G4·G5는 판정을 한 번도 뒤집지 못했다.** 38건 전부 G1·G2에서 결론이 났다.
다만 **Pet Poison Helpline은 예외**였다 — 약관 페이지 자체가 없어 G1~G3이 전부 "조항 없음"으로
통과하는데, 328행 통째 복제는 **약관과 무관하게 저작권법 제93조**에 걸린다.
즉 G4는 **차단 기준이 아니라, 약관이 침묵할 때 작동하는 사용 조건**이다. 지우지 않고 2층으로 옮긴다.

**② 초판 G1이 성격이 다른 둘을 한 칸에 섞고 있었다.**

| | 예시 | 우리에게 적용되나 |
|---|---|---|
| **목적 금지** | *"prohibited … to **train** any artificial intelligence models"* (Merck)<br>*"use of **artificial intelligence** … to repurpose"* (VCA) | ⛔ 못 피한다 |
| **수단 금지** | *"use any **robot, spider**"* (ASPCA·AKC)<br>*"**spider, crawl, or scrape**"* (WildEarth) | 수동 취득이면 해당 없음 |

이 둘을 구분하지 않아 **판정 근거가 흐려졌다.** 실제 갈림은 수단이 아니라
**G2 — 개인·비상업 복제를 허용하는가**였다.

- ASPCA — *"make a **single copy** … for your **personal and non-commercial** use"* → 허용 있음 → 조건부 통과
- Banfield — *"may not copy or **incorporate any of the Content into any other work**"* → 허용 없음 → 중단
- AKC — Reproduction Policy가 온라인 재이용을 **링크로만** 한정 → 중단

**판정 결과는 바뀌지 않았고 근거만 정정되었다.** ⛔ 4건(petMD·Banfield·AKC·WildEarth)의
사유를 G1 → **G2**로 고쳐 기록한다. *(이 4건은 이후 **D-37**로 재판정된다)*

#### 2차 개정 — 30개 도메인 전수 확인 후 (2026-07-31)

**③ `artificial intelligence`만 검색하면 놓친다.** 전수 확인 결과 **AI를 한 번도 언급하지 않고
벡터 색인을 금지하는 곳이 더 많았다.**

| 출처 | 문언 | AI 언급 |
|---|---|---|
| AVMA | *"…or **incorporate into any information retrieval system** (electronic or hard copy)"* | 없음 |
| FOUR PAWS | *"**stored, processed**, copied or distributed using **electronic systems**"* | 없음 |
| ASPCA | *"must not … **store**, **create derivative works of** …"* | 없음 |

셋 다 RAG를 정면으로 막는다. **G1a 검색어에 ②번 묶음을 추가**한다 —
`information retrieval system` · `storage and retrieval` · `stored, processed` ·
`electronic systems` · `store` · `derivative works`.

**이 검색어 추가로 ASPCA 6건이 "조건부 통과" → "사실추출 한정"으로 하향**되었다.

- **영향**: 01c §6.1 · 01b · `data/README.md` §5

---

### D-37 · G2는 복제를 규율하고, G1a는 용도를 규율한다 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: D-35 게이트를 보유 자료에 적용해 18건이 ⛔로 판정됐다. 그런데 다음 질문이 남았다 —
  **"우리는 원문을 복제하지 않고 사실만 뽑아 우리 문장으로 쓰는데, 그래도 걸리는가?"**
  이 질문에 답하지 않으면 **살릴 수 있는 자료를 버리게 된다**
- **대안**: (a) 약관에 걸리면 무조건 배제 (b) **약관이 무엇을 규율하는지 구분해서 판정**
- **결정**: (b). **G2와 G1a는 규율 대상이 다르다.**

  | | 금지하는 것 | 예시 문언 | 사실 추출로 회피? |
  |---|---|---|---|
  | **G2** | **복제** | Banfield *"may not **copy** or incorporate any of the Content"*<br>petMD *"no part of the Site may be **copied** … **downloaded**"* | ✅ **가능**. 복제를 하지 않으므로 **적용 대상이 아니다** |
  | **G1a** | **용도** | VCA *"use of artificial intelligence … to rewrite, adapt, or **repurpose** this content"*<br>Merck *"…to **extract** … for **training** … AI models"* | ⛔ **불가**. `repurpose`·`extract`가 **재가공 자체를 지목**한다 |

  **근거** — 저작권법 제2조1호가 저작물을 "**표현한** 창작물"로 정의하고 제7조5호가 사실 전달을
  보호 대상에서 제외한다. 제28조(인용) + 제37조(출처명시) + 제35조의5(공정이용)가 이를 받친다.
  Elsevier 자신도 *"Creating an original figure or table from data or factual information …
  typically does not require permission"* 이라고 명시한다.

  > **약관은 자기 콘텐츠를 규율하지만 거기 적힌 사실을 소유하지 못한다.**
  > "포도는 개에게 신장 손상을 일으킨다"는 사실은 Banfield의 것이 아니다.

- **재판정 결과 — ⛔ 18건 → 13건**

  | 도메인 | 건수 | 걸린 항목 | 재판정 |
  |---|---|---|---|
  | vcahospitals.com | 8 | **G1a** | ⛔ **중단 유지** |
  | merckvetmanual.com | 5 | **G1a** | ⛔ **중단 유지** |
  | petmd.com | 2 | G2 | ✅ **사실 추출 한정 통과** |
  | banfield.com | 1 | G2 | ✅ **사실 추출 한정 통과** |
  | akc.org | 1 | G2 | ✅ **통과** — AKC는 *"provide a **link** to the AKC website"* 를 명시 허용하므로 사실 + 링크 인용은 오히려 정책에 부합 |
  | wildearth.com | 1 | G2 | ⚠️ **제외 유지** — 법적으로는 가능하나 비건 사료 D2C 브랜드로 **출처 신뢰도 부적합** |

- **전제 조건 — 파이프라인 분리**: 이 판정은 **원문이 벡터DB에 들어가지 않을 때만** 성립한다.

  ```
  원문(스냅샷) → data_work/ 작업용 · 골든셋 채점 기준으로만 사용     [D-29]
        ↓ 사실 추출
  사실 테이블 → 자체 문장화 → 벡터DB 적재                          [D-15]
  ```

  **D-29(보관/배포 분리)와 D-15(문장화)가 이미 이 구조를 만들어 두었다.**
  04 §1.1이 요구하는 *"출처 문서에 명시된 기준을 정확히 인용했는가"* 는
  내부에 남은 원문을 채점 기준으로 삼아 검증한다.

- **트레이드오프**:
  > 약관에 걸린 자료를 전부 버리면 근거가 고갈되는 문제를 풀기 위해
  > 규율 대상(복제/용도)을 구분한 판정을 택했고,
  > 그 덕분에 **⛔ 18건을 13건으로 줄이고 판정에 논리적 일관성을 얻은** 대신,
  > **원문 청크를 그대로 적재하는 편의를 포기**하고 문장화 공정을 필수로 떠안았다.
- **비고**: 우리는 변호사가 아니며, 약관(계약)이 저작권법상 자유이용을 어디까지 배제할 수 있는지는
  다툼의 여지가 있는 영역이다. **다만 G1a처럼 "AI에 쓰지 마라"라고 의사를 명시한 곳은
  법적 다툼과 별개로 존중한다** — D-33에서 정한 기준이 그것이다

#### 적용 한계 — 사실 추출로 회피되는 조항과 안 되는 조항 (2026-07-31 전수 재판정)

⛔ 19건을 이 기준으로 하나씩 다시 읽었다. **갈림은 조항이 무엇을 목적어로 삼는가**에 있다.

**회피되지 않는다 — 조항이 우리 행위 자체를 이름으로 부른다**

| 출처 | 문언 | 왜 못 피하나 |
|---|---|---|
| **VCA** (8) | *"use of artificial intelligence … to **rewrite**, republish, **adapt**, or **repurpose** this content, **in whole or in part**"* | "사실을 뽑아 우리 문장으로 재구성"이 곧 `rewrite`·`repurpose`다. `in whole or in part`라 부분 발췌도 막힌다 |
| **Merck** (5) | *"use any **manual or automated** … processes to 'scrape', 'data mine', **extract** … any data … **for any purpose**"* | `extract`가 사실 추출이다. `manual`까지 포함해 **사람이 읽고 정리해도** 걸린다 |

**회피된다 — 조항이 "저작물·Content"를 목적어로 삼는다**

| 출처 | 문언 | 왜 피할 수 있나 |
|---|---|---|
| **AVMA** (1) | *"…incorporate into any information retrieval system … **the AVMA Services, any Content or any portion thereof**"* | 금지 대상이 **AVMA의 Content**다. 우리가 쓴 문장은 Content가 아니다 |
| **FOUR PAWS** (2) | *"None of **the works** … may be … stored, processed"* | 금지 대상이 **the works**(저작물)다. 사실은 저작물이 아니며, *"single reproductions for personal, non-commercial use"* 도 허용한다 |
| **AAFCO** (1) | G3 유료 판정 | **도메인 단위 판정 오류였다.** 유료인 것은 Official Publication이고 **우리가 보유한 S-042는 무료 소비자 페이지**다 |

**조건부**

- **S-031 (Kaggle ASPCA CSV)** — 사실 추출은 가능하나 **1,896행 전량은 불가**. 저작권법 제93조 데이터베이스제작자 권리의 *"전부 또는 상당한 부분"* 에 해당한다 (D-34). **질의에 필요한 항목만** 추출하고 원본 CSV는 아카이브에서 제거
- **S-032 (VIN)** — 약관 확인 불가로 보류. 내용이 인덱스 페이지(품질 `빈약`)라 **실익이 없어** 제외
- **S-021 (WildEarth)** — 법적으로는 가능하나 비건 사료 D2C 브랜드 마케팅. **출처 신뢰도로 제외**

> **재판정 결과: ⛔ 19건 → 13건.**

**출처 표기는 별개다.** 코퍼스에 넣지 못하는 것과 참고문헌에 적는 것은 다르다.
⛔ 13건도 `SOURCES_CITED.csv`에 남기고, 산출물에는
**"확인했으나 라이선스상 적재하지 않음"** 으로 기록한다 — **검증 과정의 근거가 된다.**

**사실은 독점되지 않는다.** ⛔ 자료에서 확인한 사실이 **통과 판정 출처에도 있으면**
그 출처로 인용하면 된다. 예: 고양이 응급 징후는 VCA(⛔) 대신 AAHA S-037·S-038로 대체 가능하다.
⛔ 자료를 **"우리가 아는 사실의 목록"** 으로 삼아 교차 확인하는 작업을 01c에 신설한다.

- **영향**: 01 §2 · 01c §6.1·§6.2·§6.4 · D-15 · D-29 · D-33 · D-34 · D-35 · D-38

---

### D-38 · 문장화는 코드가 한다 — 그리고 문장화도 검증 대상이다 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: D-37로 대부분의 자료가 **경로 ②(사실 추출 + 자체 문장화)** 로 가게 되었다.
  그런데 벡터DB에 들어가는 것이 **우리가 쓴 문장**이면 새로운 문제가 생긴다.

  ```
  [런타임]  답변 ↔ 검색된 청크        → sLLM ④ 근거 검증이 담당 (기존)
  [빌드타임] 우리 문장 ↔ 원문          → 🆕 검증 주체가 없다
  ```

  **환각 방지 지점이 답변 단계에서 문장화 단계로 이동한다.**
  D-30에서 같은 것을 이미 보았다 — *"축약된 문장을 원문으로 오인하면 그 자체가 환각의 출처가 된다."*
- **대안**: (a) LLM에게 문장화를 시키고 사후 검증 (b) **템플릿을 미리 만들고 코드가 생성**
- **결정**: (b). **축① "결정론은 코드로, 판단·해석만 LLM에게"의 적용이다.**

  ```
  원문 ──[추출]──→ 사실 표 ──[템플릿]──→ 문장
                      ↑                    ↑
                여기만 검증하면      템플릿은 한 번만
                    된다              사람이 검수
  ```

  **템플릿은 발명하지 않고 데이터에서 도출한다.** 코퍼스의 `doc_type` 축을 그대로 쓴다 —
  `toxicity_food`(27) · `nutrition`(21) · `toxicity_plant`(11) · `emergency`(9) · `symptom`(7).

  ```python
  # toxicity_food 템플릿
  "{species}에게 {substance}은(는) {risk_level}로 분류된다."
  + if threshold: "체중 1kg당 {dose}{unit} 이상 섭취 시 위험하다."
  + if signs:     "주요 증상은 {signs}이다."
  + if onset:     "증상은 {onset} 이내에 나타난다."
  + "(출처: {publisher}, {source_id})"
  ```

  **결측 필드는 해당 절을 통째로 뺀다.** "정보 없음"이라고 쓰지 않는다 —
  그 문장이 검색되면 그 자체가 오답이 된다.

  > **부수 효과**: 임계값 필드가 비면 정량 문장이 생성되지 않는다.
  > 조류는 정량 임계치가 0건이므로 **조류 청크는 자동으로 정성 문장만 나온다.**
  > **D-09의 종별 분기가 데이터 단계에서 강제되어** 규칙 테이블에서 따로 막을 필요가 없다.

- **검증 체계**: 검증 대상이 문장이 아니라 **필드**로 축소된다.

  | 층 | 검사 | 방법 |
  |---|---|---|
  | **자동** | 그 숫자가 원문에 실제로 있는가 | 원문 문자열 검색 |
  | **자동** | **단위 변환 오류** | 재계산 — S-074에 실제 사례가 있다 (*"530°F (280°C)"*, 280°C = 536°F) |
  | **자동** | 종 이름이 원문과 일치하는가 | 문자열 대조 |
  | **자동** | 필수 필드 결측 | 스키마 검사 |
  | **사람** | 템플릿 검수 | **한 번만.** 템플릿이 맞으면 필드가 맞는 한 문장도 맞다 |
  | **사람** | 표본 대조 | 무작위 N건을 원문과 대조. **작성자·검수자 분리** (04 §2.4) |

  **04에 지표 3종을 추가한다** — 추출 정확도 / 문장화 충실도(원문에 없는 내용 생성 = **0**) /
  역추적 가능성(모든 청크가 `source_id` + 원문 위치 보유 = **100%**).

- **트레이드오프**:
  > 사실 추출이 문장화라는 새 환각 지점을 만드는 문제를 풀기 위해
  > 템플릿 기반 코드 생성을 택했고,
  > 그 덕분에 **검증 대상을 문장에서 필드로 축소**하고 종별 분기까지 데이터 단계에서 강제하게 된 대신,
  > **문장 표현이 획일화되어 임베딩 변별력이 떨어지는** 위험과 템플릿 설계 공정을 떠안았다.
- **완화**: 필드 값(물질명·종·증상)이 문장에 그대로 들어가고, 청킹이 물질 단위(D-14)라 항목별로 분리된다.
  효과는 **E1(청킹 단위 비교)에 "템플릿 문장 vs 원문 청크" 비교를 추가**해 측정한다 —
  라이선스 제약에서 나온 선택이 **측정 가능한 설계 판단**이 된다
- **비고**: 이 검증은 **빌드타임 비용**이다. 런타임마다 도는 것이 아니라 코퍼스 구축 시 한 번이면 끝난다.
  그리고 이 과정 자체가 04 §7(실패 분석)의 재료가 된다 —
  **"답변 단계뿐 아니라 데이터 단계에서도 환각을 막았다"**
- **영향**: 01 §2.1 · 04 §3·§7 · 04 E1 · D-09 · D-14 · D-15 · D-30 · D-37

---

## 6. 개인정보

### D-36 · 개인정보는 LLM에 닿기 전에 코드가 벗긴다 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: 본 도메인을 고른 이유 중 하나가 **개인정보 리스크가 낮다**는 것이었고(D-01),
  법적으로도 맞다. 확인한 바는 다음과 같다.

  | 확인한 것 | 근거 |
  |---|---|
  | **반려동물 정보 자체는 개인정보가 아니다** | 개인정보보호법 제2조1호 — "**살아 있는 개인**에 관한 정보". 제2조3호 정보주체 = "**사람**" |
  | **반려동물 건강정보는 민감정보가 아니다** | 제23조의 "건강"은 **정보주체(사람)의** 건강. 동물 진료기록은 해당 없음 |

  **그런데 위험은 동물 정보가 아니라 거기 딸려 오는 것에 있다.**

  | 항목 | 왜 문제인가 |
  |---|---|
  | **동물등록번호** | 동물보호법 시행령 제11조 — 등록 시 소유자 **성명·주민등록번호·주소·전화번호**가 함께 등록된다. 시행규칙 제12조는 인식표에 **소유자 성명·전화번호와 등록번호를 함께 표시**하도록 규정. 제2조1호 **나목(쉽게 결합)** 판단 대상이다 |
  | **사진 EXIF의 GPS** | 위치정보법 제2조2호 개인위치정보 — "다른 정보와 **용이하게 결합**하여 특정 개인의 위치를 알 수 있는 것을 포함". 제15조1항의 수범자는 "**누구든지**" — 사업자 여부와 무관하다 |
  | **사진 속 사람 얼굴** | 제2조1호 **가목** — "**영상 등**을 통하여 개인을 알아볼 수 있는 정보". PIPC 표준 해석례도 얼굴 사진 자체를 개인정보로 본다 |
  | **급여·배변·체중 시계열** | 누적되면 **생활 패턴**이 드러난다 |

- **대안**: (a) 입력 단계에서 사용자에게 주의 안내 (b) **코드가 기계적으로 제거**
- **결정**: (b). **축① "결정론은 코드로, 판단·해석만 LLM에게"의 적용이다.**
  개인정보 제거는 판단이 아니라 결정론이다. LLM에게 맡길 일이 아니다

  | # | 조치 | 근거 |
  |---|---|---|
  | 1 | **동물등록번호를 슬롯에서 제외**. 프로필 식별은 앱 내부 UUID | 제16조 최소수집. 트리아지에 필요한 건 종·품종·체중·나이·기저질환뿐이다 |
  | 2 | **업로드 직후 코드가 EXIF를 전량 제거**. GPS·촬영기기·시각 모두. 원본 EXIF는 보관하지 않는다 | 위치정보법 제15조 |
  | 3 | **얼굴 검출 시 마스킹 또는 저장 거부** | 제2조1호 가목 + 제16조 — 다이어리 목적에 사람 얼굴은 불필요 |
  | 4 | **보호자 본인의 건강정보는 슬롯 추출 단계에서 폐기** | 제23조 민감정보 — 별도 동의 대상이며 우리는 받지 않는다 |
  | 5 | **LLM·외부 API 전송 전 필터를 코드로 강제**. 필터를 통과한 것만 나간다 | 축① |
  | 6 | **LangSmith 트레이스에 실입력을 남기지 않는다** | 02 §10·04 §8의 트레이스가 **입력 원문을 그대로 저장**한다. 데모 입력은 가명화하거나 트레이스에서 제외 |
  | 7 | 학습·평가는 **가상 프로필만**(D-18). 데모 입력 기록은 **발표 종료 시 전량 파기** | 제21조 — 목적 달성 시 지체 없이 파기 |

- **트레이드오프**:
  > 반려동물 도메인이라 개인정보가 안전하다는 인식이 사진·등록번호·시계열의 위험을 가리는 문제를 풀기 위해
  > 코드 단계의 기계적 제거를 택했고,
  > 그 덕분에 **사용자의 주의력에 의존하지 않고 구조로 막게 된** 대신,
  > **전처리 파이프라인에 EXIF 제거·얼굴 검출 단계를 추가**하는 비용을 떠안았다.
- **비고**: 6번이 가장 놓치기 쉽다. **관측을 위해 넣은 LangSmith가 개인정보 저장소가 된다.**
  D-20(코퍼스 버전 태깅)·D-13(과소평가율 추적)이 모두 트레이스에 의존하므로 지우기도 어렵다.
  **가명화를 트레이스 설정 단계에서 켠다**
- **미결 해소**: 얼굴 검출은 **로컬 모델** · 검출 시 **저장 거부** → **D-43**
- **영향**: 01 §14 · 02 §10 · 04 §8 · 00 §리스크 · D-18

---

### D-43 · 사진 업로드를 만든다 — 대신 업로드 경로에 관문을 둔다 🔒

- **상태**: 확정 (2026-08-01)
- **맥락**: 07 §6에서 마감 압박을 이유로 **"사진 업로드 기능은 만들지 않는다"** 고 잘라냈었다.
  안 받으면 D-36의 조치 7개 중 2개(EXIF 제거·얼굴 검출)가 통째로 불필요해지기 때문이다.

  **그런데 다이어리에서 사진을 빼면 기록이 성립은 해도 쓰이지 않는다.**
  보호자가 남기고 싶은 것은 *"오늘 밥 잘 먹음"* 이라는 문장이 아니라 그때의 사진이다.
  기록이 안 쌓이면 00 §3.3의 2단계(맥락)와 3단계(킬러 — 기간 리포트)가 함께 죽는다.
  **차별화 지점 전체가 사진 한 장에 걸려 있었다.**

  반려동물 사진이라 위험이 낮아 보이지만, **실제로 올라오는 것은 반려동물 사진이 아니다.**

  | 실제로 올라오는 것 | 왜 개인정보인가 |
  |---|---|
  | 고양이를 안고 있는 **보호자의 얼굴·손** | 개인정보보호법 제2조1호 **가목** — "영상 등을 통하여 개인을 알아볼 수 있는 정보" |
  | **집 안 배경** — 우편물·달력·창밖 풍경 | 가목 + 나목(쉽게 결합). 주소가 드러난다 |
  | EXIF의 **GPS 좌표** | 위치정보법 제2조2호 · 제15조1항 수범자는 "**누구든지**" |
  | **기록마다 쌓이는 사진의 촬영 시각** | 시계열과 결합하면 생활 패턴 + 상시 체류지가 드러난다 |

  마지막 줄이 프로필 1장짜리 설계와 결정적으로 다른 지점이다.
  **한 장은 위치이지만 백 장은 동선이다.**

- **대안**: (a) 만들지 않는다 (07 §6 원안) (b) 프로필 1장만 허용
  (c) **프로필 + 다이어리 기록 양쪽 허용, 대신 업로드 경로를 관문화**
- **결정**: (c). 기능은 되살리되 **D-36의 조치 2·3을 "하면 좋은 것"에서
  "통과하지 못하면 저장되지 않는 관문"으로 승격**한다.

  | # | 관문 (업로드 직후 · 저장 **전** · 전부 코드) | 근거 |
  |---|---|---|
  | 1 | 확장자·MIME·크기 검사 — 이미지가 아니면 거부 | — |
  | 2 | **EXIF 전량 제거**. GPS·촬영기기·촬영시각 모두. 원본 EXIF는 어디에도 남기지 않는다 | 위치정보법 제15조 |
  | 3 | **얼굴 검출 → 1건이라도 있으면 저장 거부 + 이유 안내** | 제2조1호 가목 · 제16조 |
  | 4 | 리사이즈 + **재인코딩** — 2를 빠져나간 잔여 메타데이터를 인코딩 단계에서 끊는다 | 방어 이중화 |
  | 5 | 앱 내부 UUID로 저장. **원본 파일명은 버린다** | 파일명에 이름·날짜·기기명이 들어온다 |

- **얼굴 검출은 로컬 모델로 한다** — D-36의 미결 사항을 여기서 해소한다.

  > 개인정보를 빼내기 위해 그 개인정보를 외부 API로 내보내는 것은 자기모순이다.
  > 검출 대상이 곧 보호해야 할 대상이므로, **판정이 경계 밖으로 나가면 안 된다.**

  OpenCV Haar 또는 MediaPipe 급의 경량 로컬 모델을 쓴다. 정확도는 최상이 아니지만
  **이 관문에서 요구되는 것은 정밀도가 아니라 봉쇄**다.

- **마스킹이 아니라 거부인 이유** — 실패했을 때 어느 쪽으로 틀리는가가 다르다.

  | | 검출을 놓쳤을 때 | 잘못 검출했을 때 |
  |---|---|---|
  | 마스킹 | **얼굴이 그대로 저장된다** | 사진 일부가 가려진다 |
  | **거부** | 얼굴이 그대로 저장된다 | 사용자가 다시 올린다 |

  두 방식 모두 놓치면 똑같이 위험하지만, **거부는 오검출의 대가가 불편함뿐**이다.
  게다가 마스킹은 "얼마나 잘 가려졌는가"를 검증해야 하는데 그 시간이 마감 안에 없다.
  **검증할 수 없는 안전장치는 안전장치가 아니다** (04 §8).

- **거부할 때 반드시 이유를 보여준다.** 왜 막혔는지 모르는 사용자는 우회를 시도한다.
  *"사람이 함께 나온 사진은 저장하지 않습니다. 반려동물만 나오도록 다시 올려주세요."*

- **사진은 LLM에 넣지 않는다.** 이번 범위에서 사진은 **기록의 첨부물이지 판정 입력이 아니다.**
  멀티모달로 사진을 읽는 순간 D-11(진단 금지)의 경계가 무너진다 —
  **사진으로 피부 병변을 "보는" 것은 그 자체가 진단이다.**
  트리아지 입력은 지금처럼 텍스트 슬롯뿐이다.

- **트레이드오프**:
  > 사진 없는 다이어리는 쓰이지 않아 개인화·리포트가 함께 죽는 문제를 풀기 위해
  > 업로드 허용 + 경로 관문화를 택했고,
  > 그 덕분에 **00 §3.3의 2·3단계가 실제로 성립하게 된** 대신,
  > **처리 파이프라인에 관문 5단계와 얼굴 검출 의존성을 추가**하고
  > 정상 사진이 거부당하는 오검출을 감수했다.
- **완화**: 관문은 **업로드 라우터 한 곳**에만 둔다. 우회 경로를 만들지 않기 위해서다.
  테스트가 *"관문을 거치지 않고 저장에 도달하는 경로가 없다"* 를 검사한다.
- **비고**: 00 §3.3 우선순위는 **바뀌지 않는다.** 사진은 2단계(맥락)에 속하며,
  **1단계(코어 — 트리아지 QA)가 끝나기 전에는 착수하지 않는다.**
  코어 없는 사진 업로드는 이 프로젝트의 산출물이 아니다.
- **영향**: 00 §3.3 · 02 §12 · 07 · D-11 · D-36

---

## 7. 운영

### D-21 · UI는 가드레일 뒤 / 대형 LLM 베이스라인 우선

- **상태**: 확정 (2026-07-30)
- **맥락**: 축② *"통제가 먼저, 배달이 나중"*. 그리고 파인튜닝 실패 리스크
- **대안**: (a) UI를 먼저 만들어 데모 확보 (b) **통제(예측·검증·안전) 완료 후 UI**
- **결정**: (b). 로드맵상 **가드레일 → UI** 순서. 또한 **대형 LLM 기준 RAG를 먼저 완성**한 뒤 sLLM으로 교체
- **트레이드오프**:
  > 판정 로직이 바뀔 때마다 화면을 다시 손대는 이중 비용과 파인튜닝 실패 시 시연 불가 문제를 풀기 위해
  > 통제 우선 순서를 택했고,
  > 그 덕분에 **재작업을 막고 어떤 경우에도 시연 가능한 시스템을 확보**한 대신,
  > **중간 점검 시점에 보여줄 화면이 없는** 것을 감수했다.
- **영향**: 00 §8 · 05 §7

### D-22 · 문서 5분할 + 단일 출처 원칙

- **상태**: 확정 (2026-07-30)
- **맥락**: 통합 문서가 16쪽을 넘고, 워크스트림 4~5개 내용이 한 파일에 섞여 동시 편집 충돌이 예상된다
- **대안**: (a) 통합 유지 (b) 데이터 문서만 분리 (c) **필수 산출물 매핑 기준 5분할**
- **결정**: (c). 00 마스터 + 01~05, 각 문서에 담당 워크스트림 배정. **같은 사실은 한 문서에만** 기술하고 나머지는 참조
- **트레이드오프**:
  > 한 파일을 네 명이 동시에 고치는 문제와 산출물 4종 매핑 불일치를 풀기 위해 5분할을 택했고,
  > 그 덕분에 병렬 작업과 산출물 직결 구조를 얻은 대신,
  > **문서 간 참조 관리와 일관성 유지** 부담을 떠안았다.
- **비고**: 후자는 00의 **문서 맵**과 단일 출처 원칙으로 관리한다
- **영향**: 00 §0

### D-40 · 배달 계층 — 화면이 아니라 계약을 먼저 고정한다 🔒

- **상태**: 확정 (2026-07-31)
- **맥락**: D-21이 *"UI는 가드레일 뒤"* 로 정했는데, 00 §9.3은 *"UI 스펙은 WS2와 사전 합의한다"* 를
  요구한다. **화면은 나중인데 합의는 먼저**라는 것이 모순처럼 보였다.

  모순이 아니다. **나중에 오는 것은 화면이고, 먼저 정해야 하는 것은 계약이다.**
  계약이 없으면 WS2는 무엇을 반환할지 모른 채 그래프를 짜고, WS5는 무엇이 올지 모른 채
  화면을 짠다. 둘이 만나는 시점에 재작업이 발생하는데, **D-21이 막으려던 것이 정확히 그 재작업**이다.
- **대안**:
  (a) Streamlit 단독 — 가장 빠르지만 API 계층이 없어 계약이 코드에 묻힌다.
      산출물 ③ *"연동 구현 코드"* 로 보여줄 경계가 흐려진다
  (b) React + Vite 별도 프론트 — 화면이 늘 때 유리하나 node 의존성·CORS·빌드 산출물 관리가 붙는다
  (c) **FastAPI + 정적 HTML 1장, 같은 출처 서빙**
- **결정**: (c). 그리고 **계약을 먼저 확정하고 엔진은 스텁으로 둔다.**

  ```
  app/web/index.html ──HTTP──> FastAPI ──Protocol──> QAEngine
                            (계약)                 ├─ StubEngine   ← 지금
                                                   └─ GraphEngine  ← WS2 완성 후
  ```

  빌드 도구도 node도 CORS 설정도 없다. 시연 재현이 `make serve` 한 줄로 끝난다 (04 §8).

  **세부 결정 네 가지가 이 결정의 실체다.**

  **① 거절을 4xx가 아니라 200으로 내보낸다.**

  `/api/ask` 는 항상 200을 반환하고 `status` 가 `answered` / `clarify` / `refused` 를 나른다.
  02 §9에서 **거절은 장애가 아니라 설계된 정상 경로**로 정의했다.
  4xx로 만들면 프론트가 에러 핸들러에서 처리하게 되고, **거절 화면이 장애 화면처럼 그려진다.**
  전송 계층 실패(500)와 정책상 거절을 구분하지 못하면 00 §7의 감점 항목
  *"검색 실패와 생성 실패를 구분하지 않은 오류 분석"* 을 그대로 재현하게 된다.

  **② 정책을 문서가 아니라 스키마로 강제한다.**

  | 불변식 | 위반 시 | 근거 |
  |---|---|---|
  | `answered` 인데 `citations` 가 비었다 | **응답 객체 생성 실패** | 02 §9 — 근거 없으면 답하지 않는다 |
  | `MONITOR` 인데 상승 조건이 없다 | **응답 객체 생성 실패** | D-39 — 조건 없는 "관찰"은 과소평가다 |
  | 경로 ② 근거에 원문 인용이 실렸다 | **응답 객체 생성 실패** | D-37 — 원문은 벡터DB에 없다 |

  세 항목 모두 **런타임 검사가 아니라 타입 수준**이다. 지키자고 합의한 것이 아니라
  어기면 응답이 만들어지지 않는다. 05 §6 *"LLM에 맡긴 지점마다 검증 코드가 한 겹 붙는다"* 의
  배달 계층 판본이다.

  **③ 엔진을 프로토콜 뒤에 둔다.**

  라우터는 `QAEngine` 프로토콜만 안다. WS2의 LangGraph가 완성되면 **주입 지점 한 줄**을 바꾼다.
  계약·프론트·테스트는 손대지 않는다. 지금의 `StubEngine` 은 검색과 LLM만 고정값이고
  **되묻기·상한 초과 거절·하향 금지 게이트는 실제로 통과시킨다** — 벡터DB 없이도 D-09·D-10·D-39가
  동작하는 것을 시연할 수 있다.

  **④ 등급 표현의 단일 출처를 API로 만든다.**

  `GET /api/triage-levels` 가 등급 이름·배지·행동 문장과 **도출 근거 원문(verbatim)** 을 함께 내려준다.
  프론트가 등급 표현을 자기 코드에 복사하면 D-39가 개정될 때 화면만 옛 표현으로 남는다.
  00 §9.4 단일 출처 원칙을 규칙이 아니라 **구조**로 만든 것이다.

- **트레이드오프**:
  > 판정 로직이 확정되기 전에 화면을 만들면 재작업이 생기고, 계약 없이 병렬 작업하면
  > WS2와 WS5가 만나는 지점에서 또 재작업이 생기는 문제를 **동시에** 풀기 위해
  > **계약 우선 + 스텁 엔진**을 택했고,
  > 그 덕분에 **로직 완성 전에도 WS5가 착수할 수 있고 정책 위반이 타입 오류로 잡히게 된** 대신,
  > **스텁과 실제 엔진 두 벌을 유지**하고 계약이 바뀔 때마다 양쪽을 고치는 부담을 떠안았다.

- **미채택 — SSE 스트리밍**: 토큰이 흘러나오는 체감은 좋으나, **등급이 확정되기 전에 문장이 먼저
  나가면 근거 검증 실패 시 이미 뱉은 문장을 회수할 수 없다.** 04 §4.1.0의 중대 과소평가율 0 목표와
  충돌한다. 축② *"통제가 먼저, 배달이 나중"* 이 문장 단위에도 적용된다.

- **비고**: 세션 저장소를 메모리로 두었다. 되묻기 슬롯(체중·섭취량)을 **보관하지 않는 것이
  D-36 최소수집과 맞다** — 프로세스가 죽으면 사라지는 것이 의도다.
  다중 워커로 띄울 때만 외부 저장소로 교체한다.
  또한 `answer` 만 읽는 클라이언트가 상승 조건을 빠뜨리지 않도록 `full_text` 필드를 함께 내보낸다 —
  **조건 누락은 이 도메인에서 과소평가와 같다** (D-39).

- **영향**: 00 §9.3·§10 · 02 §12·§13 · 04 §8 · 05 §2(조각 12) · D-09 · D-11 · D-21 · D-36 · D-37 · D-39

---

### D-41 · 저장소 구조 — 파라미터를 코드에서 빼고 의존성을 쪼갠다

- **상태**: 확정 (2026-07-31)
- **맥락**: 골격이 커지면서 두 가지가 걸렸다.

  **① 파라미터가 코드에 박혀 있었다.** `top_k=5`, `chunk_size=500`, LoRA rank가
  각각 다른 파일에 있었다. 04 §8이 재현성을 요구하는데, 실험 보고서에
  *"그때 top-k가 몇이었더라"* 를 적을 방법이 없다. 04의 감점 항목
  *"재현 불가 — 버전 미고정, 시드 미설정"* 을 그대로 밟는 구조였다.

  **② 의존성이 한 덩어리였다.** `requirements.txt` 하나에 torch와 fastapi가 같이 있어
  **API만 띄우려는 팀원도 수 GB를 받아야 했고**, CI도 마찬가지였다.

- **대안**:
  (a) 지금 구조 유지 — 4주 프로젝트에 과한 정비다
  (b) 모노레포(`apps/` + `packages/`) — 4인 규모에 과잉
  (c) **설정 외부화 + 의존성 그룹 분리 + 컨테이너**
- **결정**: (c). 네 가지를 바꿨다.

  **① 설정을 `configs/*.yaml` 로 뺀다.** 경계는 **재현에 필요한가**로 갈랐다.

  | | 어디 | 커밋 | 예 |
  |---|---|---|---|
  | 재현에 필요 | `configs/*.yaml` | **한다** | 모델 ID, LoRA rank, top-k, 임계값, 시드 |
  | 비밀·환경차이 | `.env` | 안 한다 | API 키, DB 주소, 트레이싱 on/off |

  **실험 결과를 보고할 때 YAML을 그대로 첨부하면 재현이 된다.**
  프로파일(`default` → `<profile>` → 환경변수) 순으로 덮으므로,
  평가는 `PETTRIAGE_PROFILE=eval` 하나로 조건이 고정된다.

  **② 의존성을 `[api] [rag] [ingest] [train] [dev]` 로 쪼갠다.**
  GPU 없는 팀원은 `pip install -e '.[api]'` 로 끝난다.
  버전 고정은 `constraints.txt` 가 따로 맡는다 — **무엇을 설치할지(extras)와
  어느 버전으로 고정할지(constraints)를 분리**해야 둘을 독립적으로 바꿀 수 있다.

  **③ 무거운 임포트를 함수 안으로 내린다.** `models/` 의 torch·transformers 임포트가
  모듈 최상단에 있으면 CI가 학습 의존성 없이 못 돈다. 이것은 스타일이 아니라 **제약**이며,
  CI의 `test` job이 `[api,dev]` 만 설치하는 것으로 강제된다.

  **④ 컨테이너를 나눈다.** API 이미지(`Dockerfile`)에 torch를 넣지 않고,
  학습은 `docker/train.Dockerfile` 로 분리한다. `compose.yaml` 에 pgvector를 함께 올려
  02 §11의 벡터DB 후보를 실제로 시험할 수 있게 한다.
  `.dockerignore` 가 `data/` 를 차단한다 — **이미지에 자료가 구워지면 배포가 곧 유출**이다 (D-29).

- **트레이드오프**:
  > 파라미터 산재로 재현이 안 되고 의존성 덩어리로 착수 비용이 큰 문제를 풀기 위해
  > 설정 외부화와 의존성 분리를 택했고,
  > 그 덕분에 **실험 조건을 파일 하나로 고정하고 GPU 없이도 개발할 수 있게 된** 대신,
  > **설정이 코드와 YAML 두 곳에 존재**하게 되어 기본값이 어긋날 여지와
  > 그룹별 설치를 팀원에게 안내해야 하는 부담을 떠안았다.
- **완화**: 어긋남이 위험한 값은 **테스트가 일치를 검사한다** —
  `triage.max_clarify_turns` 와 `contracts.MAX_CLARIFY_TURNS` 가 그 예다.
  안전에 직결되는 설정은 반드시 이 형태로 묶는다.
- **비고**: 어댑터·체크포인트는 `artifacts/` 로 보내고 커밋하지 않는다.
  **재현은 가중치가 아니라 `configs/` + `constraints.txt` + 시드로 한다.**
- **영향**: 00 §9.4 · 04 §8 · README · `configs/README.md` · D-20 · D-29 · D-40 · D-42

---

### D-44 · 벡터DB — Chroma 확정 (pgvector 우선 검토를 뒤집는다)

- **상태**: 확정 (2026-08-01)
- **맥락**: 02 §11 은 **pgvector 를 우선 검토**로 두고 있었다. 근거는 두 가지였다 —
  ① 메타데이터 필터를 SQL 로 처리 ② 프로필·정형 기준표를 같은 DB 에서 관리해
  계산 노드와 통합하기 쉽다.

  **그런데 설계가 진행되면서 ②의 전제가 사라졌다.**

  | 당시 가정 | 실제로 정해진 것 |
  |---|---|
  | 정량 임계치를 DB 에 넣고 SQL 로 조인한다 | **D-38** — 문장화도 계산도 **코드가 한다.** 임계치는 `compute/tables/*.csv` 를 코드가 직접 읽는다 |
  | 프로필·기록을 같은 DB 에 둔다 | 현재 `RecordStore` 는 인메모리이고 **인증조차 없다.** DB 에 넣을 단계가 아니다 (04 범위 밖) |
  | 복잡한 메타데이터 필터가 필요하다 | 실제 필터는 `species` · `doc_type` · `substance` 의 **동등 비교와 목록 포함**뿐이다 |

  즉 pgvector 의 이점은 **발생하지 않았고**, 서버를 띄워야 하는 비용만 남았다.
  마감이 8/04 이고 팀원 3명이 Windows 인 상황에서 이 비용은 작지 않다.

- **대안**: (a) pgvector (02 §11 원안) (b) **Chroma** (c) Qdrant
- **결정**: (b) **Chroma**, `chromadb==0.6.3`, `PersistentClient` 파일 기반.

  | | |
  |---|---|
  | **착수 비용** | Docker·서버 없이 `pip install` 만으로 돈다. 팀원이 오늘 밤 바로 검색을 붙일 수 있다 |
  | **필터** | `where` 로 충분함을 **실측**했다 — 종 동등 비교, 목록 포함(D-39 고양이 병합 검색 `["cat","mammal","all"]`)까지 동작 |
  | **증분 적재** | `upsert` 로 **재적재 멱등성 확인** (D-20 코퍼스 버전 태깅의 전제) |
  | **재현** | `persist_dir` 를 지우고 `build_index.py` 로 통째로 재생성된다. 상태가 파일 하나에 모인다 |

- **텔레메트리는 끈다.** 개인 기록을 다루는 시스템이 외부로 사용 신호를 내보내면 안 된다
  (D-36 조치 5·6 과 같은 취지). `anonymized_telemetry=False` 를 넣었고,
  0.6.3 이 기동 이벤트 2건에서 이 설정을 무시하지만 **호출 자체가 실패해 전송되는 것은 없다.**
  실패 경고만 로그에서 지웠다 — 진짜 오류가 소음에 묻히면 안 되기 때문이다.

- **트레이드오프**:
  > SQL 필터와 정형 데이터 통합이라는 pgvector 의 이점이 실제로는 발생하지 않는데
  > 서버 기동 부담만 남는 문제를 풀기 위해 Chroma 를 택했고,
  > 그 덕분에 **팀원 전원이 Docker 없이 검색 계층을 돌릴 수 있게 된** 대신,
  > **정형 데이터와의 조인 질의가 필요해지는 순간 저장소를 갈아야 하는** 부담을 떠안았다.
- **완화**: `VectorStore` **Protocol 뒤에 있다.** 갈아야 할 때 다시 쓰는 것은
  `retrieval/store.py` 한 파일이고 **그래프 노드는 손대지 않는다.**
  `compose.yaml` 의 pgvector 서비스는 지우지 않고 남겨둔다 — 여유가 생기면 그대로 시험할 수 있다.
- **비고**: 이 결정은 **성능 비교로 내린 것이 아니다.** 남은 시간과 팀 환경으로 내렸고,
  그 사실을 여기 적어둔다. 지표로 뒤집을 근거가 나오면 뒤집는다.
- **미결**: Top-k · 유사도 임계값은 **1차 평가 후** 정한다 (지금 정하면 근거가 없다)
- **영향**: 02 §11 · 04 §8 · `configs/default.yaml` · `retrieval/store.py` · D-19 · D-20 · D-36 · D-41

---

### D-45 · 경로①(원문 청크 적재)을 끄고, 자격 목록을 원장에서 유도한다 🔒

- **상태**: 확정 (2026-08-01)
- **맥락**: 사실 표 433행 추출을 마친 뒤 *"원문을 그대로 쓸 수 있는 자료도 굳이 추출해야 하나"*
  라는 질문이 나왔다. 답을 확인하려고 경로① 목록을 원장과 대조하다 **더 큰 문제가 나왔다.**

  `scripts/check_facts.py` 의 `ROUTE1_SOURCES` 는 코드에 박힌 8건짜리 목록이었는데,
  원장에서 자격을 다시 유도하니 **8건 중 7건이 어긋나 있었다.** 겹친 것은 `S-043` 하나다.

  | | 자료 | 무엇이 문제인가 |
  |---|---|---|
  | **잘못 허용하고 있었다** | `S-001` | **G1a(AI 학습 금지)로 삭제된 자료**다. DELETION_LOG 에 있다 |
  | | `S-012` `S-023` `S-064` | `usability = B 가공활용` — **복제 금지.** 원문을 실으면 약관 위반이다 |
  | | `S-042` `S-055` `S-070` | 품질 `빈약`·`X`·`요약` — D-30 상 인용 근거로 쓸 수 없다 |
  | **잘못 막고 있었다** | `S-016` `S-029` `S-046` | `usability = A 자유이용` · `submit_ok = O`. FDA·주정부 자료다 |
  | | `S-034` `S-044` `S-045` | CC BY · 정부 간행물. 원장 `license` 에 명시돼 있다 |

  > **가장 위험한 것은 `S-012`·`S-023`·`S-064` 였다.** 이 셋은 이미 사실 표 34행을 갖고 있고,
  > 목록이 `quote` 를 허용하고 있었으므로 **복제 금지 자료의 원문이 벡터DB에 들어갈 수 있었다.**
  > D-37 판정의 전제 — *"원문이 벡터DB에 들어가지 않을 때만 성립한다"* — 가 코드에서 깨져 있었다.
  > 지금은 전 행 `quote` 가 비어 있어 실제 사고로 이어지지 않았을 뿐이다.

- **대안**: (a) 목록을 고친다 (b) **목록을 없애고 원장에서 유도한다** (c) 경로①을 아예 없앤다
- **결정**: (b) + **스위치는 끈다.**

  ### ① 자격은 코드가 아니라 원장이 정한다

  ```python
  def route1_eligible() -> set[str]:      # 세 조건을 모두 만족해야 한다
      # 1. 원장이 배포 가능으로 표시 — 스냅샷 submit_ok=O / 원본 license 오픈·정부
      # 2. 품질이 완전·부분        — 요약을 원문으로 인용하면 그게 곧 환각이다 (D-30)
      # 3. 삭제 이력에 없다         — D-33 · G1a
  ```

  **손으로 관리하는 목록은 원장과 반드시 어긋난다.** D-22의 단일 출처 원칙이 데이터에만
  적용되고 코드에는 적용되지 않았던 것이 이번 사고의 원인이다.
  원장을 못 읽으면 **빈 집합**을 돌려준다 — 자격이 없는 쪽으로 실패한다.

  현재 유도 결과는 **7건** — `S-016` `S-029` `S-034` `S-043` `S-044` `S-045` `S-046`.
  01c §6.2의 라이선스 통과 8건에서 `S-070`(품질 `요약`)이 D-30으로 빠진 수다.

  ### ② 스위치는 이번 범위에서 끈다 — `ROUTE1_ENABLED = False`

  두 경로를 함께 켜면 같은 사실이 두 번 적재된다.

  - 같은 사실이 **top-k 를 두 칸 잡아먹는다**
  - **④ 근거 검증 노드가 둘을 모순으로 판정**할 수 있다 — 경로①은 원문이라
    *"An occasional apple seed will not harm"* 같은 완화 표현이 살아 있고,
    사실 표에서는 같은 항목이 `NEVER` 로 정리된다

  중복 제거 규칙과 검증 노드 예외를 함께 설계해야 하는데, **마감(8/04)까지 그 이득이
  위험보다 크지 않다.** 경로②만으로 산출물 4종이 모두 성립한다.

- **"경로① 자료는 사실 표에서 빼면 되지 않나" — 안 된다.**

  **규칙 테이블과 계산 노드는 텍스트를 읽지 않는다.** D-38이 *"문장화는 코드가 한다"* 로
  정한 순간, 코드가 판정에 쓰는 값은 전부 구조화된 칸에서 나오게 됐다.
  원문 청크에 *"2.3 g leaves/kg"* 이라 적혀 있어도 `dose`·`unit`·`threshold_type` 칸이
  없으면 `apply_gate()` 가 볼 수 없다.

  > **경로 구분은 적재 방식이지 추출 여부가 아니다.**
  > 정량·등급 값이 있는 자료는 경로와 무관하게 사실 표 행이 필요하다.

- **원문은 버리지 않는다.** 04 §1.1의 *"출처 문서에 명시된 기준을 정확히 인용했는가"* 는
  `data_work_*.zip` 에 남은 원문을 **골든셋 채점 기준**으로 삼아 검증한다 (D-29 · D-37).
  원문의 쓰임이 "적재 대상"에서 "채점 기준"으로 바뀔 뿐이다.

- **트레이드오프**:
  > 원문 인용의 충실도와 중복 적재의 위험이 충돌하는 문제를 풀기 위해
  > 경로① 차단 + 원장 유도를 택했고,
  > 그 덕분에 **복제 금지 자료가 적재될 경로가 구조적으로 사라지고 적재 경로가 하나로 단순해진**
  > 대신, **원문을 그대로 인용해 얻을 수 있는 충실도 점수를 포기**했다.
- **완화**: 스위치 하나로 되돌릴 수 있다. 1차 평가에서 인용 충실도가 낮게 나오면
  자격 7건에 한해 켜고 재측정한다 — 그때는 지표라는 근거가 생긴다.
  켜더라도 자격은 원장이 정하므로 삭제·복제금지 자료가 다시 새어 들어오지 않는다.
- **비고**: 이 사고는 **검사기가 통과했다고 안전한 것이 아님**을 보여준다.
  `check_facts.py` 는 433행을 ERROR 0 으로 통과시키는 동안, 정작 자기 화이트리스트가
  틀렸다는 것은 몰랐다. **검사기의 기준 자체를 검사할 것이 필요하다** — 그래서 원장 유도로 바꿨다.
- **영향**: 00 §5 · 01c §6.2 · 01e §0·§2 · 02 §7·§13 · 04 §1.1 · `scripts/check_facts.py` · D-22 · D-29 · D-30 · D-33 · D-37 · D-38

---

### D-46 · 유사도 임계값은 거절을 만들지 못한다 — 0.50 으로 두고 거절은 노드가 책임진다 🔒

- **상태**: 확정 (2026-08-01)
- **맥락**: 02 §8.3이 *"유사도 임계 미만이면 검색 실패로 보고 거절한다"* 로 정해 두었고,
  `configs/default.yaml` 은 `score_threshold: 0.35` 를 **근거 없이** 담고 있었다.
  `bge-m3` 로 433청크를 실제 적재한 뒤 측정했다.

  근거가 **있어야 하는** 질의 12건과 **없어야 하는** 질의 10건을 던졌다.

  | | 범위 |
  |---|---|
  | 근거 있음 | **0.547 ~ 0.733** |
  | 근거 없음 | **0.494 ~ 0.659** |

  **두 분포가 겹친다.** 겹치는 구간이 `0.547 ~ 0.659` 로 근거 있음의 아래쪽 절반을 덮는다.

  | 근거 없는 질의 | 점수 | 물어온 것 |
  |---|---|---|
  | *"앵무새 체중 100g 기준 초콜릿 몇 g부터 위험한가요"* | **0.659** | 초콜릿 (조류) |
  | *"앵무새 발톱은 얼마나 자주 깎아야 하나요"* | 0.578 | 그릿(grit) |
  | *"고양이 중성화 수술 비용이 궁금해요"* | 0.572 | 중독 의심 시 대응 |
  | *"강아지 배변 훈련은 어떻게 하나요"* | 0.553 | 강박장애(CCD) |
  | *"우리 강아지 사료 브랜드 추천해주세요"* | 0.537 | Pedigree High Protein (리콜 자료) |

  > **어떤 임계값도 이 둘을 가르지 못한다.** 0.55 로 올리면 근거가 있는
  > *"앵무새 앞에서 프라이팬을 태웠어요"*(0.547)가 거절된다 — **과소평가**다(D-13).
  > 내리면 도메인 밖 질의가 전부 통과한다.

- **대안**: (a) 근거 있음 최저(0.547) 바로 아래로 바짝 붙인다 (b) **0.50 — 완전 무관만 자르는 최소선**
  (c) 임계값을 없앤다
- **결정**: (b) **`score_threshold: 0.50`**.

  | | |
  |---|---|
  | **근거 있는 12건 전부 통과** | 하나도 거절되지 않는다. D-13이 과소평가를 최우선으로 두므로 **이 방향으로 틀린다** |
  | 근거 없는 것 중 최저 1건만 차단 | 방어 효과는 거의 없다. **그 사실을 숨기지 않는다** |

  (a)를 택하지 않은 이유 — 0.54 로 붙이면 PTFE 질의가 임계선 위에 아슬아슬하게 걸린다.
  **자료가 한 건만 늘어도 순위가 흔들려 거절로 넘어간다.** 안전 장치가 데이터에 흔들리면 안 된다.

  (c)를 택하지 않은 이유 — 임계값이 없으면 **검색이 항상 무언가를 돌려준다.**
  0.2대 문서로 답을 만드는 경로가 열린다. 최소선은 남긴다.

- **⚠️ 거절의 책임이 옮겨간다 — 이것이 이 결정의 핵심이다.**

  02 §8.3은 임계값이 거절을 만든다고 적었지만, **측정 결과 그럴 수 없다.**
  거절은 두 노드가 만들어야 한다.

  | 노드 | 무엇을 막나 | 근거 |
  |---|---|---|
  | **① 의도 분류** | **도메인 밖**을 검색 전에 걸러낸다. `intent=general` 이면 검색하지 않고 `refused / 범위밖` | 02 §6 · 05 §4 |
  | **④ 근거 검증** | 검색은 됐지만 **문장별 근거가 없는** 경우를 잡는다 | D-11 · 02 §9 |

  **`build_filter` 는 이 판정을 하지 않는다.** 필터는 결정론이고, 도메인 판정은 의미 판단이다.

  > 측정 5건 중 4건이 *"코퍼스 밖"·"도메인 밖"* 이었다 — **①이 잡아야 할 것들**이다.
  > ①을 대충 만들면 임계값이 받쳐주지 못하므로 **그대로 답으로 나간다.**

- **드러난 개별 문제 두 가지**

  **① 리콜 자료가 브랜드 추천 질의를 끌어온다.** *"사료 브랜드 추천해주세요"* 가
  `Pedigree High Protein`(S-046 리콜 목록)을 0.537로 물어온다. **리콜된 제품이 추천으로 읽히면
  최악의 오답**이다. `doc_type=recall` 은 사용자가 그 제품을 명시할 때만 검색되게 해야 한다.

  **② 조류 정량 질의가 정성 근거를 물어온다.** *"앵무새 체중 100g 기준 초콜릿 몇 g"* 이
  조류 초콜릿 청크를 **0.659** 로 가져온다. 검색은 맞다 — 그 자료는 실재한다.
  틀리는 것은 **정량으로 답할 근거가 없는데 정량 질문을 받았다**는 판정이고,
  이건 검색이 아니라 **계산 노드**가 해야 한다 (D-09: 조류 체중당 임계치 0건).

- **트레이드오프**:
  > 임계값 하나로 거절을 만들려던 설계가 측정에서 성립하지 않는 문제를 풀기 위해
  > 임계값을 최소 방어선으로 낮추고 거절 책임을 노드로 옮겼고,
  > 그 덕분에 **근거가 있는 질의를 거절하지 않게 된** 대신,
  > **① 의도 분류의 품질에 거절 정확도가 직결**되게 되었다.
- **완화**: 골든셋의 `refused` 15% 가 이 지점을 직접 채점한다 (04a §3).
  ①이 부실하면 그 지표가 먼저 떨어지므로 1차 평가에서 드러난다.
- **비고**: **측정하지 않았다면 0.35를 그대로 두고 "임계값이 거절을 만든다"고 보고했을 것이다.**
  그 보고는 04 §7의 *"잘 동작하는 것으로 보인다"* 와 같은 종류의 문장이 된다.
  재현 명령은 `python scripts/calibrate_threshold.py` 다.
- **미결**: 하이브리드 검색(BM25 + dense)이 정석 해법이나 마감 내 도입하지 않는다.
  1차 평가에서 거절 정확도가 낮으면 그때 재검토한다
- **영향**: 02 §6·§8.3 · 04 §4 · 04a §3 · `configs/default.yaml` · `graph/nodes/classify.py` ·
  `graph/nodes/retrieve.py` · `graph/nodes/compute.py` · D-09 · D-11 · D-13 · D-44

---

### D-47 · 답변에 실린 연락처는 출력 직전에 코드가 뺀다 🔒

- **상태**: 확정 (2026-08-01) · 1차 검수에서 발견
- **맥락**: 코퍼스 45건 중 **응급 지침 자료가 전부 미국 것이다.**
  ASPCA · FDA · Pet Poison Helpline · FOUR PAWS · Cornell · Banfield 가 하나같이
  말미에 24/7 핫라인을 단다.

  | 번호 | 기관 | 등장 자료 |
  |---|---|---|
  | 855-764-7661 | Pet Poison Helpline | S-027 · S-029 · S-085 · S-100 |
  | 888-426-4435 | ASPCA APCC | S-007 · S-029 · S-100 |
  | 855-289-0358 · 855-454-4130 | FOUR PAWS 안내 | S-030 |

  검수 전에는 **이 번호들이 서로 상충하는 줄 알았다.** 대조해 보니 아니었다 —
  기관이 다르거나, 같은 기관을 다른 자료가 다르게 적은 것이다. 진짜 문제는 따로 있었다.

  > **국내 사용자가 이 번호로 전화하면 아무 일도 일어나지 않는다.**
  > 응급 상황에서 걸리지 않는 번호는 오답보다 나쁘다 — 전화를 걸어보는 시간만큼
  > 병원 도착이 늦어지기 때문이다.

- **대안**
  1. **사실 표에서 해당 행 삭제** — 연락처가 본문인 행을 뺀다.
     번호와 함께 *"무엇이 위험한가"* 같은 나머지 근거까지 잃는다. **기각**
  2. **국내 연락처로 치환** — 국내에 미국 APCC 에 대응하는 **공식 중독 상담 창구가 없다.**
     있는 것처럼 적으면 그게 곧 환각이다. **기각**
  3. **④ 근거 검증에 맡긴다** — 검색 근거에 없는 번호이므로 `근거없음` 으로 잡혀야 한다.
     맞는 말이지만 **판정은 보장이 아니다.** 보조 수단으로만 둔다. **기각**
  4. **출력 직전에 코드가 뺀다** ← 채택

- **결정**: `safety/contacts.py` 의 `scrub_contacts()` 를 그래프 마지막 노드
  `finalize` 에서 부른다. 데이터는 **그대로 둔다.**

  - **문장 단위로 뺀다.** 번호만 지우면 *"ASPCA APCC에 연락하세요"* 가 남는데
    국내 사용자에겐 여전히 오답이다. ④ 검증이 이미 쓰는 조치와 같은 방식이다
  - 기관명이 나와도 **행동 지시가 없으면 살린다** —
    *"APCC 자료에 따르면 아보카도는 조류에게 치명적이다"* 는 출처 인용이다
  - 뺐으면 `"가까운 동물병원이나 24시 동물병원으로 바로 연락해 주세요"` 를 붙인다.
    **존댓말이다** — 청크는 검색 대상이라 평서체(`~다`)로 두지만
    화면에 뜨는 문장은 보호자가 응급 상황에서 읽는 말이라 명령조가 되면 안 된다
  - **국내 번호도 함께 막는다.** 지금 코퍼스에 검증된 국내 연락처가 0건이라
    통과시킬 근거가 없다. 검증된 창구가 생기면 그때 예외를 명시적으로 뚫는다
  - 거절·되묻기 응답도 통과시킨다 — 거절 문구에 연락처를 넣는 실수를 막는다
  - 뺀 문장은 `state["removed_contacts"]` 에 남긴다.
    **무엇을 뺐는지 기록하지 않으면 검증할 수 없다** (04 §8)

- **트레이드오프**: 전부 연락처인 답은 안내 문장 하나만 남는다. 답이 빈약해지지만
  **틀린 번호를 주는 것보다 낫다.** 오탐 위험은 용량 수치다 —
  `2.8-36.4 g/kg` 이 번호로 잡히면 답변이 통째로 사라지므로,
  숫자 뒤 단위(`mg`·`g`·`kg`·`mL`·`%`·`IU`·`kcal`)를 보고 제외한다.
  `tests/test_contacts.py` 가 이 경우를 고정한다.
- **비고**: 데이터를 고치지 않고 계층을 하나 더 두는 것은 D-40 과 같은 판단이다.
  **원문 충실성(D-30)과 사용자 안전이 부딪힐 때, 원문은 남기고 출력에서 막는다.**
- **부수 결정 — 출력 문체**: 사용자에게 나가는 문장은 **존댓말**로 쓴다.
  코퍼스 청크는 검색 대상이라 `ingest/templates.py` 가 평서체(`~다`)로 문장화하지만,
  화면에 뜨는 답변은 보호자가 **응급 상황에서** 읽는 말이다. 명령조가 되면 안 된다.
  `safety.GUIDANCE` 가 기준 문체이고, ⑤ 평이화 노드가 이를 따른다
  (`graph/nodes/generate.py::simplify` 계약에 명시).
- **영향**: 02 §2 · 05 §4 · `safety/contacts.py` · `graph/nodes/generate.py` ·
  `graph/nodes/__init__.py` · `graph/state.py` · `tests/test_contacts.py` · D-30 · D-38 · D-40

---

### D-48 · 팀원 코드는 **계약·주입·설정 구조 안으로 흡수한 뒤** 병합한다 🔒

- **상태**: 확정 (2026-08-01) · PR#3 검수에서 도출
- **맥락**: 이근준 팀원이 계정·프로필 백엔드 424줄을 올렸다. **동작은 한다.**
  문제는 이 저장소가 이미 정해 둔 자리를 쓰지 않고 **자기 자리를 새로 만들었다**는 것이다.

  | 우리가 정해 둔 곳 | 들어온 코드 | 무엇이 깨지나 |
  |---|---|---|
  | `contracts.py` — 계약 단일 출처 (D-40) | 라우터 파일마다 스키마 6종 | 프론트가 어디를 봐야 하는지 모른다 |
  | `deps.py` — 주입 지점 하나 | 라우터가 `database.get_db` 직접 호출 | `dependency_overrides` 로 테스트를 못 끼운다 |
  | `Species` Literal | `Literal["dog","cat","bird"]` 재정의 | 종이 늘면 두 곳을 고쳐야 한다 (D-22) |
  | `configs/*.yaml` — 파라미터 (D-41) | `Secrets` 에 만료·알고리즘 | 비밀이 아닌 값이 `.env` 로 샌다 |
  | `app/auth.py` — 인증 구현 | 라우터가 `import jwt` | 라이브러리를 바꾸면 라우터까지 바뀐다 |

  **각각은 사소하다. 합치면 구조가 두 벌이 된다.**

- **대안**
  1. **그대로 병합하고 나중에 정리** — "나중"이 오지 않는다. 그 위에 프론트가 붙으면
     계약이 두 벌인 채로 굳는다. **기각**
  2. **반려하고 팀원에게 고치게 한다** — 마감 3일 전이고, 구조는 문서에 있지 코드에
     강제돼 있지 않았다. **못 지킨 쪽이 아니라 강제하지 않은 쪽의 문제다.** 기각
  3. **팀장이 흡수한 뒤 병합** ← 채택

- **결정**: 받되 **우리 자리로 옮긴다.** 기능은 그대로 두고 배치만 바꾼다.
  이번에 실제로 한 것은 아래 여섯이다.

  | # | 조치 |
  |---|---|
  | 1 | 스키마 6종 → `contracts.py` (`EmailStr`·`Species`·`ConfigDict` 로 통일) |
  | 2 | `deps.get_db`·`deps.get_current_user_id` 신설 — 라우터는 여기만 본다 |
  | 3 | `auth.py` 가 `TokenExpiredError`/`TokenInvalidError` 로 번역 — 라우터에서 `jwt` 제거 |
  | 4 | 만료·알고리즘 → `configs/*.yaml` 의 `auth` 절. `.env` 에는 서명 키만 |
  | 5 | `[db]` extra + `constraints.txt` 핀 + CI `db-deps` 잡 |
  | 6 | `tests/test_auth_api.py` 28건 — SQLite 인메모리라 **DB 서버 없이 돈다** |

- **왜 6번이 결정의 핵심인가**

  > 424줄이 들어왔는데 **CI 에서 한 줄도 실행되지 않고 있었다.**
  > `DATABASE_URL` 이 없으면 라우터를 임포트조차 안 하는 구조라, `ruff` 가 문법만 보고 지나갔다.

  그래서 테스트를 붙이자마자 **`passlib 1.7.4` 가 `bcrypt>=4.1` 과 깨져
  9바이트 비밀번호에서도 `ValueError` 를 내는 것**이 드러났다.
  **회원가입이 한 건도 되지 않는 상태였다.** 시연 당일에 발견됐을 문제다.

  → `passlib` 을 걷어내고 `bcrypt` 를 직접 쓴다. 유지보수가 2020년에 끊긴 의존성이고,
  대체하면 72바이트 절단도 우리가 명시적으로 막을 수 있다 (`PasswordTooLongError`).

- **함께 고친 안전 항목**

  | 항목 | 고친 것 |
  |---|---|
  | 서명 키 | `"change-me-in-production"` 기본값 제거 → 없으면 `JWTKeyMissingError` |
  | 의존성 누락 | `ImportError` 를 로그로 삼키던 것 → **기동 중단** (04 §8) |
  | 되묻기 슬롯 | `ChatSession` 에서 `clarify_turns`·`weight_kg`·`amount_g` 제거 (D-36 · 05 §3) |
  | 시각 | `datetime.utcnow()` naive → `datetime.now(UTC)` + `DateTime(timezone=True)` |
  | 가입 경합 | 조회-삽입 사이 `IntegrityError` → 500 이던 것을 409 로 |
  | 세션 누수 | `get_db` 에 `rollback()` — 열린 트랜잭션이 풀로 돌아가던 것 |
  | 계정 열거 | 남의 반려동물 조회를 403 아닌 **404** 로 (403 은 "존재는 한다"를 알린다) |
  | 예외 유출 | `raise ... from None` — 스택에 키·알고리즘이 실리지 않게 |

- **트레이드오프**:
  > 팀원 코드를 그대로 받으면 구조가 두 벌이 되는 문제를 풀기 위해 팀장 흡수를 택했고,
  > 그 덕분에 **계약·주입·설정이 한 벌로 유지된** 대신,
  > **작성자와 다른 사람이 코드를 고치게 되어** 이근준 팀원이 자기 코드의 최종형을
  > 다시 읽어야 하는 비용이 생겼다. 그래서 바꾼 자리마다 **왜 옮겼는지를 주석으로 남겼다.**
- **비고**: 다음 병합부터는 **흡수 비용을 줄이는 쪽**이 낫다 —
  새 라우터를 만들기 전에 `contracts.py`·`deps.py` 를 먼저 보게 하는 것으로 충분하다.
  구조를 문서에만 두고 코드로 강제하지 않으면 같은 일이 반복된다.
- **미결 해소 (같은 날) — 관계형 DB 는 MySQL. 컨테이너로만 띄운다**

  `compose.yaml` 이 pgvector 이미지(Postgres)를 띄우고 있었는데 **D-44 로 벡터 용도가
  사라진 잔재**였다. 그대로 두면 `docker compose up` 이 아무도 안 쓰는 DB 를 띄운다.

  | | |
  |---|---|
  | 이미지 | `mysql:8.4` · utf8mb4 |
  | **로컬 설치** | **하지 않는다.** `make db` (= `docker compose up -d db`) |
  | 드라이버 | `PyMySQL` 만. `psycopg` 는 `[rag]` 쪽에만 남는다 |
  | `docker/initdb/` | **삭제.** pgvector 확장 생성 스크립트였고 MySQL 에선 돌지도 않는다 |
  | 테이블 생성 | `python -m pettriage.app.init_db` (SQLAlchemy `create_all`) |

  **팀원 PC 에 설치시키지 않는 이유** — 버전이 제각각이면 *"내 PC에선 되는데"* 가 시작된다.
  컨테이너면 넷이 같은 8.4 를 쓰고, 마감 뒤 `docker compose down -v` 로 흔적 없이 지운다.

  개발에는 **DB 가 아예 없어도 된다.** `DATABASE_URL` 을 비우면 앱이 3라우터로 정상 기동하고,
  `tests/test_auth_api.py` 는 SQLite 인메모리로 라우터까지 통째로 돈다 —
  **그래프 노드 작업은 MySQL 없이 진행할 수 있다.**
- **영향**: `app/contracts.py` · `app/deps.py` · `app/auth.py` · `app/models.py` ·
  `app/database.py` · `app/routes/*` · `config.py` · `configs/default.yaml` ·
  `pyproject.toml` · `constraints.txt` · `.github/workflows/ci.yml` ·
  `tests/test_auth_api.py` · `compose.yaml` · `Makefile` · `docker/` · 02 §11 ·
  D-17 · D-22 · D-29 · D-36 · D-40 · D-41 · D-44 · 04 §8

---

### D-49 · 증상 질의는 **검색으로 원인을 찾지 않는다** 🔒

- **상태**: 확정 (2026-08-01) · 검색 점검에서 발견
- **맥락**: 확장한 점검에 증상 질의를 하나 넣었더니 이렇게 나왔다.

  ```
  질의    고양이가 토하고 밥을 안 먹고 배를 아파해요
  1위     0.616  "고양이에게 토마토는 조건부로 분류된다.
                  주요 증상은 과도한 침 흘림, 식욕 부진, 침울, 쇠약, 동공 확대, 서맥이다."
  ```

  처음에는 `토하고`/`토마토` 의 표면 형태가 겹친 줄 알았다. **아니었다.**
  *밥을 안 먹고* → **식욕 부진**, *배를 아파해요* → **침울·쇠약**.
  **검색은 제대로 일했다** — 증상 목록이 실제로 맞는다.

  문제는 검색이 아니라 **질문의 방향**이다.

  > D-14 로 청크를 **물질 단위**로 만들었다. 각 청크는 *"이 물질 → 이런 증상"* 이다.
  > 증상만 주면 그 화살표를 **거꾸로** 타는데, **역방향은 일대일이 아니다.**

  고양이 대상 청크 418건 중 **21건이 같은 증상 조합**(구토·식욕부진·침흘림·무기력·복통)을
  나열한다 — 토마토 · 알리움류 · 백합류 · 소철 · 진달래 · 봄 구근 식물 ·
  메탄올(워셔액) · 가정용 청소용품 · 대황 잎 · 췌장염 …

  **무엇이 1위가 되든 근거로는 임의다.** 토마토가 이긴 것은 문장이 짧고 증상만
  촘촘히 나열해서지 더 그럴듯해서가 아니다.

  그리고 이것이 D-11 위반으로 이어진다. 1위를 근거로 답을 만들면
  *"토마토 중독일 수 있습니다"* 가 된다 — **증상에서 원인을 지목하는 것이 곧 진단이다.**

  > 같은 이유로 **점검의 기대값 자체가 틀렸다.** 그 질의의 정답을 `췌장염` 으로 적어 두었는데,
  > 증상에서 질환을 짚는 것도 똑같은 진단이다. **통과했어도 틀린 통과였을 것이다.**

- **대안**
  1. **증상 → 물질 역인덱스를 만든다** — 21종이 같은 증상을 공유하므로
     역인덱스를 만들어도 확률적 추정이 되고, 그 추정이 곧 진단이다. **기각**
  2. **증상 청크(`doc_type=symptom`)를 늘려 정면으로 답한다** — 지금 22청크뿐이고,
     늘려도 *"이 증상은 이 병"* 을 말하게 되어 D-11 을 정면으로 어긴다. **기각**
  3. **되묻기로 보낸다** ← 채택

- **결정**: `intent=symptom` 이고 질의에 **물질이 없으면 검색으로 원인을 찾지 않는다.**

  ```
  ① 분류   intent=symptom
  ② 슬롯   "무엇을 먹었는지 아시나요?"  → 되묻기
             · 물질을 알면 → 중독 경로로 넘긴다 (검색이 정상 동작하는 영역)
             · 모르면     → 증상 조합으로 **등급만**. 물질·질환을 지목하지 않는다
  ```

  **등급 판정에 물질이 꼭 필요하지는 않다** (D-39). *"구토 + 식욕 부진 + 복통이
  함께 있으면 CALL_NOW"* 는 물질을 몰라도 말할 수 있고, 그것이 트리아지의 본래 역할이다.
  D-11 이 포기한 것은 *"무슨 병인가"* 이지 *"얼마나 급한가"* 가 아니다.

- **검사로 고정한 것**

  | 어디 | 무엇 |
  |---|---|
  | `scripts/build_index.py` | 증상 질의를 양성 프로브에서 빼고 `SYMPTOM_PROBES` 로 옮겼다. **판정하지 않고 모호도만 보고한다** — 상위 결과가 몇 종으로 흩어지는지, 점수 폭이 얼마인지 |
  | `scripts/check_goldenset.py` | `intent=symptom` + `answered` 인데 `must_not_contain` 이 비면 WARN. **무엇을 말하면 안 되는지가 없으면 채점이 성립하지 않는다** |

  **`build_index` 에서 초록·빨강을 매기지 않은 이유** — 올바른 동작은 ①②노드의 몫인데
  그 스크립트는 저장소만 들고 있어 검증할 수 없다. **검증할 수 없는 것에 판정을 찍으면
  그 초록불이 곧 거짓 근거가 된다** (04 §8). 대신 무엇이 가려져 있는지는 출력한다.

- **트레이드오프**:
  > 증상 질의가 임의의 물질을 근거로 물어오는 문제를 풀기 위해 되묻기 경로를 택했고,
  > 그 덕분에 **진단으로 미끄러지는 경로를 구조로 막은** 대신,
  > **"뭘 먹었는지 모르겠어요" 라고 답하는 보호자에게 되묻기가 한 번 더 걸리는**
  > 마찰을 감수했다. 되묻기 상한이 2회(02 §9)라 그 안에서 끝나야 한다.
- **비고**: `intent=symptom` 에 `answered` 자체는 **정당하다.** 골든셋 `G-010`
  (*"배가 갑자기 빵빵해지고 헛구역질만 계속해요"* → EMERGENCY)이 그 예다 —
  위확장염전은 물질과 무관하고 등급만으로 답이 성립한다.
  금지하는 것은 `answered` 가 아니라 **원인 지목**이다.
- **실측 (같은 날 재적재)**: 증상 질의 3건 모두 **상위 5가 서로 다른 5종**이고
  **점수 폭이 0.019~0.042** 였다. 양성 프로브의 1위 분포 폭(0.547~0.728 = 0.18)과 비교하면
  한 자릿수 배 좁다 — **1위와 5위의 차이가 측정 잡음과 같은 크기다.**

  | 질의 | 상위 5 |
  |---|---|
  | 고양이 · 구토+식욕부진+복통 | 토마토 · 초콜릿 · 췌장염 · 알리움류 · 초콜릿 |
  | 개 · 침흘림+무기력 | 청소용품 · 천식 · 석유증류물 · 울혈성 심부전 · 흉막 질환 |
  | 앵무새 · 깃털 부풀림 | 저체온 · 깃 고르기 · 골절 · 화상 · 페놀계 세정제 |

  개 질의는 **중독과 내과 질환이 뒤섞여** 있다. 하나를 고르는 것은 나머지 넷을
  배제하는 것이고, **그 배제가 곧 진단이다.** 04 §2.5.5에 표로 남겼다.
#### D-49 후속 — **물질을 서술로만 준 질의도 같다.** 다만 실패 방식이 다르다

같은 점검에서 하나가 더 나왔다.

```
질의    강아지가 차고 바닥에 흘린 달콤한 액체를 핥았어요
1위     0.568  "개의 강박장애(CCD) … 앞발·다리 핥기·씹기 …"  (S-080)
```

질의의 *"핥았어요"* 가 청크의 *"핥기"* 에 표면적으로 걸린 것이다.
그런데 그것보다 중요한 사실이 있다.

> **코퍼스에 `달콤`·`단맛`·`차고` 가 0건이다.**
> 부동액 청크는 *"개에서 에틸렌글리콜(부동액)은 응급 상황이다"* 뿐이고 **단맛 서술이 없다.**
> 검색이 이것을 맞힐 방법이 애초에 없다.

**맞혔다면 그게 더 문제다.** 근거가 우리 문서에 없으므로, 맞혔다는 것은
임베딩의 사전지식이 답을 만들었다는 뜻이다 — 결과가 우연히 옳아도 **환각의 정의 그대로**다.
이 프로젝트가 막으려는 바로 그것이다.

| | 증상 질의 | 물질 서술 |
|---|---|---|
| 검색 결과 | 후보 5종이 **0.019 차로 동점** | 후보가 **코퍼스에 없음** |
| 하나를 고르면 | 나머지를 배제 = **진단** | 근거 없는 추측 = **환각** |
| 처방 | **되묻기** | **되묻기** |

**실패 방식은 다른데 처방이 같다** — 사용자가 모르는 것을 시스템이 추측하지 않는다.
골든셋이 이미 그렇게 되어 있다: `G-014` *"마당에서 뭔가를 주워 먹었는데 그게 뭔지 모르겠어요"* → `clarify`.

- **조치**: 이 질의를 양성 프로브에서 빼고 `UNKNOWN_SUBSTANCE_PROBES` 로 옮겼다.
  **판정하지 않고 보고만 한다** — 사람이 아는 정답(`에틸렌글리콜`)이 상위에 **없는 것이 정상**이고,
  **있으면 그때 표시**해서 코퍼스 근거인지 임베딩 사전지식인지 확인하게 한다
- **비고**: (다) 안 — *"코퍼스에 단맛 서술을 추가한다"* 도 가능하다. S-084 원문에 있다면
  사실 표에 넣어야 하고, 그러면 검색이 **정당하게** 맞힌다.
  다만 넣더라도 **되묻기가 옳다는 것은 변하지 않는다** — 차고의 달콤한 액체가
  부동액일 가능성이 높을 뿐 브레이크액(디에틸렌글리콜)일 수도 있고, 둘을 가르는 것은
  보호자가 통을 확인하는 일이지 우리가 고를 일이 아니다

- **미결**: 증상 조합 → 등급 규칙을 어디에 둘 것인가. 규칙 테이블(`compute/rules.py`)은
  물질 × 종 기준이라 증상 축이 없다. 노드 구현 때 정한다
- **영향**: 02 §2·§9 · 04 §2.5.4 · 04a · `graph/nodes/classify.py` · `graph/nodes/slots.py` ·
  `scripts/build_index.py` · `scripts/check_goldenset.py` · D-11 · D-14 · D-39 · D-46

---

### D-50 · `rule_level` 은 **정밀한 판정이 아니라 바닥**이다 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: `apply_gate(rule_level, llm_level)` 는 구현돼 있는데 **`rule_level` 을 만드는 규칙이
  어디에도 없었다.** `StubEngine` 이 하드코딩된 고정 지식으로 우회하고 있어 드러나지 않았다.

  ```
  정량임계치.csv    초콜릿 dog  20 mg/kg  임상징후 발현
                              40-50      중증
  compute/rules.py  체중당 섭취량 계산                  ✅
         ???        20 을 넘었다 → 어느 등급?           ❌ 정의 없음
  apply_gate        rule_level 을 받는다                 ✅
  ```

  `SEVERITY = {"임상징후 발현": 1, "중증": 2, "치사": 3}` 은 **정렬용 순서**일 뿐
  4등급과 무관하다.

- **프레이밍 교정** — 처음에는 *"3단계 역치를 4등급에 어떻게 대응시킬까"* 로 봤는데 틀렸다.

  > `final = max(rule_level, llm_level)` 이므로 **LLM 은 올릴 수만 있다** (D-09).
  > 규칙이 하는 일은 정확한 등급을 맞히는 것이 아니라
  > **"여기 아래로는 못 내려간다"를 못박는 것**이다.

  그래서 물어야 할 것은 *"이게 의학적으로 맞나"* 가 아니라
  **"이 바닥이 틀렸을 때 어느 쪽으로 틀리나"** 다.

  그리고 데이터를 보면 실질 결정은 **셋이 아니라 둘**이다.

  | | 행 수 | |
  |---|---|---|
  | `임상징후 발현` | **9행 (75%)** | 사실상 이것이 기본 등급이다 |
  | `중증` | 3행 | |
  | `치사` | **0행** | 계산 가능한 행이 없다 (주목은 `computable=N`) — 방어용 |

- **대안**
  1. **한 칸 높게** — 중증·치사를 모두 `EMERGENCY`, 임상징후 발현을 `CALL_NOW`
  2. **역치 이름 그대로** — 치사 `EMERGENCY` / 중증 `CALL_NOW` / 임상징후 발현 `VISIT_SOON`.
     **기각** — `VISIT_SOON` 은 *"오늘 중 진료"* 인데 **자일리톨 `0.03 g/kg` 은 4kg 개에게
     껌 한 조각이고 저혈당이 30분 안에 온다.** 단일 매핑이 물질별 발현 속도를 못 담는다
  3. **역치 미만은 `rule_level=None` 으로 두고 LLM 에 맡긴다** — **기각.**
     *"조금 먹었는데요"* 가 가장 흔한 질의인데, LLM 이 실패하면 `apply_gate` 가
     `ValueError` 로 거절한다. **가장 흔한 질문이 가장 잘 깨지는 설계**가 된다

- **결정**: (1). `compute/rules.py::rule_level_for()` 로 구현했다.

  | 조건 | 바닥 |
  |---|---|
  | `치사` 초과 | `EMERGENCY` |
  | `중증` 초과 | `EMERGENCY` |
  | **`임상징후 발현` 초과** | **`CALL_NOW`** ← 상향 여지를 남기는 자리 |
  | 역치 미만 | `MONITOR` + `signs` 를 상승 조건으로 |

  **바닥이 천장이 되지 않는다.** 9행짜리 `임상징후 발현` 이 `CALL_NOW` 라
  LLM 이 `EMERGENCY` 로 올릴 수 있고, **D-09 게이트의 증거(`overridden=True`)가 거기서 나온다.**
  천장이 되는 것은 3행뿐이다.

  **중증을 `EMERGENCY` 로 올린 이유**는 값을 보면 분명하다 —
  초콜릿 40-50 mg/kg 은 4kg 개가 다크초콜릿 20g 정도로 경련·부정맥 구간이고,
  자일리톨 0.5 g/kg 은 간부전 구간이다. *"지금 전화"* 로 답하면 그것이 과소평가다.

- **함께 결정한 것 — 출처 간 수치가 어긋날 때**

  같은 물질에 여러 자료가 다른 값을 주면 **가장 낮은 것을 바닥으로 쓴다** (가장 먼저 넘는다).
  다만 단서를 단다.

  > **같은 (물질 × 종 × 역치종류)에서 최대/최소가 10배 이상 벌어지면 정량 판정을 포기한다.**

  S-034 는 건포도를 본문에서 `2.8 mg/kg`, **같은 논문 Table 1 에서 `2.8-36.4 g/kg`** 로 적는다 —
  **1,000배 차이**다. 낮은 쪽 원칙만 두면 단위 오류가 섞이는 순간
  **거의 모든 섭취가 역치 초과**가 된다.

  **10배는 단위 오류(1,000배)와 반올림 차이(1.2배)를 가르는 선**이다.
  포기는 실패가 아니라 **정성 답변으로 내려가라는 신호**이고, 사유가 로그에 남는다 (D-46 · 04 §7).

  **역치종류를 넘어 비교하지 않는다** — `임상징후 발현 20` 과 `중증 60` 은
  상충이 아니라 **단계**다. 섞어 재면 정상 데이터를 상충으로 잡는다.

  > **부수 소득 — 서로 다른 출처가 같은 값으로 수렴하는 것이 확인됐다.**
  > S-014 는 알리움류를 `0.5%`, S-034·S-098 은 양파를 `5 g/kg` 로 적는데
  > 환산하면 **둘 다 5,000 mg/kg** 이다. `%` 는 체중 대비 백분율이라 1% = 10 g/kg 이다.
  > `tests/test_rules.py` 가 이 수렴을 고정한다 — 어긋나면 상충 검사가 잡는다.

- **함께 바꾼 것**: `build_rule_table.py` 에 **`signs` 열 추가**.
  역치 미만 `MONITOR` 의 상승 조건이 없으면 `apply_gate` 가 `MonitorWithoutConditions` 를 던지고
  부르는 쪽이 거절로 바꾼다 — **역치를 안 넘긴 질의가 전부 거절**이 된다 (D-39).

- **트레이드오프**:
  > 바닥을 만드는 규칙이 없어 `decide_triage` 를 쓸 수 없던 문제를 풀기 위해
  > 보수적 매핑을 택했고, 그 덕분에 **과소평가가 구조적으로 억제된** 대신,
  > **중증과 치사가 같은 배지로 표시되어** 문서가 나눈 것을 화면에서 지우게 됐다 (D-30과 상충).
  > 다만 근거 문장에는 *"치사량"*·*"중증"* 이 그대로 인용되므로 **정보가 아니라 배지만 같아진다.**
  > 그리고 과대평가율이 오르므로 **04 §5 비교표에 의도된 편향임을 밝히고 함께 보고한다** —
  > 숨기면 문제고 적으면 설계 선택이다.
- **미결 → D-51 로 넘어감**: `치사` 는 지금 계산 가능한 행이 0건이라
  **실제로 검증되지 않은 분기**다. 정확히는 *행이 없어서*가 아니라
  **단위가 환산되지 않아서**이며, 그 판단을 **D-51** 에 따로 기록했다
- **영향**: `compute/rules.py` · `scripts/build_rule_table.py` ·
  `graph/nodes/compute.py` · `graph/nodes/triage.py` · `tests/test_rules.py` ·
  04 §4.1 · 07 §6 · D-09 · D-13 · D-16 · D-30 · D-39 · D-46

---

### D-51 · **식물 부위 단위(`g leaves/kg`)는 체중당 용량으로 환산하지 않는다** 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: D-50 의 `치사` 분기가 *"계산 가능한 행 0건"* 이라 검증되지 않았다고 적었는데,
  888행을 다시 세어 보니 **행은 있었다.**

  ```
  F-030-010   dog   주목(yew)   2.3   g leaves/kg   치사
  ```

  `_MG_PER_KG = {"mg/kg": 1.0, "g/kg": 1000.0, "%": 10000.0}` 에 `g leaves/kg` 가 없어
  `to_mg_per_kg()` 가 `None` 을 돌려주고, 그래서 **역치 비교에 닿지 않는다.**

  즉 *"자료가 없다"* 가 아니라 **"단위 문자열 하나 때문에 EMERGENCY 근거 1건이 조용히 버려진다"** 였다.

- **결정**: **넣지 않는다.** `g leaves/kg` 를 `g/kg` 로 취급하지 않는다.

- **근거** — 산술은 되지만 **의미가 다르다.**

  | | `g/kg` | `g leaves/kg` |
  |---|---|---|
  | 재는 대상 | **독성 물질**의 질량 | **식물 조직**의 질량 |
  | 독소 함량 | 정의상 100% | **모른다** — 품종·계절·부위마다 다르다 |

  주목의 택신(taxine) 함량은 출처에 없다. `2.3 g leaves/kg → 2,300 mg/kg` 으로 환산하는 순간
  **잎 무게를 독소 무게로 바꿔치기한 숫자**가 되고, 그 숫자가 EMERGENCY 를 판정한다.

  > **D-38 이 금지한 것과 같은 종류다.** 코드가 원문에 없는 값을 만들면 안 된다.
  > 여기서는 문장이 아니라 **숫자**를 만드는 것이라 더 위험하다.

- **대신 하는 것**: 환산 실패를 **버리지 않고 드러낸다.**
  `to_mg_per_kg()` 가 `None` 을 돌려준 행은 `RuleVerdict.reason` 에 사유를 남기고,
  정량 판정 대신 **정성 경로**(증상 기반 · 조류와 같은 취급)로 내려간다.
  침묵하는 대신 **왜 정량으로 못 갔는지가 로그에 남는다.**

- **트레이드오프**:
  > 주목은 **개에게 치명적**인 것이 알려진 식물인데, 이 결정으로 그 행은
  > 정량 EMERGENCY 를 만들지 못한다. 다만 `signs` 로 정성 경로를 타고
  > LLM 판정이 `max()` 로 올릴 수 있으므로 **판정이 사라지는 것은 아니다.**
  > 안전을 잃는 대신 **우리가 만든 숫자로 응급을 선언하지 않는다.**

- **남는 사실**: `치사` 는 **여전히 실제로 검증되지 않은 분기**다.
  EMERGENCY 는 `중증` 3행(초콜릿 ×2 · 자일리톨)이 담당하고 있으며,
  04 §4.1.x 에 이 상태를 그대로 밝힌다.

- **영향**: `compute/rules.py` · `tests/test_rules.py` · 04 §4.1.x · 04 §7.2 ·
  D-30 · D-38 · D-50

---

### D-52 · 반려동물 프로필은 **자동으로 주입하지 않고 확인을 받는다** 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: `AskRequest.pet_id` 가 계약에 있고 `Pet.weight_kg` 가 MySQL 에 있는데,
  **둘을 잇는 코드가 없다.** `routes/ask.py` 는 `get_db` 를 주입받지 않고,
  `Session.merge(req)` 는 요청 본문만 복사한다.

  ```
  02 §2 다이어그램   "프로필·기록 조회 — pet_id 기반 내부 데이터"   ← 그림에만 있다
  routes/ask.py      sessions.get_or_create() → engine.ask()        ← DB 를 모른다
  ```

  그래서 **로그인한 사용자가 등록해 둔 강아지로 물어도 "체중이 어떻게 되나요?" 를 되묻는다.**

- **결정 세 가지**

  **① 프로필 값을 슬롯에 자동으로 채우지 않는다. 사용자에게 확인을 받는다.**

  ```
  등록된 코코(말티즈)의 체중이 5.2kg 으로 되어 있습니다. 지금도 맞나요?
    → 예     : 그 값으로 판정. 감사 정보에 weight_source=profile_confirmed
    → 아니오 : 현재 체중을 되묻는다 (기존 경로)
  ```

  **② 판정 경로의 DB 조회는 ORM 으로 한다. `NULL` 을 `NaN` 으로 바꾸지 않는다.**

  **③ *"미정"* 은 값이 아니라 값이 없다는 뜻이므로 `NULL` 로 저장한다.**
  화면에는 "미정"으로 표시하되 DB 컬럼은 비운다.

- **근거 ① — 등록 체중은 낡는다.**
  강아지는 몇 달 만에 두 배가 된다. 6개월 전 3kg 으로 등록된 개가 지금 7kg 인데
  3kg 으로 mg/kg 을 계산하면 값이 2.3배로 부풀고, 반대(성견 체중 감소)면 **과소평가**다.
  **우리가 가진 낡은 숫자로 응급을 선언하지 않는다** — D-51 과 같은 계열이다.

  되묻기를 없애는 것이 아니라 **되묻기를 확인으로 바꾼다.** 되묻기 횟수는 그대로이고
  (02 §2 의 환각 방지 시연도 그대로 살아 있다), 판정 근거가
  *"프로필에 있던 값"* 이 아니라 **"사용자가 확인한 값"** 으로 남는다.

- **근거 ② — pandas 를 통과시키면 미등록 체중이 조용히 판정을 내린다.**

  `weight_kg` 는 `nullable=True` 다. `pd.read_sql` 로 읽으면 그 `NULL` 이 **`NaN`** 이 되고,
  `NaN` 은 `float` 이라 **`weight_kg is None` 검사를 그대로 통과한다.**

  | 저장 | pandas | DB-API / ORM |
  |---|---|---|
  | `NULL` | `nan` · `float` · `is None → False` | `None` · `NoneType` · `is None → True` |

  그다음이 문제다. **`NaN` 은 어떤 비교에도 `False` 를 돌려준다.**

  ```
  체중 nan · 밀크초콜릿 60g → nan mg/kg
    nan >= 20 (CALL_NOW)  → False
    nan >= 40 (EMERGENCY) → False
  → 역치를 하나도 안 넘긴 것으로 판정 → BELOW_THRESHOLD_LEVEL = MONITOR
  ```

  **체중을 모르는데 "관찰하세요" 가 나간다.** 되묻기도, 하향 금지 게이트도 안 걸린다 —
  **에러가 안 나서 더 위험하다.** 로그에도 남지 않는다.
  `species` 도 같다. `NULL → NaN` 이면 D-10 의 종 미확인 강제 되묻기가 뚫린다.

  > 실측으로 확인했다 (2026-08-02). `sqlite3` + `pandas 2.x` 에서 위 표 그대로 재현된다.

- **근거 ③ — `"미정"` 을 값으로 넣으면 컬럼이 오염된다.**
  `weight_kg` 를 `String` 으로 바꿔 `"미정"` 을 담으면 pandas 컬럼 dtype 이 `object` 가 되어
  실수와 문자열이 섞이고, `pd.to_numeric(errors="coerce")` 를 쓰면 **다시 `NaN`** 이다.
  타입이 값의 의미를 지키게 둔다.

- **그럼 pandas 는 어디에 쓰나** — **집계**다.

  | 무엇 | 도구 |
  |---|---|
  | 프로필 한 건 조회 (`pet_id` → 체중) | **ORM** — `None` 을 지킨다 |
  | **기간 리포트** (02 §5) — 누적 기록 N일치 타임라인·증상 추이 | **pandas** |
  | 평가 결과·사실 표 집계 | pandas 가능 |

  **경계는 "`NULL` 이 판정에 흘러드는가" 다.** 판정에 쓰이는 값은 `None` 이 `None` 으로
  남아야 하고, 집계는 `NaN` 이 오히려 편하다.

- **어디에 붙이나**: 그래프 `extract_slots` **앞의 `load_profile` 노드**.
  라우터에만 넣으면 **평가 하네스가 같은 경로를 타지 못해** 지표와 실서비스가 갈린다.
  조회에는 `pets.py` 와 같은 `user_id` 조건을 반드시 함께 건다 —
  `pet_id` 만으로 찾으면 ID 추측으로 남의 반려동물을 읽는다.

- **트레이드오프**:
  > 확인 절차가 한 턴 늘어난다. 프로필이 정확한 사용자에게는 불필요한 질문이다.
  > 그래도 **틀린 체중으로 조용히 판정하는 것보다 낫다** —
  > 이 도메인에서 확인은 비용이고 오판은 사고다.
  > 또한 `load_profile` 이 늘어 노드가 11개에서 12개가 된다.

- **우선순위**: **P2.** 골든셋 52건에 `pet_id` 가 **0건**이라 지금 평가에는 영향이 없다.
  그래프 노드가 먼저다. 다만 **기록은 지금 한다** — 안 적으면 누군가 그냥 자동으로 채운다.

- **영향**: `app/routes/ask.py` · `app/deps.py` · `graph/nodes/slots.py` ·
  `graph/state.py` · 02 §2·§5·§11.1·§12 · 05 §4 · 07 · D-10 · D-36 · D-50 · D-51

---

### D-53 · **벡터DB는 원본이 아니라 인덱스다** — 일일 기록은 지우고 다시 만들 수 있어야 한다 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: 05 §3 이 *"일일 기록은 조각 3(기억)이 아니라 조각 4(RAG)"* 로 못박으면서
  **저장소 칸에 `벡터DB(internal)` 만 적었다.** 그러면 *"기록의 원본이 벡터DB에 있다"* 로 읽힌다.

  일일 기록은 **자주 수정된다.** 급여량 정정, 오타, 증상 서술 보완.
  원본이 벡터DB면 수정이 지옥이 된다.

- **결정**

  | | 원본 (source of truth) | 인덱스 (파생) |
  |---|---|---|
  | 공적 지식 | `data/facts/facts_ohb.csv` **888행** | Chroma `external` 888청크 |
  | **일일 기록** | **MySQL `records`** | Chroma `internal` |

  **이미 이 프로젝트에 선례가 있다.** 사실 표가 원본이고 벡터DB는
  `build_index.py` 가 재생성하는 파생물이다. 지우고 다시 만들어도 아무 일도 안 일어난다.
  **일일 기록도 같은 구조여야 한다.**

  ① 수정·삭제는 **`record_id` 기준 단건 upsert/delete**. 전체 재적재가 아니다
  ② 저장은 **동기 커밋**, 인덱싱은 **비동기**. `RecordCreated.indexed` 가 그 상태를 나른다
  ③ 임베딩 대상 텍스트의 **해시가 같으면 재임베딩하지 않는다**

- **근거 ① — 삭제를 빠뜨리면 유령 청크가 검색된다.**

  ```
  어제 기록  "사료 300g 급여"   → 임베딩 → internal
  사용자 수정 "사료 150g"        → 새 청크만 넣으면
  검색 결과   300g 과 150g 이 **둘 다** 떠오른다
  ```

  공적 지식은 안 바뀌어서 이 문제가 없었다. **기록은 바뀐다.**
  틀린 급여량이 계산 노드에 들어가면 판정이 어긋난다.

- **근거 ② — 삭제권 (D-36).** 사용자가 기록 삭제를 요청했는데 벡터DB에만 남아 있으면
  **삭제가 안 된 것이다.** MySQL 에서 지웠다고 끝이 아니다.
  이건 설계 편의가 아니라 개인정보 요건이다.

- **근거 ③ — 비용은 실측상 문제가 아니다.**

  | | |
  |---|---|
  | 실측 | 888청크 적재 **171.3초** (04 §2.5.3) = **청크당 약 193ms** |
  | 기록 1건 | 1일 1청크 → 임베딩 1회 → **약 0.2초** |
  | API 비용 | **0원** — `bge-m3` 는 로컬 모델이다 |
  | 사용자 100명 × 1기록/일 | 하루 **20초** |

  > ⚠️ **171.3초가 어떤 장비에서 나온 수치인지 04 에 적혀 있지 않다.**
  > CPU 였다면 위 계산이 그대로 맞고, GPU 였다면 CPU 에서는 몇 배 느리다.
  > 04 §8 재현성 표에 **장비 항목을 추가한다.**

  **진짜 비용은 임베딩이 아니라 모델 로딩이다.** `bge-m3` 는 568M 파라미터다.
  **로드 시간은 이 프로젝트에서 아직 측정된 적이 없다** — "수십 초" 라는 통상 관찰은
  있으나 실측이 아니므로 숫자를 문서에 박지 않는다. `BGEEmbedder._ensure()` 가
  로드 시간을 로그로 남기게 했고(`임베딩 모델 로드 완료 — …s`), 처음 도는 사람이
  **04 §8 재현성 표에 적는다.**

  #### 그래서 코드로 막았다 (2026-08-02)

  처음엔 *"앱 프로세스에 상주시킨다"* 를 **문서에만** 적었다. 그런데 코드를 보니
  `get_embedder()` 가 **부를 때마다 새 `BGEEmbedder` 인스턴스**를 만들고 있었다.
  `_ensure()` 가 인스턴스 안에서는 한 번만 로드하므로 안전해 보이지만,
  인스턴스가 매번 새로 생기면 그 캐시가 아무 소용이 없다.

  ```python
  # ✗ 노드 안에서 이렇게 쓰면 — 그리고 이게 자연스러운 코드다
  def retrieve(state):
      emb = get_embedder(cfg.retrieval.embedding_model)   # 매번 새 인스턴스
  ```

  **D-40 이 *"지키기로 한 것이 아니라 못 어기는 것"* 이라고 한 그 자리다.**
  문서로 막는 대신 넷을 넣었다.

  | | 무엇 | 어디 |
  |---|---|---|
  | ① | `get_embedder` 에 **`@lru_cache(maxsize=4)`** — 같은 이름이면 같은 인스턴스 | `retrieval/embedder.py` |
  | ② | **하네스 워밍업 1회** (측정 전 버림) · `--no-warmup` 로 끔 | `eval/harness/run_eval.py` |
  | ③ | **FastAPI `lifespan` 워밍업** — 서버가 로딩을 내고 사용자는 안 낸다 | `app/main.py` |
  | ④ | **`/api/health` 의 `model_loaded`** — 시연 전 확인. `None`=해당없음(stub) / `False`=콜드 | `app/contracts.py`·`routes/meta.py` |

  ②가 없으면 **측정 도구가 자기 측정을 오염시킨다** — `--only` 로 1건만 돌릴 때
  그 한 건이 로딩 시간으로 찍히고, `--fail-over` 게이트가 로딩 때문에 실패한다.

  ③은 **기동을 막지 않는다.** 모델을 못 받는 환경(오프라인 CI · GPU 없는 팀원)에서도
  API 계약과 화면은 돌아야 한다. 대신 로그에 남기고 `model_loaded=false` 로 드러낸다 —
  **조용히 넘어가지 않는다.**

  #### `engine=stub` 이 아닐 때 — 초안의 조건이 어설펐다

  처음 워밍업 조건을 **`cfg.serve.engine == "stub"`** 으로 걸었는데 셋이 틀렸다.

  | 문제 | 무엇이 벌어지나 |
  |---|---|
  | **엔진 생성이 첫 요청까지 미뤄져 있었다** | `engine=graph` 인데 노드가 비어 있으면 **서버는 조용히 뜨고 첫 사용자가 500 을 받는다.** 게다가 워밍업으로 모델을 다 올린 뒤에 실패한다 — 완전한 낭비 |
  | **설정만 보고 실제 엔진을 안 봤다** | `PETTRIAGE_ALLOW_ENGINE_FALLBACK=1` 이면 `graph` 설정이어도 스텁으로 내려간다. **쓰지도 않을 모델을 올린다** |
  | **끌 수가 없었다** | 노드를 고치며 서버를 자주 재시작하면 **매번 로딩을 기다린다** |

  고친 결과:

  ```python
  engine = get_engine()          # ① 기동 시점에 만든다 — EngineUnavailable 이면 여기서 실패
  if engine.name == "stub":      # ② 설정이 아니라 **실제 엔진 이름**을 본다 (폴백까지 잡힌다)
      생략
  elif not cfg.serve.warmup:     # ③ 설정으로 끈다 (D-41 — 파라미터는 configs)
      경고하고 생략
  else:
      warm_up(cfg.retrieval.embedding_model)
  ```

  **①이 별도 소득이다.** `EngineUnavailable` 은 *"스텁으로 내려가면 평가가 오염된다"* 를 위해
  일부러 크게 실패하는 예외인데(04 §8), **그 실패가 기동이 아니라 사용자에게 도착하면 의미가 없다.**
  이제 `make serve` 가 뜨는 순간 알 수 있다.

  `serve.warmup: true` 를 `configs/default.yaml` 에 두었다. 끄는 법:

  ```bash
  PETTRIAGE__SERVE__WARMUP=false make serve   # 재시작이 잦은 개발 중에만
  ```

  > **평가 하네스는 `lifespan` 을 타지 않는다** (FastAPI 앱을 띄우지 않는다).
  > 그래서 하네스가 **자체 워밍업 1회**를 갖는다 — 두 경로가 각자 자기 워밍업을 한다.

  ④는 **모델을 로드하지 않는다.** 상태를 묻는 엔드포인트가 20초짜리 로딩을 일으키면
  헬스체크가 곧 장애가 된다. 이미 만들어진 임베더의 상태만 읽는다.

  > **체감 등급** — 정상 운영에서는 프로세스당 1회라 **낮다.**
  > **시연에서는 높다** — `make serve` 후 첫 질의가 완전한 침묵이 되고,
  > 02 §12.4 로 스트리밍을 안 쓰므로 가릴 방법이 없다.
  > 그리고 ①이 없으면 **매 질의마다** 발생해 등급이 통째로 올라간다.

- **함께 정한 것 — 캐시 금지 목록**

  지연이 문제가 되면 캐시가 먼저 떠오르는데, **캐시는 안전 장치를 건너뛰는 지름길이 되면 안 된다.**
  D-47·D-50·D-52 가 전부 *"출력 직전/판정 직전에 코드가 한 번 더 본다"* 인데,
  캐시는 그 "직전"을 없애는 도구다.

  | 🔴 캐시 금지 | 왜 |
  |---|---|
  | **반려동물 프로필** | D-52 가 *"등록 체중은 낡는다"* 로 정했다. 캐시하면 **더 낡는다** |
  | **트리아지 판정** | 결과를 캐시하면 `max(rule, llm)` **게이트를 건너뛴다** |
  | **`finalize` 이전 출력** | 캐시된 문장에 연락처가 남으면 **히트마다 미국 핫라인이 나간다.** 캐시는 `finalize` **이후**여야 하고, 어려우면 히트해도 `finalize` 는 반드시 태운다 |

  **캐시 키는 이미 갖고 있다** — 재현성 장치가 그대로 쓰인다.

  ```
  키 = 질의 + configs/*.yaml 해시 + model.revision + 사실표 해시
              (D-41)              (1cfa9a72…)      (888행)
  ```

  재적재하거나 `top_k` 를 바꾸면 키가 달라져 **자동으로 무효화**된다.

- **캐시라 부르지 않는 것 셋 — 이건 지금 지킨다.**
  조각 11(캐싱) 판정과 무관하게, 안 하면 손해다.

  | | 안 하면 |
  |---|---|
  | 모델을 프로세스에 상주 | ✅ `get_embedder` `lru_cache` 로 **코드가 강제**한다 (위) |
  | 정량 임계치 테이블 `lru_cache` | 질의마다 CSV 재파싱 (12행짜리다) |
  | 프롬프트 프리픽스 고정 | `models/prompts.py` 가 이미 태스크별 고정이라 **KV 프리픽스 캐시가 그대로 먹힌다.** 안 쓰면 매번 전체 prefill |

- **실제 캐시는 측정 후에 판단한다.** 05 §2 는 조각 11 을 **🔶 선택**으로 판정해 뒀고
  그 판정은 지금도 맞다. 05 §8 체크리스트 첫 줄이
  *"앞 단계의 한계가 **실제로 관측**되었는가"* 인데, **지연을 아직 한 번도 안 재 봤다.**

  그래서 **캐시보다 측정을 먼저 넣었다** — 하네스에 `p50`·`p95`·노드별 분해와
  `--fail-over` 게이트를 붙였다 (04 §4.3). 노드가 붙는 순간 숫자가 나온다.
  **측정 없이 캐시부터 붙이면 나중에 "캐시 때문인지 원래 느린 건지" 를 구분할 수 없다** —
  04 §1.2 가 검색 실패와 생성 실패를 나눈 것과 같은 이유다.

- **트레이드오프**:
  > 저장소가 둘로 늘어 **동기화 책임이 생긴다.** DB 커밋은 됐는데 임베딩이 실패하면
  > 불일치가 남는다. `indexed` 플래그로 드러내되, **재시도·정합성 점검이 숙제로 남는다.**
  > 벡터DB만 쓰면 이 문제가 없지만 수정·삭제를 포기해야 하고, 그건 더 나쁘다.

- **우선순위**: **P3.** 기록 파이프라인 자체가 구현 3단계다.
  다만 **`RecordStore` 를 교체할 때 반드시 보는 결정**이다 — 지금 `add`·`timeline` 뿐이고
  `update`·`delete` 가 없다.

- **영향**: `app/records_store.py` · `retrieval/embedder.py` · `app/main.py` · `app/contracts.py` ·
  `app/routes/meta.py` · `app/deps.py` · `config.py` · `configs/default.yaml` ·
  `graph/nodes/retrieve.py` · `eval/harness/*` ·
  02 §3·§5·§11.1 · 04 §4.3·§8 · 05 §2·§3 · D-14 · D-36 · D-41 · D-52

---

---

### D-54 · **안전 강제는 노드가 아니라 주입·계약에 둔다** 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: 전체 코드 검토(08 §2026-08-02)에서 치명 결함 5건이 나왔는데,
  **다섯 개가 전부 같은 모양**이었다 — *로직은 옳은데 불려지지 않는다.*

  가장 분명한 것이 D-47(연락처 차단)이었다.

  ```
  scrub_contacts 의 유일한 호출부 = graph/nodes/generate.py::finalize
  finalize 는 GraphEngine 전용
  GraphEngine 은 NODES_IMPLEMENTED=False 로 생성조차 되지 않는다
      → 그때까지 /api/ask 응답은 어떤 연락처 필터도 통과하지 않았다
  ```

  D-09(하향 금지 게이트)는 더 미묘했다. `apply_gate()` 의 `max()` 산술은 옳았다.
  문제는 **그 결과를 담는 계약이 덮어쓰기를 허용**한 것이다 —
  `TriageResult(level=1, rule_level=4, llm_level=1)` 이 통과했다.
  **D-09 를 우회하는 길은 게이트를 안 부르는 것이 아니라 부른 뒤 결과를 덮는 것**이었다.

- **대안**
  - (a) 노드 구현 지침에 *"반드시 부를 것"* 을 더 크게 적는다
  - (b) 각 엔진 구현체가 자기 책임으로 부른다
  - (c) **주입 지점이 감싸고, 계약이 최종 확인한다**
- **결정**: (c). D-40 의 연장이다 — *지키기로 한 것이 아니라 못 어기는 것.*

  | 무엇 | 어디로 |
  |---|---|
  | 연락처 차단 | `deps.get_engine()` 이 **항상** `SafetyEngine` 으로 감싼다 + `AskResponse` 가 `full_text` 를 최종 검사 |
  | 게이트 하향 금지 | `TriageResult` 검증기 — `level >= max(rule_level, llm_level)` |
  | 등급 이름·배지 | `level` 에서 **파생**. 넣으면 대조만 한다 |
  | 다이어리 소유자 | `deps.get_owner_id` 주입 |

  `set_engine()` 도 감싼다. 테스트가 가짜 엔진을 꽂았다고 차단이 꺼지면
  *"연락처가 나가는지"* 를 검증하는 테스트 자체가 성립하지 않는다.

- **트레이드오프**:
  > 새 엔진을 꽂는 사람이 규율을 몰라도 안전 장치가 지켜지게 하려고 래퍼와 계약 검증을 넣었고,
  > 그 덕분에 우회 경로가 사라진 대신,
  > **계약이 거부하면 응답 자체를 못 만든다** — 정상 경로에서는 안 걸리지만
  > 걸리는 순간 `refused` 로 내려간다(`main.py::_install_response_guard`, 로그 ERROR).
  > 조용히 나가는 것보다 낫다고 판단했다.
- **영향**: `app/safety_engine.py`(신설) · `app/deps.py` · `app/contracts.py` ·
  `app/routes/records.py` · 02 §9 · 05 §4 · D-09 · D-40 · D-47
- **연관**: D-40(단일 출처) · D-47(연락처) · D-09(게이트) · D-57

---

### D-55 · **역치의 기준이 "음식"인지 "독소"인지 구분한다** 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: `StubEngine` 에 규칙 테이블을 배선하면서 `amount_g` 를 그대로 mg/kg 으로
  바꾸려다 멈췄다. 규칙 테이블 12행의 기준이 **두 종류**였다.

  ```
  양파 15-30 g/kg · 마늘 5 g/kg · 자일리톨 0.03 g/kg · 알리움 0.5%
      → 먹은 **음식의 질량**이 곧 역치의 단위다
  초콜릿(테오브로민+카페인) 20 mg/kg
      → **테오브로민의 질량**이다. 초콜릿 질량이 아니다
  ```

  구분하지 않고 곱하면 **초콜릿을 100% 테오브로민으로 계산**하게 된다 — 약 1만 배 과대평가다.
  안전한 쪽 오류라 눈에 띄지 않지만, **D-51 이 막은 것과 같은 종류의 조작**이다
  (*"잎 무게는 독소 무게가 아니다"*).

- **대안**
  - (a) 그대로 곱한다 — 안전한 쪽이니 괜찮다
  - (b) 독소 기준 물질은 정량 판정을 포기한다
  - (c) **코퍼스의 함량 값으로 환산하되, 종류를 모르면 되묻는다**
- **결정**: (c). 코퍼스에 다리가 있다 — `F-034-023~025` 의 종류별 테오브로민 함량
  (베이킹 14 · 다크 5 · 밀크 2 mg/g). **원문 값이고 우리가 만든 값이 아니다.**

  그런데 종류를 모르면 셋 중 무엇을 골라도 그 순간부터 **우리가 만든 숫자가 등급을 판정**한다.
  그래서 되묻는다. 골든셋 `G-047`(*"무슨 초콜릿인지는 모르겠어요"* → `clarify`)이
  기대하던 동작이 정확히 이것이었다.

  ```
  4kg 개 · 20g 섭취
    밀크    2 mg/g →  10.0 mg/kg → MONITOR
    다크    5 mg/g →  25.0 mg/kg → CALL_NOW    (임상징후 역치 20)
    베이킹 14 mg/g →  70.0 mg/kg → EMERGENCY   (중증 역치 40-50)
  ```

- **트레이드오프**:
  > 출처에 없는 수치를 만들지 않으려고 종류를 되묻기로 했고,
  > 그 덕분에 환산이 원문에 근거하게 된 대신,
  > **되묻기가 한 번 더 늘었다** — 되묻기 상한(D-09 · 02 §9) 안에서 소화해야 한다.
- **영향**: `app/engine.py` · `app/session.py`(`question_history` 신설) ·
  `compute/rules.py` · 04 §5 · D-16 · D-17 · D-50 · D-51
- **연관**: D-51(단위 환산 금지) · D-38(문장 생성) · D-50(바닥 등급)

---

### D-56 · **빈 필터는 "필터 없음"이 아니라 "0건"이다** 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: `to_chroma_where` 가 빈 목록을 만나면 그 조건을 건너뛰었고,
  조건이 하나도 안 남으면 `None`(= **필터 없음**)을 냈다. 실측:

  ```
  {"species": []}         → ChromaStore  : 전 종 검색 (필터가 사라짐)
                            InMemoryStore: 0건
  ```

  **종이 미확인일 때 개 보호자에게 고양이 백합 청크가 나가는 경로**였다.
  D-10(*"종 미확인이면 답하지 않는다"*)의 정확한 반대이고, 두 저장소가 갈리는 유일한 지점이었다.

  거짓 초록불이 이중으로 걸려 있었다 — `tests/test_retrieval_filter.py` 가
  이 동작을 **정답으로 단언**하며 주석에 *"필터가 깨지지 않는다"* 고 적어 두었다.
  깨진 것이 아니라 **사라진** 것이었다.

- **결정**: `EmptyFilter` 예외를 던지고 `search()` 가 잡아 **빈 리스트**를 낸다.
  **번역기는 *"이 조건은 무시해도 된다"* 를 판단할 자격이 없다.** 판단은 저장소가 한다.
  두 구현이 같아지고, `retrieve` 노드는 "결과 0건 = 거절"(D-46)을 이미 갖고 있어 손댈 것이 없다.
- **트레이드오프**:
  > 빈 필터가 조용히 전 종 검색이 되는 것을 막으려고 예외를 도입했고,
  > 그 덕분에 두 저장소가 같아진 대신,
  > **호출부가 예외를 잡아야 한다** — `VectorStore` 프로토콜 구현체가 늘어날 때마다 되풀이된다.
- **영향**: `retrieval/store.py` · `retrieval/__init__.py` ·
  `tests/test_retrieval_filter.py` · 02 §8 · D-10 · D-46
- **연관**: D-10(종 분리) · D-46(임계값은 거절을 못 만든다) · D-57

---

### D-57 · **틀린 동작을 고정한 테스트는 함께 뒤집고, 이유를 그 자리에 남긴다** 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: 검토에서 결함을 고치려 할 때마다 **그 결함을 정답으로 박아둔 테스트**가 나왔다.
  5건이었다.

  | 파일 | 무엇을 정답으로 고정하고 있었나 |
  |---|---|
  | `test_retrieval_filter.py` | 종 필터가 **사라지는** 동작 (D-56) |
  | `test_verbalize.py` | 없는 기준을 **만드는** 동작 (`체중 1kg당`, D-38 위반) |
  | `test_contacts.py` | `"24시"` 를 지우면 숫자가 없어 **공허하게 참**인 단언 |
  | `test_models_config.py` | `== max(...)` — 동률에서도 참이라 "가장 높다"를 보장 못 함 |
  | `test_auth_api.py` | `403` — 코드 주석부터 *"실패하면 401"* 이었다 |

  **테스트가 있어서 문제가 가려졌다.** 없었다면 오히려 눈에 띄었을 것이다.

- **결정**: 세 가지를 규율로 둔다.

  1. **고치기 전에 그 동작을 고정한 테스트부터 찾는다.** 안 그러면 수정하는 순간 빨간불이 뜨고,
     사람은 **코드가 아니라 코드를 되돌린다**
  2. 뒤집을 때는 **예전 단언을 docstring 에 그대로 인용**하고 왜 틀렸는지 적는다.
     지워 버리면 다음 사람이 같은 방향으로 되돌린다
  3. 전수 회귀를 함께 넣는다 — 표본 하나가 아니라 **코퍼스 전체**에 대해
     (`체중 1kg당` 0건 · `|` 0건 · 조사 병기 0건)

- **트레이드오프**:
  > 초록불이 곧 근거가 되지 않게 하려고 테스트를 뒤집고 이력을 남기기로 했고,
  > 그 덕분에 같은 실수가 되돌아오지 않게 된 대신,
  > **테스트 파일이 길어진다** — 지금 동작뿐 아니라 *예전에 무엇이 틀렸는지*까지 담기 때문이다.
- **영향**: `tests/*` 5건 · 08 §2026-08-02 · 04 §8
- **연관**: D-40(못 어기는 것) · D-54 · D-56


---

### D-58 · **검사기는 그 계층이 통제하는 것만 판정한다** 🔒

- **상태**: 확정 (2026-08-02)
- **맥락**: 2026-08-02 검토와 그 후속 논의에서 **같은 형태가 네 번** 나왔다.

  | | 검사기 | 무엇을 판정했나 | 왜 틀렸나 |
  |---|---|---|---|
  | 1 | `build_index.probe_negative` | *"음성 1위가 임계값 미만이어야 통과"* | **D-46 이 폐기한 기준.** 상시 빨간불 |
  | 2 | `check_facts` 파일 간 검사 | (`len(paths) > 1` 이라 실행 자체가 안 됨) | 거짓 초록 |
  | 3 | `test_retrieval_filter` | 종 필터가 **사라지는** 동작을 정답으로 단언 | 거짓 초록 |
  | 4 | *(제안됐다 취소)* 양성 프로브 | *"1위 등급 < 기대 등급이면 실패"* | 등급은 검색이 통제하지 않는다 |

  4번은 1번을 고치는 자리에서 나왔다. **진단하던 사람이 같은 것을 다시 만들었다.**
  개별 실수가 아니라 반복되는 형태라는 뜻이다.

- **결정**: 검사기를 놓기 전에 **"이 계층이 그것을 통제하는가"** 를 먼저 묻는다.

  | 계층 | 통제하는 것 | 판정해도 되는 것 |
  |---|---|---|
  | 인덱서 | 후보 집합 · 설정값 | `recall@k` · `score_threshold` 하한 |
  | 그래프 노드 | 상태 전이 · 슬롯 | 되묻기 분기 · 게이트 호출 |
  | 계약 | 응답 객체 | 불변식 (D-54) |
  | 평가 하네스 | 정답 대조 | 등급 · 과소평가율 · 근거 적중 |

  통제 밖을 판정하면 반드시 둘 중 하나가 된다.

  > **상시 빨간불** — 사람이 무시하게 된다. 진짜 빨간불도 함께 묻힌다
  > **거짓 초록** — 통제하지 못하는 것을 통과시켰다고 주장한다

  판정할 수 없지만 알아야 하는 것은 **보고**한다. `probe_symptom`·
  `probe_unknown_substance` 가 이미 *"통과/실패를 매기지 않는다"* 로 쓰던 방식이고,
  `probe_negative` 를 그쪽으로 옮겼다.

- **트레이드오프**:
  > 검사기가 거짓 신호를 내지 않게 하려고 판정 범위를 계층 안으로 좁혔고,
  > 그 덕분에 초록·빨강이 각각 의미를 갖게 된 대신,
  > **한 곳에서 잡히던 것이 두 곳으로 나뉜다** — 검색 품질은 인덱서가, 등급은 하네스가 본다.
  > 둘을 함께 보려면 두 번 돌려야 한다.
- **영향**: `scripts/build_index.py` · `scripts/check_facts.py` ·
  `tests/test_retrieval_filter.py` · 04 §9.1 · 08 §2026-08-02 · D-46 · D-57
- **연관**: D-46(임계값은 거절을 못 만든다) · D-54(강제는 주입·계약에) · D-57(틀린 테스트)

---

### D-59 · **물질 동정은 생성이 아니라 폐쇄 목록에서의 선택이다** 🔒

- **상태**: 확정 (2026-08-02) · **구현은 WS2 (② `extract_slots`)**
- **맥락**: *"앵무새 앞에서 프라이팬을 태웠어요"* 는 물질명을 말하지 않는다.
  사람은 **프라이팬 과열 → 유독 흄 → 앵무새 위협**으로 다리를 놓는데,
  그 다리가 우리 파이프라인 어디에도 없었다. `Slots.substance` 는 칸만 있고 비어 있다.

  다리를 LLM 에게 맡기면 환각이 문제가 된다. 처음 제안한 그물은

  > *"LLM 이 물질을 추정하고, 그 추정어로 검색해서 **0건이면** 되묻는다"*

  였는데 **D-46 이 이미 무효화한 것이다.** 근거 없는 질의도 0.494~0.659 가 나오므로
  임계값 0.50 에서 **0건은 거의 발생하지 않는다.** 그물에 구멍이 아니라 바닥이 없었다.

  실제로 새는 경로는 넷이었다.

  | | 어떻게 | 왜 안 잡히나 |
  |---|---|---|
  | 1 | 없는 물질을 지어냄 | 0건이 안 나온다 (D-46) |
  | 2 | `프라이팬` → `일산화탄소` 로 오동정 | **근거는 진짜다.** 겉보기가 더 그럴듯하다 |
  | 3 | 확장어가 검색을 엉뚱한 쪽으로 끌고 감 | 원문 질의의 신호가 묻힌다 |
  | 4 | ④ `verify_grounding` 통과 | verify 는 *문장이 답을 뒷받침하나* 를 본다. **동정이 맞는지는 모른다** |

- **결정**: 네 겹으로 나눈다. **각각 다른 장치가 막는다.**

  **① 환각 — 폐쇄 목록 선택으로 원천 봉쇄**

  코퍼스의 물질 어휘는 **533종으로 유한하다** (888행 기준 · dog 210 · cat 177 · bird 130 ·
  mammal 94 · all 76). 자유 생성이 아니라 **목록에서 고르게** 한다.

  ```
  ✗  "이 상황의 물질은?"                   → 생성 → 환각 가능
  ✓  "다음 중 어느 것인가? 없으면 '없음'"    → 선택 → 환각 불가능
  ```

  `tasks.py` 의 `SLOT` 은 원래 **추출** 태스크다. 생성처럼 쓰려던 것이 잘못이었다.
  `'없음'` 은 정상 선택지이고 D-49 되묻기로 가는 문이다.

  **② 별칭 표를 LLM 보다 먼저 본다 — 결정론**

  ```
  프라이팬 · 논스틱 · 코팅팬 · 테플론팬  →  PTFE 흄
  대파 · 쪽파 · 부추 · 리크             →  알리움류
  ```

  있으면 LLM 을 부르지 않는다. 빠르고 재현되고 감사 가능하다.
  같은 표가 `rules.lookup` 의 부분 문자열 한계도 고친다 — 골든셋 `G-039`(대파/cat)가
  `알리움류(양파·마늘·리크·차이브)` 에 안 걸리던 것이 이 결손이다.

  **③ 오분류 — 없애는 것이 아니라 재는 것으로 바꾼다**

  | | 환각 | 오분류 |
  |---|---|---|
  | 무엇 | 코퍼스에 **없는** 것을 만든다 | 있는 것 중 **틀린** 것을 고른다 |
  | 범위 | 무한 | **533종 안** |
  | 측정 | 불가능 | **골든셋으로 가능** |

  **④ 미확정일 때 — 확인하되 거절하지 않는다**

  ```
  후보 계열 최고 등급이 응급?
     ├ 예   → 즉시 답변 + 가정 명시 (D-13 상향).  확인에 쓰는 턴이 곧 시간이다
     └ 아니오 → 유도 없는 확인 1회
                "어떤 팬이었나요?  코팅팬 / 무쇠·스테인리스 / 모름"
                └ 모름·무응답 → 답변 + 가정 명시.  **거절이 아니다**
  ```

  *"논스틱 코팅 연기(PTFE)일 수 있습니다, 맞나요?"* 처럼 묻지 않는다 —
  모르면서 *"네"* 하면 **우리 추측이 사용자 확인으로 세탁된다.**

  **계산 불가 ≠ 동정 미확정.** 체중·섭취량이 없으면 정량 계산이 성립하지 않으므로 거절이 맞다.
  물질이 미확정이면 **정성 판정은 여전히 가능하다** — 후보 중 최고 등급으로.
  그래서 물질 확인 실패는 `refuse(되묻기상한)` 이 아니라 답변으로 폴백한다.
  (`configs/eval.yaml` 은 `max_clarify_turns: 0` 이라, 폴백이 없으면 평가에서
  `G-013` 이 영영 거절로 집계된다.)

  **⑤ 가정 명시는 문장에 맡기지 않고 계약이 강제한다** (D-54 방식)

  ```python
  assumed_substance: str | None = None

  @model_validator(mode="after")
  def _assumption_must_be_stated(self):
      if self.assumed_substance and self.assumed_substance not in self.full_text:
          raise ValueError("추정 물질로 답하면서 그 가정을 문장에 밝히지 않았다.")
  ```

  가정을 안 밝히면 **응답 객체가 만들어지지 않는다.** 밝히지 않은 추정은 곧 환각이다.

- **폐기한 안 — 기록해 둔다**

  > **"추정어로 검색해 0건이면 되묻는다"** — D-46 이 0건을 만들지 못하므로 무효.
  > 이 안이 한 번 제안됐다가 취소된 이유를 남긴다. **D-46 을 두 번 잊지 않기 위해서다.**

- **트레이드오프**:
  > 물질을 말하지 않는 질의에도 답하려고 LLM 동정을 넣었고,
  > 그 덕분에 보호자가 물질명을 몰라도 되게 된 대신,
  > **오분류가 새로 생긴다.** 환각과 달리 유한하고 측정 가능하지만 0은 아니다.
  > 하향 오분류는 과소평가이므로 ④의 상향 가정으로 방향을 고정했다.
- **영향**: `graph/nodes/slots.py` · `models/tasks.py`(SLOT) · `app/contracts.py`
  (`assumed_substance`) · `compute/rules.py`(별칭) · 05 §4 · D-13 · D-46 · D-49 · D-54
- **연관**: D-49(증상 질의는 되묻는다) · D-13(과소평가 최우선) · D-38(LLM은 제안·코드는 판정)

---

### D-60 · **별칭은 낱말 경계로만 걸린다 · 1글자 별칭은 두지 않는다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`compute/aliases.py` · `tests/test_aliases.py`)
- **맥락**: D-59 ②가 만든 별칭 표를 **재검토했더니 오탐 14건**이 나왔다.

  ```
  초코파이 → 알리움류        중성화 수술 → 알코올        파스타 → 알리움류
  양파    → 알리움류        마취술      → 알코올        소주방 → 알코올
  파마산 · 파파야 · 파운드케이크 → 알리움류    기술 · 예술 · 시술 · 술기운 → 알코올
  대파하듯 → 알리움류
  ```

  원인은 **부분 문자열 일치**였다. 막던 방식은 차단 목록이었다.

  ```python
  _BLOCK_AFTER = {"파": "슬프인란"}   # 파슬리·파프리카·파인애플·파란
  ```

  **뒷글자만** 막고, 그것도 **열린 집합**이다. `초코파이`는 앞글자가 문제라
  이 방식으로는 **원리적으로 못 막는다.** 목록을 아무리 늘려도 끝나지 않는다.

  이 도메인에서 오탐은 **놓침보다 위험하다** — 잘못 걸린 물질이 잘못된 근거를
  부르고 잘못된 등급으로 끝난다. `safety/contacts.py` 에서 인용 표기(`(출처: …)`)를
  검사 전에 걷어내 오탐 12건을 없앤 것과 **같은 성질의 문제**다.

- **결정**: 무엇을 막을지가 아니라 **무엇이면 낱말인지**를 정한다.

  ```
  앞:  문두이거나 한글이 아닐 것              초코|파이 · 양|파 · 수|술   → 탈락
  뒤:  문말이거나 한글이 아닐 것,
       또는 조사가 와서 낱말을 끝낼 것         파스|타 · 소주|방          → 탈락
                                            대파|를 · 프라이팬|에서      → 통과
  ```

  **한국어 조사는 닫힌 집합이다.** 차단 목록은 아니었다. 그 차이가 유지 가능성을 가른다.

  **그리고 1글자 별칭은 두지 않는다** (`MIN_ALIAS_LEN = 2`, 로더가 거부).
  경계 규칙을 넣어도 `파` 는 `파도`·`파는`(팔다)에 걸린다 — `도`·`는` 이 **조사이면서
  낱말이기도** 하기 때문이다. 1글자는 조사와 구별할 방법이 없다.
  **규칙으로 못 막는 것은 표에 두지 않는다.**

  잃는 것(*"파를 먹었어요"*)은 `대파`·`쪽파`·`실파`·`파김치` 행으로 되찾는다.
  못 되찾는 것은 못 잡는다 — **놓치면 정성 답변으로 내려가고, 잘못 걸면
  우리가 등급을 만든다** (D-46).

  **미등재 합성어는 규칙이 아니라 표로 넓힌다.** `생맥주`·`체리 씨앗`·`논스틱팬` 처럼
  행을 늘린다. 규칙에 예외를 붙이면 다시 열린 집합이 된다.

  **경계 규칙이 못 막는 것 — 동음이의 지명.**

  ```
  청주에서 갈 만한 동물병원 있나요   → 알코올     ← `에서` 는 정상적인 조사다
  양주에 사는데 병원 추천해 주세요    → 알코올
  ```

  `청주`·`양주` 는 술이면서 **도시 이름**이다. 규칙으로는 구별할 수 없으므로
  **표에서 뺐다.** 동음이의어는 규칙이 아니라 **사람이 표에서 판단한다.**

- **실측 (2026-08-02)**: 오탐 **14건 → 0건** · 누락 **5건 → 0건** (실문장 42건 기준).
  표 16행 → **51행**. 테스트 `tests/test_aliases.py` **20건 → 50건**.

- **트레이드오프**:
  > 오탐을 없애려고 경계를 좁혔고, 그 덕분에 잘못된 물질 동정이 사라진 대신,
  > **표기 변형을 표가 떠안는다.** `체리씨`·`체리 씨`·`체리씨앗`·`체리 씨앗` 이
  > 각각 한 행이라 벚나무속만 20행이다. 규칙이 작아진 만큼 표가 커졌다.
  > 표가 커지는 것은 사람이 읽고 고칠 수 있는 종류의 비용이므로 이쪽을 택했다.
  >
  > **⚠ 그 비용을 뒤에 쟀다 (2026-08-02 존재의의 재검토).** 51행 중 **20행(39%)이
  > 표기 변형**이다 — `체리씨`·`체리 씨`·`체리씨앗`·`체리 씨앗` 이 각각 한 행이라
  > 과일 하나에 4행이다. 이 20행은 **다리를 놓지 않는다.** 표의 존재의의는
  > *"코드가 놓을 수 없는 다리를 사람이 근거와 함께 놓는 것"* 인데, 철자를 맞추는 일은
  > **매처가 할 일을 표가 떠안은 것**이다.
  >
  > 여기서 하나를 잘못 봤다. **띄어쓰기는 "규칙에 붙이는 예외"가 아니라 정규화다.**
  > 닫힌 변형이고 규칙이 한 줄이다 — 매칭 전 공백 제거만으로 20행 → 10행이 된다.
  > `씨`↔`씨앗` 까지 보면 10 → 5. **다음 회차 항목으로 남긴다** — 마감이 이틀이고
  > 지금 전부 초록이라, 매처를 바꾸면 무엇이 무엇을 바꿨는지 못 가른다
  > (`rules._match` 를 이번에 안 건드린 것과 같은 이유).
- **영향**: `compute/aliases.py` · `compute/tables/별칭.csv` · `compute/rules.py`(docstring) ·
  `tests/test_aliases.py` · D-46 · D-59 ②
- **연관**: D-59(물질 동정은 폐쇄 목록에서의 선택) · D-46(놓치는 것은 실패가 아니다) ·
  D-47(연락처 차단에서 같은 판단을 했다)

---

### D-61 · **검사기는 존재가 아니라 관련성을 본다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`tests/test_aliases.py`)
- **맥락**: 별칭 표의 `basis` 열을 재검토했더니 **틀린 근거 3건**이 있었다.

  | 별칭 | 표에 적힌 근거 | 그 사실의 실제 내용 |
  |---|---|---|
  | `술 → 알코올` | `F-029-002` | **아보카도** (mammal) |
  | `살구씨 → 핵과 씨앗` | `F-029-016` | **위장관 폐색 이물**(아보카도 씨·옥수수속대·뼈) |
  | `자두씨 → 핵과 씨앗` | `F-029-016` | 위와 같다 |

  검사는 **초록이었다.** `test_근거_fact_id_가_실재한다` 가 id 의 **실재**만 봤기 때문이다.
  아무 id 나 적어도 통과한다.

  더 있었다. `살구씨`·`자두씨` 는 `species` 가 비어 **전 종**인데, 대상 `핵과 씨앗` 의
  근거(`F-005-005`·`F-091-005`)는 **둘 다 조류**다. 개 보호자에게 줄 근거가 없는데 걸렸다 —
  `프라이팬` 을 `bird` 로 좁힌 것과 **같은 문제인데 거기만 좁혀져** 있었다.

  근본 원인은 하나다. **`basis` 열이 무엇인지 아무 데도 안 적혀 있었다.**
  `대파` 의 근거 어디에도 '대파'가 없는데 그것이 옳은지 틀린지 판단할 기준이 없었다.

- **결정**: 열의 의미를 먼저 못 박고, 검사기가 그것을 본다.

  > **`basis` = 대상 물질(`substance`)이 그 이름으로 실재함을 보이는 사실 행들.**
  > 별칭 자체가 거기 적혀 있을 필요는 없다.

  그 정의에서 검사 두 가지가 따라 나온다.

  ```
  관련성   근거 전부의 `substance` 안에 대상 물질명이 들어 있을 것
  종 범위  이 행이 적용되는 종을 근거들이 덮을 것  (all → 전 종, mammal → dog·cat)
  ```

  **D-58 의 연장이다.** D-58 은 *"검사기는 그 계층이 통제하는 것만 판정한다"* 였다.
  여기서는 반대 방향의 같은 병이었다 — **통제할 수 있는 것을 판정하지 않았다.**
  검사기가 좁으면 거짓 초록이 되고, 넓으면 거짓 빨강이 된다. **어느 쪽도 신호가 아니다.**

  같은 이유로 `species` 열을 다중값(`dog|cat`)으로 넓혔다. 근거가 덮는 종을
  **한 값으로는 적을 수 없어서** 전 종으로 비워 두는 일이 벌어졌기 때문이다.

- **함께 고친 것**:
  - `술 → 알코올` → `소주`·`맥주`·`생맥주`·`막걸리`·`위스키`·`와인`, 근거 `F-029-001`(all) 외 3건
  - `살구씨`·`자두씨 → 핵과 씨앗`(조류 전용) → **`벚나무속 과일 씨`**(`F-029-017`, all)
  - `살구씨` 는 코퍼스 목록(체리·천도복숭아·복숭아·자두)에 **살구가 없으므로 `추정`** 으로 내렸다
  - `테팔 → PTFE` 를 `확정` → **`추정`** 으로. 브랜드명이고 같은 브랜드가 무쇠 라인도 판다 —
    **코퍼스 밖 상식을 확정으로 적은 것**이었다
  - `대파` 근거에서 `F-023-007`(양파·마늘·부추(Allium 속)) 제거 — `알리움류` 라는 이름이 없다

- **트레이드오프**:
  > 근거를 문자열 일치로 검사해 틀린 근거를 잡게 된 대신,
  > **맞는데 표기가 다른 근거를 못 쓴다.** `F-074-004`(마모·손상된 논스틱 조리기구)는
  > PTFE 자료가 맞지만 `PTFE` 라는 글자가 없어 근거로 못 넣는다.
  > 표기 통일을 사실 표에 요구하지 않고 **검사를 엄격하게 두는 쪽**을 택했다 —
  > 원본 CSV 를 고쳐 맞추면 출처와 어긋난 표가 남는다 (08 §규율 2).
- **영향**: `tests/test_aliases.py` · `compute/aliases.py`(`Alias.species` 다중값 ·
  `covers`) · `compute/tables/별칭.csv` · 08 §2026-08-02
- **연관**: D-58(검사기의 판정 범위) · D-38(없는 것을 만들지 않는다) · D-59(별칭 표)

---

### D-62 · **폐쇄 목록은 거부기가 아니라 정규화기다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`compute/vocabulary.py` · `app/contracts.py`)
- **맥락**: D-59 ①은 *"물질 동정은 생성이 아니라 533종 폐쇄 목록에서의 선택"* 으로 정했다.
  그런데 **실제로 만든 것은 프롬프트가 그렇게 부탁하는 것까지**였다.

  ```
  모델이 "일산화탄소" 라고 답하면?   → 코퍼스에 없다.  그런데 아무것도 막지 않았다
  ```

  D-59 ②가 오동정의 예로 든 그 이름이다. 목록은 문서에만 있었다.

  **그래서 계약이 거부하게 만들었다가, 다시 뒤집었다.** 재검토에서 두 가지가 드러났다.

  | | 무엇 |
  |---|---|
  | ① 실패 방식이 문서와 달랐다 | 05 §6 은 ①분류를 *"허용목록에 없으면 **폴백 + 로그**"*, ②슬롯을 *"검증 실패 시 **되묻기**"* 로 정해 뒀다. 02 §6 그래프도 `extract_slots ─ 결측·**물질미상** → ask_clarify`. **예외는 그 경로를 우회한다** |
  | ② 일치 규칙이 파이프라인과 달랐다 | `rules.lookup` 은 **부분 일치**인데 폐쇄 목록은 **정확 일치**다. ②가 뽑는 것은 보호자의 **표면형**이고, 실측 결과 흔한 표현 30개 중 **12개가 목록 밖**이었다 — `커피`·`우유`·`이부프로펜`, 그리고 **`대파`**(골든셋 `G-039` 의 질의다) |

  거부기로 두면 **정상 질의가 계약에서 죽는다.**

- **결정**: 폐쇄 목록을 **두 겹**으로 쓴다. 순서가 중요하다.

  ```
  ① 정규화   vocabulary.resolve_substance(표면형, species)
       직접      목록에 그대로 있다            초콜릿 → 초콜릿
       별칭      별칭 표가 가리킨다 (확정 먼저)  대파   → 알리움류
       부분일치   코퍼스 이름 하나가 품는다      자몽   → 감귤류(레몬·자몽·라임)
       모호      둘 이상이 품는다              커피   → None + 후보 5건
       없음      아무것도 없다                 일산화탄소 → None
                                             └ 예외를 던지지 않는다

  ② 안전망   contracts.SubstanceName
       정규화를 거친 값만 올라오므로 **정상 경로에서는 안 걸린다.**
       걸렸다면 정규화를 안 거쳤다는 뜻이다 — `_no_foreign_contacts` 와 같은 지위.
  ```

  **`모호` 는 실패가 아니다.** 하나를 고르면 나머지를 배제한 것이고 **그 배제가 곧 진단**이다
  (D-11 · D-49). 후보는 버리지 않고 **검색어로 전부 넘겨 LLM 이 다 읽게** 한다 (D-58).
  `없음` 만 되묻기로 간다.

  **프롬프트에 목록을 싣지 않는다.** ②는 `retrieve` **앞**이라 후보를 좁힐 수 없고,
  `candidates_for("dog")` 는 363종 = 4,716자(≈2.4k~4.7k 토큰)다. Qwen3-4B 에
  매 질의마다 얹기엔 크다. **②는 계속 추출(표면형)하고 정규화는 코드가 한다** —
  D-38 그대로 *LLM은 제안 · 코드는 판정.*

  > **이것은 D-59 ①의 해석을 좁히는 것이다.** *"선택"* 은 프롬프트에서가 아니라
  > **정규화 단계**에서 실현된다. 결과는 같다 — 목록 밖 이름으로는 답이 안 나간다.

- **목록은 어디서 오나** — 두 파일의 합집합이고 **둘 다 파일에서 온다.**

  ```
  물질어휘.csv  코퍼스 물질명 533종.  facts_*.csv 에서 생성 (make vocab)
  별칭.csv      계열명 7종.          사람이 쓰고 근거를 붙인다 (D-61 이 검사한다)
  ```

  ⚠️ **그 계열명 5개는 코퍼스 어휘가 아니라 우리 어휘다** (존재의의 재검토, 2026-08-02).
  `PTFE`·`알리움류`·`벚나무속 과일 씨`·`에센셜 오일`·`이소프로필알코올` — 전부 코퍼스
  이름의 **부분 문자열**이고 검사가 대조하므로 지어낸 이름은 아니다. 그러나
  `identified_substance="PTFE"` 로 나가면 **출처 문서에 그 이름은 없다.**
  *"폐쇄 목록 = 코퍼스"* 는 정확한 문장이 아니다 — **533 + 우리 축약형 5** 다.

  런타임에 코퍼스를 안 읽는다 — `data/facts/*.csv` 는 **패키지에 안 들어간다.**
  설치본에서 어휘가 조용히 비면 검사가 전부 통과하거나 전부 실패한다.
  **둘 다 신호가 아니다** (D-58). `정량임계치.csv` 와 같이 파일에 고정한다 (D-22).

- **부분 일치에도 낱말 경계를 쓴다** (D-60 재사용). 실측으로 두 가지가 샜다.

  ```
  '카페인'      → 후보 10건.  그 안에 코퍼스 물질명 '인'(무기질)  ← 역방향 포함
  '에틸렌글리콜'  → 후보 2건.   '디에틸렌글리콜(브레이크액)'        ← 다른 화합물이다
  ```

  **규칙을 새로 만들지 않았다.** `is_word_hit` 을 `vocabulary` 로 올려 별칭 표와
  같은 것을 쓴다. `MIN_TERM_LEN` 도 하나다 (P2).

  ⚠️ **`rules._match` 는 아직 이 경계를 안 본다.** 같은 오탐이 거기에도 있다
  (`lookup('카페인')` 이 `인` 행에 걸린다). 이 회차에서는 **기록만 한다** —
  규칙 테이블 조회를 바꾸면 골든셋 판정이 함께 움직여서, 별칭 재검토와 섞으면
  무엇이 무엇을 바꿨는지 못 가른다.

- **무엇을 못 막는지 함께 적는다**:

  | | 환각 | 오분류 |
  |---|---|---|
  | 무엇 | 코퍼스에 **없는** 것을 만든다 | 있는 것 중 **틀린** 것을 고른다 |
  | 범위 | 무한 | **538종 안** |
  | 이 결정 | **막는다** | **못 막는다 — 골든셋으로 잰다** |

  `identified_substance` 는 **선택 필드**다. 계약이 강제하는 것은
  *"넣는다면 반드시 목록 안"* 이지 *"반드시 넣어야 한다"* 가 아니다 —
  ② 노드가 아직 없어서 강제할 대상이 없다. **못 하는 것을 한다고 적지 않는다** (D-58).

- **추정을 슬롯에서 잃지 않는다** (존재의의 재검토, 2026-08-02):

  ```
  "앵무새 앞에서 프라이팬을 태웠어요"
    resolve_substance → PTFE · assumption=True        ← 알고 있었다
    set_substance     → {'substance': 'PTFE'}          ← 여기서 버려졌다
    AskResponse(assumed_substance=None)                ← 계약이 발동하지 않는다
  ```

  `_assumption_must_be_stated` 는 **이 도약을 막으려고 만든 계약인데, 필드를 채워 줄
  경로가 끊겨 있어 한 번도 발동할 수 없었다.** `Slots` 에 담을 자리가 없었기 때문이다.
  D-40 이 경계한 그대로다 — 기억에 맡긴 것은 D-47 에서 이미 한 번 뚫렸다.

  `Slots.substance_is_assumed` 를 신설하고 `set_substance` 가 채운다.
  거짓이면 키를 두지 않는다 (D-10). 노드가 그것을 `assumed_substance` 로 옮기는지는
  `tests/todo/test_graph_nodes.py::test_assumed_substance_is_carried_to_the_response` 가 본다.
  **추정 12행**(`프라이팬`·`후라이팬`·`냄비`·`테팔`·`테팔팬`·`손소독제`·`구강청결제`·
  `디퓨저`·`살구씨` 4형태)이 전부 이 경로를 탄다.

- **미결 — 근준 팀원 코드 병합 후**:
  - **확정 별칭도 치환 사실을 안 밝힌다.** `대파 → 알리움류` 로 답하면 근거가 알리움류인데
    코퍼스에 대파 행은 없다. 확정이라 D-59 ⑤ 대상은 아니나, D-51 이 *"같은 속이라도
    함량이 같다는 근거가 없다"* 고 한 자리다. ③생성 노드가 있어야 손댈 수 있다
  - **문맥이 다른 오탐.** `냄비`→PTFE 인데 실제로는 **화상**인 경우(골든셋 `G-103`).
    답이 그 상황에 없는 물질을 말한다. 케이스는 넣었으나 **파이프라인이 없어 미검증**이다

- **함께 잡힌 것**: `tests/test_api.py` 가 `"PTFE 흄"` 으로 검증하고 있었다.
  코퍼스에 없는 이름이다 (코퍼스는 `PTFE(테플론) 과열 흄`). **테스트가 만들어 낸 이름으로
  통과하고 있었다.** 계약을 넣자마자 걸렸고 코퍼스 이름으로 바꿨다 (D-57).

- **트레이드오프**:
  > 정규화를 코드에 두어 프롬프트 토큰을 안 쓰고 결정론을 얻은 대신,
  > **`모호` 가 늘어난다.** 실측(dog, 표면형 24개) — 확정 14 · 모호 5 · 없음 5.
  > 모호는 후보를 넘기므로 손실이 아니지만, **`identified_substance` 가 비는 질의가
  > 그만큼 생긴다.** 그 비율을 골든셋으로 재야 하고, 아직 재지 않았다.
- **영향**: `compute/vocabulary.py`(신규 · `is_word_hit`·`SPECIES`·`MIN_TERM_LEN` 단일 출처) ·
  `compute/tables/물질어휘.csv`(신규·생성물) · `scripts/build_vocabulary.py`(신규) ·
  `compute/aliases.py` · `app/contracts.py` · `graph/state.py` · `models/tasks.py` ·
  `tests/test_vocabulary.py`(신규) · `tests/test_api.py` · `tests/todo/test_graph_nodes.py` ·
  `Makefile`(`make vocab`) · **05 §6 표** · **02 §9 응답 스키마**
- **연관**: D-59 ①(폐쇄 목록 선택) · D-40(강제는 주입·계약에) · D-38(LLM은 제안·코드는 판정) ·
  D-22(단일 출처) · D-58(못 하는 것을 한다고 적지 않는다) · D-57(틀린 테스트) · D-60(낱말 경계)


### D-63 · **추정은 검색을 넓히고, 응답에서 밝힌다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/engine.py` · `graph/state.py` · `app/contracts.py`)
- **맥락**: D-59 ⑤가 *"가정을 밝히지 않으면 그것이 곧 환각"* 이라 정했고, 계약
  `_assumption_must_be_stated` 를 만들어 뒀다. 그런데 2026-08-02 별칭표 활용 점검에서
  **그 계약이 한 번도 발동한 적이 없다**는 것이 드러났다.

  ```
  슬롯:  {'species':'bird', 'substance':'PTFE', 'substance_is_assumed': True}
  응답:  assumed_substance=None · identified_substance=None
  ```

  `_build_response` 가 옮기지 않았다. **빈 필드는 검사할 것이 없다.**
  별칭표의 `kind`(확정/추정) 열이 슬롯까지만 가고 응답에 안 닿고 있었다.

  고치는 방법으로 두 가지가 나왔고 **둘은 대안이지 보완이 아니다.**

  | | 무엇을 한다 | 무엇을 잃는다 |
  |---|---|---|
  | ㉮ 검색어에서 추정을 뺀다 | `냄비` 로는 PTFE 근거를 안 끌어온다 | **근거가 얇아진다.** 실측상 보강은 8/8 질의에서 점수를 올렸다 (`냄비` 0.588→0.668). 빼면 `근거없음` 거절이거나 등급이 내려간다 — **과소평가 방향**(D-13) |
  | ㉯ 응답에서 가정을 밝힌다 | 문장 맨 앞에 *"확인되지 않은 가정"* 을 세운다 | 문장이 길어진다 |

- **결정**: **㉯만 한다.** ㉮는 채택하지 않는다.

  > **추정의 위험은 검색이 아니라 표현에 있다.**
  > 검색에는 출력면이 없다 — 사용자가 보는 것은 응답이다. 근거를 좁히는 방향으로
  > 안전을 사면, 실제로 사는 것은 안전이 아니라 **과소평가**다 (D-13 이 최우선 위험으로
  > 둔 바로 그것). 넓게 찾고, 무엇을 가정했는지 밝힌다.

  ```
  ① 상태    set_substance 가 substance_is_assumed 와 함께 substance_surface 를 남긴다
              — 무엇을 무엇으로 봤는지 **둘 다** 있어야 사용자가 정정할 수 있다 (D-59 ④)
  ② 응답    _build_response 가 옮긴다.  추정이면 assumed_substance 만,
              확정이면 identified_substance 만.  **한 필드에 담지 않는다** —
              담으면 읽는 쪽이 구분 안 하고 쓰고, 그 순간 도약이 확정이 된다
  ③ 문장    engine._assumption_notice() 가 맨 앞에 세운다
              [확인되지 않은 가정] '프라이팬' = 'PTFE'. 확인된 것이 아니니 다르면 알려주세요.
  ④ 계약    _assumption_must_be_stated 가 full_text 에 그 이름이 있는지 본다
  ```

  ⚠️ **문장을 계약 안에서 만들지 않는다.** `full_text` 가 스스로 붙이면
  계약이 **자기가 붙인 것을 자기가 확인하는 꼴**이라 검사가 항상 통과한다.
  만드는 층(엔진)과 검사하는 층(계약)을 분리한다 (D-57 · D-58).

- **검사가 왜 세 번이나 놓쳤나** — `tests/todo` 의 기존 검사가 **슬롯까지만** 봤다.
  노드 출력이 옳으면 초록이었고, 그 값이 응답까지 가는지는 아무도 안 봤다.
  **검사는 경계에서 한다** — `test_assumption_reaches_the_contract_not_just_the_slots`
  가 나가는 응답을 보고, 가정을 안 밝히면 `AskResponse` 생성 자체가 터지는 것까지 확인한다.
- **트레이드오프**:
  > 추정을 검색어에 실은 채로 두어 근거를 지켰고 D-13 을 지켰다. 대신
  > **추정 질의는 매번 문장이 한 줄 길어지고**, `identified_substance` 가 비어
  > 그 질의는 *"무엇에 관한 답인가"* 를 기계가 단정할 수 없다. 그것이 정확한 상태다.
- **영향**: `graph/engine.py`(`_assumption_notice`·`_build_response`) · `graph/state.py`
  (`substance_surface`) · `tests/todo/test_graph_nodes.py` · D-59 ⑤ · D-62
- **연관**: D-13(과소평가가 최우선 위험) · D-59(물질 동정) · D-62(정규화기) ·
  D-57(틀린 테스트) · D-58(검사기의 범위) · D-40(계약이 단일 출처)

---

### D-64 · **랭그래프를 실제로 쓴다 — 이름만 빌리지 않는다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/build.py` 신규)
- **맥락**: 05 §5 가 랭그래프를 고른 이유는 하나였다 —
  *"되묻기 순환 + 근거 검증 실패 시 재검색이 있어 **선형 체인으로 표현 불가**"*.

  그런데 2026-08-02 점검에서 **`import langgraph` 가 소스 어디에도 없었다.**

  ```
  폴더 이름          src/pettriage/graph/            ✅ 있다
  엔진 docstring     "LangGraph 기반 질의 엔진"       ✅ 적혀 있다
  pyproject          langgraph>=0.2.62  ([rag])       ✅ 선언돼 있다
  import langgraph                                    ❌ 0곳
  ```

  실체는 `GraphEngine._run_pipeline` **79줄짜리 손으로 펼친 선형 실행기**였고,
  05 §5 가 *"표현 불가"* 라고 한 그 순환은 이렇게 복붙돼 있었다.

  ```python
  state.update(compress_context(state)); state.update(generate_draft(state))
  state.update(verify_grounding(state))
  if refused and retry_count < MAX_RETRY:
      state.update(retrieve(state))
      state.update(compress_context(state)); state.update(generate_draft(state))
      state.update(verify_grounding(state))      # ← 같은 4줄이 또
  ```

  **409건이 전부 초록이었다.** 검사가 *"파이프라인이 옳은 답을 내는가"* 만 보고
  *"무엇이 그 파이프라인인가"* 를 안 봤기 때문이다. D-48 교훈 #6과 같은 모양이다.

- **결정**: `graph/build.py` 의 `build_graph()` 가 **유일한 순서 출처**다.
  `_run_pipeline` 은 컴파일된 그래프를 부르기만 한다 (79줄 → 5줄).

  ```
  classify ─general/unknown→ refuse_scope
      └→ extract ─결측→ clarify
            └→ filter → retrieve ─0건→ refuse_nohit(첫)·refuse_verify(재검색)
                  └→ compute → rules → compress → generate → judge → decide
                        └→ verify ─통과→ simplify → answered
                                  └─실패→ retry → retrieve      ← 05 §5 가 말한 그 순환
  ```

- **리듀서를 두지 않는다.** `GraphState` 에 `Annotated[..., reducer]` 를 하나도 안 붙인다.
  기본 채널이 last-value 라 **기존 `state.update(...)` 와 의미가 정확히 같다.**

  > ⚠️ 잘못 붙이면 `rule_level` 이 조용히 덮인다. 그것은 `apply_gate` 의 **바닥**이고,
  > 바닥이 무너지면 **하향 금지 게이트(D-09)가 통째로 무력해진다.**
  > 이식에서 노드 함수 본문은 하나도 안 건드렸다 — 바뀐 것은 *순서를 누가 정하는가* 뿐이다.

- **얻은 것 넷**

  | | 무엇 |
  |---|---|
  | ① | 재시도 복붙 4줄 → **조건부 엣지 하나** |
  | ② | `retry_count` 상한이 `verify_grounding` 안과 엔진 두 곳에 있었다 → 라우터 한 곳 (P2) |
  | ③ | **스키마 밖 키를 못 쓴다.** `state["llm_fallbacks"]` 를 `# type: ignore[typeddict-unknown-key]` 로 넘기던 것이 런타임에서 터진다 → `GraphState` 에 정식 등록 (D-40) |
  | ④ | **구조도를 코드에서 뽑는다.** `python scripts/draw_graph.py` → `docs/그림/질의그래프.mmd` (필수 산출물 ②) |

- **`langgraph`·`langchain-core` 를 핵심 의존성으로 올린다.** `[rag]` 에 두면
  `.[api]` 만 깐 CI test 잡에서 **그래프가 한 줄도 실행되지 않는다** — `email-validator`
  와 같은 사고이고 D-48 교훈 #6의 재발이다. 둘 다 순수 파이썬이라 `chromadb`·`torch` 를
  안 끌고 온다. `tests/test_graph_build.py::test_langgraph_is_a_core_dependency` 가 지킨다.
- **`finalize` 는 그래프에 넣지 않는다.** 연락처 차단(D-47)은 `deps.get_engine()` 이
  `SafetyEngine` 으로 **감싸서** 한다. 그래프 안에 두면 `answered` 경로만 훑어
  **거절 문구에 들어간 미국 핫라인을 놓친다.** `nodes/__init__.py` 머리말의 아스키
  구조도가 `finalize` 를 *"모든 경로가 마지막에 만난다"* 로 그려 두고 있었으나
  **파이프라인은 그것을 한 번도 부르지 않았다** — 그림을 지우고 생성으로 대체했다.
  **틀린 그림은 없는 그림보다 나쁘다** (D-22 · D-38).
- **트레이드오프**:
  > 마감 이틀 전에 런타임을 바꿨다. 위험을 감수한 이유는 ⑴ 필수 산출물이
  > *"LangChain을 활용하여 벡터DB와 LLM 연동"* 을 명시하고 ⑵ 노드가 전부
  > `state → 부분 dict` 순수 함수라 랭그래프 노드 시그니처와 **이미 같아서**
  > 이식이 기계적이었기 때문이다. 대신 **의존성 하나가 핵심으로 올라갔고**,
  > 노드 이름이 상태 키와 겹칠 수 없다는 제약이 생겼다 (`slots`→`extract`,
  > `draft`→`generate` 로 개명). 그래프가 안 뜨면 엔진이 통째로 안 뜬다.
- **영향**: `graph/build.py`(신규) · `graph/engine.py`(`_run_pipeline` 79→5줄,
  `_apply_rule_table` → `nodes/triage.apply_rule_table` 이동) · `graph/state.py`
  (`typing_extensions.TypedDict` · `slot_llm_used`·`substance_candidates`·`llm_fallbacks` 등록) ·
  `graph/nodes/triage.py` · `graph/nodes/__init__.py` · `pyproject.toml` ·
  `scripts/draw_graph.py`(신규) · `tests/test_graph_build.py`(신규 12건) ·
  `docs/그림/질의그래프.mmd`(신규·생성물) · **05 §5** · **02 §6**
- **연관**: D-48(교훈 #6 — CI에서 안 도는 코드) · D-40(강제는 구조로) · D-22(단일 출처) ·
  D-09(하향 금지 게이트의 바닥) · D-38(틀린 문서) · P2(한 개념은 한 곳에서만)

---

### D-65 · **어느 모델로 답하는가는 설정이 정한다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`models/serving/factory.py` 신규)
- **맥락**: `client.py` 머리말은 처음부터 이렇게 적혀 있었다 —
  *"두 구현이 같은 프로토콜 뒤에 있어야 교체가 **설정 한 줄로** 끝난다."*
  **그 한 줄이 없었다.**

  ```
  graph/nodes/classify.py:154   client = APIClient()      ← 하드코딩
  graph/nodes/slots.py:96       APIClient().run(...)      ← 하드코딩
  graph/nodes/generate.py:81    APIClient().run_raw(...)  ← 하드코딩
  graph/nodes/generate.py:120   client = APIClient()      ← 하드코딩
  LocalQwenClient                아무도 생성하지 않는다
  ```

  `configs/*.yaml` 의 `model.base_id`·`revision`·`adapter_path` 는 **학습 경로만**
  읽었다. 그래서 **04 §3 비교표의 C·D 열을 채울 방법 자체가 없었다.**
  D-42 가 정한 배포 모델(Qwen3-4B)이 아니라 `gpt-4o-mini` 만 잴 수 있었다.

  **`LocalQwenClient` 에는 `run_raw` 가 아예 없었다.** `generate.py` 가 그것을
  부르므로 Qwen 으로 바꾸는 순간 `AttributeError` 다. 프로토콜이 요구하지 않아서
  구현이 빠졌고, **아무도 그 클라이언트를 만들지 않아 드러나지 않았다.**

- **결정**: `model.provider` 가 유일한 선택 지점이고, `factory.get_client()` 가 유일한 문이다.

  | `provider` | 무엇 | 04 §3 |
  |---|---|---|
  | `none` | 모델 없음 — 5태스크 전부 폴백 | 기준선 |
  | `api` | 대형 LLM 또는 **OpenAI 호환 엔드포인트**(`api_base_url`) | A / D-근사 |
  | `qwen` | `base_id` 직접 로드 (`adapter_path` 없으면 베이스) | D / C |
  | `echo` | 고정 응답 | — |

  **`none` 을 명시적인 값으로 둔다.** 예전에는 *"키가 없으면 폴백"* 이라 모델 없이
  돈 것이 **설정이 아니라 사고**였고, 리포트에 그 조건을 적을 이름이 없었다.
  이름이 있어야 측정 조건으로 기록된다 (04 §8).

- **`revision` 핀이 서빙에 걸리는 것은 이번이 처음이다.** `1cfa9a72…` 는 `qlora.py`
  두 줄에만 걸려 있었다. 07 의 `✅ model.revision 커밋 해시` 는 **학습 경로에 한한
  참**이었고, 그 범위를 밝히지 않아 *"평가한 모델이 그것"* 으로 읽혔다.
- **호스팅 API 로 Qwen 을 부르는 길을 연다** — GPU 없이 비교군 D 에 가장 가까운 조건을
  잰다. 다만 **같은 조건이 아니다**: 사업자 가중치의 revision·양자화를 확인할 수 없어
  이 핀이 걸리지 않는다. 리포트에 **`D(근사·호스팅)`** 로 적고 사유를 단다.
- **리포트에 모델 이름을 박는다.** 엔진 이름만 있어서 같은 `graph` 라도 무엇으로 잰
  건지 구분할 수 없었다. `프로토콜`에 `run_raw` 를 올려 다음 구현이 또 빠뜨리지 못하게 한다.
- **트레이드오프**:
  > 선택지를 설정으로 올려 04 비교표 4열을 전부 돌릴 수 있게 된 대신,
  > **`provider` 라는 축이 하나 늘었다.** `PROFILE × PROVIDER` 조합이 생기고,
  > 리포트에 둘 다 안 적히면 이전보다 더 헷갈린다. 그래서 모델명 출력이 선택이 아니다.
- **영향**: `models/serving/factory.py`(신규) · `models/serving/client.py`
  (`run_raw` 프로토콜화 · `_generate` 통합 · `base_url`) · `config.py`
  (`provider`·`api_model`·`api_base_url` · **`.env` 오버라이드 반영**) ·
  `configs/default.yaml` · `.env.example` · `graph/nodes/{classify,slots,generate}.py` ·
  `eval/harness/run_eval.py` · `tests/test_models_config.py` · 03 · 04 §3 · 07
- **연관**: D-40(강제는 주입·설정에) · D-42(베이스 sLLM) · D-21(대형 LLM 먼저) ·
  D-64(이름만 있던 랭그래프) · D-22(단일 출처) · D-41(비밀과 파라미터의 분리)

---

### D-66 · **되묻기는 평가에서 끄지 않는다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`configs/eval.yaml`)
- **맥락**: 평가 프로파일이 `max_clarify_turns: 0` 이었고 근거는
  *"되묻기가 섞이면 과소평가율 분모가 흔들린다 (04 §4.1)"* 였다.

  **첫 실측(2026-08-02)에서 그 근거가 전부 성립하지 않는다는 것이 드러났다.**

  | 걱정 | 실제 |
  |---|---|
  | 다회차로 번진다 | 하네스 `run()` 이 **첫 응답만** 채점한다. `ask()` 는 응답 하나만 낸다 |
  | 등급 분모가 흔들린다 | `clarify` 도 `refused` 도 `triage` 가 없다 — **똑같이 분모에서 빠진다.** `level_n` 은 동일하다 |
  | 전부 되묻기로 도피 | `--fail-missed` 가 *"정답이 CALL_NOW 이상인데 **거절·되묻기로** 빠진 건"* 을 이미 잡는다 |
  | 근거가 04 §4.1 | **04 §4.1 에 그런 문장이 없었다.** 인용이 가리키는 곳이 비어 있었다 |

  대가는 컸다. 골든셋이 `clarify` 를 정답으로 적은 **15건이 구조적으로 통과 불가**였고
  (`slot` 유형 9건 통과율 **0.0%**), *"필요한 것이 없다는 걸 알아채고 멈췄다"* 는
  **이 시스템의 핵심 안전 동작**(D-10 · D-49 · 02 §6.2)이 **측정에서 통째로 사라져 있었다.**

- **결정**: `max_clarify_turns` 를 계약 기본값(`MAX_CLARIFY_TURNS` = 2)과 같게 둔다.
  되묻기는 **정답으로 채점하고**, `refused` 로 대체 인정하지 않는다.

  ```
  clarify  →  "어떤 동물인가요? 체중은 어떻게 되나요?"           대화가 이어진다
  refused  →  "필요한 정보를 확인하지 못해 답변을 드릴 수 없습니다."   막다른 길
  ```

  응급 도메인에서 이 둘은 같은 결과가 아니다. 같다고 세면 **보고서가 시스템이
  보여주지 않은 능력을 주장**하게 된다 (원칙 2). 게다가 끈 상태에서는
  `clarify_question` 이 **애초에 생성되지 않아** 무엇을 되물었는지 §7 실패 분석에
  쓸 수 없다 — 되묻기의 *품질*은 측정 대상 밖이었다.

- **골든셋을 고쳐 맞추지 않는다.** 골든셋은 *"사용자에게 무엇이 옳은가"* 를 적는 곳이지
  *"이 프로파일이 무엇을 내는가"* 를 적는 곳이 아니다. 라벨을 `refused` 로 바꾸면
  **측정 도구의 설정을 정답에 굽는 것**이고, 02 §6.2·D-49 와 정면으로 모순된다.
- **뒤집은 것 둘** (D-57) — `tests/test_api.py::test_eval_profile_disables_clarify` 를
  `..._keeps_clarify` 로, `test_profile_overrides_default` 의 `== 0` 단언을 분리해
  `test_eval_profile_does_not_disable_clarify` 로. **지우지 않고 뒤집고 사유를 남긴다.**
- **예상되는 재라벨링** — 기존 `refused` 정답 중 실제로는 슬롯 결측인 건이 이제
  `clarify` 로 나온다. **회귀가 아니라 라벨 오류를 찾아낸 것**이다.
  자동으로 받아들이지 말고 **건별로 검수한다** (WS4).
- **트레이드오프**:
  > 되묻기를 살려 안전 동작을 측정 안으로 들여온 대신, **`answered` 분모가 줄어든다.**
  > 결측이 있는 질의가 등급까지 가지 않으므로 `level_n` 이 작아질 수 있고,
  > 분모가 작으면 비율이 무의미하다 (`--min-graded` 가 그것을 막는다).
  > 골든셋 60건 중 15건이 `clarify` 기대라는 **구성 자체를 다시 볼 필요**가 생긴다.
- **영향**: `configs/eval.yaml` · `tests/test_api.py` · `tests/test_models_config.py` ·
  **04 §4.1.1(신설 — 인용이 가리키던 빈 자리를 채웠다)** · 골든셋 검수(WS4)
- **연관**: D-10(종 없으면 검색 안 함) · D-49(증상 질의는 되묻는다) · D-57(틀린 테스트는
  뒤집는다) · D-58(검사기의 범위) · 02 §6.2 · 04 §1.2(분모를 나눈다)

---

### D-67 · **종은 물질이 아니다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`compute/vocabulary.py`)
- **맥락**: 1차 평가에서 `"고양이 캣타워 추천 좀 해주세요"`(G-015)가 D-46 의 범위밖
  방어를 그대로 통과했다. 추적하니 **`고양이` 가 물질로 매칭되고 있었다.**

  ```
  췌장염(고양이)          → 괄호로 쪼개면 부품 '고양이'   ← 종 한정어다
  자일리톨(무설탕 껌·사탕)  → 부품 '무설탕 껌'            ← 진짜 열거다 ✅
  ```

  괄호 안을 *"코퍼스가 열거한 이름"* 으로 가정했는데 **종 한정어**인 경우가 있었다.
  그 결과 `"고양이가 ~"` 로 시작하는 모든 질의가 *물질을 말한 것* 이 되고,
  `고양이` 가 6건 모호라 물질미상 되묻기로 빠졌다.

  **더 나쁜 것은 가림이었다.** `mention_in` 이 첫 매칭을 반환하므로 —

  ```
  "고양이가 향초를 핥았는데"  →  고양이   ← 코퍼스에 실재하는 '향초' 를 지웠다
  ```

  **오탐 하나가 정탐 하나를 지웠다.** `개` 는 `MIN_TERM_LEN=2` 에 막혀 살아 있었다 —
  설계가 아니라 운이었다.

- **결정**: 종을 가리키는 말은 물질 어휘에서 뺀다. **차단 목록이 아니라 타입 구분이다** —
  종은 이 모듈이 이미 아는 다른 종류의 어휘이고, 물질과 겹칠 수 없다 (D-60 이 걷어낸
  `_BLOCK_AFTER` 와는 성격이 다르다).
- **함께 고친 것 둘**
  - **부품도 가장 긴 것을 고른다** — 전체 이름(`whole`)과 같은 규칙이다. 예전에는 첫
    매칭을 반환해서 **`load_vocabulary()` 의 파일 순서가 답을 정했다.** 순서에 의존하는
    판정은 자료가 한 줄 늘면 조용히 바뀐다 (D-22).
  - **`resolve_substance` 의 문에서도 막는다** — ②가 종을 물질로 올려도 폐쇄 목록에
    못 들어간다 (D-59 · 문에서 막는다).
- **`SPECIES_WORDS` 를 `compute/vocabulary.py` 로 옮겼다.** 예전에는 `graph/nodes/slots.py`
  가 들고 있었고, **그래서 물질 어휘 쪽에서 종을 걸러낼 방법이 없었다.** 근본 원인이
  거기였다 (P2 · D-22).
- **영향**: `compute/vocabulary.py`(`SPECIES_WORDS`·`_SPECIES_TERMS`) ·
  `graph/nodes/slots.py` · `tests/test_vocabulary.py`(신규 5건)
- **연관**: D-60(낱말 경계) · D-61(관련성) · D-62(정규화기) · D-46(도메인 밖 거절) · D-22

---

### D-68 · **물질을 못 올린 이유를 셋으로 가른다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/nodes/slots.py` · `graph/build.py`)
- **맥락**: ②가 물질을 못 올리면 **무조건 결측 → 되묻기**였다. 그런데 못 올린 이유가
  셋이고, **셋의 옳은 행선지가 서로 다르다.**

  | 이유 | 예 | 예전 | 옳은 것 |
  |---|---|---|---|
  | **모호** | `세제`(후보 2건) | 되묻기 | **진행.** 후보를 검색어로 넘긴다 |
  | **종밖** | `향초`(covers={'bird'}) 인데 고양이 질의 | 되묻기 | **근거없음 거절** |
  | **없음** | `실리카겔` | 되묻기 | 되묻기 ✅ |

  **`모호` 는 D-62 가 이미 정해 둔 것이었다** — *"모호는 실패가 아니다. 후보는 버리지
  않고 검색어로 전부 넘긴다. `없음` 만 되묻기로 간다."* 그런데 `slots.py` 는
  `substance_candidates` 를 **실어 놓고도 결측으로 셌다.** 결정의 절반이 미구현이었다.

  **`종밖` 은 새 갈림길이다.** `향초` 자료는 조류 것뿐이라 고양이에게는 **근거가 없는
  것**이지 물질을 모르는 것이 아니다. 되물어도 답이 안 나온다 —
  **응급 상황에서 못 쓸 질문은 거절보다 나쁘다.** 조류 자료를 고양이에 적용하는 것은
  더 나쁘다 (D-10). 슬롯에 **넣지 않고** 신호만 남겨 `근거없음` 으로 보낸다.

- **결정**: `_after_slots` 라우터에 세 번째 가지를 둔다.

  ```
  종밖 → refuse_nohit(근거없음)   ·   결측 → clarify   ·   그 외 → filter
  ```

- **트레이드오프**:
  > 되묻기를 줄여 *"모르겠다"* 대신 *"찾아봤는데 근거가 없다"* 로 답하게 됐다.
  > 정직해졌지만 **`answered → refused` 가 늘었다** — 사용자가 받는 것은 여전히 거절이다.
  > 후보 2건으로 검색해도 근거가 안 나오는 경우(`세제`·`방향제`)가 그것이고,
  > 그건 어휘 문제가 아니라 **자료 결손**이다 (04 §7).
- **영향**: `graph/nodes/slots.py` · `graph/build.py`(`_after_slots`) ·
  `graph/state.py`(`off_species_substance`)
- **연관**: D-62(모호는 실패가 아니다) · D-49(증상·미상은 되묻는다) · D-10(종 격리) ·
  D-58(검사기의 범위) · D-64(그래프가 순서의 단일 출처)

---

### D-69 · **설정에 키를 두면 코드가 읽어야 한다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`tests/test_config_is_read.py`)
- **맥락**: 2026-08-02 하루에 **같은 사고가 네 번** 났다.

  | | 무엇이 적혀 있었나 | 실제 |
  |---|---|---|
  | `langgraph` | `pyproject` 선언 · 폴더 이름 `graph/` · docstring *"LangGraph 기반"* | `import langgraph` **0곳** (D-64) |
  | `model.base_id`·`revision`·`adapter_path` | `configs/*.yaml` | 학습만 읽고 **서빙엔 안 닿음** (D-65) |
  | `model.load_in_4bit: true` | `configs/*.yaml` | `LocalQwenClient` 가 **안 읽음.** bf16 으로 올라감 |
  | `PETTRIAGE__…` | `.env.example` 이 예시로 안내 | `.env` 의 그 줄이 **한 번도 안 먹음** |

  마지막 것이 특히 나쁘다. `_env_overrides` 가 `os.environ` 만 봤고 pydantic-settings 는
  `.env` 를 `os.environ` 에 내보내지 않는다. `extra="ignore"` 라 **경고도 예외도 없이
  버려졌다.** `UnknownConfigKey` 가 오타를 잡으려고 만들어져 있었는데, 그 옆에
  **파일 전체가 안 읽히는 경로**가 있었다.

  네 건의 결과가 같다 — **사람이 설정을 바꿨다고 믿고 안 바뀐 조건으로 측정한다.**
  04 §8 재현성이 무너지는 방식이 오타와 똑같다.

- **결정**: `tests/test_config_is_read.py` 가 검사한다. **문서로 적어 두지 않는다.**

  | 검사 | 무엇을 막나 |
  |---|---|
  | `every_config_key_is_read` | 설정에 있는데 코드가 아무 데서도 안 읽는 키 |
  | `unread_list_has_no_stale_entries` | 구현했는데 면제 목록에 남아 **검사가 무뎌지는 것** |
  | `serving_model_config_reaches_the_client` | 학습만 읽고 **서빙엔 안 닿는** 사각지대 (가중치 없이) |
  | `dotenv_example_only_advertises_working_overrides` | `.env.example` 이 없는 키를 안내하는 것 |
  | `revision_pin_is_paired_with_base_id` | 저장소를 바꾸고 커밋 해시를 안 바꿔 **8GB 받다가 404** |

- **못 읽는 것이 맞으면 사유와 함께 면제한다** (`UNREAD_BY_DESIGN`). 지우지 않는다 —
  *"못 하는 것을 한다고 적지 않는다"* (D-58). 첫 실행에서 **4개**가 나왔다.

  ```
  retrieval.rerank            리랭커 미구현. 켜도 아무 일도 안 일어난다 (04 E2)
  retrieval.chunk_strategy    적재 전략은 build_index.py 가 고정 (04 E1)
  retrieval.fixed_chunk_size  위와 같다
  triage.llm_fallback         게이트는 llm_level 유무로 이미 분기. 미배선
  ```

  **면제 목록이 길어지는 것 자체가 신호다.** `configs/default.yaml` 에도 `⚠ 미구현` 을 달았다.
- **`.env` 오버라이드를 실제로 동작하게 만들었다.** `.env` + 셸 둘 다 보고 **셸이 이긴다** —
  `.env` 는 그 컴퓨터의 상시 설정이고 셸 변수는 *"이번 한 번"* 이다. 한 번짜리가
  상시를 이겨야 04 §3 비교군을 한 줄로 갈아끼울 수 있다.
- **트레이드오프**:
  > 이름 기반 grep 이라 **정확하지 않다.** `load_in_4bit` 이 `qlora.py` 에 있으면
  > "읽힌다"로 세어져 *서빙에서 안 읽는* 사각지대는 못 잡는다. 그래서
  > `serving_model_config_reaches_the_client` 를 따로 뒀다. **검사의 한계를
  > 아는 것까지가 검사다** (D-58).
- **영향**: `tests/test_config_is_read.py`(신규 5건) · `config.py`(`_override_sources`) ·
  `models/serving/client.py`(dtype·4bit·device_map 반영) · `configs/default.yaml` ·
  `.env.example` · `tests/test_models_config.py`(신규 3건)
- **연관**: D-40(강제는 구조로) · D-22(단일 출처) · D-58(못 하는 것을 적지 않는다) ·
  D-64 · D-65 · 04 §8(재현성)

---

### D-70 · **선택 의존성은 최상단에서 임포트하지 않는다** 🔒 (D-48 보강)

- **상태**: 확정 (2026-08-02) · **구현 완료** (`app/chat_logger.py`)
- **맥락**: **같은 사고가 두 번째다.**

  ```
  1차  auth/pets      DATABASE_URL 이 있을 때만 로드되는 구조라 CI 에서 임포트조차 안 됐다
                      → ci.yml 에 `db-deps` 잡을 만들었다 (D-48)
  2차  chat_logger    routes/ask.py → chat_logger → `from sqlalchemy import func` (최상단)
  ```

  `ask` 는 **DB 없이도 도는 핵심 경로**인데, `.[api,dev]` 만 깐 CI test 잡에서
  `conftest.py` 조차 못 열려 **pytest 가 시작도 못 했다.**

  ```
  ImportError while loading conftest '.../tests/conftest.py'
  src/pettriage/app/chat_logger.py:18: from sqlalchemy import func
  ModuleNotFoundError: No module named 'sqlalchemy'
  → exit code 4 (usage error — 테스트 실패가 아니라 시작 실패)
  ```

  docker 잡에서는 uvicorn 이 앱을 못 올렸다. **세 잡 중 둘이 같은 한 줄 때문에 죽었다.**

  D-48 이 *"팀원 코드는 계약·주입·설정 구조 안으로 흡수한 뒤 병합한다"* 고 정했는데,
  **흡수 체크리스트에 이 항목이 없었다.** 있었으면 1차에서 끝났을 일이다.

- **결정**: D-48 의 흡수 체크리스트에 넣는다.

  > **선택 의존성(DB·GPU·학습)을 모듈 최상단에서 임포트하지 않는다.**
  > 그것을 쓰는 임포트는 **쓰는 자리 안**에 둔다. `db is None` 이면 안 쓰므로 안 부른다.

  ```python
  # ✗  from sqlalchemy import func          ← 모듈 최상단
  # ✓  def log_chat_turn(db, ...):
  #        if db is None: return
  #        from sqlalchemy import func      ← db 가 있다 = [db] 가 깔렸다
  ```

- **예외 처리도 함께 바꿨다.** `except SQLAlchemyError` 를 잡으려면 그 이름을 최상단에서
  임포트해야 하고, **그것이 원인이었다.** `except Exception` 으로 합치고 예외 **종류를
  로그에 남겨** 구분을 유지한다. *잡는 이름 때문에 임포트가 올라가는 것*을 경계한다.
- **왜 ruff 가 못 잡았나** — 문법은 맞다. `pyproject` 의 `[db]` extra 선언도 맞다.
  **틀린 것은 "이 경로가 그것 없이 돌아야 한다"는 사실뿐**이고, 그건 정적 검사가 모른다.
  CI 의 `db-deps`·`docker` 잡이 그것을 아는 유일한 자리다 (D-48 교훈 #6).
- **트레이드오프**:
  > 함수 안 임포트는 호출마다 조회 비용이 있고(캐시되므로 무시 가능),
  > 무엇보다 **읽는 사람이 의존성을 한눈에 못 본다.** 그래서 머리말에 이유를 길게 적는다 —
  > 다음 사람이 *"왜 여기 있지"* 하며 최상단으로 올리면 같은 사고가 세 번째다.
- **영향**: `app/chat_logger.py` · `ci.yml`(잡 셋이 이것을 잡는다) · **D-48 흡수 체크리스트**
- **연관**: D-48(흡수 후 병합 · 교훈 #6) · D-64(CI 에서 안 도는 코드) · D-69

---

### D-71 · **LangChain 은 LLM 연동에 쓰고, 벡터DB는 직접 붙인다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`models/serving/client.py::LangChainClient`)
- **맥락**: 필수 산출물이 이렇게 적고 있다.

  > · **LangChain을 활용하여** 벡터데이터베이스와 LLM 연동

  그런데 점검하니 **`langchain` 이 소스 어디에서도 쓰이지 않았다.**

  ```
  src/pettriage/config.py:274   langchain_api_key   ← 설정 키 이름뿐
  ```

  실제로 쓰던 것 — 벡터DB `chromadb` **직접** · LLM `openai` SDK **직접** ·
  오케스트레이션 `langgraph`. LangGraph 가 LangChain 생태계이고 `langchain-core` 가
  핵심 의존성이긴 하나, **`langchain_chroma`·`langchain_openai` 는 하나도 안 썼다.**
  D-64 와 같은 모양이다 — 이름만 있고 물건이 없었다.

- **결정**: **LLM 연동은 LangChain 으로. 벡터DB는 직접.** 반반이 아니라 **이유가 있는 반반**이다.

  ```
  provider=langchain   langchain_openai.ChatOpenAI + langchain_core 메시지 타입
  provider=api         같은 모델을 openai SDK 로 직접
  ```

  **둘은 같은 모델을 부른다.** 다른 것은 *무엇을 거쳐 부르는가* 뿐이다.
  그래서 결과가 같아야 하고, **같다는 것이 곧 `LLMClient` 프로토콜이 실제로 갈아
  끼울 수 있는 추상이라는 증거다** (D-21). 04 §5 에 그대로 싣는다.

- **벡터DB를 추상 뒤에 두지 않는 이유** — 게을러서가 아니다. 우리 검색 계층에는
  LangChain 기본 동작에 없거나 우회가 필요한 것이 다섯 있다.

  | | 무엇 | 근거 |
  |---|---|---|
  | ① | `filter_by_threshold` | D-46 — 실측 임계값. 근거있음 0.531~0.728 / 없음 최고 0.574로 **겹친다** |
  | ② | `dedupe_by_substance` | 같은 물질 접기. `Hit.merged_sources` 로 출처를 보존한다 |
  | ③ | `to_chroma_where` | 빈 필터를 `EmptyFilter` 로 (D-56) |
  | ④ | `publisher`·`locator` 메타 | `Citation.publisher` 가 필수다 (D-37) |
  | ⑤ | `route` 별 인용 가부 | 경로②는 원문 인용을 실을 수 없다 (D-37) |

  > **D-46 은 실측에서 나온 우리 도메인의 사실이고, 그런 판단은 추상 뒤에서 못 한다.**
  > 임계값이 거절을 만들지 못한다는 것을 알려면 분포를 직접 재야 했고,
  > 재고 나서 *"임계값은 잡음 하한일 뿐"* 이라는 설계를 코드에 새겨야 했다.

- **프롬프트는 갈라지지 않는다.** `models/prompts.py` 가 단일 출처이고
  `LangChainClient._to_messages` 가 그것을 LangChain 메시지 타입으로 **옮기기만** 한다.
  갈라지면 04 §3 이 태스크별로 재는 지표가 **무엇을 잰 건지 모르게 된다** (D-22).
- **트레이드오프**:
  > 의존성이 하나 늘고(`langchain-openai`), 같은 일을 하는 클라이언트가 둘이 됐다.
  > 대신 필수 산출물 문구를 **실물로** 충족하고, 프로토콜 추상이 진짜인지
  > 검증할 수단이 생겼다. **벡터DB까지 옮기지 않은 것은 마감 이틀 전이라는 사정도
  > 있다** — 위 다섯을 어떻게 유지할지 먼저 답해야 하고, 그 답 없이 옮기면
  > 오늘 잰 기준선이 통째로 무효가 된다.
- **미결**: 벡터DB를 `langchain_chroma` 로 옮길 것인가. **옮긴다면 위 다섯을 어떻게
  유지할지가 선행 조건이다.** `tests/test_langchain_wiring.py` 의 마지막 테스트가
  그 표시로 남아 있다 — 금지가 아니라 기록이다.
- **영향**: `models/serving/client.py`(`LangChainClient` 신규) · `factory.py` ·
  `arms.py`(`A-LC`) · `config.py`(`provider` 에 `langchain`) · `pyproject.toml`
  (`langchain-openai`) · `tests/test_langchain_wiring.py`(신규 5건) · **04 §3 · §5**
- **연관**: D-21(대형 LLM 먼저, 프로토콜 뒤에서 교체) · D-65(서빙 모델 선택) ·
  D-46(임계값) · D-37(인용) · D-22(단일 출처) · D-64

---

### D-72 · **계약이 검증하는 필드는 만드는 쪽이 반드시 채운다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/engine.py::_triage_result`)
- **맥락**: 응답 계약이 검증하는 필드를 **아무도 채우지 않는** 일이 하루에 세 번 나왔다.

  | 필드 | 계약이 무엇을 보나 | 실제 |
  |---|---|---|
  | `assumed_substance` | 가정이 문장에 실렸나 | `_build_response` 가 안 옮김 → **계약이 한 번도 발동 안 함** (D-63) |
  | `identified_substance` | 폐쇄 목록 안인가 | 위와 같음 |
  | `overridden` | `overridden == (llm < rule)` 인가 | `_triage_result` 가 **안 실음** |

  앞의 둘은 **빈 필드라 검사할 것이 없어** 조용히 통과했다. 세 번째는 반대로 **터졌다** —

  ```
  rule_level=4 · llm_level=1   (하향 금지 게이트가 막아야 하는 바로 그 경우)
  → ValidationError: overridden=False 이 rule_level=4 · llm_level=1 과 맞지 않는다
  → 응답을 만들 수 없다 → 판정불가 거절
  ```

  **게이트가 가장 중요한 일을 하는 그 순간에 답이 안 나갔다.** `llm_level` 이 늘
  `None` 이었기 때문에(D-65) 이 경로가 **한 번도 안 돌아** 드러나지 않았다.

- **결정**: 계약에 검증기가 붙은 필드는 **만드는 층이 반드시 채운다.** 기본값에 기대지 않는다.

  > 검사기와 만드는 쪽이 다른 층에 있는 것은 옳다 (D-58). 그런데 **만드는 쪽이 안
  > 채우면 검사기는 아무 말도 하지 않거나(빈 필드) 엉뚱한 때 터진다(기본값 불일치).**
  > 둘 다 *"검사가 있으니 괜찮다"* 를 거짓으로 만든다.

- **`overridden` 의 정의는 `gate.py` 한 곳에서 온다** (`llm < rule`). 엔진이 다시 계산하는
  것은 중복이지만, `apply_gate` 의 `TriageDecision` 이 상태로 넘어오지 않는 현재 구조에서는
  불가피하다. **정의가 두 곳에 적히는 것 자체가 부채**이므로 기록한다.
- **어떻게 잡았나** — GPU 도 모델도 없이 잡았다. `llm_level` 을 손으로 넣어 응답을
  만들어 보면 된다. **"게이트가 작동한다"는 코드의 성질이라 LLM 품질과 무관하게
  증명할 수 있고, 오히려 가짜 LLM 으로 증명하는 것이 더 엄밀하다** —
  실제 모델이 우연히 낮은 등급을 안 냈을 뿐인 것을 *"작동한다"* 고 말할 수 없다.
- **영향**: `graph/engine.py`(`_triage_result`) · D-63 · D-09(하향 금지 게이트) · 04 §5
- **연관**: D-09 · D-58(검사기의 범위) · D-63(추정 공개) · D-65 · D-40

---

### D-73 · **코드가 검증하는 목록을 모델도 알아야 한다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`models/tasks.py` · `models/prompts.py`)
- **발견**: **이서은 팀원** — 골든셋 60건을 API 키 없이/있이 두 번 돌려 비교하다 찾았다.
- **맥락**: ①분류 프롬프트가 이렇게 적혀 있었다.

  ```
  "허용된 라벨 중 **하나만** 출력한다."      ← 그 라벨이 무엇인지는 없다
  ```

  코드는 `ALLOWED_INTENTS` 로 **정확히** 대조했다. 모델은 보기를 모르니 그럴듯한 말을 냈다.

  ```
  intent 허용목록 밖: '위험성우려' → 'unknown'
  intent 허용목록 밖: 'high_risk'  → 'unknown'
  ```

  `unknown` 은 D-46 상 **검색 없이 거절**이다. 그래서 결과가 뒤집혔다 —

  | 조건 | 통과율 |
  |---|---|
  | 키 없음 (키워드 폴백) | **10.0%** |
  | gpt-4o-mini | **3.3%** |

  **진짜 LLM 을 붙였더니 더 나빠졌다.** 폴백은 목록을 아는 코드가 만들고,
  LLM 은 목록을 모르는 채 답했기 때문이다.

  같은 파일 안에서 ④검증은 `근거있음`/`근거없음`/`모순` 을 **적어 두고 있었다.**
  하나는 적고 하나는 안 적었다 — 그래서 아무도 못 봤다.

- **결정**: **출력 어휘는 `TaskSpec.labels` 한 곳에 둔다.** 프롬프트가 렌더하고,
  검증기가 같은 것을 읽는다.

  ```python
  SPECS[Task.CLASSIFY].labels          # 단일 출처
  ALLOWED_INTENTS = SPECS[...].labels  # 검증기
  _label_block(task)                   # 프롬프트가 렌더
  ```

  **프롬프트에 손으로 적지 않는다.** 적으면 라벨이 하나 늘 때 같은 사고가 반복된다.
  실제로 `ALLOWED_INTENTS` 에 `" "` 가 들어가 있던 사고(같은 날)도 **두 곳에 적혀
  있어서** 드러나지 않았다.

- **라벨은 영문 토큰 그대로 싣는다.** 코드가 대조하는 것은 `intoxication`·`symptom`·
  `nutrition`·`general` 이다. 프롬프트에 *"중독/증상/영양/일반"* 이라고 적으면
  모델이 한글을 내고 **여전히 전부 걸린다.** 뜻은 `label_hints` 로 따로 붙인다.
- **`general` 의 설명을 반드시 붙인다.** D-46 상 `general` 은 *"우리가 다루지 않는
  질문"* 인데, 설명 없이는 모델이 *"일반적인 질문"* 으로 읽는다. 그러면 중독 질의가
  `general` 로 가 **검색 없이 거절**된다 — 과소평가 방향이다 (D-13).
- **왜 검사가 못 잡았나** — 폴백 경로(키 없음)에서는 `_keyword_classify` 가 답을
  만들어 **LLM 경로가 한 번도 안 돌았다.** D-65 가 고쳐지고 실제 모델이 붙은
  **첫날에 드러났다.** *"키 없이 돌린 초록"* 이 무엇을 증명하지 않는지 보여주는 사례다.
- **트레이드오프**:
  > 프롬프트가 길어진다(라벨 4줄 + 설명). ①분류는 출력이 한 단어라 입력 토큰이
  > 상대적으로 크고, Qwen3-4B 에서는 그 비용이 더 눈에 띈다. 그래도 **보기를 모르는
  > 모델의 답은 버려지므로**, 안 싣는 것은 비용을 아끼는 게 아니라 호출을 버리는 것이다.
- **영향**: `models/tasks.py`(`TaskSpec.labels`·`label_hints`) · `models/prompts.py`
  (`_label_block`) · `graph/nodes/classify.py`(`ALLOWED_INTENTS`) ·
  `tests/test_models_config.py`(신규 5건) · 03 §2 · 05 §4 ①
- **연관**: D-22(단일 출처) · D-38(LLM은 제안·코드는 판정) · D-46(도메인 밖 거절) ·
  D-65(서빙 모델 선택) · D-69(설정이 코드에 닿는가) · P2

---

### D-74 · **의존성을 올릴 때는 연쇄를 먼저 본다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`constraints.txt` · `pyproject.toml`)
- **맥락**: D-71 로 `langchain-openai>=0.3.35` 를 넣었더니 **설치가 통째로 죽었다.**

  ```
  ERROR: ResolutionImpossible
  langchain-openai 0.3.35  →  langchain-core >= 0.3.78   (핀은 0.3.63)
                           →  openai        >= 1.104.2   (핀은 1.59.6)
  langchain-core   0.3.86  →  langsmith     >= 0.3.45    (핀은 0.2.10)
  langchain-community 0.3.24 → langchain    >= 0.3.25    (핀 없음 → pip 이 최신을 집는다)
  ```

  **한 줄을 넣었더니 넷을 올려야 했다.** 마감 이틀 전에 임베딩·오케스트레이션 계열을
  통째로 올리는 것은 **오늘 잰 기준선을 무효로 만드는 일**이다 (04 §8).

- **결정**: **연쇄를 만들지 않는 가장 높은 버전을 고른다.** `langchain-openai==0.3.8` 이
  기존 핀 전부와 그대로 맞는다. 넷 다 그대로 두고 450건 통과를 확인했다.

  > 최신이 좋은 것이 아니라 **맞물리는 것이 좋다.** 우리가 쓰는 것은 `ChatOpenAI`
  > 하나이고, 그 API 는 0.3.8 과 0.3.35 사이에 바뀌지 않았다.
  > **한 칸 낮은 버전이 넷을 올리는 것보다 싸다.**

- **`langchain` 을 핀에 명시한다.** `langchain-community` 가 umbrella 패키지를 끌고 오는데
  핀이 없어서 pip 이 최신(0.3.30)을 집었고, 그것이 `core>=0.3.85` 를 요구해 충돌했다.
  **전이 의존성이 해석을 흔들면 그것도 핀 대상이다** (04 §8 재현성).
- **방법** — 손으로 맞추지 않는다. 버전을 **열어 두고 pip 에게 풀게 한 뒤 결과를 박는다.**

  ```
  pip install --dry-run -e '.[api,rag,ingest,dev]' -c constraints.txt
  → Would install ... langchain-openai-0.3.8 langchain-0.3.25 ...
  ```

- **트레이드오프**:
  > 낡은 버전에 묶인다. `langchain-openai 0.3.8` 은 새 모델·새 파라미터를 모를 수 있고,
  > 언젠가는 넷을 함께 올려야 한다. **그 시점은 마감 이후이고, 그때는 기준선을
  > 다시 재는 것까지가 작업이다.**
- **영향**: `constraints.txt`(`langchain-openai`·`langchain` 핀 추가) · `pyproject.toml`
- **연관**: D-71(LangChain 사용 범위) · D-64(langgraph 핵심 의존성화) · 04 §8(재현성)

---

### D-75 · **채우는 사람만 있고 읽는 사람이 없는 값은 없는 값이다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/state.py` · `graph/engine.py` · `app/contracts.py`)
- **맥락**: `GraphState` 의 키를 **쓰는 곳 / 읽는 곳**으로 훑었더니 다섯이 끊겨 있었다.

  | 키 | 채우는 곳 | 읽는 곳 |
  |---|---|---|
  | `llm_fallbacks` | `build._answered` | **없음** |
  | `computed` | `nodes/compute.py` | **없음** |
  | `removed_contacts` | `nodes/generate.finalize` | **없음** |
  | `slot_llm_used` | `nodes/slots.py` | **없음** |
  | `risk` | `nodes/classify.py` (`intent` 사본) | **없음** |

  전부 *"노드가 돌려주기는 한다"* 라서 노드 테스트는 초록이었고, 계약 테스트는
  응답이 유효한지부터 봤다. **그 사이를 보는 테스트가 없었다.**

- **결정**:
  1. **응답까지 나가게 한다** — `llm_fallbacks` · `computed` · `removed_contact_count`
     를 `AskResponse` 에 싣는다.
  2. **읽는 곳을 만들 수 없으면 지운다** — `risk` · `slot_llm_used` 는 상태에서 뺀다.
     남겨 두면 *"누군가 쓰고 있겠지"* 로 보여 다음 사람이 못 지운다.
  3. `tests/test_state_reaches_response.py` 가 **노드와 계약 사이**를 지킨다.
- **근거**: D-16 이 *"수치는 코드가 계산한다"* 로 정한 그 수치(`dose_per_kg`)가
  **응답 어디에도 없었다.** 등급은 나가는데 그 등급을 만든 수치는 안 나갔다 —
  산출물 ④에서 *"코드가 계산했다"* 를 보일 물증이 없다.
- **트레이드오프**:
  > 계약이 넓어진다. 대신 **관측되지 않는 계산은 안 한 것과 구별되지 않는다.**
  > 답변 *문장*에 수치를 넣는 것은 이 결정에 **포함하지 않는다** — 초안 프롬프트가
  > 바뀌면 답이 달라지고, 그러면 기준선과 비교군을 같은 자로 잴 수 없다 (04 §8).
- **영향**: `graph/state.py` · `graph/engine.py` · `graph/build.py` ·
  `graph/nodes/{classify,slots,generate}.py` · `app/contracts.py` · `app/safety_engine.py`
- **연관**: D-16(수치는 코드가) · D-22(단일 출처) · D-47(연락처) · D-72(계약 필드는 만드는 쪽이 채운다)

---

### D-76 · **폴백은 다섯 태스크가 같은 문으로 남긴다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/fallbacks.py` · `eval/harness/`)
- **맥락**: 폴백 기록(`LLM_FALLBACKS`)이 `nodes/generate.py` **안에** 있었다.
  그래서 그 파일의 노드(③압축·⑤평이화·초안·트리아지)만 남겼고,
  **①분류와 ②슬롯은 `get_client()` 가 `None` 이어도 조용히 넘어갔다.**

  결과 — 비교군 A 를 재도 *"다섯 태스크 중 몇 개가 실제로 모델을 탔나"* 를 셀 수 없다.
  성적이 나쁘면 **모델이 못한 것인지 모델이 안 불린 것인지 구분이 안 된다.**
  이서은 팀원이 잡은 D-73(라벨 누락으로 LLM 이 키워드 폴백보다 나빴다)이 오래 안 보인
  이유가 정확히 그 구분이 없었기 때문이다.

- **결정**:
  1. 기록을 노드 밖 `graph/fallbacks.py` 로 꺼낸다. **다섯 태스크가 같은 문을 쓴다.**
  2. 채우는 곳은 `engine._run_pipeline` **하나**다 — 성공 종료 노드에 두면
     되묻기·거절로 끝난 건이 기록을 잃는다. 비우기와 읽기가 같은 요청 경계에 있다.
  3. 하네스 리포트에 **다른 지표보다 먼저** 싣는다. 전 건이 폴백이면 🔴 로 경고한다.
- **근거**: 04 §8 *"조용히 스텁으로 내려가면 지표가 스텁으로 산출된다"* 와 같은 성질이다.
  **기록하는 자리가 한 노드 안에 있으면, 다른 노드는 안 하는 것이 기본값이 된다.**
- **트레이드오프**:
  > 프로세스 전역 집합이라 **스레드 안전하지 않다.** 지금 하네스·테스트·개발 서버는
  > 요청을 겹쳐 돌리지 않는다. 동시 요청을 받게 되면 상태 채널로 옮겨야 한다 —
  > 그 조건을 모듈 머리말에 적어 둔다.
- **영향**: `graph/fallbacks.py`(신규) · `graph/nodes/{classify,slots,generate}.py` ·
  `graph/{engine,build}.py` · `eval/harness/{metrics,run_eval}.py`
- **연관**: D-73(라벨) · D-65(모델 선택) · 05 §6(폴백은 기록한다) · 04 §3(비교군)

---

### D-77 · **주석이 막는다고 적은 것은 코드가 막아야 한다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/nodes/generate.py::judge_triage`)
- **맥락**: `build._after_retrieve` 가 재검색 경로에 이렇게 적어 두었다 —

  > *"재검색이면 계산·판정을 다시 하지 않는다. 다시 돌리면 같은 입력에 LLM 판정이
  > 한 번 더 끼어들어 등급이 흔들린다."*

  그런데 목적지 `compress` 뒤에 `generate → judge → decide` 가 그대로 이어진다.
  **실제로 건너뛴 것은 `compute`·`rules` 둘뿐이고 `judge` 는 매번 다시 돌았다.**
  막으려던 것을 못 막고 있었고, 주석이 있어서 **막고 있는 줄 알았다.**
- **결정**: 차단을 `judge_triage` 안에 둔다 (`retry_count > 0` 이면 그대로 반환).
  라우터에 간선을 하나 더 두지 않는다 — *"어느 경로로 왔나"* 를 라우터 둘이
  나눠 알게 되면 그 자체가 다음 사고다 (D-40).
- **근거**: 같은 질의가 회차마다 다른 등급을 내면 04 §8 의 재현성이 깨진다.
- **트레이드오프**:
  > 재검색으로 근거가 **좋아진** 경우에도 LLM 등급을 다시 받지 않는다.
  > 등급은 규칙이 바닥을 잡고 있고(D-50), 재검색의 목적은 *근거 문장*이지 등급이 아니다.
- **영향**: `graph/nodes/generate.py` · `graph/build.py`(주석을 사실에 맞춘다)
- **연관**: D-40(단일 출처) · D-09(하향 금지 게이트) · 04 §8(재현성)

---

### D-78 · **먹은 무게와 유효성분 무게는 다르다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`compute/content.py` · `compute/tables/성분함량.csv`)
- **맥락**: 역치는 **유효성분** 기준이다. `F-034-020` 은 *"개는 테오브로민·카페인
  20 mg/kg 에서 임상징후"* 라고 말한다. 그런데 계산 노드는 **초콜릿 무게**를 그 역치에
  그대로 댔다.

  ```
  밀크초콜릿 20 g / 5 kg = 4 g/kg = 4,000 mg/kg → 중증(60) 초과 → EMERGENCY
  테오브로민 2 mg/g 적용   20 g × 2 = 40 mg
                          40 mg / 5 kg =   8 mg/kg → 임상징후(20) 미만 → MONITOR
  ```

  **200배 크게 쟀다.** 과대는 이 도메인에서 의도된 편향이지만(D-13), 골든셋이
  `MONITOR` 라고 적어 둔 질의에 *"지금 병원"* 을 내는 것은 편향이 아니라 오답이다.

  ⚠️ **계수는 처음부터 자료에 있었다.** `F-034-023~026` 이 `threshold_type=성분 함량`
  으로 `>14 · 5 · 2 mg/g` 를 적어 두었는데, `build_rule_table.py` 가 *계산 가능한 역치
  종류만* 남기면서 **그 네 행을 통째로 버렸다.** 07 의 해결 과제에 *"초콜릿 테오브로민
  함량 계수 없음(WS1)"* 이라고 적혀 있었으나 **사실이 아니었다** — 자료가 아니라
  파생이 빠뜨린 것이다.

- **결정**:
  1. `compute/tables/성분함량.csv` 를 둔다. **`별칭.csv` 와 같은 지위** — 손으로 적되
     `basis` 를 남기고 검사기가 사실 표와 한 줄씩 대조한다 (`tests/test_content.py`).
     생성물이 아닌 이유는 `active_substance`(함량 행 ↔ 역치 행의 연결)가 **사실 표에
     없는 연결**이기 때문이다. 같은 논문에서 왔다는 것은 사람이 읽어야 안다.
  2. `compute_metrics` 가 계수가 있는 물질만 *무게 → 유효성분* 을 거친다.
     **계수가 없으면 예전 그대로** — 마늘 5 g/kg 처럼 역치 자체가 물질 무게 기준인
     경우가 있다.
  3. `apply_rule_table` 은 `active_substance` 로 역치를 찾는다. 이 치환이 없으면
     **물질을 정확히 알아낸 질의가 판정불가로 거절된다** (`밀크 초콜릿` 은 역치 행이 0개다).
  4. **수치가 없는 행을 0 으로 적지 않는다.** 화이트 초콜릿의 원문은 *"테오브로민
     급원으로서는 무의미한 수준으로 간주됨"* 이고 숫자가 아니다. `bound=무의미` 로 두고
     정량 판정을 건너뛴다 → 역치 미만(`MONITOR`) 경로로 간다. **답은 같지만
     말할 수 있는 것만 말한 답이다** (D-16).
  5. 붙여 쓴 표기(`밀크초콜릿`)를 별칭으로 올린다 — 어휘표는 `밀크 초콜릿` 으로 띄어 쓴다.
     이 공백 하나 때문에 프로브 8건 중 **5건이 되묻기로 빠졌다.**
- **검산**: 골든셋 네 건이 **적어 둔 등급에 정확히** 떨어진다 (계수 없이는 넷 다 EMERGENCY).

  | 케이스 | 계산 | 등급 | 골든셋 |
  |---|---|---|---|
  | G-028 밀크 20g/5kg | 8 mg/kg | MONITOR | MONITOR ✅ |
  | G-029 밀크 60g/5kg | 24 mg/kg | CALL_NOW | CALL_NOW ✅ |
  | G-030 다크 50g/5kg | 50 mg/kg | EMERGENCY | EMERGENCY ✅ |
  | G-041 화이트 100g | (정량 안 함) | MONITOR | MONITOR ✅ |

- **트레이드오프**:
  > 🔴 **개에만 적용된다.** 코퍼스에 고양이 초콜릿 정량 역치가 **0행**이고 정성 등급도
  > 없다 — `초콜릿` 계열 고양이 사실 4건이 전부 수치·등급 없는 서술이다. 계수를
  > `dog|cat` 으로 적으면 고양이는 *역치가 없는 이름*을 가리키게 되어 **판정불가**가 된다.
  > 그래서 표와 별칭 모두 `dog` 로 좁혔다. **고양이가 `밀크초콜릿` 이라고 적으면 지금도
  > 물질을 못 올린다** — 자료의 공백이고, 코드로 메우면 그것이 환각이다 (D-09 와 같은 판단).
  >
  > `>14 mg/g` 는 **하한**으로 읽는다(`bound=이상`). 14 로 계산하면 실제보다 낮게 나올 수
  > 있고 그 방향이 과소평가다. 지금은 표시만 남기고, 경계에서 올릴지는 미결이다.
- **영향**: `compute/content.py`(신규) · `compute/tables/성분함량.csv`(신규) ·
  `compute/tables/별칭.csv`(5행) · `graph/nodes/{compute,triage}.py` · `tests/test_content.py`
- **연관**: D-16(수치는 코드가) · D-13(과소평가 우선) · D-51(식물 부위 환산 — 같은 성질) ·
  D-50(바닥 등급) · D-09(종별로 자료가 다르다) · D-60(별칭은 낱말 경계로)

---

### D-79 · **눈을 가리고 시킨 판단은 판단이 아니다** 🔒

- **상태**: 확정 (2026-08-02) · **구현 완료** (`graph/nodes/compute.py::numeric_evidence` · `graph/nodes/triage.py`)
- **맥락**: D-78 로 계산이 맞아진 뒤 프로브를 다시 돌렸더니 **과대평가 50%(4건)** 가 남았고,
  넷이 전부 같은 경로였다.

  ```
  G-028  밀크초콜릿 20 g / 5 kg
         코드: 테오브로민 8 mg/kg (임상징후 역치 20 미만)  → rule=MONITOR
         LLM : EMERGENCY
         게이트 max(rule, llm)                             → 최종 4
         골든셋 기대                                        → MONITOR
  ```

  `judge_triage` 에 넘어가던 것은 **검색 문맥뿐**이었다. 8 mg/kg 인지 4,000 mg/kg 인지
  모른 채 `개 + 초콜릿` 만 보고 답했고, **그 상황에서는 그것이 합리적인 답이다.**
  게이트가 `max` 이므로 코드가 맞게 계산한 등급이 매번 덮였다.

  반대 방향도 하나 있었다 — G-001(`다크초콜릿을 새끼손톱만큼`)은 양을 몰라
  `BELOW_THRESHOLD_LEVEL`(MONITOR)이 깔렸다. 주석은 *"양을 모르므로 역치 미만과 같이
  다룬다"* 였는데, 그것은 **모르는 것을 안전한 쪽으로 읽은 것**이고 방향이 과소평가다.
  골든셋 메모가 이 케이스를 *"소량이라 안전하다고 짐작하는 과소평가 유도형"* 이라고
  적어 두었는데, **시스템이 정확히 그 함정에 빠졌다.**

- **결정**:
  1. `judge_triage` 에 **코드가 계산한 값 · 자료의 역치 · 확인 안 된 것**을 함께 준다.
  2. ⚠️ **규칙의 결론(등급)은 주지 않는다.** 주면 LLM 이 따라 읽어 `llm_level` 이
     독립적이지 않게 되고, `overridden` 이 영원히 거짓이 되어 산출물 ④에서
     *"하향 금지 게이트가 작동했다"* 를 보일 증거가 사라진다 (D-09).
     주는 것은 수치와 역치뿐이고 **판단은 LLM 이 그대로 한다.**
  3. 역치 숫자를 프롬프트에 적지 않는다. `lookup()` 에서 온다 (D-22) —
     `tests/test_triage_evidence.py` 가 표를 갈아 끼워 확인한다.
  4. **양 미상의 바닥을 `CALL_NOW` 로 올린다.** 역치가 있다는 것은 이 종에 이 물질의
     독성이 확인됐다는 뜻이고, 거기에 양을 모르면 할 일은 관찰이 아니라
     **지금 전화해서 물어보는 것**이다. 등급을 어림해 올리는 것이 아니라
     **모름을 해소하는 행동**을 부른다.
  5. **재서 낮게 나온 것은 그대로 MONITOR 다.** 바꾼 것은 *못 쟀다* 쪽뿐이다.
     역치 행이 없으면 바닥을 만들지 않는다 — *"모르면 일단 3"* 이 되면 안 된다 (D-10).
- **근거**: D-38 이 *"LLM 은 제안, 코드는 판정"* 으로 나눴는데, **근거 없이 낸 제안은
  판정을 도울 수 없고 게이트를 통해 판정을 덮기만 한다.** 나눈 것은 역할이지 정보가 아니다.
- **트레이드오프**:
  > 양 미상 바닥이 걸리는 자리는 골든셋 60건 중 **2건**(G-001·G-011)이고 둘 다 기대가
  > `CALL_NOW` 이상이다. 실제 서비스에서는 *"조금 먹었는데요"* 가 가장 흔한 질의이므로
  > **전화 안내가 늘어난다.** 그것이 이 도메인에서 치를 값이라고 본다 (D-13).
  >
  > 증거를 준 뒤 `llm_level` 이 규칙과 자주 같아지면 A 와 기준선의 차이가 줄어 보일 수
  > 있다. 그러나 LLM 의 몫은 **규칙이 못 미치는 자리**(증상·정성·복합 상황)이지
  > 계산된 수치를 다시 어림하는 것이 아니다.
- **후속 (같은 날 2차 프로브)**: 과소평가는 0% 로 떨어졌으나 **과대 4건이 그대로였다.**
  두 가지를 더 고쳤다.

  1. 🔴 **단위가 다른 두 숫자를 나란히 냈다.** G-041 에서
     `화이트 초콜릿 20 g/kg`(초콜릿 무게)과 `임상징후 발현 20 mg/kg`(테오브로민 기준)을
     함께 냈고, 그 정보만 보면 **1000배 넘긴 것**이다. EMERGENCY 가 합리적인 답이 된다.
     **오도한 것은 모델이 아니라 우리다.** 계산값은 항상 `mg/kg` 으로 내고, 역치는
     원문 표기를 남기되 환산값(`5 g/kg = 5000 mg/kg`)을 함께 적는다.
  2. **없음과 무의미함을 같은 칸에 두지 않는다.** 화이트 초콜릿의 함량은 *모르는 것*이
     아니라 자료가 *"무의미한 수준"* 이라고 **말한 것**이다. `[확인 안 된 것]` 에 넣으면
     뒤에 붙는 *"모르는 것을 안전으로 읽지 않는다"* 가 **자료가 말한 것까지 밀어 올린다.**
     `[자료가 말하는 것]` 으로 분리했다.
  3. 프롬프트가 **한쪽으로만 기울어 있었다** — 올리라는 문장만 있고 *"계산된 값이 모든
     역치보다 낮으면 그 사실을 그대로 받아들인다"* 가 없었다. 대칭을 맞췄다.

- **계기 (D-75 의 재발)**: 위 진단을 **추론으로 했다.** `TriageResult` 가
  `rule_level`·`llm_level`·`overridden` 을 싣고 있는데 **하네스가 안 읽어서**
  *규칙이 틀렸나 LLM 이 덮었나* 를 볼 수 없었다. 채점 결과에 세 값을 남기고 리포트에
  `■ 하향 금지 게이트` 절을 넣는다 — 진단 도구이자 **산출물 ④ 그 자체**다.
  등급 실패 줄에도 `(rule=1 llm=4 ↑LLM)` 을 함께 낸다.

- **후속 2 (3차 프로브 · 계기가 곧바로 값을 했다)**: 통과 37.5% → 62.5% · 과대 50% → 25%.
  남은 등급 실패 둘이 **정반대 방향**이라는 것이 게이트 감사로 드러났다.

  ```
  G-040  등급 3→4  (rule=3 llm=4 ↑LLM)     ← LLM 이 계산된 등급을 덮었다
  G-041  등급 1→3  (rule=3 llm=1 🔒차단)    ← 규칙이 틀렸고 LLM 이 맞았다
  ```

  **G-041 은 위 4번(양 미상 바닥)의 결함이었다.** 질의가 *"강아지가 화이트초콜릿
  100g을 먹었는데"* 라 **체중이 없어** 바닥이 걸렸다. 그런데 화이트 초콜릿은 자료가
  *"테오브로민 급원으로서는 무의미한 수준"* 이라고 **말한** 물질이다 — 체중을 알든
  모르든 유효성분 용량은 무의미하다. 바닥의 논리(*"독성이 확인된 물질 + 양 미상 →
  전화해서 확인"*)가 **전제부터 성립하지 않는다.** 물어볼 이유가 없는 것을 모른다고
  등급을 올리면 되묻기가 아니라 **겁주기**다.

  → `Content.negligible` 을 두고, 무의미한 물질에는 양 미상 바닥을 적용하지 않는다.
  ①에서 프롬프트의 `[자료가 말하는 것]` 과 `[확인 안 된 것]` 을 갈라 놓고
  **정작 판정 코드는 안 갈라 놓았던 자리**다.

  ⚠️ **이 두 건이 D-09 를 잠그지 않은 이유다.** G-040 만 보면 *"정량 계산이 성립하면
  LLM 상승을 막는다"* 가 옳아 보이지만, G-041 은 **규칙이 틀리고 LLM 이 맞은** 경우다.
  그 방향으로 잠갔다면 G-041 은 고쳐지지 않았다. 표본 1건으로 🔒 결정을 뒤집지 않는다 —
  60건에서 `LLM 이 올렸다 (그중 과대로 끝난 것 n)` 의 비율을 보고 정한다.

- **영향**: `graph/nodes/compute.py`(`numeric_evidence`) · `graph/nodes/generate.py`
  (`_TRIAGE_PROMPT` · `judge_triage`) · `graph/nodes/triage.py`(양 미상 바닥) ·
  `compute/content.py`(`negligible`) · `eval/harness/{metrics,run_eval}.py`(게이트 감사) ·
  `tests/test_triage_evidence.py`
- **연관**: D-09(하향 금지 게이트) · D-13(과소평가 우선) · D-38(제안과 판정) ·
  D-50(바닥 등급) · D-78(유효성분 환산) · D-22(숫자의 단일 출처)

---

### D-80 · **잰 자리에서는 규칙이, 못 잰 자리에서는 LLM 이 맞다** 🔒 (D-09 개정)

- **상태**: 확정 (2026-08-03) · **구현 완료** (`triage/gate.py` · `app/contracts.py` · `graph/nodes/{compute,triage}.py`)
- **맥락**: 골든셋 60건을 두 축으로 재고 게이트 감사를 붙였더니 원인이 한 곳으로 모였다.

  ```
  ■ 하향 금지 게이트 (분모 26 · --arm A · gpt-4o-mini)
    일치              11
    LLM 이 올렸다      10   그중 **과대로 끝난 것 7**   ← 상승의 70% 가 틀렸다
    🔒 하향 차단        5
  ```

  과대 9건 중 **7건이 LLM 상승**이고, 그중 6건이 `rule=3 llm=4` — CALL_NOW 를
  EMERGENCY 로 한 칸씩 올렸다. 유형별로 `dose` 가 가장 나빴다 (과대 42.9%).

  **그런데 옳았던 상승도 있다.** 기준선에서 과소였던 G-011(백합·cat)·G-017
  (아보카도·bird)이 A 에서 사라졌다 — 규칙이 3 에서 천장을 쳤고 LLM 이 4 로 올려 고쳤다.
  D-09 가 있어야 하는 이유 그 자체다.

  둘의 차이가 선명하다 —

  | | 규칙의 출처 | LLM 상승 |
  |---|---|---|
  | G-011·G-017 | 정성 표 · 양 미상 바닥 — **양이 없어 못 쟀다** | **옳다** |
  | 과대 7건 | 섭취량 ÷ 체중 → 출처 달린 역치와 비교 | **틀리다** |

- **결정**: `rule_level` 이 **실제 용량 계산**에서 나왔을 때만 LLM 상승을 막는다.
  1. `apply_gate(..., rule_is_quantitative=True)` 면 `final = rule_level`. 그 외에는
     D-09 그대로 `max(rule, llm)`.
  2. **`compute_metrics` 만 그 표시를 세운다.** 정성 표(`qualitative_level_for`)와
     양 미상 바닥(D-79)은 세우지 않는다.
  3. **하향 금지는 그대로다.** 개정한 것은 상향의 적용 범위뿐이다.
  4. ⚠️ **막았다는 사실을 지우지 않는다.** `llm_level` 은 그대로 싣고 `llm_capped` 로
     표시해 게이트 감사에 남긴다. 조용히 무시하면 *"LLM 이 규칙과 늘 같다"* 로 보이고
     그것은 거짓이다 — 산출물 ④가 이 값을 근거로 쓴다.
  5. 계약도 함께 고친다. `_level_is_final` 이 `level >= max(rule, llm)` 을 요구하므로
     **코드 한 곳만 고치면 응답을 만들 수 없다** (D-72). `llm_capped` 일 때 바닥은
     `rule_level` 이고, 그때 `level != rule_level` 이면 계약이 거부한다.
- **근거**: D-16 이 *"수치는 코드가 계산한다"* 로 정했다. 출처 달린 역치와 계산된 용량이
  있는데 그 위로 올리는 것은 **근거 없는 상승**이고, 근거 없는 주장은 방향이 위든 아래든
  자료 밖으로 나가는 것이다 (D-10). D-38 이 나눈 것은 **역할이지 정보가 아니다** —
  LLM 의 몫은 규칙이 못 미치는 자리이지 계산된 수치를 다시 어림하는 것이 아니다.
- **트레이드오프**:
  > 🔴 **과대는 이 도메인에서 의도된 편향이다** (D-13). 그것을 줄이는 결정이므로
  > 위험 방향이 뒤집힌다. 받아들이는 근거는 두 가지다 — ① 막는 자리에서 등급은
  > **출처 달린 역치**가 정하고 우리가 어림하지 않는다. ② 하향 금지가 그대로라
  > 규칙 아래로는 못 내려간다. 60건 실측에서 **과소 0% 는 규칙과 하향 금지가 만든 것**이지
  > LLM 상승이 만든 것이 아니었다.
  >
  > 역치가 **한 축만** 덮는다는 한계는 남는다 (초콜릿 포장지 이물 등). 그 경우 LLM 이
  > 올리려 해도 막힌다. `llm_capped` 를 남기는 것이 그 한계를 보이게 하는 장치다 —
  > 막은 건이 계속 과대로 남으면 규칙 쪽을 봐야 한다는 신호이고, 하네스가
  > `상승 차단 n (그중 여전히 어긋난 것 m)` 으로 낸다.
- **영향**: `triage/gate.py`(`rule_is_quantitative` · `llm_capped`) ·
  `app/contracts.py`(`TriageResult.llm_capped` · `_level_is_final`) ·
  `graph/state.py` · `graph/nodes/{compute,triage}.py` · `graph/engine.py` ·
  `eval/harness/{metrics,run_eval}.py` · `tests/test_triage_evidence.py`
- **연관**: **D-09(개정 대상)** · D-13(과소평가 우선) · D-16(수치는 코드가) ·
  D-38(제안과 판정) · D-79(판정에 수치를 준다) · D-72(계약이 함께 바뀐다)

---

### D-81 · **근거를 각주가 아니라 필드로 낸다** 🔒

- **상태**: 확정 (2026-08-03) · **구현 완료** (`triage/basis.py` · `app/contracts.py` · `graph/engine.py`)
- **맥락**: 팀장 지적 — *"결손의 경우 애초에 근거가 불확실해 정확한 판단을 할 수 없는데,
  대답이 코드에서 정해진 대로 나가면 오해가 생기지 않나."*

  ```
  🔴 지금 전화        ← 50 mg/kg 을 계산해서 나온 3
  🔴 지금 전화        ← 양을 몰라 바닥으로 깔아 둔 3
  ```

  **두 개가 화면에서 똑같이 생겼다.** 등급이라는 형식 자체가 확신을 표현하는데
  그 확신의 출처가 응답 어디에도 없다. 밑에 한 줄 붙이는 것으로는 부족하다 —
  사람은 배지를 읽고 문장은 흘린다. **밝혔지만 밝힌 것이 아니다.**

  얼마나 걸리는 일인가 — 골든셋 `answered` 39건 중 **체중·섭취량이 둘 다 있는 건은
  13건(33%)뿐**이다. **답변 셋 중 둘이 결손 상태에서 등급을 낸다.**

- **결정**:
  1. `TriageResult.basis` 를 **`level` 과 같은 급의 필드**로 낸다 —
     `정량계산 · 최악가정 · 양미상 · 정성 · 모델판정`.
  2. 근거를 **문장 앞에 세운다.** `_assumption_notice`(D-59 ⑤)와 같은 자리·같은 표기다.
  3. **계약이 강제한다** (`_basis_must_be_stated`). 밝히지 않으면 응답이 만들어지지 않는다.
     붙이기로 *약속*하면 언젠가 빠진다 (D-40).
  4. 태그 문자열은 `triage/basis.py` 한 곳에 둔다. 만드는 쪽과 검사하는 쪽이 각자 적으면
     어긋나고, **어긋나는 순간 계약이 통과시켜 강제가 사라진다** (D-22).
  5. `정량계산` 도 밝힌다. 결손이 아니니 사과할 일은 아니나, **D-16 이 정한 그 수치가
     응답 문장에 한 번도 나간 적이 없었다** (D-75 에서 필드로만 올렸다). 계산했다는
     증거가 화면에 없으면 산출물로 보일 것이 없다.
  6. `모델판정`(규칙이 아무것도 못 냄) 은 **수치 근거가 전혀 없는 등급**이므로 가장
     분명하게 밝힌다.
- **트레이드오프**:
  > 🔴 **환각을 줄이는 결정이 아니다.** 환각은 넷으로 갈린다 — ①문장 ②수치 ③물질
  > ④확신. ①②③은 프롬프트·D-16·폐쇄목록이 이미 막고 있고 이 결정은 **④만** 다룬다.
  > 보고서에 *"환각이 줄었다"* 로 적지 않는다. **"확신의 과잉 표현을 막았다"** 가 정확하다.
  >
  > ⚠️ **단서를 많이 달면 아무도 안 읽는다** (경고 피로). 그래서 근거당 한 문장이고,
  > *"불확실할 수 있습니다"* 같은 말은 쓰지 않는다 — 아무 정보가 없으면서 신뢰만 깎는다.
  > 무엇을 몰랐고 그래서 무엇을 했는지만 적는다.
  >
  > ⚠️ **공시 문장이 골든셋 `must_not_contain` 을 밟으면 지금 통과하는 건이 실패로
  > 돌아선다.** 실제로 하나 걸렸다 — `[계산]` 문안의 `mg/kg` 가 G-005·G-013·G-017 의
  > 금지어다. 셋 다 조류이고 조류는 정량 역치가 0행이라(D-09) `정량계산` 이 될 수 없어
  > 구조적으로 안 밟는다. **말로만 안전한 것은 안 되므로 `tests/test_basis.py` 가
  > 박아 뒀다** — 조류에 정량 역치가 생기는 날 그 검사가 먼저 깨진다.
- **연쇄로 드러난 것**: 04 는 ④ 근거 검증의 지표를 *"근거없음 탐지 재현율"* 로 정해
  뒀는데 **하네스에 그 지표가 없다.** `verdicts` 는 상태에 있으나 아무도 집계하지 않는다
  (D-75 와 같은 모양의 끊김). `must_not_contain` 은 *금지 문구를 안 썼나*이지
  *없는 사실을 지어냈나*가 아니다 — **①문장 환각은 아직 한 번도 측정되지 않았다.**
- **영향**: `triage/basis.py`(신규) · `app/contracts.py` · `graph/state.py` ·
  `graph/engine.py` · `graph/nodes/{compute,triage}.py` · `tests/test_basis.py`
- **연관**: D-59 ⑤(밝히지 않은 추정은 환각) · D-40(못 어기게 둔다) · D-16(수치는 코드가) ·
  D-75(끊긴 값) · D-79(양 미상 바닥) · D-13(과소평가 우선)

---

### D-82 · **부르지 않는 태스크를 성공으로 세지 않는다** 🔒

- **상태**: 확정 (2026-08-03) · **구현 완료** (`graph/fallbacks.py` · `app/contracts.py` · `eval/harness/`)
- **맥락**: 팀장 질문 — *"근거는 코드가 가져오고 LLM 은 그 근거에 의거한 대답만 하게
  한다는 건가?"* 에 답하려고 ④ 근거 검증의 실측치를 꺼내다가 **둘이 드러났다.**

  ```
  Task.CLASSIFY  → client.run  ✅
  Task.SLOT      → client.run  ✅
  Task.COMPRESS  → _call_llm   ✅
  Task.SIMPLIFY  → _call_llm   ✅
  Task.VERIFY    → 없음        ❌   어디서도 호출되지 않는다
  ```

  ① **④ 검증은 LLM 이 아니다.** `verify_grounding` 은 2-gram 문자 일치율로 판정한다.
     `Task.VERIFY` 의 프롬프트와 라벨은 D-73 에서 고치기까지 했는데 **한 번도 쓰이지
     않았다.** 05 §4 는 ④를 LLM 태스크로 적어 두었다 — **문서와 코드가 어긋나 있다.**

  ② 그래서 리포트의 **`5태스크 전부 모델 100.0%` 가 거짓 신호였다.** 폴백은 *호출이
     실패했을 때* 기록되는데, ④는 애초에 부르지 않으니 실패할 일이 없다.
     **안 한 것이 잘한 것으로 집계됐다.**

  ③ 그리고 `verdicts` 는 상태에 있는데 **아무도 집계하지 않았다** — D-75 와 같은
     모양의 끊김이다. 04 가 ④의 지표를 요구했는데 하네스에 그 칸이 없었다.

- **결정**:
  1. `fallbacks.WIRED` / `UNWIRED` 로 **세는 대상을 이름으로 밝힌다.** 리포트가
     `4태스크 [classify · slot · compress · simplify]` 로 찍고, `verify` 는
     *"집계에 없음 — 호출부가 없다"* 로 따로 적는다.
  2. `AskResponse.grounding` 으로 ④가 무엇을 봤는지 응답에 싣는다
     (검사 문장 수 · 근거없음 · 모순 · 재검색 여부).
  3. **`재현율`이라고 부르지 않는다.** 04 는 *"근거없음 탐지 재현율"* 을 요구했지만
     재현율은 *"실제로 근거 없는 문장"* 의 정답 라벨이 있어야 한다. 우리에겐 없다.
     리포트에 **탐지율**로 적고 그 한계를 함께 적는다.
  4. `unsupported == 0` 이면 **경고를 찍는다** — *"환각이 없었다는 뜻이 아니다.
     검증기가 약해서 못 잡은 것과 구별되지 않는다."*
  5. 지표를 못 만드는 대신 **음성 대조**를 둔다 (`tests/test_grounding.py`) —
     근거에 없는 문장을 일부러 넣고 걸리는지 본다. **못 걸르면 이 노드는 장식이다.**
  6. `UNWIRED` 선언이 코드와 어긋나면 테스트가 깨진다. 배선하는 날 그 테스트가
     *"UNWIRED 에서 빼고 05 §4 와 맞출 것"* 이라고 말한다.
- **근거**: 04 §8 — *"측정하지 않은 것을 0 으로 적지 않는다."* 여기서는 한 걸음 더 간다.
  **하지 않은 것을 100% 로 적지 않는다.** 앞은 빈칸을 채우는 잘못이고 뒤는 빈칸이
  만점으로 보이는 잘못인데, **뒤가 더 나쁘다** — 아무도 찾아보지 않는다.
- **트레이드오프**:
  > ④를 LLM 으로 배선하는 것은 **이 결정에 포함하지 않았다.** 질의당 LLM 호출이
  > 문장 수만큼 늘고, 그 변화는 등급·상태를 바꿔 지금 측정치를 무효로 만든다.
  > **어긋나 있다는 사실을 먼저 드러내고, 고치는 것은 측정을 새로 할 여유가 있을 때** 한다.
  > 그때까지 리포트가 매번 그 사실을 찍는다.
- **영향**: `graph/fallbacks.py`(`WIRED`·`UNWIRED`) · `app/contracts.py`(`GroundingReport`) ·
  `graph/engine.py` · `eval/harness/{metrics,run_eval}.py` · `tests/test_grounding.py`
- **연관**: D-75(끊긴 값) · D-76(폴백 기록) · D-73(라벨) · 04 §8(재현성) · 05 §4(5태스크)

---

### D-83 · **③ 압축을 질의 경로에서 빼고 기간 리포트로 옮긴다** 🔒

- **상태**: 확정 (2026-08-03) · **구현 완료** (`graph/nodes/generate.py::build_context` ·
  `app/report.py` · `graph/build.py` · `models/{tasks,prompts}.py`)
- **맥락**: 이서은 팀원이 ③ 학습 데이터를 만들다 `compress_context` 의
  `max_tokens=400` 잘림을 보고했다. 그 자리를 들여다보다 세 가지가 함께 드러났다.

  ① 🔴 **검증의 정답지가 LLM 생성물이었다.** `verify_grounding` 은 초안을
     `context` 에 대고 2-gram 으로 판정하는데, 압축이 돌면 그 `context` 는
     **모델이 다시 쓴 문장**이다. 압축 단계에 들어온 환각을 검증기가 근거로
     인정한다 — 04 가 *"가장 나쁜 경우"* 로 적어 둔 그것이다.
  ② **풀려는 문제가 없었다.** 근거는 실측 393~533자이고 컨텍스트 창은 128k 다.
     `dose` 5건에서 길이 임계(800자)를 **한 번도 넘지 않았다.**
  ③ **근거가 모자라 실패하는 시스템에서 근거를 깎고 있었다.** 등급 미판정 15.4% ·
     `must_cite` 64.1% 가 전부 근거 부족 쪽이다.

  그리고 03 §1.1 이 세운 태스크 선정 원칙과 어긋나 있었다 —
  *"파이프라인이 실제로 필요로 하는 태스크만 고른다. 억지로 채운 태스크는
  왜 하는가에 답할 수 없다."* ③의 필연성은 처음부터 질의 경로가 아니라
  **다이어리**에 있었다. D-02 가 그렇게 적어 두었다 — *"원안(중독 응급 QA)은
  요약 태스크의 필연성도 약했다 → 다이어리로 확장하면 기간 요약 리포트라는
  킬러 유스케이스가 생긴다."*

- **대안**: (a) 그대로 둔다 (b) 빼고 ③을 4태스크로 줄인다
  (c) **빼되 ③을 기간 리포트로 옮긴다** (d) 두되 검증만 원문에 댄다
- **결정**: **(c)**. 05 §4 의 다섯 태스크는 그대로이고 **③이 도는 경로가 바뀐다.**
  질의 경로에 남은 것은 히트를 잇는 조립뿐이므로 이름도 `build_context` 로 바꾼다.
  `GET /api/report` 의 `summary` 는 그때까지 문자열 하나로 비어 있었다
  (*"요약 생성은 구현 3단계에서 붙인다"*) — 두 자리를 맞바꾼 셈이다.
- **근거**: ①이 결정적이다. 과제 목표 1번이 환각 방지이고 그 장치가 ④인데,
  ④의 정답지를 모델이 쓰면 **LLM 이 쓴 것으로 LLM 을 검증**하게 된다.
  사람이 쓴 코퍼스 문장이 그대로 정답지로 남아야 ④가 성립한다.
- **트레이드오프**:
  > 리포트 요약은 **기록 원문을 외부 모델에 보낸다.** D-18(가상 프로필만)이
  > 지켜지는 동안에만 안전하고, `privacy/` 필터가 없으므로 코드가
  > `_notes_may_leave_the_machine()` 으로 **메모를 입력에서 뺀다.** 필터가 생기면
  > 그 함수 한 줄만 고친다 (D-36 · D-40 — 주석이 아니라 코드가 막는다).
  >
  > `context` 가 압축본이 아니라 원문 결합이 되어 2-gram 판정이 **느슨해진다.**
  > 근거없음 탐지가 떨어지는 방향이다. 대신 순환이 끊긴다.
- **영향**: `graph/nodes/generate.py`(`compress_context` → `build_context`) ·
  `graph/build.py`(노드 `compress` → `evidence`) · `graph/fallbacks.py`(`WIRED` 에서
  `compress` 제거, `ELSEWHERE` 신설) · `models/tasks.py` · `models/prompts.py`(③ 지시문을
  기간 요약으로 — **학습·추론 공용이므로 ③ 학습 데이터의 형식이 여기서 정해진다**) ·
  `app/report.py`(신규) · `app/routes/records.py` · `app/contracts.py`(`summary_by`) ·
  `tests/test_graph_build.py`
- **연관**: D-02(요약의 필연성) · D-78(파생이 빠뜨린다) · D-82(안 부른 태스크) ·
  03 §1.1 · 05 §4 · **02·03·05 문서 갱신 필요 — 아직 안 했다**

> ⚠️ 노드 이름을 `context` 로 지었다가 조립이 터졌다 —
> `'context' is already being used as a state key`. `build_graph()` 머리말이
> `slots`·`draft` 전례로 이미 경고하던 함정이다. **주석이 두 번째로 못 막았다.**
> `tests/test_graph_build.py::test_node_names_never_collide_with_state_keys` 가
> 이제 규칙 자체를 고정한다 (D-40).

---

### D-84 · **조건 없는 MONITOR 를 침묵으로 바꾸지 않는다** 🔒

- **상태**: 확정 (2026-08-03) · **구현 완료** (`graph/nodes/triage.py::decide_triage` ·
  `triage/basis.py`)
- **맥락**: D-39 는 *"조건 없는 관찰은 그 자체가 과소평가"* 라고 **옳게 판정**했다.
  문제는 그에 대한 **조치**로 출력을 막은 것이다. 60건 측정에서 그 조치가 무엇을
  만드는지 드러났다 —

  ```
  G-012  "마당에 뿌려둔 살충제를 핥아먹은 것 같아요"   정답 CALL_NOW → 거절
  G-018  "알약 하나를 주워 먹었는데 아스피린 같아요"    정답 CALL_NOW → 거절
  G-046  "고양이가 초콜릿을 조금 핥아 먹었어요"        정답 CALL_NOW → 거절
  ```

  **사용자가 아무것도 받지 못했다.** 원인은 셋 다 사실 표에는 있는데
  (`F-084-008`·`F-029-011`·`F-097-006`) `triage_level`·`threshold_type` 이 공란이라
  규칙 표 두 장 어디에도 못 들어간 것이다. `rule_level` 이 서지 않으니
  D-50(규칙은 바닥)·D-09(하향 금지)가 막을 기준을 잃고, LLM 의 MONITOR 가
  그대로 최종이 된다. 거기서 출력을 막으면 **과소평가가 침묵이 될 뿐이다.**

  숫자가 이것을 그대로 말한다 — 답변까지 간 건을 규칙 바닥 유무로 가르면
  **있음 n=25 등급일치 92.0% 과소 0.0% · 없음 n=6 등급일치 50.0% 과소 16.7%.**

- **대안**: (a) 그대로 거절 (b) **독성 근거면 CALL_NOW 로 올린다**
  (c) 무조건 CALL_NOW 로 올린다 (d) 규칙 표를 채운다
- **결정**: **(b)**. 상승 조건을 못 만든 것은 **관찰로 끝낼 수 없다는 뜻**이지
  안전하다는 뜻이 아니다. D-79 가 같은 자리에서 이미 답을 냈다 —
  *"모르는 것을 안전으로 읽지 않는다. 할 일은 관찰이 아니라 지금 전화해서
  물어보는 것이다."*

  가르는 기준은 **`Chunk.doc_type`** 이다 (`toxicity_food`·`toxicity_plant`·`emergency`).
  **물질 이름을 코드에 적지 않는다** — 적는 순간 `engine.py` 안에 `_RULE_TABLE`
  12줄이 있던 그 자리로 돌아간다 (D-16 · D-38).

  ⚠️ `nutrition` 근거에는 적용하지 않는다. 블루베리(G-007)처럼 정답이 MONITOR 인
  급여 질의까지 올리면 **되묻기가 아니라 겁주기**가 된다 — D-79 주석이 G-041 에서
  얻은 교훈 그대로다.

  **왜 올렸는지 밝힌다.** `Basis` 에 `조건미비`(태그 `[조건 없음]`)를 추가했고
  D-81 의 `MUST_STATE` 를 그대로 타므로 **문장에 안 실리면 계약이 응답을 거부한다.**
- **근거**: 04 §4.1 — *"두 지표가 상충할 때는 과소평가 억제를 우선한다."*
  거절은 과대평가를 피하는 대신 **사용자에게 아무것도 주지 않는다.**
  응급 도메인에서 침묵은 중립이 아니다.
- **트레이드오프**:
  > 정답이 MONITOR 인데 상승 조건이 없고 근거가 독성 자료인 건은 **과대평가가 된다.**
  > 감수한다 (D-13). 다만 이 결정은 **규칙 표 커버리지 12%(888행 → 105행)를
  > 코드로 보정하는 것**이고, 근본 해법은 표를 채우는 것이다 — 마감 뒤 과제다.
- **영향**: `graph/nodes/triage.py`(`_evidence_is_toxic`) · `triage/basis.py`(`조건미비`) ·
  `tests/test_triage_evidence.py`
- **연관**: D-39(조건 없는 MONITOR) · D-79(모름을 안전으로 읽지 않는다) ·
  D-50(규칙은 바닥) · D-81(근거를 필드로) · 04 §4.1.0

> **채점에도 반영했다.** 04 §4.1.0 이 *"조건 없는 관찰은 과소평가로 채점한다"* 고
> 정해 두었는데 하네스에 그 규칙이 한 줄도 없었다 — `triage=None` 인 거절로 들어와
> **등급 분모에서 통째로 빠졌고**, 그래서 `과소평가율 0.0%` 가 나왔다.
> `AskResponse.monitor_without_conditions` 를 더해 하네스가 예측 등급을 `MONITOR`
> 로 세운다. 이 정정 하나로 분모가 29 → 35, 과소평가가 0.0% → 11.4% 로 바뀌었다.
> **성능이 나빠진 것이 아니라 안 세던 것을 세게 된 것이다** — 등급일치 분자는 27로 같다.

---

### D-85 · **말 안 한 것은 되묻고, 말했는데 없는 것은 거절한다** 🔒

- **상태**: 확정 (2026-08-03) · **구현 완료** (`graph/nodes/slots.py` · `graph/build.py::_after_slots`)
- **맥락**: 되묻기는 *"물으면 답이 온다"* 는 가정 위에 서 있다. 60건 측정에서
  그 가정이 깨지는 자리가 드러났다 — **사용자가 물질을 말했는데 우리 코퍼스에
  그 물질이 없는** 경우다.

  ```
  목캔디 · 달팽이약 · 실리카겔 · 매니큐어 · 모기향 · 계피가루
  ```

  여섯 건 모두 되묻기로 갔다. *"무엇을 먹었나요?"* 를 다시 물으면 **같은 말이
  돌아온다.** 되묻기 상한 2회를 태우고 결국 거절하는데, 그 사이 사용자는
  두 턴을 잃는다. 응급 도메인에서 그건 지연이다.

- **대안**: (a) 그대로 되묻기 (b) **바로 거절 · 근거없음** (c) 유사 물질로 답한다
- **결정**: **(b)**. 되묻기와 거절을 가르는 것은 **답이 올 수 있는가**이지
  슬롯이 비었는가가 아니다. (c)는 D-49 가 이미 막았다 — 추정하는 순간
  아니었을 때 되돌릴 방법이 없다.

  **물질이 미해결일 때 네 갈래다.** 갈래마다 이유가 다르므로 한 덩어리로 묶지 않는다 —

  | 상태 | 판정 | 왜 |
  |---|---|---|
  | 표면형이 없다 | 되묻기 | 아직 안 물어봤다 |
  | 표면형이 모호어다 (`뭔가`·`무언가`·`미상`) | 되묻기 | 검색할 대상 자체가 없다 |
  | 후보가 여럿이다 | 전부 검색어로 | 좁히는 것이 추정이다 (D-62) |
  | **폐쇄 목록 밖이다** | **거절 · 근거없음** | 되물어도 답이 없다 |

  ⚠️ **모호어 목록은 되묻기 쪽에 있다.** *"뭔가 먹었어요"* 를 거절로 보내면
  물어보면 답이 나올 건을 잘라 버린다.

- **근거**: D-49(추정하지 않는다) 와 **충돌하지 않는다.** D-49 는 *"모르는 것을
  추정하지 마라"* 이고 이 결정은 *"모른다고 답하라"* 다. 둘 다 지어내지 않는다.
- **트레이드오프**:
  > 코퍼스가 넓어지면 이 거절은 줄어든다. **거절률은 시스템의 약점을 그대로
  > 드러내는 지표**이므로 숨기지 않는다 — 되묻기로 감추면 커버리지 구멍이
  > 상한 초과 거절로 위장된다.
- **영향**: `graph/nodes/slots.py`(`_is_vague`·`unknown_substance`) ·
  `graph/state.py` · `graph/build.py` · `tests/test_unknown_substance.py`
- **연관**: D-49(되묻기) · D-59/D-62(폐쇄 목록·모호) · D-79(모름을 안전으로 읽지 않는다)

> **2026-08-03 후속 — 되돌린 시도가 하나 있다 (D-87 사고).** 표면형이 구(句)일 때
> 안쪽 어휘를 찾으려고 `mention_in(surface, species)` 를 썼다. 그 함수는 표면형을
> **문장처럼** 훑으므로 부분 매칭이 일어나 `substance` 가 잘못 서고, 통과가
> **33 → 25** 로 떨어졌다. 되돌렸다.
>
> 🔴 **더 큰 교훈은 그 옆에 있다.** 당시 테스트에 `assert mention_in("목캔디","dog") is None`
> 이라고 적어 두고 **한 번도 실행하지 않았다.** 나중에 `scripts/probe_vocab.py` 로
> 3초 만에 확인해 보니 여섯 개 전부 `None` 이었다 — 진단 자체가 틀렸던 것이다.
> **검증 안 한 가정을 테스트로 적으면 그 테스트가 가정을 사실로 보이게 만든다.**

---

### D-86 · **모델이 쓸 키는 코드가 정한다** 🔒 (D-73 을 ② 에도 적용)

- **상태**: 확정 (2026-08-03) · **구현 완료** (`models/tasks.py` · `models/prompts.py::_schema_block`)
- **맥락**: D-85 를 넣었는데 여섯 건 중 **한 건만** 통과했다. 코드를 읽어서는
  이유를 못 좁혀 `scripts/probe_slots.py` 로 ② 슬롯의 날출력을 찍어 봤다 —

  ```
  {'species': '개', 'concern': '목캔디'}                ← 키가 concern
  {'species': '개', 'action': '…', 'item': '실리카겔'}   ← 키가 item
  ```

  ② 슬롯 프롬프트에 **JSON 스키마가 한 글자도 없었다.** *"슬롯을 추출해 JSON
  객체 하나로 출력한다"* 라고만 적혀 있었고 어떤 키를 쓸지는 안 적혀 있었다.
  코드는 `llm.get("substance")` 만 보므로 **뽑아 놓고 버렸다** — 6건 중
  `substance` 키를 쓴 것은 1건뿐이다.

  ②슬롯은 사실상 키워드 폴백으로 돌고 있었는데 **폴백 집계는 100% 로 찍혔다.**
  LLM 이 응답은 했으니 폴백이 아니라고 센 것이다 (D-82 의 사각).

  03 §5 는 이미 요구하고 있었다 — *"②는 출력이 JSON이므로 **스키마를 프롬프트에
  고정**하고 파싱 실패율을 관리한다."* **구현이 안 된 채 남아 있었다** (D-48).

- **결정**: `TaskSpec.output_keys` · `key_hints` 를 **단일 출처**로 두고
  `_schema_block()` 이 프롬프트를 그 목록에서 **생성**한다. 손으로 적지 않는다.
  `labels`(D-73)와 **정확히 같은 처방**이다 — 그때 ① 에만 하고 ② 에는 안 했던 것이다.

  **값의 형식까지 적는다.** `species` 를 `개` 로 낼지 `dog` 로 낼지는 설명 없이는
  알 수 없다. `amount_g` 는 *"목캔디 하나"* 에서 `1` 이 나왔다 —
  **1개이지 1그램이 아니다.** 이 값은 mg/kg 환산(D-78)에 그대로 들어가
  **틀린 수치 하나가 등급을 통째로 바꾼다.** 힌트에서 개수·어림을 명시적으로 막았다.

- **실측**: `substance` 정답 1/6 → **6/6**. 전체 통과 50.0% → **55.0%**.
- **트레이드오프**:
  > 프롬프트가 길어진다 (태스크당 ~200자). 지연이 늘지만 **뽑아 놓고 버리는 것보다 싸다.**
- **영향**: `models/tasks.py`(`output_keys`·`key_hints`) · `models/prompts.py`(`_schema_block`) ·
  `tests/test_slot_schema.py`
- **연관**: D-73(라벨 단일출처) · D-22(단일 출처) · D-82(폴백 집계) · D-48(문서≠코드) · 03 §5

> **이 결정의 진짜 내용은 스키마가 아니다.** D-73 을 만들 때 *"프롬프트와 검증기가
> 같은 목록을 봐야 한다"* 고 적어 놓고 **그 원칙을 한 태스크에만 적용했다.**
> 원칙을 세운 자리와 적용해야 할 자리가 다르면, 원칙은 적힌 곳에서만 지켜진다.
> `tests/test_slot_schema.py` 가 `output_keys ⊆ Slots` 포함관계를 강제한다 — 다음에는
> 사람이 기억하지 않아도 된다.

---


### D-87 · **표면형을 문장처럼 훑지 않는다** 🔒 (시도 후 **폐기** · D-86 후속)

> **용어 주의** — 본문 여러 곳의 *"D-87 사고"* 는 **폐기된 시도**를 가리킨다.
> **되살린 것이 아니다.** 폐기된 것은 *시도*이고, *"쓰지 않는다"* 는 **결정은 확정**이다.

- **상태**: **시도 폐기** (2026-08-03) · 코드에 남은 것 없음 ·
  **결정 자체는 확정** — 같은 접근을 다시 꺼내지 않는다
- **맥락**: D-86 으로 ② 슬롯이 구(句) 표면형을 정확히 뽑아 오게 되자, 그 안쪽 어휘를
  찾아 폐쇄 목록에 태우려 했다. 이미 있는 `mention_in(surface, species)` 를 그대로 썼다.
- **무엇이 틀렸나**: 그 함수는 **문장에서 언급을 찾는** 용도라 표면형을 문장처럼 훑는다.
  부분 매칭이 일어나 `substance` 가 잘못 서고, 통과가 **33 → 25** 로 떨어졌다.
- **결정**: **이 접근을 폐기한다.** `mention_in` 을 표면형에 걸지 않는다.
  같은 함수를 **원래 용도로** 쓰는 길은 D-88 이 따로 냈다 — **D-88 은 D-87 의
  재시도가 아니다.**
- **🔴 더 큰 교훈은 그 옆에 있다**: 당시 테스트에
  `assert mention_in("목캔디","dog") is None` 이라고 적어 두고 **한 번도 실행하지
  않았다.** 나중에 `scripts/probe_vocab.py` 로 3초 만에 확인해 보니 여섯 개 전부
  `None` 이었다 — **진단 자체가 틀렸다.**

  > **검증 안 한 가정을 테스트로 적으면 그 테스트가 가정을 사실로 보이게 만든다.**

- **영향**: **없음** — 코드에 남은 것이 없다. 교훈은 D-88 의 검증 절차에 흡수됐다
- **연관**: D-86(출력 스키마) · D-88(구 표면형의 처리 — **다른 방법**) ·
  D-60(별칭 일치 규칙) · D-90(같은 교훈의 재확인)

> **번호를 비워 두지 않고 항목으로 남긴다.** 폐기한 시도도 기록이며, D-85·D-88·§8.1 이
> 이 사고를 근거로 논증한다. **폐기했다고 지우면 그 논증들이 근거를 잃는다.**

---


### D-88 · **성공 판정은 "값을 냈다" 가 아니라 "폐쇄 목록에 올랐다" 이다** 🔒

- **상태**: 확정 (2026-08-03) · **구현 완료** (`graph/nodes/slots.py::_resolvable`)
- **맥락**: 기준선 3판(B1·B2·B3)에서 실패 26건의 뿌리를 갈랐더니 **검색 0건 거절이
  8건**으로 가장 컸고, 그중 4건이 **D-85 가 아는 물질을 모른다고 거절한 것**이었다 —

  ```
  세제 거품(G-019) · 니코틴 껌 조각(G-107) · 양파국(G-048) · 감기약(G-022)
  ```

  `slots.py` 는 `surface = llm.get("substance") or _extract_substance_fallback(...)`
  였다. **D-86 전에는** 모델이 `concern`·`item` 같은 키를 내서 `llm_surface` 가
  `None` 이었고, 그래서 문장 스캔이 돌아 `세제 거품` 안의 `세제` 를 잡았다.
  **D-86 후에는** 모델이 구(句)를 정확히 뽑아 오고, 그 값이 폴백을 가린 채
  폐쇄 목록에서 떨어진다. `resolve` 실측 —

  ```
  세제 거품      → None      문장으로 읽으면 '세제'      → 모호 2후보 (D-62)
  니코틴 껌 조각   → None      문장으로 읽으면 '니코틴 껌'   → 니코틴(...) 로 오른다
  양파국·감기약    → None      문장으로 읽어도 None       → 진짜 어휘 공백
  ```

  **② 슬롯을 고쳤더니 그 성공이 폴백을 껐다.** D-82(*"안 부른 태스크를 성공으로
  세지 않는다"*)와 같은 부류다 — **성공의 판정 기준이 잘못된 자리에 있었다.**
  `or` 는 *"앞이 값을 안 냈을 때"* 뒤로 넘기지만, 여기서 필요한 것은
  *"앞이 **쓸 수 있는** 값을 냈을 때"* 다.

- **결정**: `_resolvable(surface, species)` 를 성공 판정으로 세운다. 이름이 서거나
  (직접·별칭·부분일치), 후보가 여럿이거나(모호 · D-62), 종만 안 맞으면(종밖 · D-68)
  **안다**로 본다 — 셋 다 다음 행선지가 이미 설계되어 있다. 못 오르면 **질문 문장**으로
  내려간다.

  🔴 **표면형이 아니라 질문 문장으로 내려간다.** `mention_in` 은 문장을 훑는 함수이고,
  표면형에 쓰면 부분 매칭이 터진다 — 2026-08-03 **D-87 사고가 정확히 그것이었다**
  (통과 33 → 25). 같은 함수를 **원래 용도로** 쓰는 것이 이 결정이고, D-87 의 재시도가 아니다.

  ⚠️ **모호어(`뭔가`)는 내려가지 않는다.** 사용자가 *모른다*고 말한 것이고, 문장을
  뒤져 물질을 세우면 그게 추정이다 (D-49). 이 가드가 없으면 되묻기 경로가 무너진다.

- **검증**: 새 단언을 **쓰기 전에 전부 실행해서 확인했다.** D-87 때 
  `assert mention_in("목캔디","dog") is None` 을 적어 두고 한 번도 안 돌린 것이
  진단 전체를 틀리게 만들었다. 실측 결과 —

  | 표면형 | 이전 | 이후 |
  |---|---|---|
  | 세제 거품 | 근거없음 (D-85) | 모호 2후보 → 검색어 (D-62) |
  | 니코틴 껌 조각 | 근거없음 (D-85) | **슬롯 = 니코틴(담배·전자담배·니코틴 껌/패치)** |
  | 양파국 · 감기약 | 근거없음 | 근거없음 (변화 없음 — 진짜 공백) |
  | 목캔디 외 D-85 여섯 | 근거없음 | **근거없음 (변화 없음)** |
  | 뭔가 (G-014) | 되묻기 | **되묻기 (변화 없음)** |
  | 초콜릿 케이크 | 슬롯 = 초콜릿 | 변화 없음 (애초에 오른다) |

- **트레이드오프**:
  > **2026-08-03 C1 실측으로 닫혔다 — 모호 경로는 답을 만든다.**
  > G-019(`세제 거품` → `세제` → 후보 2)가 `refused/근거없음 인용 0` 에서
  > **`answered/등급 3 인용 5`** 로 갔다. G-107 은 `니코틴 껌` 이 폐쇄 목록에 올라
  > **규칙 바닥까지 섰다**(rule None→3) — 통과로 전환됐다.
  > 전체 통과 56.7% → **60.0%**, **등급 못 낸 긴급 건 11.5%(3/26) → 3.6%(1/28).**
  > 남은 2건(`양파국`·`감기약`)은 문장으로 읽어도 안 잡히는 **진짜 어휘 공백**이다.
- **영향**: `graph/nodes/slots.py` · `tests/test_unknown_substance.py::Test구표면형`
- **연관**: D-85(없음/모름) · D-86(출력 스키마) · D-87(되돌린 시도) · D-82(성공 판정) ·
  D-62(모호) · D-68(종밖) · D-49(추정 금지)

> **이 결함의 자리도 코드 사이다.** `slots.py` 도 `vocabulary.py` 도 각자 옳았다.
> 틀린 것은 **둘을 잇는 `or` 한 글자**였고, D-86 이 앞쪽을 고치기 전까지는
> 그 `or` 가 우연히 맞게 동작했다. **고쳐서 드러난 결함**이라는 점이 중요하다 —
> 앞을 개선할 때마다 뒤의 우연한 성립이 무너질 수 있다.

---


### D-89 · **등급이 시킨 일을 문장에도 싣는다** 🔒 (D-81 의 반대편)

- **상태**: 확정 (2026-08-03) · **구현 완료** (`graph/engine.py::_advice_line`)
- **맥락**: C1 측정에서 `못 채운 문구` 1·2위가 **`전화`×13 · `병원`×7** 이었다.
  등급별로 갈랐더니 —

      등급 CALL_NOW 이상 36건 중 **19건(53%)** 이 행동 문구를 본문에 안 썼다.

  `levels.py::TriageLevel.message` 는 문장을 이미 들고 있고, 02 §7 이 그것을
  등급의 행동 언어로 정해 놨다. **응답 조립이 배달하지 않았다** (D-48).
  등급은 `triage` 구조화 필드로만 나가고, **답변 문장은 무엇을 해야 하는지
  말하지 않았다.** 배지를 못 보는 경로(음성·요약·로그·스크린리더)에서는
  지시가 통째로 사라진다.

  D-81 이 근거에 대해 편 논증이 그대로다 — *"밑에 한 줄 붙여도 사람은 배지를
  읽고 문장은 흘린다. 밝혔지만 밝힌 것이 아니다."* 여기서는 **방향이 반대**였다.
  배지만 있고 문장이 없었다.

- **대안**: (a) 그대로 둔다 (b) **행동 문장을 답변 끝에 싣는다**
  (c) 계약으로 강제한다 (`MUST_STATE` 처럼 — 안 실리면 응답을 못 만든다)
- **결정**: **(b)**. (c)는 옳은 방향이지만 마감 하루 전에 응답 42건 전부의
  계약을 조이는 것은 위험 대비 이득이 없다 — §8 에 올린다.

  **맨 끝에 둔다.** 근거는 **단서**라 앞에 서고(D-81), 행동은 **결론**이라 뒤에 선다.
  앞에 가정 공시·근거 공시·행동까지 세 덩이가 쌓이면 아무도 안 읽는다.

  **파이프라인 밖에 둔다.** 행동 권고는 근거 문서에 없는 문장이라 ④ 안쪽에
  넣으면 전부 `근거없음` 으로 잡힌다. *사실 주장 vs 행동 권고* 를 가르는
  선행 결정이 아직 없고(§8), 그 결정을 **건드리지 않고 지나가는 자리**가 여기다.

  **중복은 붙이지 않는다.** 초안이 이미 같은 말을 했으면 한 번 말한 것으로 둔다.

  **`MONITOR` 는 상승 조건을 함께 낸다** — *"아래 증상이 나타나면"* 이라고 해 놓고
  아래가 비면 D-39 를 문장에서 어기는 것이다.

- **사전 점검** (측정 전에 결정론으로 돌렸다):

  | | 결과 |
  |---|---|
  | 새로 금지 문구를 들여오는 건 | **0건** |
  | 필수 문구가 채워져 통과로 도는 건 | **3건** (G-017 · G-019 · G-034) |
  | 이미 통과 중이라 점수엔 영향 없이 문장만 채워지는 건 | **18건** |

  `MONITOR` 문장의 `지켜보` 를 금지한 케이스가 15건 있으나 **전부 정답 등급이
  `CALL_NOW` 이상**이라 그 문장을 받지 않는다. 회귀 테스트가 이 포함관계를 고정한다.

- **부수 효과 — 채점이 더 정직해진다.** 지금은 과소평가된 건도 `must_not_contain`
  을 **거저 통과**한다(답이 *"지켜보"* 라고 말한 적이 없으니까). 행동을 문장으로
  실으면 과소평가가 **텍스트로도 잡힌다.** 지표를 느슨하게 만드는 변경이 아니라
  조이는 변경이다.
- **트레이드오프**:
  > 답변 42건 전부의 문장이 바뀐다. **점수(+3)보다 안전(22건)이 이 결정의 목적이다** —
  > `CALL_NOW` 를 내고도 전화하라고 말하지 않던 상태가 사라진다. 두 숫자를 섞어
  > 보고하지 않는다.
- **영향**: `graph/engine.py`(`_advice_line`) · `tests/test_basis.py::Test행동문장`
- **연관**: D-81(근거를 필드로) · D-39(상승 조건) · D-48(문서≠코드) · D-11(진단 금지) · 02 §7

---


### D-90 · **④의 상한 판단은 라우터가 단독으로 한다. 노드는 판정만 한다** 🔒

- **상태**: 확정 (2026-08-03) · **구현 완료** (`graph/nodes/verify.py` ·
  `graph/build.py::_after_verify`) — **코드가 결정 기록을 앞질렀다**
- **맥락**: `verify_grounding` 이 `all_ungrounded` **그리고** `retry_count >= MAX_RETRY`
  일 때만 `refused` 를 세웠다. 라우터 `_after_verify` 는 `retry_count < MAX_RETRY`
  일 때만 `retry` 로 보냈다. **두 조건이 상호배타**다 —

  ```
  · verify 가 refused 를 세우려면            retry_count >= 1
  · retry_count 를 1로 만드는 _retry 에 가려면   refused
  ```

  `retry_count` 는 영원히 0이었고, **④의 세 조치(문장 제거·재검색·거절) 중 어느 것도
  일어난 적이 없다.** 05 §5 가 랭그래프를 고른 **유일한 근거**인 `retry → retrieve`
  엣지가 죽은 코드였다. 전 문장이 근거없음이어도 그대로 답변으로 나갔다.

  `build.py` 머리말은 **이미 옳은 설계를 적어 두었다** — *"상한 판단은 라우터 한 곳이고
  `verify_grounding` 은 판정만 한다."* 노드 쪽 검사를 지우지 않아 **이관이 절반만** 됐다.
- **결정**: 노드는 `all_ungrounded` 만 보고 실패를 세운다. **상한은 라우터가 본다.**
- **실측**: 수정 뒤에도 `retried` 는 세 판(`D1`·`D1_none`·`D1_lc`) 모두 **0** 이다 —
  `all_ungrounded` **자체가 0건**이기 때문이다. 답변당 문장이 4개 안팎이고 인용률이
  66% 라 전 문장 탈락이 사실상 일어나지 않는다.

  > **순환은 살아났으나 발동 조건이 사문(死文)이다.** 조건 재설계는 §8 에 올린다.
  > **고쳤다고 도는 것이 아니다.**

- **트레이드오프**:
  > 라우터 한 곳만 상한을 보므로 라우터를 잘못 고치면 무한 루프가 된다.
  > `build.py` 의 재귀 상한이 그 뒷받침이며 **실제 상한은 `MAX_RETRY`** 다.
- **영향**: `graph/nodes/verify.py`(`verify_grounding`) ·
  `graph/build.py`(`_after_verify`) · `tests/test_graph_build.py`
- **연관**: D-58(검사기의 판정 범위) · D-87(같은 교훈) · 05 §5 · 04b §5.1

> **같은 불변식을 두 곳에서 검사하면 조건이 상호배타가 될 수 있고, 그때 죽는 것은
> 검사가 아니라 분기다.** 그리고 그 분기를 검사하는 테스트가 **불가능한 상태를 손으로
> 세워** 통과하면 죽은 분기가 초록으로 보인다 — **D-87 교훈의 재확인이다.**

---


### D-91 · **④ 판정은 규칙이 바닥이고 LLM 은 조이기만 한다** 🔒 (D-09 · D-11 의 세 번째 사례)

- **상태**: 확정 (2026-08-03 · 오한빈·이서은 합의) · **구현은 `lse` 브랜치** ·
  **`main` 통합은 마감 후** (§8)
- **맥락**: ④를 LLM 으로 배선할 때 갈림길이 하나 있었다 — **LLM 이 성공하면 LLM 단독으로
  판정할 것인가.** 그렇게 하면 모델이 *"근거있음"* 을 남발할 때 막을 것이 없다.
  ④는 환각을 잡는 자리인데, 그 자리를 다시 모델에게 맡기는 셈이 된다.
- **대안**: (a) LLM 성공 시 LLM 단독 (b) **2-gram 이 바닥, LLM 은 조이기만**
  (c) 2-gram 단독 유지
- **결정**: **(b)**. 2-gram 이 `근거없음` 으로 판정한 문장을 LLM 이 `근거있음` 으로
  **뒤집을 수 없다.** 반대 방향 — 2-gram 이 통과시킨 문장을 LLM 이 떨어뜨리는 것 — 만
  허용한다. **LLM 은 조이는 쪽으로만 움직인다.**
- **근거**: 이 저장소는 **같은 비대칭을 이미 두 번 발명했다.**

  | 층위 | 규칙 | 결정 |
  |---|---|---|
  | 등급 | `max(rule_level, llm_level)` — 모델은 올릴 수만 있다 | D-09 · D-80 |
  | 문장 | 완곡 표현 제거 — 모델은 경고를 뺄 수 없다 | D-11 · D-39 |
  | **④ 검증** | **2-gram 이 바닥, LLM 은 조이기만** | **D-91** |

  > **모델은 경고를 더할 수 있고 뺄 수 없다.**
  > 세 층은 서로 다른 시점에 각자 발명됐다. **우연이 세 번이면 원리다.**

- **실측** (이서은 · 학습셋 held-out 20건 · 정확 일치):
  **베이스 10/20(50%) → 어댑터 20/20(100%)** · Fisher 정확검정 **p = 0.0004**.
  학습 데이터의 ④ 표본을 `raw_context`(LLM 미개입 원본 검색 결과) 기준으로 다시
  만들면서 **위험 방향(근거없음→근거있음) 오판이 46 → 21건**으로 줄었다.

  ⚠️ **골든셋 60건 기준이 아니다.** 04 의 비교군 C·D 와 다른 축이며 같은 표에 놓지 않는다.
- **트레이드오프**:
  > 2-gram 이 바닥이므로 **2-gram 의 맹점이 그대로 바닥의 맹점이 된다.** G-025 가 그
  > 실물이다 — 무관한 출처를 그대로 옮긴 문장에 일치율 0.688 로 `근거있음` 도장이
  > 찍혔고, LLM 은 조이기만 하므로 이것을 **뒤집을 수 없다.** 근본 대책은 ④가 아니라
  > **검색 쪽의 근거 적합성 검사**다 (§8 · 04b §6).
- **영향**: `graph/nodes/verify.py` · `graph/fallbacks.py` · `graph/state.py` ·
  `models/serving/client.py` (전부 `lse` 브랜치) · `docs/archive-3rd/03` 학습 데이터 절
- **연관**: D-05(④가 핵심) · D-09/D-80(등급) · D-11/D-39(문장) · D-58 · D-90 · 04b §2.5

> 🔴 **통합 시 재측정이 필요하다.** `verify.py` 가 바뀌면 D1 의 `grounding`
> 수치(인용률 66.1% · `unsupported` 57)가 **잰 적 없는 코드의 값**이 된다.
> 마감 후 통합 즉시 `--arm none|A|A-LC` 세 판을 다시 잰다.

---


### D-92 · **다이어리는 하루 1건이다 — 같은 날짜면 갱신한다** 🔒

- **상태**: 확정 (2026-08-03 · lgj 머지로 유입) · **구현 완료**
  (`app/routes/records.py::create_record`)
- **맥락**: 프론트가 다이어리 "수정" 을 **별도 API 없이 다시 저장**으로 처리한다.
  그대로 두면 같은 날 기록이 여러 건 쌓이고, 기간 리포트(D-83)의 입력이 **하루에 여러 번
  중복 집계**된다.
- **결정**: `POST /api/records` 를 **upsert** 로 한다. 같은
  `(pet_id, user_id, recorded_date)` 가 있으면 갱신하고 **`record_id` 는 유지**한다.
- **트레이드오프**:
  > **하루에 두 번 기록할 수 없다.** 아침·저녁 증상이 다른 경우를 표현하지 못한다.
  > 프론트의 수정 동선과 맞바꿨다. 시간대별 기록이 필요해지면 **정책이 아니라 스키마**를
  > 바꿔야 한다 (`recorded_date` 유니크 제약).
- **영향**: `app/routes/records.py` · `app/models.py`(`DiaryEntry.recorded_date`) ·
  **`app/report.py::summarize_period`** — 기간 N일치의 **상한이 N건**이 된다.
  ③ 학습 데이터를 만드는 쪽(WS3)이 이 상한을 전제로 표본을 만들어야 한다
- **연관**: D-83(③ 압축을 기간 리포트로) · D-52(소유자 확인) · D-36(최소 수집) · 02 §12

> **결정 없이 코드로 들어온 변경이다.** 머지 시점에 기록되지 않아 WS3 가 ③ 학습 표본을
> 만들 때 이 상한을 모르고 있었다. **받아온 변경도 기록한다** — 자기가 내린 결정만
> 적으면 목록이 절반만 참이 된다.

---


### D-93 · **코드·문서·데이터를 라이선스에서 분리한다** 🔒

- **상태**: **부분 개정** (2026-08-06) — **3분할 원칙은 그대로 유효하고, 코드 쪽 라이선스만
  Apache-2.0 → PolyForm Noncommercial 1.0.0 으로 바뀌었다 → D-97.** 아래 표의 코드 행을
  읽을 때 D-97 을 함께 본다. 나머지(문서 CC BY-NC 4.0 · 데이터 해당 없음 · 소급 안 됨)는
  변동 없다.
- **맥락**: MIT 하나가 저장소 전체를 덮고 있었다. 그런데 이 저장소의 세 부분은
  **우리가 줄 수 있는 권리가 서로 다르다.**

      코드    우리 것이다
      문서    우리 것이다. 다만 이 저장소의 차별점이 여기 몰려 있다 (06 300KB)
      data/   **우리 것이 아니다.** 사실을 추출해 재구성했을 뿐이고
              원문 이용 조건은 출처마다 다르다 (01d · DELETION_LOG)

  MIT 의 *"the Software"* 가 `data/` 까지 덮는 것으로 읽히면 **줄 권리가 없는 것을
  주는** 셈이 된다. 2026-08-04 확인에서 ASPCA 유래 154행이 그 지점에 걸렸다(04b §3.3).

- **결정**:

  | 대상 | 라이선스 |
  |---|---|
  | 코드 `src/` `scripts/` `tests/` `eval/harness/` `notebooks/` | **Apache-2.0** |
  | 문서 `docs/` `eval/reports/` `README.md` | **CC BY-NC 4.0** |
  | 데이터 `data/` | **해당 없음** — 각 출처 조건. 원문은 재배포하지 않는다 |

  MIT 대신 Apache-2.0 을 고른 이유는 자유도가 아니라 **세 조항** 때문이다 —
  **변경 사실 표시 의무(§4b)** · **`NOTICE` 파생물 전파(§4d)** · **상표 불허(§6)**.
  MIT 의 표시 의무는 라이선스 텍스트만 덮지만, `NOTICE` 는 **저작권자와 세 갈래
  조건이 파생물을 따라간다.**

- **소급하지 않는다**: MIT 에는 철회 조항이 없다. **이전 커밋에서 배포된 사본의
  허락은 유효하고 회수할 수 없다.** Traffic 통계에서 14일간 **고유 클론 120건**이
  관측됐다(고유 방문자 16 — 대부분 자동화). 이력을 다시 쓰는 것도 하지 않는다 —
  배포된 사본은 어차피 회수되지 않고, **커밋 타임스탬프는 표절 방어의 핵심 증거**라
  훼손하면 손해가 더 크다.

  > **무변경 부분은 사실상 두 라이선스로 동시에 얻을 수 있다.** 받는 사람이 어느
  > 사본을 가져갈지로 라이선스를 고르게 되고, 사람은 느슨한 쪽을 고른다.
  > 새 조건이 유일한 통로인 것은 **이 커밋 이후에 만들어진 것**뿐이다.

- **미채택**: **AGPL-3.0** — 이 규모에서 과잉이고 *"참고해도 되나"* 를 묻는 사람이
  줄어 포트폴리오 효과가 깎인다. **무라이선스·독점** — 법적으로는 가장 강하지만
  아무도 안심하고 보지 못한다.
- **한계**: 어떤 라이선스도 **설계 사상이 읽히는 것**은 막지 못한다. 저작권은 표현을
  보호하지 아이디어를 보호하지 않는다. 그것은 **공개/비공개의 축**이지 라이선스의 축이 아니다.
- **동의**: 저작권자 4인 전원 (오한빈 · 이근준 · 권소라 · 이서은)
- **영향**: `LICENSE`(Apache-2.0 원문) · `NOTICE`(신규) · `LICENSE-docs`(신규) · `README`
- **연관**: D-41(비밀의 자리) · D-36(개인정보) · 01d(자료 보관 규칙) · 04b §3.3

> ⚠️ `LICENSE` 는 **손으로 옮겨 적지 않는다.** 한 글자만 달라도 GitHub 인식이
> 실패하고 *"…을 적용했다"* 고 말하기 어려워진다. 원문을 받는다.
> (Apache 당시: `Invoke-WebRequest https://www.apache.org/licenses/LICENSE-2.0.txt -OutFile LICENSE`.
> 현행 PolyForm 은 D-97 참조.)

---


### D-94 · **환경변수를 사람이 외우게 하지 않는다 — 진입점을 분리한다** 🔒

- **상태**: 확정 (2026-08-04) · 구현 완료 (`PetTriage_Launcher/` · `실행.bat`)
- **맥락**: 2026-08-04 하루에 같은 종류로 여섯 번 막혔다.

      PETTRIAGE_PROFILE 을 안 세워 engine 이 stub 인 채로 돌 뻔했다
      OPENAI_API_KEY 가 .env 정리 중에 지워졌는데 doctor 는 ✅ 로 찍었다
      셸의 빈 환경변수가 .env 를 가렸다
      pettriage-initdb 가 os.getenv 만 봐서 .env 를 못 읽었다
      uvicorn --reload 로 터미널이 굳었다
      DATABASE_URL 을 비워 두는 규칙이 lgj 머지로 성립하지 않게 됐다

  공통점은 **사람이 외워야 하는 절차**라는 것이다. 그리고 **외워야 하는 것은
  언젠가 잊힌다.** 저장소를 클론한 심사자는 이 여섯 개를 전부 모른다.

- **대안 검토 — `.env` 를 시나리오별로 나눈다**: **기각.** 비밀은 시나리오가 달라도
  같은 값이라, 파일을 나누면 **같은 키를 여러 곳에 복사**하게 된다. 오늘 겪은
  `JWT_SECRET_KEY` 중복의 확대판이고 D-22(단일 출처)와 정면으로 어긋난다.
  위 여섯 중 파일 분리로 해결되는 것은 하나도 없다.

- **결정**: **설정은 하나(`.env`), 진입점은 여럿.**

      실행.bat                     더블클릭 하나로 시작한다
      PetTriage_Launcher/          목적별 메뉴 — 설치 · .env · 인덱스 · 점검 ·
                                   시연 · 평가 · 테스트 · DB 초기화 · 자유질의
      scripts/setup_env.py         .env 를 만들고 **쓸 수 있는지까지** 검증한다

  런처가 **`PETTRIAGE_PROFILE=eval` 을 대신 세운다.** `stub` 이 답하는 사고가
  구조적으로 사라진다. `uvicorn` 은 `--reload` 없이 띄우고, **포트가 실제로 열릴
  때까지 기다렸다가** 브라우저를 연다(모델 로딩에 수십 초가 걸린다).

- **`doctor.py` 와 `setup_env.py` 를 나눈 이유**: `doctor` 는 *"있나"*, `setup_env` 는
  ***"쓸 수 있나"*** 를 본다. `OPENAI_API_KEY=...` 세 글자 자리표시자가 `doctor` 에서
  ✅ 로 찍힌 것이 그 차이다. 길이·중복·인코딩·셸 충돌까지 본다.
- **비용을 표시한다**: LLM 을 부르는 항목 셋(시연·평가·자유질의)에 **비용 경고와
  확인**을 붙였다. 시연은 **무료(폴백) 모드**를 따로 두어 화면·흐름만 볼 수 있게 했다
  (`PETTRIAGE__MODEL__PROVIDER=none` — `engine=stub` 으로 내리는 것과 다르다.
  그건 파이프라인 자체가 가짜다).
- **영향**: `PetTriage_Launcher/`(신규 3파일) · `실행.bat`(신규 2) ·
  `scripts/setup_env.py`(신규) · `.env.example`(146줄 → 90줄, 변수 불변)
- **연관**: D-22(단일 출처) · D-40(못 어기게 둔다) · D-48(문서≠코드) · 09

> 🔴 **콘솔 코드페이지는 프로세스가 아니라 콘솔의 상태다 — 빌렸으면 돌려준다.**
>
> 런처가 한글 출력을 위해 `chcp 65001` 을 실행했는데, `chcp` 는 자식 프로세스가
> 아니라 **콘솔 자체**를 바꾼다. 윈도우는 부모 PowerShell 과 같은 콘솔을 공유하므로
> **런처를 닫은 뒤에도 그 창이 65001 로 남았고**, cp949 로 출력하는 다른 프로그램의
> 한글이 전부 깨졌다 (2026-08-04 실제로 겪음).
>
> 시작할 때 원래 값을 읽어 두고 **정상 종료·예외·재실행 어느 경로로 끝나든**
> `finally` 에서 복원한다. 같은 모양의 것이 하나 더 있다 — `--reload` 로 굳은
> 터미널. **도구가 남긴 상태를 사용자가 치우게 하지 않는다.**

> **`.env.example` 은 필수 두 줄을 맨 위로 올렸다.** 전에는 `JWT_SECRET_KEY` 가
> 118행에 있어서, 새로 받은 사람이 두 줄을 채우려면 파일 끝까지 스크롤해야 했다.
> 뺀 것은 *경위* 이고(06 으로), 남긴 것은 **실제로 당하는 함정 셋**이다.

---


### D-95 · **골든셋이 물질 중심으로 편향돼 있다** 🔒 (04a 지침 개정)

- **상태**: 확정 (2026-08-04) · 지침 반영 대기 (04b §7-19)
- **맥락**: 골든셋 밖 질문 20건을 돌린 결과(04b §4.5), **`answered` 11건 중 3건(27%)
  에서 질문과 무관한 자료가 근거로 실렸다.**

      "강아지가 발을 계속 핥아요 부동액인 것 같아요"
        슬롯   물질 = 부동액        ← 정확히 뽑았다
        인용   S-080 ×4            ← 개 강박장애 자료
        본문   부동액 이야기가 **한 마디도 없다**

  원인은 둘이다. ① `S-080` 만 `substance` 열이 **동사구**다(핥기·쫓기·물기) —
  다른 문서는 전부 명사(양파·포도·부동액)라, 행동·증상 서술 질의를 독점적으로
  끌어당긴다. ② `build_filter` 는 **`species` 만** 필터로 쓴다. 물질을 알아도
  검색이 제약으로 쓰지 않는다.

- **그런데 이것이 골든셋 60건에서 한 번도 안 보였다.** 60건이 **전부 물질 중심으로
  설계**돼 있어서다. **통과 65.0% 는 그 편향 위의 숫자다.**
- **결정**: 04a 골든셋 작성 지침에 **행동·증상으로 시작하는 질의**를 유형으로 추가한다.
  *"발을 핥아요"* · *"자꾸 긁어요"* · *"계속 토해요"* 처럼 **물질이 뒤에 오거나
  없는** 형태다.
- **부수 결정**: `eval/reports/` 는 git 추적 대상이다. **실제 사용자 입력으로 probe 를
  돌린 결과 JSON 은 커밋하지 않는다** (D-36).
- **영향**: `docs/04a_골든셋작성지침.md` · `scripts/probe_freeform.py`(신규) · 04b §4.5
- **연관**: D-36(최소 수집) · D-45(경로②) · 04a · 04b §7-15·16·19

> **골든셋은 우리가 설계한 문제다.** 그 위의 점수는 *"우리가 예상한 상황"* 에서의
> 성적이지 실사용 성적이 아니다. 예상 밖을 넣어 보기 전까지는 **무엇을 예상하지
> 않았는지조차 모른다.**

---

### D-96 · **게이트의 검사 목록은 한 벌만 둔다 — CI 는 `Makefile` 을 부른다** 🔒

- **상태**: 확정 (2026-08-04) · **구현 완료** (`.github/workflows/ci.yml`)
- **맥락**: *"CI 게이트가 제대로 작동하지 않는 것 같다"* 는 관찰에서 시작해 네 군데가 나왔다.

  | | 있던 것 | 무엇이 새는가 |
  |---|---|---|
  | 트리거 | `branches: [main, develop]` | **`develop` 은 존재한 적이 없다.** 작업은 전부 `ohb`·`lgj` 같은 개인 브랜치에서 일어나므로 **push 시점에는 한 번도 안 돌았다** — PR 을 열어야만 돌았다 |
  | 린트 경로 | `ruff check src tests scripts` | `eval/` 이 밖. 하네스는 **보고서 숫자를 만드는 코드**다 |
  | 테스트 | `pytest -q` | `addopts` 에 이미 `-q` 가 있어 **`-qq`** 가 됐다. 실패한 테스트의 **이름조차 안 나온다** |
  | `db-deps` | `pytest tests/test_auth_api.py` | 파일 지목이라, `[db]` 가 필요한 테스트를 새로 만들어도 **여기에 이름을 적기 전까지 어느 잡에서도 안 돈다.** `test_records_api.py` 7건이 실제로 그 사이에 끼어 있었다 |

- 🔴 **핵심은 두 번째다 — 그리고 그것은 이미 고쳐져 있었다.**

      Makefile (2026-08-02)
        # `eval` 을 뺐더니 하네스 800여 줄이 린트 밖에 있었다.
        LINT_PATHS = src tests scripts eval

      ci.yml (안 고쳐짐)
        ruff check src tests scripts

  8월 2일에 발견해서 `Makefile` 은 고쳤는데 **`ci.yml` 은 자기 목록을 따로 갖고 있었다.**
  그래서 `make lint` 는 빨간데 CI 는 초록인 상태로 이틀이 갔다.

- **결정**: **경로 목록도 도구 버전도 CI 에 손으로 적지 않는다.**

      - run: pip install -c constraints.txt ruff   # 버전 → constraints.txt
      - run: make lint                             # 경로  → Makefile

  트리거는 `branches: ["**"]` 로 넓히고, 넓힌 대가는 `concurrency` + `cancel-in-progress`
  로 되돌려 받는다. `pytest` 는 `-v` 로 `addopts` 의 `-q` 를 상쇄한다
  (verbosity 는 합산이다 — `addopts` 자체는 건드리지 않는다. 로컬이 조용한 것은 그대로 둔다).
- **실측**: 고치고 나서 린트가 **22건** 나왔다 — `E501` 20 · `SIM102` 1 · `UP009` 2,
  포맷 15파일. 전부 CI 를 통과해 온 것들이다. 수정 후 `pytest 585 passed` 유지.
  `ohb` 브랜치의 **push 이벤트 실행(CI #50)이 이 저장소에서 처음** 생겼다.
- **예외 하나**: `scripts/make_submission.py` 만 `E501` 을 끈다. 이 파일의 긴 줄은 코드가
  아니라 **`CODE_MAP` 문자열 안의 제출용 마크다운 표**이고, 표 행은 접을 수 없다.
  접으면 **린트를 지키려고 산출물을 망가뜨린 것**이 된다. 마크다운을 별도 `.md` 템플릿으로
  빼는 것이 본래 해법이며 이 예외는 그때까지의 임시다.
- **영향**: `.github/workflows/ci.yml` · `pyproject.toml`(per-file-ignores) ·
  린트 수정 17파일 · `docs/09`(윈도우엔 `make` 가 없다는 안내)
- **연관**: D-48(문서≠코드) · D-90(같은 불변식을 두 곳에서 검사) · D-82(안 부른 것을
  성공으로 세지 않는다) · D-87(검증 안 한 가정)

> **검사 목록이 두 벌이면 언젠가 갈리고, 갈린 쪽이 CI 면 게이트가 거짓말을 한다.**
>
> D-48 은 문서와 코드가 갈린 것이었고 D-90 은 같은 불변식을 두 곳에서 검사한 것이었다.
> 이번엔 **검사기 자신**에서 같은 일이 일어났다. 게이트는 틀렸을 때 조용하다 —
> 빨개지지 않는 것과 통과하는 것은 화면에서 똑같이 생겼다.

- ⚠️ **부록 — 이 건을 진단하면서 낸 내 실수 둘** (05 §7 의 태도를 따라 남긴다)

  ① **게이트의 범위가 좁다고 지적하면서 내 확인 범위도 똑같이 좁았다.** `eval/` 과
  `src/report.py` 만 보고 *"린트는 2건"* 이라고 보고했다. 실제로는 **22건**이었고,
  `scripts/` 를 아예 안 봤다. **게이트를 넓히기 전에 자기 확인 범위부터 넓혀야 한다.**

  ② **결과가 어긋나자 상대 환경부터 의심했다.** 포매터 결과가 갈리자(팀장 15파일 /
  내 쪽 12파일) *"ruff 버전이 다르다"* 고 단정했다. 아니었다 — **내가 받아 온 파일
  사본이 낡았다.** 다시 받으니 같은 버전에서 15파일이 나왔다. 버전 고정은
  `constraints.txt`·`.pre-commit-config.yaml`·`pyproject.toml` **세 곳에 이미** 있었고
  멀쩡했다. **어긋남을 봤을 때 먼저 의심할 것은 내 입력이다.**

---

### D-97 · **코드는 PolyForm Noncommercial 1.0.0 으로 간다** 🔒 (D-93 코드 행 개정)

- **상태**: 확정 (2026-08-06) · 적용 완료 (`LICENSE` · `NOTICE` · `LICENSE-docs` · `README`)
- **결정 주체**: 오한빈(팀장) · 커밋 `c6fbb03`
- **맥락**: D-93 이 코드를 **Apache-2.0**, 문서를 **CC BY-NC 4.0** 으로 갈랐다.
  그런데 그 조합은 **두 축이 어긋나 있었다.**

      문서   비상업 제한 있음 (CC BY-NC)
      코드   비상업 제한 없음 (Apache-2.0)  ← 상업적 이용이 열려 있다

  이 저장소에서 **코드와 문서는 분리해서 쓸 수 있는 물건이 아니다.** 등급 게이트가
  왜 그렇게 생겼는지는 `docs/06` 이 설명하고, 코드만 떼어 가면 안전 설계의 근거가
  사라진다. 문서에만 비상업 제한을 건 것은 **실효가 없는 방어**였다 —
  가치 있는 쪽(코드)이 열려 있었기 때문이다.

- **결정**: 코드를 **PolyForm Noncommercial License 1.0.0** 으로 바꾼다.
  문서(CC BY-NC 4.0)와 데이터(해당 없음)는 그대로다. **D-93 의 3분할 구조 자체는 유지된다.**

  | 대상 | 이전 (D-93) | 현행 (D-97) |
  |---|---|---|
  | 코드 | Apache-2.0 | **PolyForm Noncommercial 1.0.0** |
  | 문서 | CC BY-NC 4.0 | 변동 없음 |
  | 데이터 | 해당 없음 | 변동 없음 |

- **무엇을 얻고 무엇을 잃었나** — 교체는 순이득이 아니다. 둘 다 적는다.

  | | Apache-2.0 | PolyForm NC 1.0.0 |
  |---|---|---|
  | 상업적 이용 | 허용 | **금지** ← 바꾼 이유 |
  | 저작권자 표시 전파 | `NOTICE` §4(d) | **`Required Notice:` 줄** (Notices 절) |
  | 변경 사실 표시 의무 | §4(b) 있음 | **없음** ← 잃었다 |
  | 상표 조항 | §6 있음 | **없음** ← 잃었다 |
  | 특허 실시권 | §3 | Patent License 절 (있음) |
  | 위반 시 | 즉시 종료 | **32일 시정 기간** |
  | OSI 오픈소스 | ✅ | **❌ source-available** |

  🔴 **`Required Notice:` 가 이 교체의 기술적 핵심이다.** PolyForm 은 `NOTICE` **파일**을
  전파시키지 않는다. 전파되는 것은 라이선스 본문에 있는 **`Required Notice:` 로 시작하는
  플레인텍스트 줄**이다. 그래서 `LICENSE` 5줄에 저작권자 4인과 저장소 URL 을 적었다.
  이 줄이 없으면 Apache 의 §4(d) 를 잃기만 하고 대체물이 없는 상태가 된다.

- **비상업의 범위**: *"Any noncommercial purpose is a permitted purpose."*
  라이선스가 **개인 이용 · 비영리 단체 · 교육기관 · 공정이용**을 별도 절로 명시해 둔다.
  즉 **다른 학습자가 보고 배우는 것은 그대로 허용된다** — 막는 것은 상업적 전용뿐이다.
- **대가**: **OSI 기준의 오픈소스가 아니다.** D-93 이 AGPL 을 미채택하며 적었던
  *"참고해도 되나를 묻는 사람이 줄어 포트폴리오 효과가 깎인다"* 는 우려가 여기에도 걸린다.
  **그 대가를 알고 골랐다** — 비상업 제한을 실효 있게 만드는 것이 우선이라고 판단했다.
  `NOTICE` 끝에 *"상업적 이용을 원하시면 저작권자에게 문의해 주십시오"* 를 둔 이유다.
- **소급하지 않는다 — 이번엔 층이 셋이다.** `NOTICE` 에 시점 표를 넣었다.

      ~ 2026-08-03    MIT License
      2026-08-04 ~    Apache License 2.0
      2026-08-06 ~    PolyForm Noncommercial 1.0.0 (코드) + CC BY-NC 4.0 (문서)

  MIT 기간에 나간 사본(고유 클론 120건)은 **지금도 MIT 조건으로 상업적 이용이 가능하다.**
  비상업 제한은 **이 커밋 이후에 만들어진 것에만** 유일한 통로다.
  D-93 의 그 문장이 그대로 적용된다 — *"사람은 느슨한 쪽을 고른다."*
- **동의**: 저작권자 4인 전원 (D-93 과 같은 조건). 외부 기여가 붙기 전이라 아직 가능하다.
- **영향**: `LICENSE`(PolyForm 원문 + `Required Notice:`) · `NOTICE`(3분할·시점 표 개정) ·
  `LICENSE-docs`(코드 라이선스 참조 갱신) · `README`(라이선스 절) ·
  `docs/06` D-93(상태 → 부분 개정)
- **연관**: D-93(3분할 원칙 — **폐기가 아니라 코드 행만 개정**) · D-41 · D-36 · 01d

> **비상업 제한은 가장 가치 있는 부분에 걸려 있지 않으면 장식이다.**
>
> 문서에만 NC 를 걸고 코드를 Apache 로 열어 둔 상태는, 지키려던 것을 **정확히 안 지키는**
> 배치였다. 라이선스는 파일 단위로 고르는 것이 아니라 **무엇을 막고 싶은지**로 고르는 것이다.

- ⚠️ **확인할 것 둘** (마감 후)

  ① **GitHub 라이선스 인식.** 저장소 About 칸이 `PolyForm Noncommercial 1.0.0` 으로
  뜨는지 본다. SPDX 식별자는 `PolyForm-Noncommercial-1.0.0` 이지만 GitHub 의 자동
  인식은 OSI 승인 목록 위주라 **`Other` 로 표시될 수 있다.** 그래도 라이선스의 효력에는
  영향이 없다 — `LICENSE` 파일 본문이 조건이다.

  ② **`NOTICE` 문구 하나가 라이선스보다 강하다.** *"이 NOTICE 와 위 Required Notice 줄을
  함께 실어야 합니다"* 라고 적었는데, PolyForm 이 실제로 강제하는 것은 **`Required Notice:`
  줄뿐**이다. `NOTICE` 파일 전체는 요청이지 의무가 아니다. 의무로 만들려면
  `Required Notice:` 줄이 `NOTICE` 를 가리키게 써야 한다.

---

### D-98 · **폐기한 결정은 「구현자가 읽는 곳」에 적는다** 🔒 (D-48 · D-96 의 세 번째 사례)

- **상태**: 확정 (2026-08-18) · 적용 완료 (`graph/nodes/verify.py` 머리말)
- **맥락**: 브랜치를 머지하니 **문장 제거가 구현돼 있었다.** 그런데 그 조치는
  **실측 근거로 폐기한 것**이었다.

  🔴 **시간 순서가 이 항목의 전부다.**

      2026-08-03 12:17   문장 제거 구현 (`2fb2534`)
      2026-08-05 01:25   04b 측정에서 폐기 판정 — 지우면 등급 권고가 함께 사라진다
      2026-08-18         머지 · 발견 · 되돌림

  **구현이 폐기보다 이틀 빠르다.** 폐기된 것을 잘못 읽고 구현한 게 아니라,
  **구현한 뒤에 그 설계가 폐기된 것**이다. 커밋 메시지의 *"문장 제거 미구현 수정"* 도
  **그 시점에는 정확한 서술**이었다 — 당시 이 조치는 실제로 미구현이었고 폐기되지도 않았다.

  즉 **아무도 틀리지 않았는데 폐기된 설계가 코드가 됐다.**

  폐기 판정은 네 곳에 적혔다.

      04b §3.4   갈래 ㄱ(문장 제거)은 여기서 폐기된다
      04b §7-3   ~~문장 제거(ㄱ)~~ | 폐기 (§3.4)
      02  §117   일부 근거없음 ► 그대로 통과 (문장 제거는 폐기됐다)
      02  §580   ⚠️ 문장 제거는 폐기됐다 — 지우면 "즉시 수의사와 상담하세요" 가 사라진다

  **그런데 그 넷 중 어느 것도 코드 옆에 없었다.** 정작 `verify.py` 머리말은
  폐기 전 문구를 그대로 달고 있었다.

      """④ 근거 검증 노드 — 이 프로젝트의 핵심이다.
          판정에 따른 조치는 코드가 한다 — 문장 제거 · 재검색 1회 · 거절.
                                          ^^^^^^^^^ 폐기 뒤에도 그대로였다
      """

  **폐기를 결정한 쪽이 코드를 안 고쳤고, 그래서 이미 존재하던 구현이
  아무 저항 없이 머지됐다.** 되돌린 것은 그 머지 시점이다.

- 🔴 **폐기 사유는 안전 문제였다.** 근거없음으로 떨어지는 문장의 다수가
  **등급이 시킨 행동 권고**다. *"지금 바로 동물병원으로 가세요"* 는 코퍼스가 아니라
  **코드가 등급에서 만든 문장**이라 2-gram 일치율이 낮다(실측 0.304).

  > **제거를 켜면 등급 4인데 병원 가라는 문장이 사라진 답변이 나간다.**
  > `none` 판에서 못 채운 문구가 **즉시×7 · 전화×5 · 병원×4** 였다.

- **결정**: 폐기 사유를 **문서가 아니라 코드 머리말에** 적는다. 되돌린 커밋에서
  머리말을 이렇게 바꿨다.

      🔴 조치 「문장 제거」는 폐기됐다 (04b §3.4 · 02 §9).
         … 폐기 사실이 문서에만 있어서, 이미 있던 구현(2026-08-03)이
         머지로 들어온 뒤 되돌렸다(2026-08-18). 그래서 사유를 여기 적는다.
         제대로 하려면 등급 파생 문장을 코드가 식별해 제거 대상에서 빼야 한다.

  **일반 규칙** — 결정을 폐기하면 `docs/06` 에 적는 것으로 끝내지 않고,
  **그 결정이 구현될 자리(모듈 머리말 · 노드 안내 · 부품표)에 폐기 표시를 남긴다.**

  🔴 **특히 폐기 시점에 그 설계가 이미 어딘가에 구현돼 있을 수 있다.**
  브랜치가 여럿이면 *"아직 안 만들었으니 폐기해도 된다"* 는 가정이 성립하지 않는다.
  **폐기는 만들어질 자리가 아니라 이미 만들어졌을지 모르는 자리에 적어야 한다.**
- **되돌리지 않은 것**: 같은 머지의 나머지는 전부 유지했다 — `Task.VERIFY` 호출부 배선,
  2-gram 하이브리드 결합(D-91 구현), `all_ungrounded` 를 「모순」까지 포함하도록 확장,
  SLOT·VERIFY 프롬프트 개선. **되돌린 것은 문장 제거 하나다.**
- **영향**: `graph/nodes/verify.py` · `README`(아직 못 한 것) · `docs/02` §9
- **연관**: D-48(문서≠코드) · D-96(검사 목록이 두 벌) · D-87(폐기 항목을 남기는 이유) ·
  D-91(하이브리드 결합) · 04b §3.4 · §7-5

> **폐기는 결정이 적힌 곳이 아니라 구현될 곳에 적어야 한다.**
>
> D-48 은 문서와 코드가 갈렸고, D-96 은 검사 목록이 두 벌이었고, 이번엔
> **폐기가 한쪽에만 적혔다.** 세 번 다 같은 모양이다 — **읽는 사람이 있는 자리에
> 최신 상태가 없으면, 낡은 쪽이 실행된다.**
>
> 🔴 그리고 이번이 가장 나쁜 형태였다. 앞의 둘은 **검사가 안 돈 것**이었지만
> 이번엔 **폐기된 설계가 실제로 동작하는 코드가 됐다.** 테스트도 통과했다 —
> 문장 제거를 검증하는 테스트가 없었기 때문이다.

- ⚠️ **구현한 사람의 잘못이 아니다 — 애초에 잘못이 아니었다.**
  구현 시점(08-03)에 이 조치는 **머리말이 명시한 설계였고 폐기되지도 않았다.**
  폐기는 그 이틀 뒤에 났고, **폐기를 낸 쪽(팀장)이 코드 머리말을 안 고쳤다.**
  이 항목은 그 책임 소재를 남기려고 쓴다.

---


## 8. 결정 대기 목록

아직 기록되지 않은, 곧 내려야 할 결정들이다.

| 항목 | 결정 주체 | 선행 조건 |
|---|---|---|
| 조류 `species_detail` 분류 체계 | WS1 | — |
| Top-k 최적값 | WS2 | 골든 셋 |
| 과소평가율 허용 상한 | WS4 | 초기 측정치 |
| LangSmith 가명화 설정 방식 | WS2 | D-36 |
| **D-43 사진 관문 5단계 구현 담당** | 팀장 | D-43 |
| **증상 조합 → 등급 규칙을 어디에 둘 것인가** | WS2 | D-49 · 물질 축(D-50)은 정해졌다 |
| **"도메인 밖"의 경계 — `intent=general` 이 곧 거절인가** | WS2 | **실측으로는 해소 · 명문화만 남았다.** D-85 로 `none` 유형이 33.3% → **100.0% (9/60 중 9건 전건)** 이 됐다. 기준을 글로 적는 일이 남는다 |
| **동음이의 별칭을 `kind` 세 번째 값으로 둘 것인가** | WS2 | D-60 이 `청주`·`양주` 를 **제거로 회피**했다. 표가 모호함을 삼키지 않고 ②에 넘기려면 세 번째 값이 필요하나, **아직 넘길 곳이 없다** |
| 🔴 **예방형 질의(축 B)가 축 A 를 타는 문제** | 팀장 | **2026-08-03 측정으로 확인.** `prevent` 통과 **0.0%**. G-003·G-021·G-108·G-007. 아래 §8.1 |
| **④ 근거 검증의 `main` 통합 + 재측정** | 팀장 · WS3 | **선행 결정 완료 → D-91.** 구현은 `lse` 브랜치에 있다. 통합하면 D1 의 `grounding` 수치가 잰 적 없는 코드의 값이 되므로 **즉시 세 판 재측정** |
| **규칙 표 커버리지** | WS1(자료) | **채우는 작업이 아니다** — 아래 §8.2 |
| **D-89 를 계약으로 강제할 것인가** | WS2 | 지금은 붙이기만 하고 검사하지 않는다. `MUST_STATE` 와 같은 방식 |
| 어휘 공백 (`양파국`·`감기약`) | WS1 | D-88 로도 안 잡힌다. 별칭 표 추가 건 |
| 🔴 **재검색 발동 조건 재설계** | WS2 | **D-90 으로 순환은 살아났으나 `all_ungrounded` 가 사문이다.** 세 판 300건에서 **0회 발동**. 비율 기반 또는 핵심 문장 기반으로 다시 설계한다 |
| 🔴 **근거 적합성 검사 — 질문 물질 ≠ 출처 물질** | WS2 | **G-025.** ④는 `draft` 를 `context` 하고만 비교하므로 **검색이 틀리면 함께 틀린다.** `substance` 슬롯과 사실표 열의 **문자열 대조 한 번**이면 된다 — 가드레일 모델도 LLM 호출도 필요 없다 |
| **`arms.py` 의 `C` 어댑터 경로** | WS3 | `artifacts/adapters/qwen3-4b-mt` 로 박혀 있으나 실제는 `artifacts/adapters/multitask-v2/checkpoint-484`. **지금 `--arm C` 는 없는 경로를 본다** |

> 각 항목은 결정되는 즉시 **다음 번호(D-93~)로 기록**한다.
>
> **해소됨** — 트리아지 등급 체계 → **D-39** · WS5 구현 방식 → **D-40** · **베이스 sLLM → D-42** · 얼굴 검출 방식 → **D-43** · WS 담당자 배정 → **07 §0** (2026-08-01 개정) · **벡터DB → D-44** · 경로① → **D-45** · **⑥ 번역 미채택 → D-19 후속** · **미국 핫라인 처리 → D-47** · **초콜릿 F-097-005/006 등급 → 비워 둔다(1차 검수)** · **팀원 코드 흡수 기준 → D-48** · **체중 단위 → D-17 후속(개정)** · **관계형 DB → MySQL 컨테이너 (D-48 미결 해소)** · **증상 질의 처리 → D-49** · **역치 → 등급 매핑·출처 상충 → D-50** · **식물 부위 단위 환산 → D-51** · **프로필 참조 방식 → D-52** · **벡터DB는 인덱스·캐시 금지 목록 → D-53** · **안전 강제 위치 → D-54** · **역치의 기준(음식/독소) → D-55** · **빈 필터 처리 → D-56** · **틀린 테스트 처리 → D-57** · **검사기의 판정 범위 → D-58** · **물질 동정 방식 → D-59** · **별칭 일치 규칙 → D-60** · **별칭 근거 검사 → D-61** · **폐쇄 목록 강제 방식 → D-62** · **추정 물질의 취급 → D-63** · **랭그래프 실체 도입 → D-64** · **서빙 모델 선택 방식 → D-65** · **평가 중 되묻기 처리 → D-66** · **종 어휘 분리 → D-67** · **물질 미해결의 세 갈래 → D-68** · **설정이 코드에 닿는가 → D-69** · **선택 의존성 임포트 위치 → D-70** · **LangChain 사용 범위 → D-71** · **계약 필드를 누가 채우나 → D-72** · **라벨을 모델도 아는가 → D-73(이서은)** · **의존성 연쇄 → D-74** · **끊긴 값의 처리 → D-75** · **폴백 기록의 자리 → D-76** · **주석과 동작의 어긋남 → D-77** · **유효성분 환산 → D-78** · **판정에 수치를 준다 → D-79** · **정량이면 상승 차단 → D-80(D-09 개정)** · **근거를 필드로 낸다 → D-81** · **안 부른 태스크를 성공으로 세지 않는다 → D-82** · **③ 압축의 자리 → D-83** · **조건 없는 MONITOR 의 조치 → D-84** · **말했는데 코퍼스에 없는 물질 → D-85** · **② 슬롯 출력 스키마 → D-86** · **구(句) 표면형의 처리 → D-88** · **등급의 행동 문장 → D-89** · **폐기한 시도(표면형 훑기) → D-87** · **④ 상한 판단의 자리 → D-90** · **④ 판정의 바닥 → D-91** · **다이어리 하루 1건 → D-92** · **라이선스 분리 → D-93** · **실행 진입점 분리 → D-94** · **골든셋 편향 → D-95** · **CI 검사 목록의 단일 출처 → D-96** · **코드 라이선스 비상업화 → D-97(D-93 코드 행 개정)** · **폐기 표시의 자리 → D-98** · **GPU 자원 확보 → RTX 3080 (03a §2.2)**

### 8.1 예방형 질의 — 측정 결과와 설계안 (미결)

**증상.** *"양파를 줘도 될까요"* · *"방향제를 놔둬도 될까요"* 처럼 **아직 아무도 아무것도
먹지 않은** 질의가 축 A(긴급도) 파이프라인을 그대로 탄다. C1 실측 —

| 케이스 | 질문 | 실제 |
|---|---|---|
| G-003 | 양파를 **줘도** 괜찮을까요 | `rule=3` 정성 → **CALL_NOW** |
| G-021 | 방향제를 **놔둬도** 괜찮을까요 | `llm=1` 조건없음 → **D-84 승격 CALL_NOW** |
| G-108 | 우유를 **줘도** 되나요 | `llm=1` 조건없음 → **D-39 거절** |
| G-007 | 블루베리 한 줌 | `llm=1` 조건없음 → **D-84 승격** |

`feeding_level`(축 B)은 사실표 221행·문장화·스키마에 전부 있는데 **`graph/` 안에는
한 번도 나오지 않는다.** 축이 있는데 배선이 없다.

🔴 **D-89 가 이 결함을 키웠다.** 전에는 잘못된 등급이 배지였으나 이제
*"[해야 할 일] 지금 수의사에게 전화해 상태를 알리세요."* 라는 **문장**으로 나간다.
D-89 의 의도는 *상충을 보이게 하는 것*이었는데 이 건은 상충이 아니라 **틀린 등급**이다.

**왜 지금 안 고치나.** C1 로 각 건의 걸림돌을 전부 뽑았더니 축 B 만 고쳐서 통과로
도는 것은 **G-007 하나**다. 나머지는 ①분류(G-004)·검색(G-108)·문구(G-021)·
골든셋(G-003)이 함께 걸려 있다. **코드 4~5곳을 건드려 +1건**이고, 등급 분모가
또 바뀌어 파인튜닝 팀원이 비교할 기준선이 사라진다.

**대신 계량했다** (2026-08-03). 예방형 3건의 `must_not_contain` 에
`지금 바로|수의사에게 전화|응급` 을 넣어 **과잉경보가 채점되게** 했다. 통과율은
안 바뀌고(이미 실패 중) `must_not_contain` 이 내려간다 — **D-84 때와 같다:
성능이 나빠진 것이 아니라 안 세던 것을 세게 된 것이다.**

**설계안 (다음 사람에게).** ①분류에 라벨을 늘리지 **않는다** — D-73 단일 출처를
타고 프롬프트·검증기가 함께 바뀌어 전 케이스 재측정이 된다. 대신 —

    models/tasks.py   SPECS[SLOT].output_keys 에 `ingested` 를 더한다
                      key_hints: "먹었거나 접촉했으면 true. 아직 안 먹었고
                                  줘도 되는지 묻는 것이면 false. 모르면 null"
    graph/state.py    Slots 에 같은 키
    triage.py         `ingested is False` 면 축 A 등급을 매기지 않고
                      feeding_level(축 B)로 답한다

`_schema_block`(D-86)이 프롬프트를 **자동 생성**하므로 손으로 적을 곳이 없다.
`null` 이면 *"먹었다"* 로 본다 — **모름을 안전으로 읽지 않는다** (D-79). 그래서
이 변경은 **덧셈**이고, 모델이 명시적으로 `false` 를 낼 때만 동작이 바뀐다.

### 8.2 규칙 표 커버리지 — **채우는 작업이 아니다** (2026-08-03 조사)

파생 표는 107행(`정성등급` 91 + `정량임계치` 16)이고 사실표는 888행이다. 12%.
측정된 효과는 크다 — **규칙 바닥 있음 등급일치 91.3%(n=23) vs 없음 66.7%(n=9)**,
두 판이 소수점까지 같다. **안정성의 유일한 원천이다.**

그런데 사실표를 열어 보면 공란의 성격이 드러난다 —

| | 등급 매겨진 80행 | 공란 448행 |
|---|---|---|
| `effect` | 71% | 50% |
| `signs` | 62% | 30% |
| **`escalation_conditions`** | **32%** | **0.4%** |

등급이 매겨진 행은 **원문이 행동이나 치명성을 명시한 경우**다 —
`F-007-001` *"Avocados are deadly for birds"*, `F-007-002` *"수 분 내에 폐사"*.
공란 행의 검수 메모는 **왜 비었는지를 적어 놨다** —

    F-003-002 초콜릿(bird)  원문 'If overdosed' 라고만 하고 수치 없음
    F-003-004 알코올(bird)  쉽게 중독된다고만 서술 — 수치 없음
    F-071-006 연기          만성 노출 — 장기 노출 시 영구 손상

**최소 69행은 자료가 등급을 말하지 않았다고 명시**되어 있고, 나머지 432행은
미분류라 **원문을 다시 읽어야** 안다 — 888행 1차 검수 재수행이다.

🔒 **추측으로 채우면 D-30·D-38 위반이다.** G-021 검수에서 이미 같은 이유로
반려한 적이 있다 — *"급성 등급을 매기면 원문에 없는 분류를 만드는 것이다."*

**따라서 12% 는 게으름이 아니라 자료의 성질이다.** 보고서에서 사과할 항목이 아니라
**측정된 발견**으로 적는다 — *"자료가 말하지 않은 자리에서는 LLM 이 흔들린다."*
그 자리를 메우는 것은 표의 위조가 아니라 **D-84 의 코드 보정**이다.


>
> 아직 번호를 붙이지 않은 미기록 논의: **다국어(중국어·일본어) 자료 추가 시 처리 방식**
> — 번역 태스크는 닫혔으므로, 추가한다면 **한국어 사실 표로 추출**하는 경로다
> (원문 보존 + 필드 매핑 + 학명·수치 앵커 검증 방안까지 논의됨. 결정 시 기록한다)
