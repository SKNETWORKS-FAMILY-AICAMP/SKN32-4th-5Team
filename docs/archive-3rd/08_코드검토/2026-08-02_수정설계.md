# 수정 설계 — 2026-08-02

> **SKN 3차 단위 프로젝트** · 팀 `save the pet` · **작성자: 오한빈 (팀장)**
>
> 대상: `2026-08-02_검토보고.md` 의 🔴 치명 5 · 🟠 중대 11
> **이 문서를 확인한 뒤 착수했다.** 결과는 `2026-08-02_수정완료.md`.
>
> **이 문서의 성격** — 팀이 만든 코드 **전체**를 팀장이 검토한 기록이다.
> 대상에는 **검토자 자신이 작성한 코드가 포함**되며, 실제로 지적 중 상당수가 그쪽이다.
> 특정 팀원을 지목하지 않는다 — 결함은 전부 파일·함수 단위로 적었다.

---

## 왜 설계부터 하는가

검토에서 나온 치명 5건은 전부 같은 모양이었다 — **로직은 옳은데 불려지지 않는다.**
그래서 "빠진 호출을 채워 넣는" 식으로 고치면 **다음 사람이 또 빠뜨린다.** 같은 버그를 다시 만드는 것이다.

그리고 이번 검토가 실증한 것이 하나 더 있다 — **틀린 동작을 정답으로 고정한 테스트가 3건**이었다.
고칠 때 그 테스트를 함께 보지 않으면, 고치는 순간 빨간불이 뜨고 사람은 **코드가 아니라 코드를 되돌리게 된다.**

## 수정 원칙

> ⚠️ 아래 넷은 착수 시점의 것이다. 검증 중에 **P5** 가 추가됐다 —
> `2026-08-02_검토보고.md` §추가 37·38 과 **D-58** 참조.
> *"검사기는 그 계층이 통제하는 것만 판정한다."* 네 번째 사례가 **이 검토를 하던 중에
> 내가 만든 것**이라 원칙으로 올렸다.


| | 원칙 | 이번에 적용되는 곳 |
|---|---|---|
| **P1** | **강제는 우회 불가능한 지점에 둔다** — 호출부가 아니라 타입·계약·주입 (D-40) | ① 연락처 · ② 게이트 |
| **P2** | **한 개념은 한 곳에서만 정의한다** — 두 곳에 적으면 반드시 어긋난다 | ⑦ 단위 · ② 배지 |
| **P3** | **모르면 만들지 않는다** — 기본값을 채우는 대신 절을 생략한다 (D-38 · D-51) | ⑧ 문장화 · ③ 필터 |
| **P4** | **고치기 전에 그 동작을 고정한 테스트부터 본다** | ③ · ⑧ |

## 또 오류를 만들지 않기 위한 규율 3가지

1. **수정마다 "지금 실패해야 하는 테스트"를 먼저 쓴다.** 통과하면 그 수정은 아무것도 안 한 것이다.
2. **원본 CSV 는 건드리지 않는다.** 문장화(⑧)·검사(⑮)의 결함은 문장화·검사에서 고친다. 데이터를 고쳐 맞추면 **출처와 어긋난 표가 남는다.**
3. **매 수정 후 269건 전량 + 재현 스크립트를 돌린다.** 실측 숫자가 바뀌면 그 자리에서 안다.

---

# 1차 — 안전 불변식 (시연 전)

## ① 연락처 차단을 **엔진 경계**로 올린다 (D-47)

### 지금

```
StubEngine.ask() ──▶ AskResponse          ← 스크럽 없음
GraphEngine ─ finalize() ─▶ scrub         ← 여기만. 그런데 GraphEngine 은 못 만든다
```

### 고친 뒤

```
                  ┌─ StubEngine ─┐
deps.get_engine() ┤              ├──▶ SafetyEngine(inner)  ──▶ AskResponse
                  └─ GraphEngine ┘         │                        │
                                    ①필드별 스크럽            ②계약 검증(최종 안전망)
```

**`SafetyEngine` 은 `QAEngine` 프로토콜을 구현하는 얇은 래퍼**다. `deps.get_engine()` 이 **무엇을 만들든 반드시 이걸로 감싼다.** 새 엔진을 꽂는 사람이 D-47 을 몰라도 지켜진다 — P1.

스크럽 대상 (`full_text` 에 들어가는 것 **전부**):

| 필드 | 처리 |
|---|---|
| `answer` | 문장 단위 스크럽 |
| `clarify.question` | 문장 단위 스크럽 |
| `refusal.message` · `refusal.advice` | 문장 단위 스크럽 |
| `triage.message` | 문장 단위 스크럽 |
| `triage.escalation_conditions` | **항목 단위** — 연락처가 든 항목만 뺀다 |

`escalation_conditions` 를 항목 단위로 하는 이유는 실측이다. **코퍼스에 이미 유출이 하나 있다.**

```
F-021-012.escalation_conditions
  '수의사·응급병원 또는 Pet Poison Helpline (855)-764-7661 에 즉시 연락'
```

이 항목이 `compute` 노드를 타면 `full_text` 로 그대로 나간다. 문장 단위로 처리하면 같은 리스트의 다른 조건("원인 물질에서 떼어놓기", "구토 유도 금지")까지 사라지므로 **항목 단위가 맞다.** 빠진 자리는 `GUIDANCE`("가까운 동물병원이나 24시 동물병원으로 바로 연락해 주세요")가 메운다 — 정보 손실 없음.

### ②최종 안전망 — `AskResponse` 검증

```python
@model_validator(mode="after")
def _no_foreign_contacts(self) -> AskResponse:
    if has_contact(self.full_text):
        raise ValueError("응답에 국내에서 쓸 수 없는 연락처가 남아 있다 (D-47).")
    return self
```

래퍼가 제대로 돌면 **절대 안 걸린다.** 걸렸다면 그건 래퍼 밖에서 응답이 만들어졌다는 뜻이고, 그때는 `_install_response_guard` 가 이미 만들어 둔 경로(200 + `refused` + 로그 ERROR)로 내려간다. 조용히 나가지 않는다.

`full_text` 를 검사하므로 **②번 결함(`full_text` 가 scrub 뒤에 문장을 덧붙이는 것)이 같은 수정으로 함께 막힌다.**

### ①-b 함께 고쳐야 하는 것 — 안 그러면 새 오류를 만든다

래퍼를 넣는 순간 `scrub_contacts` 가 실전 경로에 들어온다. 지금 상태로 넣으면 **오탐 12건이 즉시 사고가 된다.**

**(1) 출처 표기를 검사에서 뺀다** — 실측:

```
888청크 중 has_contact=True : 13건
  ├ 출처 표기 (출처: Pet Poison Helpline, S-084) 때문 : 12건   ← 전부 오탐
  └ 진짜 유출 (F-021-012)                              :  1건
```

`(출처: …, S-xxx)` 는 **우리가 만든 고정 형식**이고 "거기 연락하라"가 아니다. 검사 전에 떼어낸다. 안 떼면 에틸렌글리콜·발작·호흡곤란 청크로 만든 답변이 통째로 삭제된다 — **응급 상황에서 답이 사라진다.**

**(2) 문장 경계에 줄바꿈을 넣는다** — 지금은 종결부호+공백만 본다. LLM 불릿 출력이 통째로 한 문장이 되어 답변 전체가 지워진다(재현함).

```python
_SENT = re.compile(r"(?<=[.!?。？！])\s+|\n+")
```

**(3) 놓치는 형식을 보강하되, 애매한 것은 행동 지시어와 함께일 때만.**

| 패턴 | 처리 | 이유 |
|---|---|---|
| `(888)426-4435` · `888–426–4435`(en dash) | 무조건 차단 | 명백한 전화번호 |
| URL (`petpoisonhelpline.com`) | 무조건 차단 | URL 자체가 행동 유도 |
| `8884264435`(10~11자리 연속) · `1588-1234`(4-4) | **행동 지시어가 같은 문장에 있을 때만** | 연도·용량·범위 표기와 구별 불가 — 오탐이 더 위험 |
| `_ACTION` 에 `참고`·`요청`·`도움`·`방문`·`접속` 추가 | | 현재 6개로는 *"…을 참고하세요"* 를 못 잡는다 |

`_PHONE` docstring 이 *"국내 형식(`1588-1234`)을 함께 잡는다"* 고 적었지만 실제로는 안 잡는다 — **문서를 코드에 맞추는 게 아니라 코드를 의도에 맞춘다.**

### 검증

- 새 테스트: 불릿 3줄 → 용량 정보 2줄이 살아남는다 · F-021-012 조건 → 그 항목만 빠진다 · 출처 표기 12건 → 하나도 안 지워진다 · `AskResponse(answer="… 855-764-7661 …")` → `ValidationError`
- 회귀: `test_contacts.py` 전량 + 888청크 전수 오탐 0건

---

## ② `TriageResult` 가 게이트 결과를 못 덮게 한다 (D-09)

### 설계

`TriageLevel` 에 **이미 `.badge` · `.message` 프로퍼티가 있다** — 단일 출처가 있는데 `TriageResult` 가 자유 문자열로 다시 받고 있었다. P2 위반.

```python
class TriageResult(_Strict):
    level: int = Field(ge=1, le=4)
    name:  str | None = None      # 안 주면 level 에서 파생. 주면 대조 후 불일치 시 거부
    badge: str | None = None      # 〃
    message: str                  # 자유 — LLM·템플릿이 더 구체적으로 쓸 수 있다
    ...
    @model_validator(mode="after")
    def _level_is_final(self):
        floor = max(self.rule_level or 0, self.llm_level or 0)
        if self.level < floor:
            raise ValueError(f"level={self.level} 이 게이트 바닥 {floor} 보다 낮다 (D-09).")
        ...
    @model_validator(mode="before")
    def _derive_name_badge(cls, data):   # level 에서 채운다
```

- **안 쓰면 자동으로 맞고, 쓰면 검사받는다.** 다음 사람이 `name` 을 잊어도 틀리지 않는다
- `message` 만 자유로 남긴다 — 등급 이름·배지는 UI 계약이지만 행동 문장은 상황에 따라 구체화되어야 한다
- `level >= max(rule, llm)` — D-09 우회의 유일한 남은 문
- `overridden` 은 `gate.py:90` 정의(`llm < rule`)와 대조

### 새 오류를 만들지 않기 위한 확인

- `to_triage_result()` 는 `decision.level.name` · `decision.badge` 를 넣고 있어 **파생값과 일치**한다 → 그대로 통과
- `_monitor_needs_conditions` 는 유지 (D-39)
- 267건 중 `TriageResult` 를 직접 만드는 테스트가 있으면 `name`/`badge` 는 생략 가능해지므로 깨지지 않는다. 틀리게 만든 것만 걸린다 — 그게 목적

---

## ③ 빈 필터는 "필터 없음"이 아니라 **"0건"** 이다 (D-10)

### 설계 — 번역 계층이 결정하지 않는다

```python
class EmptyFilter(ValueError):
    """필터 값이 비어 어떤 문서도 만족할 수 없다. 0건이 정답이다 (D-10)."""

def to_chroma_where(where):
    for k, v in where.items():
        if isinstance(v, list | tuple | set):
            vals = [x for x in v if x not in (None, "")]
            if not vals:
                raise EmptyFilter(f"{k} 필터가 비었다 — 조건을 만족하는 문서가 없다")
```

- `ChromaStore.search` 가 `EmptyFilter` 를 잡아 **`[]` 반환** (로그 INFO)
- `InMemoryStore._matches` 는 이미 `False` 를 낸다 → 그대로 0건
- **두 구현이 같아진다.** 지금 갈리는 유일한 케이스가 이것이었다
- `retrieve` 노드는 "0건 = 거절"(D-46)을 이미 갖고 있으므로 **노드를 안 고쳐도 옳게 동작한다**

`continue` 로 조건을 빼는 대신 예외를 던지는 이유 — **번역기가 "이 조건은 무시해도 된다"를 판단할 자격이 없다.** 판단은 저장소가 한다.

### P4 — 함께 고칠 테스트 2건

`tests/test_retrieval_filter.py:41-44` 가 `assert to_chroma_where({"species": ["", None]}) is None` 을 **정답으로 못 박고** 주석에 *"필터가 깨지지 않는다"* 고 적어 두었다. 필터가 깨진 게 아니라 **사라진** 것이다. 단언을 `pytest.raises(EmptyFilter)` 로 바꾸고, **왜 뒤집었는지 주석에 남긴다.**

`TestInMemoryMatchesSameSemantics` 에 **빈 목록 케이스를 추가**한다 — 두 구현이 갈리는 유일한 지점인데 지금 테스트가 없다.

---

## ⑤ `deps.py` 의 except 절이 스스로 터지는 것 (7번)

```python
def _build_engine() -> QAEngine:
    if get_config().serve.engine != "graph":
        return StubEngine()
    try:
        from ..graph.engine import GraphEngine
        return GraphEngine()
    except Exception as e:          # ImportError · EngineNotReady · 그 밖의 기동 실패
        msg = f"serve.engine=graph 인데 GraphEngine 을 쓸 수 없다 ({type(e).__name__}). …"
        if os.getenv("PETTRIAGE_ALLOW_ENGINE_FALLBACK") != "1":
            raise EngineUnavailable(msg) from e
        log.warning("%s PETTRIAGE_ALLOW_ENGINE_FALLBACK=1 이라 스텁으로 진행한다.", msg)
    return StubEngine()
```

`except (ImportError, EngineNotReady)` 에서 `EngineNotReady` 를 **뺀다.** 그 이름은 같은 `try` 가 바인딩하는 지역 변수라 임포트가 실패하면 존재하지 않는다.

넓은 `except Exception` 이 여기서는 옳다 — **graph 를 못 만든 이유가 무엇이든 결과는 둘 중 하나**다(크게 실패 / 명시적 폴백). 지금은 좁은 except 때문에 오히려 `UnboundLocalError` 로 **둘 다 안 되고 있다.**

---

# 2차 — 계약·데이터 정합 (노드·데이터 마감 전)

## ⑥ `Citation.publisher` 를 인덱스가 들고 있게 한다

`Chunk` 에 `publisher` 추가 → `to_chunk` 가 `fact.publisher` 전달 → `_meta()` 에 `publisher`·`locator`·`fact_ids` 추가 → `ChromaStore.search` 가 복원.

- 데이터 왜곡 없음 — 원본 CSV 의 `publisher` 를 **그대로** 옮긴다. 만들지 않는다
- **인덱스 재구축이 필요하다** (`build_index.py`). D-53 이 "벡터DB는 인덱스"라고 정한 이유가 이것이다
- 재구축 전까지 실사용 영향 0 — 지금 `answered` 를 내는 것은 `StubEngine`(하드코딩 publisher)뿐이다
- `locator` 를 함께 넣는 이유: '근거 보기' 화면이 없으면 산출물 ③의 "출처 추적"이 성립하지 않는다

## ⑦ 단위 목록을 한 곳에서만 정의한다 (P2)

```python
# compute/rules.py — 단일 출처
COMPUTABLE_UNITS = frozenset(_MG_PER_KG)      # 환산표에 있는 단위 = 계산 가능한 단위

# scripts/build_rule_table.py
from pettriage.compute.rules import COMPUTABLE_UNITS   # 자기 목록을 갖지 않는다
```

`mL/kg` 는 **밀도를 모르면 mg/kg 로 못 바꾼다.** 만들어서 바꾸면 D-51("잎 무게는 독소 무게가 아니다")과 같은 종류의 조작이 된다 → **계산 불가가 맞다.** 목록을 파생시키면 이 결론이 자동으로 두 곳에 반영된다.

`min()` 빈 시퀀스 가드도 함께 — 계산 가능한 행이 하나도 안 남으면 크래시가 아니라 `rule_level=None`(= LLM 판정에 맡긴다).

`tests/test_rules.py:112` 의 허용 단위 집합도 같은 상수를 import 하게 바꾼다 — 테스트가 자기 목록을 들면 P2 위반이 테스트에서 재발한다.

## ⑧ `_dose_phrase` 가 없는 기준을 만들지 않게 한다 (P3 · D-38)

```python
def _dose_phrase(f: Fact) -> str:
    unit = (f.unit or "").replace(" ", "")
    if "/kg" in unit:
        return f"{f.dose}{f.unit}"                 # 이미 체중당
    if f.basis:
        return f"{f.dose}{f.unit}({f.basis})"      # 출처가 밝힌 기준을 쓴다
    return f"{f.dose}{f.unit}"                     # 기준을 모르면 붙이지 않는다
```

**CSV 는 건드리지 않는다.** 현재 코퍼스에서 이 경로를 타는 2건이 원문과 일치하게 된다:

| | 원문 | 지금 (왜곡) | 고친 뒤 |
|---|---|---|---|
| F-014-001 | "0.5% of body weight" · `basis="체중 대비"` | 체중 1kg당 0.5% | **0.5%(체중 대비)** |
| F-034-013 | "8.2kg 개가 포도 4-5알" | 체중 1kg당 4-5알 (**약 8배**) | **4-5알(체중 8.2 kg 개)** |

`tests/test_verbalize.py:47-50` 이 이 왜곡을 정답으로 고정하고 있다 → P4 로 함께 뒤집고 이유를 남긴다.

## ⑮ `check_facts` 의 파일 간 검사를 항상 돌린다

- `if len(paths) > 1:` 조건 제거 — 한 파일 안에서도 출처 간 충돌은 일어난다
- `check_cross` 키를 `(substance, species)` → **종 그룹을 포함**하도록 확장. `cat` 과 `mammal` 은 D-39 병합 검색에서 같은 질의로 함께 나온다
- 나온 충돌(사과·복숭아·수박·망고 + 우유)은 **표를 고치는 게 아니라** 어느 출처를 채택할지 결정해 `note` 에 근거를 남긴다 — 규율 2

---

# 결정한 것 — 착수 전에 정하고 들어갔다

## ㉮ `StubEngine` 의 정량 판정 (검토 5번) → **규칙 테이블을 연결한다**

되물어 받은 체중·섭취량을 쓰지 않고 `_STUB_RULES` 의 상수를 그대로 게이트에 넣고 있었다.
4kg 개가 초콜릿 `0.1g` 을 먹든 `500g` 을 먹든 답이 `CALL_NOW` 로 같았다.

세 안을 놓고 골랐다.

| | 안 | 얻는 것 | 잃는 것 |
|---|---|---|---|
| **(a) ← 채택** | 규칙 테이블을 연결한다 (`rule_level_for` → `apply_gate`) | 정량 계산 모듈이 **처음으로 실제로 돈다.** 노드 짜기 전에 검증된다 | 04 §5 구성 A 정의에 한 문단 추가 |
| (b) | 섭취량을 **안 묻는다** | "쓰지 않을 값을 묻지 않는다" — 설계 정합. 수정 최소 | 정량 모듈이 여전히 미검증으로 남는다 |
| (c) | 그대로 두고 문서에만 명시 | 0 비용 | 시연에서 그대로 드러난다 |

**(a)를 고른 이유** — `rule_level_for`·`computable_for`·`to_mg_per_kg` 는 **실행 가능한
호출자가 저장소에 하나도 없었다.** 이 프로젝트가 가장 공들인 계산 모듈이 한 번도 실전 경로를
밟아 본 적이 없었던 것이다. 여기서 부르면 `compute` 노드를 짜는 사람이 **이미 검증된 부품**을
쓰게 된다.

### ⚠️ 배선하면서 드러난 것 — 역치의 기준이 두 종류다

`amount_g` 를 그대로 mg/kg 으로 바꾸려다 멈췄다. 규칙 테이블 12행의 기준이 갈린다.

```
양파 15-30 g/kg · 마늘 5 g/kg · 자일리톨 0.03 g/kg · 알리움 0.5%
    → 먹은 **음식의 질량**이 곧 역치의 단위다. 그대로 쓴다
초콜릿(테오브로민+카페인) 20 mg/kg
    → **테오브로민의 질량**이다. 초콜릿 질량이 아니다
```

구분하지 않고 곱하면 **초콜릿을 100% 테오브로민으로 계산**하게 된다 — 약 1만 배 과대평가다.
안전한 쪽 오류라 눈에 안 띄지만, D-51(*"잎 무게는 독소 무게가 아니다"*)이 막은 것과 같은 조작이다.

코퍼스에 다리가 있었다 — `F-034-023~025` 의 종류별 함량(베이킹 14 · 다크 5 · 밀크 2 mg/g).
그런데 **종류를 모르면 어떤 값을 골라도 우리가 만든 숫자**이므로 되묻는다.
골든셋 `G-047`(*"무슨 초콜릿인지는 모르겠어요"* → `clarify`)이 기대하던 동작이 정확히 이것이다.

## ㉯ 범위 → **전부 고친다**

1차(①②③⑤) · 2차(⑥⑦⑧⑮) · 3차(records 인증 · `recorded_at` · config 검증 · 조사/파이프 ·
비밀번호 · 하네스 게이트 등) 를 **한 번에** 처리하기로 했다.
아래 §3차 이후로 미루는 것 은 착수 시점의 기록이며, **실제로는 전부 반영됐다** —
결과는 `2026-08-02_수정완료.md`.

특히 `|` 누출 18건과 조사 오류 173건은 **⑥ 재인덱싱과 묶어야 1회로 끝난다**는 점이
전부 처리하기로 한 이유 중 하나였다.

---

# (착수 시점 기록) 3차 이후로 미루려 했던 것

> ⚠️ 아래는 **설계 시점의 판단**이다. 범위를 '전부'로 정하면서 **모두 이번에 반영됐다.**

| | 무엇 | 왜 미루나 |
|---|---|---|
| 8 | `/api/records` 무인증 | 발표 범위에 다이어리 API 가 들어가는지에 달렸다 |
| 9 | `recorded_at` 검증 | 위와 같음 |
| 16 | config 오타 무시 · 범위 검증 | 실험 기록 오염이지만 지금 실험이 없다 |
| 23·24 | `|` 누출 18건 · 조사 오류 173건 | 임베딩·노출 문장에 걸린다. **재인덱싱과 함께 하는 게 싸다** (⑥과 묶으면 1회) |
| 30 | 한글 비밀번호 500 | 인증 시연 시 |
| 17~22 | 하네스 게이트·분모·골든셋 | 1차 평가를 돌린 뒤 숫자를 보고 정하는 게 맞다 |
| 34 | `test_토큰_없이는_403` | 401 이 맞다. 테스트를 고친다 (1줄) |
