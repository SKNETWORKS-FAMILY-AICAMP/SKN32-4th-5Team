# 전체 코드 검토 보고 — 2026-08-02

> **SKN 3차 단위 프로젝트** · 팀 `save the pet` · **작성자: 오한빈 (팀장)**
>
> 대상: `src/` `eval/` `scripts/` 7,740행 · `tests/` 269건
> 방법: 축 4개 병렬 검토 후 **전 항목을 파이썬으로 직접 재현.** 재현 못 한 것은 그렇게 표시했다.
>
> **이 문서의 성격** — 팀이 만든 코드 **전체**를 팀장이 검토한 기록이다.
> 대상에는 **검토자 자신이 작성한 코드가 포함**되며, 실제로 지적 중 상당수가 그쪽이다.
> 특정 팀원을 지목하지 않는다 — 결함은 전부 파일·함수 단위로 적었다.

---

## 요약

테스트 269건 중 268건이 통과한다. 그런데 **통과하는 초록불 뒤에 안전 불변식 3개가 실제로는 강제되지 않고 있다.**

| | 무엇 | 어디 |
|---|---|---|
| 🔴 치명 5 | 안전 게이트가 우회 가능하거나, 코드가 살아있는 경로에 연결되지 않음 | D-47 · D-09 · D-10 · D-50 |
| 🟠 중대 11 | 계약 구멍 · 무인증 노출 · 조용한 오답 | 계약 · 라우터 · 데이터 |
| 🟡 경미 13 | 지표 왜곡 · 문장 품질 · 빌드 설정 | 하네스 · 템플릿 · Makefile |

**로직 자체는 대부분 옳다.** 등급 순서(D-39), `max()` 합성 산술, 잎 단위 차단(D-51), 역치 비교 방향(`>=`), 체중 0/NaN 거절, 경로① 0건(D-37), 적재 멱등성 — 전부 재현해 확인했고 문제없다.

문제는 **"그 옳은 로직이 실제로 불려지는가"** 다.

---

# 🔴 치명 — 시연·평가 전에 반드시

## 1. 연락처 차단(D-47)이 살아있는 응답 경로에 없다

`safety/contacts.py` · `graph/nodes/generate.py:50-69` · `app/engine.py`

`scrub_contacts` 의 **유일한 호출부는 `finalize` 하나**다(전체 grep 확인). `finalize` 는 `GraphEngine` 전용이고, `GraphEngine` 은 `NODES_IMPLEMENTED=False` 로 생성 자체가 막혀 있다. 지금 `/api/ask` 를 타는 것은 `StubEngine` 이고, 이쪽은 `AskResponse(answer=self._compose(...))` 를 **scrub 없이** 만든다.

즉 **현재 응답은 어떤 연락처 필터도 통과하지 않는다.** 지금은 템플릿에 번호가 없어 사고가 없을 뿐이다.

구조적 문제는 이것이다 — 차단이 **응답 계약(`AskResponse`)이 아니라 선택적 파이프라인 노드**에 붙어 있다. `QAEngine` 프로토콜을 만족하는 엔진을 하나 더 꽂는 순간 차단이 0% 적용된다.

## 2. `full_text` 가 `finalize` 뒤에 문장을 덧붙인다 — 금지한 패턴이 계약 코드에 있다

`app/contracts.py:188-207`

`nodes/__init__.py` 가 *"`finalize` 가 마지막이다. 뒤에 문장을 덧붙이면 연락처 차단이 무력해진다"* 고 못 박았는데, `full_text` 가 정확히 그것을 한다 — scrub 된 `answer` 뒤에 `escalation_conditions` 와 `disclaimer` 를 붙인다. `escalation_conditions` 는 CSV `signs` 또는 LLM `decide_triage` 에서 오는 **데이터**다.

```
answer   : 초콜릿은 개에게 독성이 있습니다. 연락처는 안내하지 않습니다.
full_text: … 다음 증상이 나타나면 즉시 알리세요 — 구토가 계속되면 855-764-7661 로 연락. 본 안내는 …
번호가 full_text 에 있나: True          ← 재현함
```

화면 없는 클라이언트(챗봇·음성)는 `full_text` 를 쓴다.

## 3. `TriageResult` 가 게이트 결과를 무효화할 수 있다 (D-09)

`app/contracts.py:115-137`

`apply_gate()` 안의 산술은 완벽하다. 그런데 그 결과를 담는 `TriageResult` 에 **`level >= max(rule_level, llm_level)` 을 검사하는 검증기가 없다.**

```
apply_gate(EMERGENCY, MONITOR) -> level=4 overridden=True
TriageResult(level=1, rule_level=4, llm_level=1)  →  [통과]   ← 재현함
TriageResult(level=4, name="MONITOR", badge="관찰") →  [통과]   ← 등급/배지 불일치도 통과
```

D-09 우회 경로는 "게이트를 안 부르는 것"이 아니라 **"부른 뒤 결과를 덮는 것"** 이고, 계약이 그 문을 열어두고 있다. `name`·`badge`·`message` 도 `level` 과 무관한 자유 문자열이라 응답이 스스로 모순될 수 있다.

## 4. 종 필터가 빈 목록에서 **사라진다** — 두 저장소가 반대로 동작 (D-10)

`retrieval/store.py:95-104`

```
                     ChromaStore          InMemoryStore
{"species": []}   →  where=None (전 종)  |  0건
{"species":["",None]} → where=None      |  0건        ← 재현함
```

`to_chroma_where` 는 값이 비면 그 키를 조건에서 빼고, 조건이 하나도 안 남으면 `None`(= 필터 없음)을 낸다. 종이 미확인일 때 개 보호자에게 고양이 백합 청크가 나가는 경로다. **D-10(종 미확인 시 답변 금지)의 정확한 반대**다.

거짓 초록불이 이중으로 걸려 있다 —
`tests/test_retrieval_filter.py:41-44` 가 이 동작을 **정답으로 단언**한다(`assert to_chroma_where({"species":["",None]}) is None`, 주석은 *"필터가 깨지지 않는다"*). 필터가 깨진 게 아니라 **사라진** 것이다. 같은 파일의 `TestInMemoryMatchesSameSemantics`("두 구현이 같은 뜻이어야 한다")는 두 구현이 갈리는 **유일한 케이스인 빈 목록을 테스트하지 않는다.**

## 5. `StubEngine` 이 되물어 받은 섭취량을 쓰지 않는다 (D-50)

`app/engine.py:199-236`

```
amount_g=  0.1  → answered / CALL_NOW(3)   | 규칙 테이블(0 mg/kg)    = MONITOR
amount_g=500.0  → answered / CALL_NOW(3)   | 규칙 테이블(1750 mg/kg) = EMERGENCY   ← 재현함
```

4kg 개가 초콜릿 500g(`중증` 역치의 40배 이상)을 먹어도 *"지금 수의사에게 전화하세요"* 다. `rule_level_for` / `computable_for` / `to_mg_per_kg` 의 **실행 가능한 호출자가 저장소에 0개**다(유일한 참조인 `nodes/compute.py:33` 은 docstring 안의 예시 코드).

스텁이라는 것은 문서에 명시돼 있지만, *"체중당 섭취량으로 판단합니다"* 라고 사용자에게 말하면서 받은 값을 무시하는 것은 **D-50 의 바닥이 상수**라는 뜻이다. 시연에서 그대로 드러난다.

---

# 🟠 중대

## 6. 상태 배타성이 절반만 강제된다

`app/contracts.py:209-229` — 조합을 전부 만들어 본 결과:

| 조합 | 결과 |
|---|---|
| `answered` + `citations=[]` / `answer` 없음 / `triage` 없음 | 차단 ✅ |
| `clarify` + `answer`, `refused` + `answer` | 차단 ✅ |
| **`refused` + `triage`(level=4 배지)** | **통과** ❌ |
| **`answered` + `refusal` 동시** | **통과** ❌ |
| **`answered` + `answer="   "`(공백만)** | **통과** ❌ |

`refused` + `triage` 는 특히 나쁘다 — `full_text` 가 거절 문구 뒤에 `escalation_conditions` 를 이어 붙여 *"…수의사와 상담하세요. 다음 증상이 나타나면 즉시 알리세요 — …"* 가 나간다. 화면 분기와 문장이 어긋난다.

## 7. `deps.py:48` 의 `except` 절 자체가 터진다

```python
try:
    from ..graph.engine import EngineNotReady, GraphEngine
    return GraphEngine()
except (ImportError, EngineNotReady) as e:     # ← EngineNotReady 가 아직 안 묶였다
```

`EngineNotReady` 는 같은 `try` 블록이 바인딩하는 지역 이름이다. 그 임포트가 `ImportError` 를 내면 except 튜플 평가 시점에 이름이 없다.

```
-> UnboundLocalError: cannot access local variable 'EngineNotReady'      ← 재현함
```

**ImportError 경로에서는 `EngineUnavailable`(명확한 실패)도, `PETTRIAGE_ALLOW_ENGINE_FALLBACK=1`(폴백)도 둘 다 동작하지 않는다.** 지금은 `graph/engine.py` 가 무거운 의존성을 안 써서 가려져 있고, WS2 가 노드에 langgraph·chromadb 를 붙이는 순간 드러난다.

## 8. `/api/records` · `/api/report` 가 무인증이다

`app/routes/records.py`

`pets.py` 는 모든 쿼리에 `user_id` 를 붙이는데 `records` 는 인증 의존성 자체가 없다. `pet_id` 만 알면 남의 다이어리를 읽는다.

```
POST /api/records (인증 헤더 없음) -> 201
GET  /api/report?pet_id=pet-A     -> 200
  유출: [{'note': '어제 구토, 주인 전화 010-1234-5678', 'symptoms': ['구토'], …}]   ← 재현함
```

`records_store.py:5-13` 이 *"인증이 없다"* 고 적어 두었지만, 그 주석이 쓰인 뒤 인증 체계(auth/pets)가 들어왔는데 records 만 남았다. `RecordStore` 에는 `SessionStore` 와 달리 TTL·상한도 없다.

## 9. `recorded_at` 이 검증되지 않아 리포트에서 기록이 사라진다

`app/contracts.py:69` — ISO 8601 은 주석뿐이고 `records_store.timeline` 은 **문자열 비교**로 자른다.

```
저장:   '2026-07-02T09:00' · '2026-7-3' · '어제' · ''      (전부 201)
2026-07 리포트: ['2026-07-02T09:00']      ← '2026-7-3' 이 7월 리포트에서 빠졌다   ← 재현함
```

`'2026-7-3' > '2026-07-31'` 이 문자열로는 참이다. 건강 다이어리에서 조용한 누락은 오답과 같다.

## 10. `mL/kg` — 빌더는 "계산 가능"이라 쓰고, 리더는 환산하지 못한다

`scripts/build_rule_table.py:43` vs `compute/rules.py:83`

```
build COMPUTABLE_UNITS : ['%', 'g/kg', 'mL/kg', 'mg/kg']
rules._MG_PER_KG keys  : ['%', 'g/kg', 'mg/kg']
차집합                 : ['mL/kg']
to_mg_per_kg(1,'mL/kg') = None                                ← 재현함
```

두 가지로 터진다 — (a) `mL/kg` 행이 최저 역치면 `normalized` 에서 빠져 **조용히 과소평가**, (b) 그 물질의 계산 가능 행이 전부 `mL/kg` 이면 `min()` 이 빈 시퀀스로 크래시(API 에서는 `refused/판정불가` 로 삼켜진다). `tests/test_rules.py:112` 의 허용 단위 집합에도 `mL/kg` 가 있어 테스트가 못 잡는다.

에틸렌글리콜·에센셜오일을 사실 표에 넣는 순간 발생한다.

## 11. `_coerce` 가 다른 축의 enum 을 세탁한다

`triage/gate.py:49-57` · `triage/levels.py:77-101`

`levels.py` 가 *"긴급도(축 A)와 섞지 않는다"* 고 선언하지만, `_coerce` 는 `TriageLevel(int(level))` 로 아무 int 나 받는다. `FeedingLevel` 도 `IntEnum` 1..3 이라 범위 안이다.

```
apply_gate(FeedingLevel.NEVER, None)      -> CALL_NOW(3)
apply_gate(EMERGENCY, FeedingLevel.SAFE)  -> EMERGENCY, overridden=True
apply_gate(3.9, None)                     -> CALL_NOW(3)     ← int() 는 항상 하향   재현함
```

## 12. `Citation.publisher` 가 필수인데 검색 결과에 없다 — `answered` 를 만들 방법이 없다

```
Chunk 필드    : chunk_id text source_id species doc_type substance route quote locator fact_ids
Citation 필수 : source_id, publisher                                       ← publisher 가 없다
Chroma 저장 메타: source_id species doc_type substance route                ← locator·fact_ids 도 소실
```

`source_id → publisher` 를 되찾는 코드 경로가 저장소 어디에도 없다(`StubEngine` 은 문자열을 하드코딩한다). `answered` 는 citation ≥1 을 요구하므로, GraphEngine 이 붙는 순간 **publisher 를 지어내거나 전부 거절하거나** 둘 중 하나가 된다. `locator` 소실로 '근거 보기' 화면도 성립하지 않는다.

## 13. 연락처 정규식이 놓치는 형식들 + 불릿 답변 통째 삭제

`safety/contacts.py:69-79, 101`

```
(888)426-4435 로 연락하세요.               -> False   괄호 뒤 구분자 없는 미국 표기
888–426–4435 (en dash)                    -> False   LLM·한글 편집기가 흔히 낸다
8884264435 로 전화하세요.                  -> False   구분자 없는 10자리
1588-1234 로 연락하세요.                   -> False   ← docstring 은 "국내 형식을 잡는다"고 적었다
https://www.petpoisonhelpline.com 참고     -> False   `_ACTION` 화이트리스트에 "참고" 없음
Pet Poison Helpline 에 도움을 요청하세요.   -> False   "요청"이 없음                 재현함
```

반대 방향도 있다 — `_SENT` 가 **종결부호+공백**에만 분리하므로 불릿 목록(줄바꿈 구분)은 전체가 한 "문장"이 되어 답변이 통째로 지워진다.

```
입력 3줄(용량 정보 2줄 + 번호 1줄)
출력: '연락처는 지역마다 달라 안내해 드리지 않습니다. …'      ← 안전 정보까지 사라짐   재현함
```

이 상태로도 `AskResponse(status="answered")` 검증을 통과해 **내용 없는 "답변"** 이 나간다.

## 14. `_dose_phrase` 가 출처에 없는 기준을 만든다 (D-38 위반)

`ingest/templates.py:176-183` — `/kg` 이 없으면 무조건 `"체중 1kg당"` 을 붙인다. 현재 코퍼스에 **2건**이 이 경로를 타고, **둘 다 원문과 다른 값**이 된다.

```
F-014-001  원문 "0.5% of body weight"(체중의 0.5%) · basis="체중 대비"
   → "체중 1kg당 0.5% 이상 섭취 시"        ← 뜻이 성립하지 않는다
F-034-013  원문 "8.2kg 개가 포도 4-5알"
   → "체중 1kg당 4-5알(체중 8.2 kg 개)"    ← 8.2kg 기준 33-41알, 약 8배 왜곡   재현함
```

`basis` 칸에 "체중 대비"가 이미 적혀 있는데 `TOXICITY_FOOD` 템플릿은 `basis` 절이 없어 쓰지 않는다.
그리고 `tests/test_verbalize.py:47-50` 이 **이 동작을 정답으로 못 박고 있다**(`unit="g"` 로는 그럴듯해 보인다).

## 15. `check_facts.py` 의 파일 간 검사가 한 번도 돌지 않는다

`scripts/check_facts.py:328` — `if len(paths) > 1:` 인데 사실 표는 `facts_ohb.csv` **한 개**뿐이다. `make facts` 는 `ERROR 0` 으로 통과한다. 강제 실행하면 실제 충돌이 나온다:

```
⚠ 망고(dog) 급여 등급이 엇갈린다: ['CAUTION','SAFE']
⚠ 복숭아(dog) · 사과(dog) · 수박(dog) 동일                     ← 재현함
```

그 결과 벡터DB에 모순 청크가 나란히 들어간다:

```
개에게 사과는 안전로 분류된다. (출처: American Kennel Club, S-064)
개에게 사과는 조건부 급여다. 1회 권장량은 얇은 조각 1~2쪽까지다. (출처: Hill's, S-047)
```

추가로 `check_cross` 의 키가 `(substance, species)` 라 **종 그룹을 넘는 충돌**을 못 잡는다 — `우유·유제품` 은 `cat=NEVER` / `mammal=CAUTION` 인데, D-39 병합 검색은 이 둘을 같은 질의에서 함께 반환한다.

## 16. 오타 난 환경변수가 조용히 무시되고, 로그는 "덮었다"고 말한다

`config.py:213-234`

```
WARNING 환경변수가 설정을 덮었다 — retrieval.topk=99 · serve.engin=graph
실제 적용값 → serve.engine=stub · retrieval.top_k=5                    ← 재현함
```

04 §8 재현성의 근거로 쓰겠다고 한 그 로그가 실험 기록을 오염시킨다. 덧붙여 안전 직결 파라미터에 범위 검증이 없다 — `score_threshold=2.0`(전부 거절) · `top_k=0` · `max_turns=999`(계약 상수 2 무시)가 전부 통과한다.

---

# 🟡 경미

**평가 하네스**

17. **게이트가 분모 1에서 통과한다.** `--fail-under 0.0 --fail-over 100` 이 `exit=0` 이다 — 등급을 낸 건이 1건뿐이라 과소평가율 0.0%. 같은 실행의 `통과 21.2%` · `등급 못 낸 긴급 건 96%` · `must_cite 0%` 에는 게이트가 아예 없다. **재현함**
18. **`must_not_contain 100% (38/38)` 의 분모가 거절 30 · 되묻기 7 · answered 1 이다.** "답을 안 해서 금지어를 안 썼다"가 100%로 보고된다. `missed_urgent` 는 분모를 분리했는데 문구 지표는 안 했다.
19. **`scored_text` 의 고지문구 제거가 정확 일치뿐이다.** 줄바꿈 하나, 공백 하나만 달라도 G-004 실패가 되돌아온다. **재현함**
20. **`must_contain` 은 AND 인데 골든셋 36건이 `|` 를 OR 로 쓴 것으로 보인다.** G-047 `어떤 초콜릿\|종류\|다크\|밀크` 는 4개를 동시에 포함해야 통과한다. `must_cite` 는 any/all 두 지표를 다 내는데 `must_contain` 은 all 하나뿐이다.
21. **G-004 · G-021 이 `answered` 인데 `expected_triage` 가 공란이다.** MONITOR 로 답하든 EMERGENCY 로 답하든 통과한다. `check_goldenset.py` 도 이 조합을 안 잡는다.
22. **등급·유형 편중** — `VISIT_SOON` 1건 · `symptom` 1건 · `prevent` 2건. 혼동행렬이 4×4로 안 나오고 유형별 버킷이 0%/100% 밖에 못 낸다.

**데이터·문장**

23. **`|` 구분자가 문장으로 새어나간다 — 18청크.** `LIST_FIELDS` 에 `toxic_part`·`effect` 가 빠졌다. *"잎\|꽃잎\|꽃가루\|꽃병 물이 독성 부위다"* **재현함**
24. **조사 하드코딩** — `안전로`(7건, → 안전**으로**) · `과(와)`(15건) · `g가 권장`(142건) · `g다.`(9건). `_josa` 기계와 `_UNIT_BATCHIM` 이 이미 있는데 네 절만 하드코딩이다. 임베딩과 노출 문장을 함께 해친다. **재현함**
25. **`basis`·`max_value` 가 문장에서 통째로 사라진다.** `TOXICITY_PLANT` 에 `basis` 절이 없어 *"백합류 독성량: 잎·꽃잎 1–2장, 꽃가루, 꽃병 물"* 같은 그 행의 가장 중요한 정보가 빠진다. 같은 `%` 를 `TOXICITY_FOOD` 는 (잘못) "체중 1kg당"을 붙이고 `TOXICITY_PLANT` 는 아무 기준도 안 붙인다 — **두 템플릿이 정반대로 틀렸다.**
26. **`ChromaStore` 가 `locator`·`fact_ids` 를 버린다.** `InMemoryStore` 는 `Chunk` 를 그대로 들고 있어 모든 테스트가 통과한다 — 저장소를 바꿀 때만 드러난다. (chromadb 미설치, 코드 경로 확인)
27. **삭제 경로가 없다.** `add` 는 upsert 라 재실행 멱등성은 있지만, 사실 행을 지우면 옛 `c-{fact_id}` 청크가 그대로 검색된다. "벡터DB는 인덱스"(D-53)라면 전량 재구축 경로가 있어야 한다.
28. **중복 접기가 `top_k` 절단 뒤에 온다.** over-fetch 가 없어 `top_k=5` 요구가 실제 2종이 된다. **재현함**

**빌드·죽은 코드**

29. **`init_db()` 호출부가 없다.** 정의만 있고 `python -m pettriage.app.init_db` 모듈도 없다. 빈 DB 로 띄우면 signup 이 500.
30. **한글 비밀번호 25자(75바이트)가 500 이다.** `SignupRequest.password` 는 `max_length=64` **문자**인데 bcrypt 는 72 **바이트** 상한이고 `routes/auth.py:50` 이 `PasswordTooLongError` 를 안 잡는다. **재현함**
31. **`get_engine()` 이 스레드 안전하지 않다.** 라우터가 동기 함수라 실제 병렬 실행된다. StubEngine 은 무해하나 GraphEngine 이면 8중 초기화가 된다.
32. **죽은 코드** — `models/serving/client.py`(119행, import 하는 곳 0) · `privacy/`(빈 패키지인데 D-36 근거로 문서화됨) · `configs/train-local.yaml`(참조 0) · `eval/harness/README.md`("아직 비어 있다" — 809행 구현됨) · 문서가 약속한 `compare.py` 부재.
33. **`Makefile` / `pyproject`** — `make train` 이 없는 경로를 가리킴 · `lint`/`fmt` 대상에서 `eval/` 제외(하네스 809행이 린트 밖) · **`run_eval.py` 를 실행하는 타깃이 아예 없다**(산출물 ④의 진입점이 없고, 17번 게이트를 CI 에 배선할 자리도 없다) · 핀 불일치(fastapi `<0.116` ↔ 검토 환경 0.141).

    > ⚠️ **정정 (2026-08-02).** 초안에는 *"`constraints.txt`·`LICENSE`·`Dockerfile`·`docker-compose` 부재"* 가
    > 함께 적혀 있었으나 **넷 다 저장소에 실재한다.** 검토가 `.git` 없는 스냅샷 사본 위에서
    > 돌았고 그 파일들이 스테이징에서 빠졌던 것이다 — **검토 환경의 결손을 저장소의 결손으로 보고**했다.
    > 초안이 *"일부 파일은 스냅샷 과정에서 빠졌을 수 있음"* 이라고 단서를 달아 두었는데도
    > 항목에는 단정으로 적었다. **단서를 달았다는 것이 확인을 대신하지 못한다.**
    >
    > 핀 불일치도 성격이 다르다 — `constraints.txt` 가 있으므로 `make install` 로 설치한
    > 팀원 환경은 핀이 지켜진다. 어긋난 것은 **검토 환경**이었다 (실제로 `ruff` 0.16 이
    > `UP038` 을 못 잡아 한빈님 0.8.x 에서만 걸렸다).
34. **`test_토큰_없이는_403` 실패(269건 중 유일).** `HTTPBearer` 의 상태 코드가 FastAPI 0.13x 에서 403→401 로 바뀌었다. `deps.py:125` 코드 주석도 *"실패하면 401"* 이라고 적고 있으므로 **테스트가 옛 관용구를 정답으로 고정**한 쪽이다.
35. **항상 참인 단언 3건** — `test_contacts.py:127`(`"24시"` 를 지우면 숫자가 없어 공허하게 참) · `test_eval_metrics.py:233-271`(넘긴 객체에 고지문구가 없어 `scored_text` 의 제거 로직을 한 번도 안 밟는다 — 19번이 통과한 채 남아 있는 이유) · `test_models_config.py:88`(동률에서도 참).
36. **`test_records_do_not_leak_across_app_instances`** 가 중간에 `deps.reset_state()` 를 직접 불러 사실상 `reset_state` 만 테스트한다. 저장소는 실제로 모듈 전역이라 앱 간 공유된다.

---

# 문제없음을 확인한 것

재현해 확인했고 손댈 필요가 없다.

- **D-39 등급 순서** — `IntEnum` 1→4, `max()` 합성 정상, `TriageLevel[name]` 은 이름 기준이라 뒤집힘 없음
- **`apply_gate` 산술** — 둘 다 None → `ValueError`, 한쪽 None → 나머지 채택, MONITOR 조건 가드 정상
- **D-51 잎 단위** — `g leaves/kg` 를 환산하는 코드가 저장소 어디에도 없음 (`_MG_PER_KG` 키 없음 + `computable=N` 이중 차단)
- **역치 비교 방향** — `amount >= threshold`, 경계값이 초과로 잡히는 안전한 쪽. `%`→10,000 mg/kg 환산 정확
- **체중 0/None/NaN** — `AskRequest.weight_kg` 가 `gt=0` 이라 pydantic 단계에서 거절. 0 나눗셈 도달 불가
- **D-37/D-45 경로① 0건** — `quote` 채워진 행 0, 청크 `route` 전부 `사실추출`. `check_facts` 가 ERROR 로 막고 `test_route1.py` 가 원장 대조까지 함
- **pandas 없음** — `facts_io`·`check_facts`·`build_rule_table` 모두 표준 `csv` 모듈. `NULL`→`NaN` 경로가 없다 (D-52 의 우려가 데이터 계층에는 해당 없음)
- **인코딩** — 전 리더가 `utf-8-sig`, `newline=""`. BOM·CRLF 정상
- **`fact_id` 중복** — 파일 내·간 모두 `Counter` 로 잡아 `DuplicateFactId` 로 터뜨림. 청크 ID 충돌 불가
- **적재 멱등성** — `chunk_id` upsert. 같은 표를 두 번 돌려도 중복 없음
- **`metrics.py` 지표 계산** — 6건 합성 케이스 손계산 대조 전부 일치. `missed_urgent` 분모 분리, 분모 0 → `None` 규칙 정확
- **`percentile`** — n=1,2,4,5,20,52 에서 nearest-rank 정의와 완전 일치. 보간 없음
- **워밍업** — `--only` 필터 뒤 실행, 결과 폐기, 예외 삼킴. 문서대로 동작
- **하네스 예외 처리** — 케이스 단위 `except` 라 전체 중단 없음
- **정량 케이스 10건 중 9건**이 규칙 테이블 재현과 정확히 일치 (초콜릿·자일리톨·양파·마늘). 나머지 1건(G-039 대파/cat)은 `알리움류(…)` 부분일치가 `대파` 를 못 잡는 별칭 누락
- **`ruff check .`** — All checks passed
- **`FastAPI lifespan`** — 기동 시 엔진 생성 + 실제 엔진 이름 기준 워밍업 + 기동 비차단, 전부 의도대로 배선됨

---

# 손대는 순서 (제안)

**하루 안에 · 시연 전에 (①~③)**

| | 무엇 | 비용 |
|---|---|---|
| ① | `scrub_contacts` 를 `AskResponse` 생성 지점(또는 `full_text`)으로 올린다 — 노드가 아니라 **계약이 강제**하게 | 30분 |
| ② | `TriageResult` 에 `level >= max(rule_level, llm_level)` 검증기 + `name`/`badge` 를 `level` 에서 파생 | 30분 |
| ③ | `to_chroma_where` 가 빈 목록에서 `continue` 대신 **"0건 반환" 신호**를 내게 + 그 동작을 고정한 테스트 2건 수정 | 40분 |

**노드 구현 전에 (④~⑥)** — 지금 고쳐두지 않으면 노드가 이 구멍 위에 얹힌다

| | 무엇 |
|---|---|
| ④ | `deps.py` 임포트를 `try` 밖으로 (7번) |
| ⑤ | `Chunk`/Chroma 메타에 `publisher`·`locator` 추가, 또는 `source_id → publisher` 조회 (12번) |
| ⑥ | `mL/kg` 를 `_MG_PER_KG` 에 넣거나 `COMPUTABLE_UNITS` 에서 빼거나 — 둘 중 하나 (10번) |

**데이터 마감 전에 (⑦~⑧)**

| | 무엇 |
|---|---|
| ⑦ | `_dose_phrase` 를 `basis` 기반으로. 판단 불가면 절을 생략 — 지금은 없는 숫자를 만들고 있다 (14번) |
| ⑧ | `check_cross` 를 파일 수와 무관하게 실행 + 종 그룹 포함 키 → 나온 충돌 4건 정리 (15번) |

**남는 시간에** — 조사·`|` 누출(23·24번)은 문장 품질과 임베딩에 함께 걸린다. `records` 인증(8번)은 발표 범위에 따라.

---

## 이 검토가 다시 확인한 것

문서가 반복해 말해 온 **"초록불이 곧 근거가 되지 않게 한다"** 가 이번에 세 번 걸렸다.

- `tests/test_retrieval_filter.py` 가 **종 필터가 사라지는 동작을 정답으로 고정**하고 있었다 (4번)
- `tests/test_verbalize.py` 가 **없는 기준을 만드는 동작을 정답으로 고정**하고 있었다 (14번)
- `tests/test_eval_metrics.py` 가 **제거 로직을 한 번도 밟지 않은 채** 고지문구 제거를 통과시키고 있었다 (19번·35번)

세 경우 모두 테스트가 있어서 문제가 가려졌다. 테스트가 없었다면 오히려 눈에 띄었을 것이다.

---

# 🔴 추가 — 1차 검토가 **놓친** 것 4건 (후속 논의 3건 + **PR CI** 1건)

이 넷은 위 36건에 없었다. **수정본을 검증하다** 나왔고, 마지막 하나(40번)는
**이 변경의 PR CI 가 잡아 줬다.**

**검토 문서가 스스로 완전하다고 주장하면 그것도 거짓 초록이므로** 여기 남긴다.
33번은 아예 **틀려서 정정**했다 (해당 항목 참조).

## 37. `build_index.probe_negative` 가 D-46 을 인용하며 D-46 을 위반한다

```python
print(f"음성 점검 — 1위가 {threshold} **미만**이어야 통과")
ok = top.score < threshold
# 실패 메시지: "임계값을 넘었다 — 이 질의가 근거를 얻으면 거절이 안 된다 (D-46)"
```

D-46 은 실측으로 근거 있음(0.547~0.733)과 없음(0.494~0.659)이 **겹친다**는 것을 확인하고,
`0.50` 을 고르면서 *"근거 없는 것 중 **최저 1건만 차단** · 방어 효과는 거의 없다 ·
**그 사실을 숨기지 않는다**"* 라고 적었다. **10건 중 1건이 예상값인데 이 검사는 4건 중 4건을
요구한다.** `make index` 가 상수로 빨간불이고, 상수 빨간불은 아무도 안 본다.

인덱스 재구축에서 실제로 확인됨 — 음성 최고 **0.574**(캣타워) > 양성 최저 **0.531**(프라이팬).
**어떤 임계값도 못 가른다.** D-46 이 다시 측정된 것이지 실패가 아니다. → **D-58**

## 38. `probe()` 의 초록불이 사람이 믿는 것보다 헐겁다 — 그러나 **기준은 옳다**

```python
found = any(expect in h.chunk.substance or expect in h.chunk.text for h in hits)
```

`"PTFE"` 문자열에 걸리는 청크가 **6건**이고 내용이 천차만별이다.

| fact_id | 등급 | 증상 |
|---|---|---|
| **F-071-005** | **응급** | **4종** |
| F-074-001 | 전화 | 5종 |
| F-093-008 · F-078-001 · F-074-002 | **없음** | 0 |

**아무거나 하나**만 후보에 들어와도 통과한다. 등급 없는 것 하나만 걸려도 초록이다.

⚠️ **다만 기준 자체는 옳다.** 검토 중 *"1위 등급 < 기대 등급이면 실패"* 로 바꾸려다
취소했다 — `retrieve` 는 `{"hits": [...]}` 로 **후보 집합 전체**를 넘기고
`compress_context`·`generate_draft` 가 상위 k 를 **전부** 읽는다. 순위·등급은 검색이
통제하지 않는다. `recall@k` 가 계층에 맞는 유일한 기준이다. → **D-58**

**진단하던 사람이 같은 오류를 다시 만들었다.** 그래서 D-58 이 개별 수정이 아니라 원칙이 됐다.

## 39. 물질을 말하지 않는 질의에 다리가 없다

*"앵무새 앞에서 프라이팬을 태웠어요"* 는 물질명을 말하지 않는다. 사람은
**프라이팬 과열 → 유독 흄 → 앵무새 위협**으로 잇는데 그 다리가 파이프라인에 없다.
`Slots.substance` 는 칸만 있고 비어 있다.

골든셋 `G-013`(같은 상황 · `EMERGENCY` 기대)이 이미 이 자리를 겨냥하고 있다. → **D-59**

---

## 이 셋을 어떻게 처리했나

| | 처리 |
|---|---|
| 37 | `probe_negative` → **보고 전용** + 겹침 구간 재측정 출력 |
| 37 | `score_threshold < 0.50` **게이트 신설** — 설정값은 인덱서가 판정할 수 있다 |
| 38 | 기준 **유지**. 기대 계열 후보 내역을 **표시**만 추가 (판정 아님) |
| 39 | **D-59** 로 설계 확정 · `assumed_substance` 계약 신설. 구현은 WS2 |

**37·38 은 이번에 코드까지 반영했고, 39 는 계약만 고정하고 노드는 8/3 이다.**

---

## 40. 인증 상태 코드를 **라이브러리가 정하고 있었다** (PR CI 에서 드러남)

34번(`test_토큰_없이는_403`)을 *"테스트가 틀렸다"* 로 보고 `401` 로 바꿨더니 **CI 가 빨개졌다.**

```
로컬  fastapi 0.141.1  → HTTPBearer(auto_error=True) 가 401
CI    fastapi 0.115.6  → 같은 코드가 403          ← constraints.txt 의 핀
```

**둘 중 하나가 틀린 게 아니었다. 우리가 정하지 않은 것을 둘이 각자 정하고 있었다.**
`deps.get_current_user_id` 는 주석에 *"실패하면 401"* 이라고 적어 두고도,
그 판단을 `HTTPBearer` 에 위임하고 있었다 — **적어 둔 것과 강제한 것이 달랐다** (D-54 와 같은 형태).

**처리** — `HTTPBearer(auto_error=False)` 로 바꾸고 **401 을 우리가 낸다.**
403 은 *"누군지 알지만 권한이 없다"* 이고 여기는 **누군지조차 모른다.**
`WWW-Authenticate: Bearer` 헤더도 함께 붙인다.

검증 — **두 버전에서 같은 답**을 낸다.

```
fastapi 0.115.6 (CI)   무인증 → 401 · WWW-Authenticate=Bearer  · 잘못된 토큰 → 401
fastapi 0.141.1 (로컬)  무인증 → 401 · WWW-Authenticate=Bearer  · 잘못된 토큰 → 401
전체 테스트            281 passed (양쪽 동일)
```

> **이 건이 33번 정정과 짝을 이룬다.** 33번은 *"검토 환경의 결손을 저장소의 결손으로 보고"* 했고,
> 40번은 *"로컬 환경의 동작을 코드의 사양으로 보고"* 했다.
> **환경이 답을 만들고 있으면 그 답은 내 것이 아니다.**
