# 현재 상태 — 2026-08-03

작성자: **오한빈(팀장)** · 마감 **2026-08-04**

> **다른 기기에서 이어갈 때 이 문서를 먼저 읽는다.**
> 환경 세팅은 `09_새환경-준비.md`, 왜 그렇게 했는지는 `06_설계결정기록.md`,
> 측정 결과 전문은 `eval/reports/2026-08-03_결과보고서.md`.
>
> ⚠️ `현재상태_2026-08-02.md` 는 **어제 것이다.** 그 뒤로 결정 8건과 60건 측정 3회가 있었다.

---

## 1. 필수 산출물 — 넷 다 서 있다

| | 상태 | 어디 |
|---|---|---|
| 수집 데이터 및 전처리 문서 | ✅ | `docs/01*` · `data/facts/facts_ohb.csv` 888행 |
| 시스템 아키텍처 | ✅ | `docs/02` · LangGraph 실물 (`graph/build.py`) |
| **RAG + 벡터DB + LangChain 연동 코드** | ✅ | `--arm A` ≡ `--arm A-LC` 결과 동일로 증명 |
| **테스트 계획 및 결과 보고서** | ✅ | `docs/04` + `eval/reports/2026-08-03_결과보고서.md` |

**남은 작업은 산출물을 만드는 일이 아니라 점수와 정직성을 올리는 일이다.**

## 2. 지금 수치 (골든셋 60건)

| | none (코드·규칙만) | **A (gpt-4o-mini)** |
|---|---|---|
| 통과 | 23.3% (14) | **46.7%** (28) |
| 상태 일치 | 58.3% | 65.0% |
| 등급 일치도 | 88.0% *(분모 25)* | **93.1%** *(분모 29)* |
| 🔴 과소평가율 | 8.0% (2) | **0.0%** |
| 🔴 중대 과소 | 0 | **0** |
| 과대평가율 | 4.0% | 6.9% (2) |
| 등급 못 낸 긴급 건 | 26.9% (7/26) | 15.4% (4/26) |
| must_cite (any) | 59.0% | 64.1% |
| must_contain (any) | 37.5% | **68.8%** |
| answered p50 지연 | 155 ms | 8.3 s |
| `dose` 유형 통과 | 33.3% | **93.3%** *(등급일치 100% · 과대 0%)* |

**하향 금지 게이트** (분모 26) — 일치 11 · LLM 올림 4(과대 1) · 🔒 하향 차단 5 · 🔒 상승 차단 6(어긋난 것 0)

테스트 **546 passed, 22 deselected** · 결정 **76건** (D-01~D-82)

> ⚠️ **이 숫자는 `[db]` extra 를 깐 환경에서만 나온다.** 없으면 `518 passed, 1 skipped`
> 인데, 그 `1 skipped` 가 `test_auth_api.py` **모듈 전체(25건)** 다. 그리고
> **`pytest -q` 로 돌리면 개수 자체가 안 보인다** (`addopts` 와 겹쳐 `-qq`).
> 둘 다 2026-08-03 에 새 기기에서 실제로 밟았다 — `09_새환경-준비.md` 참조.

## 3. 🔴 이 숫자를 읽을 때 반드시 함께 아는 것 넷

**① 같은 커밋에서 ±2건 흔들린다.** 세 번 돌려 통과 26/27/28. `temperature=0` 은 박혀 있으나
`seed` 를 주지 않아 OpenAI 가 재현을 보장하지 않는다. **1~2건 차이를 개선으로 읽지 않는다.**
비교는 반드시 `python scripts/diff_reports.py A.json B.json` 으로 케이스 단위로 한다.

**② ④ 근거 검증은 LLM 이 아니다.** `Task.VERIFY` 프롬프트·라벨이 있는데 호출부가 없다.
2-gram 문자 일치율로 판정한다. 05 §4 와 코드가 어긋나 있다 (D-82).
리포트가 매번 이 사실을 찍는다.

**③ 🔴 걸러 놓고 아무것도 안 한다.** 155문장 중 **54문장(35%)** 이 `근거없음` 판정을 받고
**그대로 답변에 실려 나간다.** `verify.py` 머리말은 *"판정에 따른 조치는 코드가 한다 —
문장 제거 · 재검색 1회 · 거절"* 이라고 적었는데 **문장 제거가 구현돼 있지 않다.**
전 문장이 근거없음일 때만 거절한다.

**④ 46.7% 는 커밋되지 않은 코드에서 잰 값이다.** 재현 커밋 `e6600d9` 는 D-80 시점이고,
D-81(근거 공시)·D-82(④ 집계)는 아직 미커밋이다. **푸시하고 그 해시를 보고서 §0 에 다시 적는다.**

## 4. 오늘 한 것 (D-75 ~ D-82)

| | 무엇 | 효과 |
|---|---|---|
| D-75 | 상태에만 남고 응답에 안 나가던 값 5개 연결 | `computed`·`llm_fallbacks`·`removed_contacts` |
| D-76 | 폴백 기록을 노드 밖으로 — ①분류·②슬롯이 기록조차 안 했다 | 비교군 해석 가능해짐 |
| D-77 | 주석이 막는다고 적은 것을 코드가 막게 | 재검색 시 재판정 차단 |
| **D-78** | **유효성분 환산** — 초콜릿 무게를 테오브로민 역치에 그대로 대던 것 | `dose` 등급일치 100% |
| **D-79** | **판정에 수치를 준다** + 양 미상 바닥을 CALL_NOW 로 | 과소 0% |
| **D-80** | **정량 계산이 성립하면 LLM 상승을 막는다** (D-09 개정) | 과대 30% → 7% |
| **D-81** | **근거를 필드로** (`basis` + 문장 공시 + 계약 강제) | 답변 67%가 결손 상태임을 밝힌다 |
| **D-82** | 안 부른 태스크를 성공으로 세지 않는다 | `5태스크` → `4태스크` 정정 |

**함께**: `_provenance()` provider별 기록 · `must_contain` 을 answered/clarify 로 분리 ·
cp949 크래시 수정

**새 도구** — `scripts/doctor.py`(환경 점검) · `scripts/diff_reports.py`(리포트 비교) ·
`docs/09_새환경-준비.md`

## 5. 🔴 남은 과제 (우선순위)

### ① `근거없음` 문장 표본을 남긴다 — **가장 먼저**

54문장이 오탐인지 진짜인지 **모른다.** 지금 JSON 에 개수만 있고 문장이 없다.
표본 몇 개만 보면 10분에 정해진다 —

- **오탐이면** 2-gram 임계값(0.3)을 손본다
- **진짜면** 문장 제거를 구현한다. ⚠️ **35%를 지우면 답변이 비어 계약이 거부할 수 있다**

### ② 보고서 갱신

46.7% · 잡음 ±2건 · ④ 실측 · `seed` 미설정을 §6 한계에 반영. 재현 커밋 재기입.

### ③ 근거없음 갈래 6건 + G-101

`G-020 목캔디 · G-026 달팽이약 · G-049 실리카겔 · G-050 매니큐어 · G-051 모기향 · G-052 계피가루`
— 전부 **코퍼스에 없는 물질**인데 시스템은 *"무엇을 먹었나요?"* 를 되묻는다. **사용자는 이미 말했다.**

> **없음(사용자가 말 안 함) → 되묻기** · **모름(말했는데 우리가 모름) → 근거없음**

⚠️ LLM 이 `substance: "뭔가"` 를 내면 되물어야 할 것을 거절한다 — **모호어 목록**이 함께 필요하다.

`G-101`(앵무새 증상)은 **근거없음 거절**로 간다 (팀장 결정 2026-08-03). 코퍼스에 조류 증상
등급 행이 0건이다 — `mammal` 행은 D-10 으로 못 쓴다. **골든셋도 `clarify` → `refused/근거없음`
으로 함께 고쳐야** 통과로 잡힌다.

### ④ G-047 최악 계수 · G-003 예방 경로

**G-047** *"무슨 초콜릿인지 모르겠어요"* → 되묻지 않고 **최악 계수(14 mg/g)로 계산하고 그 가정을
밝힌다.** `성분함량.csv` 에 `parent` 열 하나면 데이터로 된다.

**G-003** *"양파를 조금 줘도 괜찮을까요"* → **아직 안 먹였는데 CALL_NOW 가 나간다.** 범주 오류다.
*"이미 먹였나요, 먹이려는 건가요"* 를 되묻고 최고 위험만 알린다.
⚠️ **계약을 고쳐야 한다** — 지금은 `clarify` 에 근거를 못 싣는다. 실질 주장을 되묻기에 실으려면
근거도 함께 실려야 한다. `prevent` 유형 3건 통과 0% 가 같은 뿌리다.

### ⑤ 그 밖에

- 골든셋 정정 — `G-023` 은 시스템이 맞다(사용자가 못 봤다고 명시). `G-016`·`G-003` 은 ④ 이후 재판단
- `must_contain (all) 10.4%` — 채점이 빡빡한지 실패 9건을 열어 본다
- `seed` + `system_fingerprint` 를 provenance 에 (04 §8)
- 조류 — 종별 통과 30%, 등급일치 80%. 정량 개선이 조류에 안 닿는다
- 개인정보 필터 미구현 (`privacy/__init__.py` 0바이트) while `chat_logger` 가 원문을 저장 (D-36)

## 6. 다음 한 수

```powershell
python scripts/doctor.py                    # 새 기계면 먼저
pytest                                      # 546 passed, 22 deselected  (-q 를 붙이지 않는다)

git add -A ; git commit -m "..." ; git push origin ohb
git rev-parse HEAD                          # → 보고서 §0 재현 커밋에 기입
```

그다음 **①(근거없음 표본)** 부터. 그게 정해져야 ②③④의 순서가 정해진다.

## 7. 알아 두면 시간을 아끼는 것

| | |
|---|---|
| `--arm` 이 `PETTRIAGE__MODEL__*` 를 **전부 비운다** | 상시 설정은 `.env` 에. 셸에 넣으면 지워진다 |
| `PETTRIAGE_PROFILE` 은 `.env` 에서 **안 먹는다** | 밑줄이 하나라 오버라이드로 안 잡힌다. 셸에서 세운다 |
| `eval/goldenset/` 은 `golden_*.csv` 만 채점 | `골든셋_양식.csv` 는 양식이다 |
| 커밋 메시지에 `—`(em dash)를 쓰지 않는다 | PowerShell 에서 `?` 로 깨진다. `-m` 을 여러 번 쓴다 |
| `python scripts/diff_reports.py` | 총계만 보면 *"틀리던 건이 거절로 빠져 분모가 줄어 비율이 오른 것"* 을 못 읽는다 |
