# 04. 테스트 계획 및 평가 설계

> **SKN 3차 단위 프로젝트** · 필수 산출물 ④ · 담당 **WS4**
> 상위 문서: `00_기획-요구사항분석.md`
>
> 최종 갱신: **2026-08-04** · 상태: **골든셋 60/60 ✅ · 평가 하네스 ✅ · 그래프 엔진 ✅ ·
> 결과 측정 완료** (`freeze-0803` = `093d101` · 통과 65.0%)
>
> 🔴 **본문은 계획 문서다. 결과에 맞춰 소급 수정하지 않는다** — 무엇을 예측했고
> 무엇이 맞았는지 확인할 수 없게 된다. 결과는 `eval/reports/결과보고서_제출용/` 에 있다.
>
> ⚠️ **비교군 이름은 코드가 정한다.** 아래 §5 는 `A` = 베이스 sLLM · `D` = 대형 LLM 으로
> 적었으나, `models/serving/arms.py` 실물은 **`A` = 대형 LLM · `D` = Qwen 베이스 ·
> `C` = 베이스+LoRA** 이고 `A-LC`(LangChain)·`none`(기준선)이 추가됐다.
> **측정과 보고서는 코드 쪽 이름을 쓴다** (2026-08-04 확인).
>
> **관련 설계 결정**: D-09 · D-12 · D-13 · D-19 · D-37 · D-38 · D-39 ·
> **D-45 · D-46 · D-49 · D-50~D-53** → `06_설계결정기록.md`
>
> **작성자: 오한빈 (팀장)**

---

## 1. 평가 철학

### 1.1 무엇을 정답으로 볼 것인가

본 도메인은 임상 판단이 개체차·품종·기저질환에 좌우되며, **팀에 수의학 전문 지식이 없다.**
따라서 평가 목표를 다음과 같이 재정의한다.

> **"의학적으로 옳은가"가 아니라 "출처 문서에 명시된 기준을 정확히 인용했는가"**

이는 회피가 아니라 **RAG 평가의 정석**(Faithfulness)이며, 시스템 포지셔닝과도 일치한다 (00 §3.2).

- ✅ 평가 대상 — 검색 정확도, 근거 충실도, 트리아지 등급 일치도, 거절·되묻기 적절성
- ❌ 평가 대상 아님 — 수의학적 진단의 임상적 타당성

### 1.2 검색 실패와 생성 실패를 분리한다

두 층으로 나누어 측정하지 않으면 원인 분석이 불가능하다.

| 계층 | 질문 |
|---|---|
| 검색 | 정답 근거를 가져왔는가 |
| 생성 | 가져온 근거대로만 답했는가 |

---

## 2. 골든 셋

모든 평가의 전제이며, **도메인 확정 직후 최우선으로 착수**한다.

### 2.1 구성 방식

**시나리오 기반**: (가상 프로필 + 누적 기록) + 질의 → 기대 트리아지 등급 · 근거 문서(`source_id`) · 정답 요지

**규모: 60건** — 2026-08-02 개정. 원안은 100건이었다.

> **왜 줄였나.** 임계 경로가 골든셋이 아니라 **§6 그래프 노드**로 옮겨갔다.
> 노드가 안 돌면 100건을 써도 지표를 뽑을 수 없고, 60건이면 종별 분리 보고(§4.2)와
> 과소평가율(§4.1)을 낼 수 있다. 인원도 넷에서 둘로 좁혔다 (07 · 04a §7).
>
> **총량보다 종별 최소가 먼저다** (§2.3). 총량이 100이어도 조류가 5건이면
> D-09 종별 분기라는 이 프로젝트의 핵심 주장을 지표로 보여줄 수 없다.

### 2.2 질의 유형 배분

**유형은 CSV의 `case_type` 컬럼으로 적는다** (04a §스키마). 컬럼이 없으면 이 표는 채점되지 않는다.

| `case_type` | 비중 | 60건 목표 | 무엇을 채점하나 |
|---|---|---|---|
| `fact` 단순 사실형 | 20% | 12 | 검색 정확도 — "포도는 강아지에게 위험한가요" |
| **`dose` 용량·개체 조건형** | **25%** | **15** | **계산 노드 · D-50 바닥 등급** — "5kg 푸들이 초콜릿 20g" |
| **`species` 종 구분형** | 15% | 9 | **D-09 종별 분기** — 조류에 체중당 수치를 묻지 않는가 |
| **`slot` 슬롯 결측형** | 15% | 9 | 되묻기 — 체중·섭취량·**종**·**물질 미상**(D-49) |
| **`none` 답 없음** | 15% | 9 | 거절 — 코퍼스에 근거가 없는 물질 |
| `symptom` 증상 서술형 | 5% | 3 | **D-49 미결 지점** — 증상만으로 등급을 낼 수 있는가 |
| `prevent` 예방형 | 5% | 3 | **축 B(급여 가부)** — 아직 먹지 않았다. 등급을 비운다 |

> **`recall` 기록 참조형은 이번 범위에서 뺐다** (2026-08-02).
> 원안은 15%였으나 **내부 기록 파이프라인이 구현 3단계**라 채점 대상이 없다.
> 코퍼스의 `recall` 청크도 11건뿐이다. 채울 수 없는 목표를 남기면
> 산출물에 미달로 박히므로, **구현 3단계에 되살린다**고 적어 두고 뺀다.

#### 실측 — 27건 소급 분류 (2026-08-02)

| `case_type` | 27건 | **52건** | 60 목표 | 남음 |
|---|---|---|---|---|
| `fact` | 6 | 10 | 12 | 2 |
| **`dose`** | **2** | **15** | 15 | **0** ✅ |
| `species` | 4 | 6 | 9 | 3 |
| `slot` | 7 | 9 | 9 | 0 ✅ |
| `none` | 5 | 9 | 9 | 0 ✅ |
| `prevent` | 2 | 2 | 3 | 1 |
| `symptom` | 1 | 1 | 3 | 2 |
| **계** | **27** | **52** | **60** | **8** |

> ### `dose` 는 자유 창작이 아니었다
> 27건 시점에 `dose` 가 2건뿐이었다. **이 프로젝트가 가장 공들인 부분**(계산 노드 · D-50 바닥 등급 ·
> 역치 매핑 · 상충 10배 룰)을 검증할 케이스가 없었다는 뜻이다. `slot` 7건이
> *"체중을 안 알려줘서 되묻는"* 케이스였던 것과 대비된다 —
> **되묻는 것까지는 채점되고, 되물어서 받은 다음이 채점되지 않았다.**
>
> 채우려고 코퍼스를 뒤지다 알게 된 것: **환산 가능한 역치가 (물질, 종) 7쌍뿐이다.**
>
> | 물질 | 종 | 역치 | 만들 수 있는 구간 |
> |---|---|---|---|
> | 초콜릿 | dog | 20 · 40-50 mg/kg | 3 |
> | 자일리톨 | dog | 0.03 · 0.5 g/kg | 3 |
> | 양파 | dog | 15-30 g/kg | 2 |
> | 양파 | cat | 5 g/kg | 2 |
> | 알리움류 | cat | 0.5 % | 2 |
> | 란타나 | dog | ≥1 % | 2 |
> | 마늘 | mammal | 5 g/kg | 2 |
>
> **그래서 `dose` 는 물질을 고르는 일이 아니라 역치 구간을 채우는 일이다.**
> 물질 6개로 15건이 나온다 — 같은 물질에 **양만 바꿔** 쓰기 때문이고,
> 그게 계산 노드가 실제로 도는지 보는 유일한 방법이다.
> 초콜릿은 함량(2 / 5 / >14 mg/g)까지 코퍼스에 있어 종류를 바꾸는 축이 하나 더 있다.

**종별 × 유형** — `bird` 는 `dose` **0건**이다. 체중당 임계치가 0건이라(02 §3.1) 정상이며,
**조류에 `dose` 를 만들면 안 된다** — 정답을 만들려면 우리가 숫자를 지어내야 한다.

**등급 분포 (52건)** — `CALL_NOW` 16 · `EMERGENCY` 9 · `MONITOR` 6 · **`VISIT_SOON` 1** · 비움 20.

> ⚠️ **`VISIT_SOON` 은 27건 시점에 0건이었다.** 4등급 중 하나가 없으면
> 등급 일치도의 혼동행렬이 4×4로 나오지 않고, **인접 오차 허용 일치도**(§4.1.0)도 왜곡된다.
> `G-042`(마카다미아 — *"사망 보고는 현재까지 없음"*)로 한 건 만들었으나 **여전히 1건이다.**
> 남은 8건에서 조류로 한둘 더 만드는 것이 좋다.

### 2.3 종별 최소 배정 — **총량보다 이쪽이 먼저다**

자료 분량이 달라도 **평가는 균등하게** 한다.

| 종 | 최소 | 27건 시점 | 팀장 8건 후 |
|---|---|---|---|
| 개 | **10건** | 12 ✅ | 12 |
| 고양이 | **10건** | 7 | **10** ✅ |
| **앵무새** | **10건** | 5 | **10** ✅ |
| 종 미지정 (되묻기용) | — | 3 | 3 |

> 팀장 8건(bird +5 · cat +3)이 정확히 이 구멍을 메우도록 배정돼 있다 (04a §7).
> **이서은 25건에도 종별 하한을 둔다** — `bird` 최소 5 · `cat` 최소 5.
> 하한이 없으면 25건이 전부 `dog` 으로 몰려도 지침 위반이 아니고,
> 그러면 최종 60건에서 조류 비중이 17%에 머문다.

**필수 포함 항목**

- 조류 특이 항목 — 아보카도, 조리기구 코팅 흄, 중금속, 씨앗 편중 급여
- 개 특이 항목 — 자일리톨, 포도·건포도, 마카다미아, GDV
- **출처 간 기준값이 상충하는 사례** (01 §14) → 복수 근거 제시 동작 평가.
  **코퍼스 실측으로 9건이 이미 확보되어 있다** (D-39 도출 과정). 대표 사례:
  <!-- D-50 이후 이 9건은 골든셋 항목이자 `_detect_conflict()` 회귀 케이스를 겸한다 -->

  - **발작** — AAHA S-037 *"as soon as the seizure ends, immediately contact"*(`CALL_NOW`)
    vs FOUR PAWS S-030 *"if… convulsing… go immediately to the nearest emergency hospital"*(`EMERGENCY`)
    → **하향 금지 게이트가 `EMERGENCY`를 채택해야 정답**
  - 포도(개) — 같은 종에 3가지 긴급도 · 사과씨(조류) — `NEVER` vs "우발적 1개는 무해"
  - 아보카도 — FDA *"only mildly toxic to dogs and cats"* vs Banfield 무조건 독성 목록

> **D-50 이후 이 9건은 두 가지를 겸한다.** 골든셋 항목이면서
> `compute/rules.py::_detect_conflict()` 의 **회귀 케이스**다.
> 같은 물질·같은 역치 종류인데 10배 이상 벌어지면 낮은 쪽을 믿지 않도록 코드가 막는데,
> 그 동작을 실제 상충 자료로 채점한다. `G-013`(PTFE — S-071 `EMERGENCY` vs S-007·S-074 `CALL_NOW`)이
> 이미 그 케이스다.

### 2.4 작성 체계 — **넷에서 둘로** (2026-08-02 개정)

**작성 실적 (52건 · `written_by` 실측)**

| 작성자 | `case_id` | 건수 | 검수 |
|---|---|---|---|
| **이서은** | **G-001 ~ G-025** | **25** | ✅ 오한빈 |
| **오한빈** | G-026 · G-027 | 2 | ☐ 이서은 |
| **오한빈** | G-028 ~ G-052 | 25 | ☐ 이서은 |

**남은 배정**

| 담당 | 몫 | `case_id` | 상태 |
|---|---|---|---|
| **오한빈** | **8건** — 조류·고양이 정성 케이스 | G-101 ~ G-108 | ☐ |
| **이서은** | **검수 27건** (G-026 ~ G-052) → 이후 파인튜닝 데이터 | — | ☐ |
| 이근준·권소라 | **질문 문장만 5개씩** (판정은 쓰지 않는다) | — | ☐ |

상세 지침은 **`04a_골든셋작성지침.md`** 가 단일 출처다.

> **G-028~G-052 를 팀장이 쓴 것은 배정 변경이지 대체가 아니다.** 마감(8/4)이 이틀 남아
> 대기를 없앴다. **골든셋의 기준선은 이서은의 G-001~G-025** 이고, 유형·난이도 배분과
> `must_not_contain` 사용법이 거기서 잡혔다. 뒤의 27건은 그 기준으로 빈 자리를 채운 것이다.

> **52/60건.** `dose`·`slot`·`none` 은 목표를 채웠고 남은 8건은 **조류로만 채울 수 있는 자리**다.

- **인원을 줄인 이유** — 원안의 경고(*"넷이 동시에 착수하면 기준이 넷으로 갈린다"*)가
  맞았고, 임계 경로가 그래프 노드로 옮겨가면서 **아예 인원을 줄이는 쪽**을 택했다 (07)
- 각 항목은 **근거 `source_id`를 반드시 명시**한다. 없으면 검색 지표를 계산할 수 없다
- **대장에 있어도 사실 표에 0행이면 쓸 수 없다** — S-001·S-025·S-057·S-083 이 실제로 걸렸다.
  `check_goldenset.py` 가 이제 ERROR 로 잡는다
- **작성자와 검수자는 반드시 다르다.** 자기가 쓴 정답을 자기가 검수하면
  같은 오해가 두 번 통과한다. `written_by` · `reviewed_by` 를 분리 기록한다
  — 27건 실측: 25건 `lse→ohb`, 2건은 팀장 작성이라 `reviewed_by` 공란(이서은이 채운다)
- 검사기가 미기입을 경고한다 — `python scripts/check_goldenset.py`

---

## 2.5 층 0 — 데이터 파이프라인 검증 (D-38)

**대부분의 자료가 원문이 아니라 우리가 문장화한 형태로 적재된다** (D-37 · 01 §2.1).
따라서 **문장화 자체가 검증 대상**이 된다. 이것이 없으면 라이선스는 지켰는데 사실이 틀어진다.

> **환각 방지 지점이 답변 단계에서 문장화 단계로 이동한다.**
> 축약된 문장을 원문으로 오인하면 그 자체가 환각의 출처가 된다 (D-30과 같은 구조).

### 2.5.1 지표 3종

| 지표 | 정의 | 목표 |
|---|---|---|
| **추출 정확도** | 사실 표의 숫자·단위·종 이름이 원문과 일치하는 비율 | — |
| **문장화 충실도** | 생성 문장에 **원문에 없는 내용**이 포함된 비율 | **0** |
| **역추적 가능성** | 모든 청크가 `source_id` + 원문 위치를 보유한 비율 | **100%** |

### 2.5.2 검사 방법 — 검증 대상은 문장이 아니라 필드다

문장화를 **템플릿 + 코드**로 하므로(D-38), 필드만 맞으면 문장은 자동으로 맞다.

| 층 | 검사 | 방법 |
|---|---|---|
| 자동 | 그 숫자가 원문에 실제로 있는가 | 원문 문자열 검색 |
| 자동 | **단위 변환 오류** | 재계산 — S-074에 실제 사례가 있다 (*"530°F (280°C)"*, 280°C = **536°F**) |
| 자동 | 종 이름이 원문과 일치하는가 | 문자열 대조 |
| 자동 | 필수 필드 결측 | 스키마 검사 |
| 사람 | 템플릿 검수 | **한 번만** |
| 사람 | 표본 대조 | 무작위 N건을 원문과 대조. **작성자·검수자 분리** (§2.4) |

> **원문은 `data_work/`에 남아 채점표 역할을 한다** (D-29).
> §1.1의 *"출처 문서에 명시된 기준을 정확히 인용했는가"* 는 이 원문과 대조해 성립한다.
> **원문은 채점표이지 적재물이 아니다.**

**이 검증은 빌드타임 비용이다.** 런타임마다 도는 것이 아니라 코퍼스 구축 시 한 번이면 끝난다.

### 2.5.3 실측 — 검색 점검 (2026-08-01)

**층 0의 첫 실측치다.** 재현:

```bash
python scripts/build_index.py --store chroma
```

| | |
|---|---|
| 임베딩 | `BAAI/bge-m3` · 1024차원 · cosine |
| 벡터DB | Chroma 0.6.3 (D-44) · 컬렉션 `external` |
| 적재 | 사실 **888행 → 청크 888건** · 171.3s |
| 종별 | dog 311 · cat 241 · bird 159 · mammal 100 · all 77 |
| 설정 | `top_k=5` · `score_threshold=0.50` (D-46) |

**양성 점검 7건 전부 통과.**

| 질의 | 1위 점수 | 물어온 것 |
|---|---|---|
| 강아지가 초콜릿을 먹었어요 | 0.657 | 초콜릿 (S-029) |
| 우리 개가 포도를 먹었는데 괜찮을까요 | 0.703 | 포도·건포도 (S-021) |
| 강아지가 자일리톨 껌을 삼켰어요 | 0.695 | 자일리톨 (S-029) |
| 고양이가 백합을 씹었어요 | **0.728** | 백합 (S-010) |
| 고양이가 양파 들어간 음식을 먹었어요 | 0.711 | 양파 (S-010) |
| 앵무새가 아보카도를 먹었어요 | 0.687 | 아보카도 (S-029) |
| 앵무새 앞에서 프라이팬을 태웠어요 | **0.547** | 논스틱(PTFE) 조리기구 (S-074) |

#### 읽어야 할 것 세 가지

**① 코퍼스가 두 배가 됐는데 점수가 안 떨어졌다.**
433청크 시점 측정도 0.547~0.728 이었다. 비슷한 문서가 늘면 1위가 희석될 것으로 봤으나
그렇지 않았다 — `score_threshold: 0.50` 을 그대로 둔다.

**② PTFE 만 여유가 0.047 이다.**
질의에 *"PTFE"*·*"테플론"* 이 하나도 없고 **"프라이팬을 태웠다"는 상황 서술뿐**이라서다.
골든셋 `G-013` 이 같은 경로를 타므로, 평가에서 이 케이스가 거절로 빠지면 원인은 여기다.

**③ 앵무새 아보카도가 조류 전용 자료를 1위로 물어오지 않는다.**
S-029(FDA, `species=all`)가 이겼고 S-007·S-071(조류 전용)이 밀렸다.
D-39 병합 검색이 도는 증거이긴 하나, **골든셋 `must_cite` 를 조류 자료로 적으면
그 케이스는 상위 5 안에서 근거를 못 찾을 수 있다.**

#### 이 측정으로 드러난 점검 항목의 결함

**점검 7건이 코퍼스를 대표하지 못했다.**

```
nutrition       274청크  ← 프로브 0건   (최대 doc_type 인데 사각지대)
toxicity_food   225        3건
toxicity_plant  225        2건
emergency       131        2건
symptom          22      ← 프로브 0건
recall           11      ← 프로브 0건
```

게다가 **7건 중 6건은 질의에 물질명이 그대로 있어** 어휘 일치만 확인됐다.
진짜 의미 검색은 PTFE 하나였고 그것만 턱걸이였다 — **어려운 것을 하나만 넣었기 때문이다.**
음성 질의도 없어 *"근거가 없을 때 거절하는가"* 는 점검 대상이 아니었다.

→ **양성 16건 + 음성 4건으로 확장했다** (같은 날, `scripts/build_index.py`).
영양·증상·리콜, 물질명 없는 의미 질의 3건, 종 분기(개 백합), 검수 회귀(남천·국화)를 넣고,
음성은 **1위가 임계값 미만이어야 통과**로 뒤집어 판정한다.
### 2.5.4 확장 점검 실측 (2026-08-01, 같은 날)

**양성 16건 중 15건 통과 · 음성 4건 중 1건 통과.**

#### 확장이 곧바로 잡은 것 — 문장화 반전

> ```
> 원문 (S-003 AAV)   앵무새에게 소금 블록은 주지 마라
> 적재된 문장          "앵무새 전 생애단계의 소금 블록(salt block) 권장량이다."
> ```

`NUTRITION` 템플릿이 **`feeding_level`(축 B)을 한 번도 쓰지 않았다.**
`NEVER` 도 `CAUTION` 도 전부 *"권장량이다"* 로 렌더됐다 — **13행** 해당, 그중 1건이 급여 금지.

**§2.5.1의 문장화 충실도 목표 `0` 을 깨는 유일한 종류다.**
원문에 없는 내용이 아니라 **원문과 반대되는 내용**이기 때문이다.

함께 드러난 두 번째 결함 — `max_value` 에 `unit` 이 없으면 절이 통째로 빠졌다.
`"얇은 조각 1~2쪽"` 처럼 값에 단위가 든 경우로, **S-047 과일 6종의 급여량이 소실**돼 있었다.

→ 둘 다 고치고 `tests/test_verbalize.py` 에 회귀 8건을 고정했다.
수정 후 문장:

```
앵무새에게 소금 블록(salt block)은 급여 금지다.
개에게 사과는 조건부 급여다. 1회 권장량은 얇은 조각 1~2쪽까지다.
```

#### 남은 실패 4건 — **고칠 수 있는 것과 아닌 것**

| | 실패 | 성격 |
|---|---|---|
| 양성 1 | 차고의 달콤한 액체 → 1위 `강박 핥기·씹기`(S-080) | **점검 설계가 틀렸다.** 코퍼스에 `달콤`·`단맛`·`차고` 가 **0건**이라 맞힐 방법이 없다 — 맞혔다면 임베딩 사전지식이 답을 만든 것이다. `UNKNOWN_SUBSTANCE_PROBES` 로 옮겼다 (D-49 후속) |
| 음성 3 | 캣타워 0.574 · 미용 0.520 · 보험료 0.564 | **임계값으로 못 고친다** — 아래 |

**음성이 양성보다 높다.**

```
양성 최저   0.547   PTFE
음성 최고   0.574   캣타워 → 트립토판
```

캣타워를 막으려면 0.574 위로 올려야 하는데 그러면 PTFE·개 백합(0.605)·리콜(0.615)·
췌장염(0.616)이 함께 죽는다. **어떤 임계값도 이 둘을 가르지 못한다.**

> **D-46 이 433청크에서 내린 결론이 888청크에서 재확인됐다.**
> *"유사도 임계값은 거절을 만들지 못한다 — 거절은 ①분류·④검증 책임이다."*
> 캣타워·미용·보험료는 **검색 전에** `intent=general` 로 걸러야 한다.
> 골든셋 `G-006`(날씨)·`G-015`(캣타워)가 `refused` 인 것이 같은 근거다.

음성 3건 중 2건이 **영양 청크**를 물어온 것(트립토판·BMR)은 따로 볼 값이다.
`"반려동물 전 생애단계의 기초대사율(BMR) 권장량이다."` 처럼
**수치가 하나도 없는 껍데기 청크가 17건** 있고, 이런 문장이 아무 질의에나 떠오른다.

#### 아직 판정하지 못한 것

- 껍데기 청크를 인덱스에서 뺄지 — **먼저 문장을 고쳤다** (아래 §2.5.5)

### 2.5.5 점검이 **통과시킨** 것 — 증상 질의 (D-49)

```
✓ '고양이가 토하고 밥을 안 먹고 배를 아파해요'
      1위 0.616 · 토마토 (S-016)
```

**초록불이었다.** 기대한 `췌장염` 이 상위 5 안에 있으면 통과하는 기준이라
1위가 엉뚱해도 드러나지 않았다.

처음에는 `토하고`/`토마토` 의 표면 형태가 겹친 줄 알았다. **오진이었다.**
1위 청크의 원문은 이렇다.

> "고양이에게 토마토는 조건부로 분류된다.
>  주요 증상은 과도한 침 흘림, **식욕 부진**, **침울**, **쇠약**, 동공 확대, 서맥이다."

*밥을 안 먹고* → 식욕 부진, *배를 아파해요* → 침울·쇠약.
**검색은 제대로 일했다.** 문제는 검색이 아니라 질문의 방향이었다.

코퍼스는 D-14 로 물질 단위이므로 각 청크가 *"이 물질 → 이런 증상"* 이다.
증상만 주면 그 화살표를 거꾸로 타는데 **역방향은 일대일이 아니다** —
고양이 청크 418건 중 **21건이 같은 증상 조합**을 나열한다.

**여기서 1위를 근거로 답하면 "토마토 중독일 수 있습니다" 가 되고, 그것이 진단이다** (D-11).
같은 이유로 **기대값 `췌장염` 자체가 틀렸다** — 증상에서 질환을 짚는 것도 진단이다.
**통과했어도 틀린 통과였다.**

→ **D-49** 로 기록하고 두 가지를 바꿨다.

| 어디 | 무엇 |
|---|---|
| `build_index.py` | 증상 질의를 양성 프로브에서 빼고 `SYMPTOM_PROBES` 로 옮겨 **판정하지 않고 모호도만 보고**한다 (상위가 몇 종으로 흩어지는지 · 점수 폭) |
| `check_goldenset.py` | `intent=symptom` + `answered` 인데 `must_not_contain` 이 비면 WARN |

**`build_index` 에 초록·빨강을 안 매긴 이유** — 올바른 동작은 ①분류·②되묻기의 몫인데
그 스크립트는 저장소만 들고 있어 검증할 수 없다.
**검증할 수 없는 것에 판정을 찍으면 그 초록불이 곧 거짓 근거가 된다.**

#### 모호도 실측 — 1위는 **잡음 안에서 결정된다**

| 질의 | 상위 5의 물질 | 서로 다른 종 | 점수 폭 |
|---|---|---|---|
| 고양이가 토하고 밥을 안 먹고 배를 아파해요 | 토마토 · 초콜릿 · 췌장염 · 알리움류 · 초콜릿 | **5종** | **0.030** |
| 강아지가 자꾸 침을 흘리고 기운이 없어요 | 가정용 청소용품 · 천식 · 석유증류물 · 울혈성 심부전 · 흉막 질환 | **5종** | **0.019** |
| 앵무새가 깃털을 부풀리고 바닥에 앉아 있어요 | 저체온 · 깃 고르기 · 골절 · 화상 · 페놀계 세정제 | **5종** | **0.042** |

**점수 폭이 0.019~0.042 다.** 양성 프로브의 1위–최저 간격이 0.18(0.547~0.728)인 것과 비교하면
**한 자릿수 배 좁다.** 이 구간 안에서 순위는 사실상 임의이고,
임베딩을 재적재하거나 청크 문장을 한 글자 고치면 1위가 바뀔 수 있다.

내용을 보면 더 분명하다. 개 질의의 상위 5는 **청소용품 · 천식 · 석유증류물 · 심부전 · 흉막 질환** 이다 —
중독과 내과 질환이 뒤섞여 있고, **어느 하나를 골라 답하면 나머지 넷을 배제한 셈**이 된다.
그 배제가 곧 진단이다.

> **이 표가 D-49의 근거다.** "1위가 애매하다"는 인상이 아니라
> **1위와 5위의 차이가 측정 잡음과 같은 크기**라는 사실이다.

### 2.5.6 확장 후 실측 (2026-08-01, 3차 재적재)

**양성 14건 전부 통과 · 음성 4건 중 1건 · 보고 6건.**

빨간불이 없어진 것이 아니라 **판정 대상이 아니게 된 것**이다.
증상 질의와 물질 서술 질의를 통과/실패 채점에서 빼고 보고로 돌렸다 (D-49).

#### 보고가 곧바로 잡은 것 — **검사기 자신의 결함**

```
· '강아지가 차고 바닥에 흘린 달콤한 액체를 핥았어요'
    상위 5: 앞발·다리 강박 핥기 · 표면 강박 핥기 · 강박장애(CCD) · **부동액** · 청소용품
    사람이 아는 정답 '에틸렌글리콜': 상위에 없음 (정상)
```

**"상위에 없음" 이 아니다. 4위에 `부동액`(S-029)이 있다.**
정답을 `에틸렌글리콜` 한 이름으로만 적어 두어 검사기가 못 알아봤다.

> §8과 같은 종류의 실수다 — **검사기가 틀리면 그 초록불이 곧 거짓 근거다.**
> 정답을 별칭 묶음(`에틸렌글리콜`·`부동액`)으로 바꾸고 **몇 위인지**를 함께 찍게 했다.

그래서 D-49 후속의 서술도 정확히는 이렇다. 코퍼스에 `달콤`·`단맛`·`차고` 는 **0건이 맞고**,
1~3위가 `핥기` 표면 일치로 채워진 것도 맞다. 다만 **부동액이 4위로는 올라온다** —
`반려동물과 부동액에 관한 자료다`(S-029)라는 **내용이 거의 없는 청크**다.
근거로 쓰기에는 부족하고, 되묻기가 옳다는 결론은 변하지 않는다.

#### 또 하나 — 같은 물질이 상위를 채운다

```
· '고양이가 베란다에 둔 파란 알갱이를 주워 먹었어요'
    상위 5: 사람 음식 · **양파 · 양파 · 양파** · 토란
```

`양파` 청크가 **8건**이다 (S-010·S-019·S-029·S-034 ×2·S-063·S-098 ×2).
고양이 질의는 D-39 병합(`cat`+`mammal`+`all`)으로 그중 4건을 함께 본다.

**`top_k=5` 가 실질적으로 서로 다른 물질 3종이 된다.**
검색이 틀린 것은 아니지만 **문맥 예산을 같은 말로 채운다** —
`compute/tables/README.md` 가 계산 노드에 대해 적어 둔 *"중복 히트를 하나로 접어야 한다"* 가
검색 단계에도 그대로 필요하다.

> **조치 완료 (2026-08-02).** `retrieval/store.py::dedupe_by_substance()` 를 구현했다.
> 같은 `(substance, species)` 청크를 하나로 접고 출처는 `Hit.merged_sources` 로 합쳐
> **근거를 잃지 않는다.** 호출 순서를 `search → filter_by_threshold → dedupe_by_substance`
> 로 고정했다 (02 §8.3) — 임계값보다 먼저 접으면 잡음 청크가 좋은 출처를 흡수한다.
>
> ⚠️ **아직 재측정하지 않았다.** 코드는 들어갔고 단위 테스트로 고정했으나,
> `'고양이가 베란다에 둔 파란 알갱이를 주워 먹었어요'` 를 **접은 뒤 다시 돌린 실측치가 없다.**
> 접기가 상위 5를 실제로 몇 종으로 늘렸는지는 다음 `build_index.py` 실행에서 채운다.
> **구현했다는 것과 효과가 측정됐다는 것은 다르다** (§8 · 이 문서가 반복해서 경계하는 지점).

> **점검이 통과했다는 것과 검색이 된다는 것은 다르다.**
> 무엇을 점검하지 않았는지를 함께 적지 않으면, 초록불이 곧 근거로 둔갑한다 (§8).
> **이번에는 초록불 자체가 틀린 경우였고, 그다음에는 검사기가 틀린 경우였다.**

---

## 3. 층 1 — 태스크별 성능

| 태스크 | 지표 |
|---|---|
| ① 분류 | Macro-F1, 혼동행렬 (특히 **위험도 등급 혼동**, **종 오분류**) |
| ② 슬롯 추출 | 슬롯별 Exact Match / F1, **JSON 파싱 성공률**, 결측 시 되묻기 적절성 |
| ③ 요약 | ROUGE-L + **LLM-as-judge 충실도** (원문에 없는 내용 생성 여부) |
| ④ 근거 검증 | Precision / Recall (환각 탐지 관점), **왜곡 유형별 탐지율** |
| ⑤ 평이화 | 가독성 지표, 의미 보존도 (LLM-as-judge) |
| ~~⑥ 번역~~ | **미채택 확정** — 청크가 전부 한국어라 번역 대상이 없다 (§6 E2 · D-19 후속). **멀티태스크는 5종이다** (D-05) |

> ④의 **왜곡 유형별 탐지율**을 반드시 분리 보고한다.
> 특히 **종 혼동·단위 혼동 탐지율**은 이 도메인에서 가장 중요한 안전 지표다.

---

## 4. 층 2 — 통합 시스템 성능

| 계층 | 지표 |
|---|---|
| 검색 | Recall@k, MRR, Context Precision |
| 생성 | Faithfulness, Answer Relevancy |
| **트리아지** | 등급 일치도, **과소평가율(under-triage)**, 과대평가율(over-triage) |
| 거절 | 오답률, 모름 응답률, **되묻기 발생률·적절성** |
| **종별** | 개 / 고양이 / 앵무새 각각의 위 지표를 **분리 보고** |
| **운영** | **지연 `p50`·`p95` · 노드별 분해** (§4.3) · 토큰 비용 · VRAM |

### 4.1 과소평가율을 최우선 지표로 선언한다

#### 4.1.0 등급 체계와 계산 정의 (D-39)

등급은 **4단계 순서형**이며 **숫자가 클수록 위험**하다.

| 값 | 정수 | 배지 |
|---|---|---|
| `EMERGENCY` | **4** | 응급 |
| `CALL_NOW` | **3** | 전화 |
| `VISIT_SOON` | **2** | 내원 |
| `MONITOR` | **1** | 관찰 |

| 지표 | 정의 |
|---|---|
| **등급 일치도** | `예측 == 정답` 비율 |
| **인접 오차 허용 일치도** | `|예측 − 정답| ≤ 1` 비율 — 보조 지표 |
| **🔴 과소평가율** | `예측 < 정답` 비율. **최우선 지표** |
| 과대평가율 | `예측 > 정답` 비율 |
| **중대 과소평가율** | **`정답 = EMERGENCY(4)` 인데 `예측 ≤ 2`** — 별도 분리 보고. **0을 목표로 한다** |

> **`MONITOR` 판정은 상승 조건을 함께 출력해야 정답으로 인정한다.**
> D-39의 이 단계는 *"if not resolving"* 이라는 조건부 상승이 본질이므로,
> 조건 없는 "관찰"은 **그 자체가 과소평가**로 채점한다.

**정답 등급은 골든셋 작성 시 근거 문서의 행동 지시어에서 결정한다.**
작성자가 임의로 매기지 않고, `chunk_id`와 함께 **근거 문장을 인용**한다 (§2.4).

> 실제로는 내원이 필요한 사례를 "관찰"로 답한 비율이며,
> 이 도메인에서 **유일하게 생명과 직결되는 오류**다.
>
> **일반 정확도가 높아도 과소평가율이 높으면 실패한 시스템으로 간주한다.**

이 기준을 명시하는 것 자체가 안전 설계를 의식했다는 신호이며, 보고서의 차별화 요소가 된다.

과대평가(over-triage)는 불필요한 병원 방문 비용을 유발하므로 함께 보고하되,
**두 지표가 상충할 때는 과소평가 억제를 우선**한다.

> **아키텍처 차원의 억제 장치가 있다.** 트리아지 **하향 금지 게이트**(02 §7.2 · 05 §5)는
> LLM이 규칙 등급보다 낮은 등급을 제시하면 무시한다.
> 지표로만 관리하는 것이 아니라 **구조로 막는다.** 효과는 실험 E6으로 측정한다.

### 4.1.1 되묻기는 **끄지 않고 채점한다** (D-66)

평가 프로파일(`configs/eval.yaml`)은 2026-08-02 까지 `max_clarify_turns: 0` 이었고,
근거로 **이 절(§4.1)을 인용**하고 있었다. 그런데 이 절에는 그런 문장이 없었다 —
**인용이 가리키는 곳이 비어 있었다.** 첫 실측에서 드러났고 여기서 정리한다.

**끄면 안 되는 이유**

되묻기를 끄면 결측이 전부 `refused / 되묻기상한` 이 된다. 그러면 골든셋에서
`clarify` 를 정답으로 적은 15건이 **구조적으로 통과 불가**가 된다 (1차 실측:
`slot` 유형 9건 통과율 **0.0%**). *"필요한 것이 없다는 걸 알아채고 멈췄다"* 는
이 시스템의 **핵심 안전 동작**(D-10 · D-49 · 02 §6.2)이 측정에서 통째로 사라진다.

**끄지 않아도 안전한 이유** — 원래 걱정이 이미 다른 방법으로 막혀 있다.

| 걱정 | 무엇이 이미 막고 있나 |
|---|---|
| 다회차로 번진다 | 하네스 `run()` 이 **첫 응답만** 채점한다. `ask()` 는 응답 하나만 낸다 |
| 등급 분모가 흔들린다 | `clarify` 도 `refused` 도 `triage` 가 없다. **똑같이 분모에서 빠진다** — `level_n` 은 동일하다 |
| 전부 되묻기로 도피 | `--fail-missed` 게이트가 *"정답이 CALL_NOW 이상인데 **거절·되묻기로** 빠진 건"* 을 별도로 잡는다 |

**`refused` 로 대체 채점하지 않는 이유** — 사용자가 받는 것이 다르다.

```
clarify  →  "어떤 동물인가요? 체중은 어떻게 되나요?"           대화가 이어진다
refused  →  "필요한 정보를 확인하지 못해 답변을 드릴 수 없습니다."   막다른 길
```

응급 도메인에서 이 둘은 같은 결과가 아니다. 같다고 세면 **보고서가 시스템이
보여주지 않은 능력을 주장**하게 된다 (원칙 2 — *초록불이 곧 근거가 되지 않게 한다*).
게다가 끈 상태에서는 `clarify_question` 이 **애초에 생성되지 않아** *무엇을 되물었는지*
를 §7 실패 분석에 쓸 수 없다.

**이 변경이 드러내는 것** — 혼동 행렬이 비로소 정보를 갖는다.

```
끈 상태:   clarify → refused   6      무엇이 문제인지 알 수 없다
켠 뒤:     clarify → clarify   n      결측을 알아챘다 ✅
          clarify → answered  n      **없는 정보로 답했다. D-10 위반 후보**
          clarify → refused   n      알아챘지만 되묻지 못했다
```

⚠️ 기존 `refused` 정답 중 실제로는 슬롯 결측인 건은 이제 `clarify` 가 나온다.
**회귀가 아니라 라벨 오류를 찾아낸 것**이다 — 자동으로 받아들이지 말고 건별로 검수한다.

### 4.1.x 과대평가는 **의도된 편향**이다 — 숨기지 않고 함께 보고한다

D-50 이 역치 매핑을 보수적으로 잡았다.

```
치사·중증 초과     → EMERGENCY
임상징후 발현 초과  → CALL_NOW
역치 미만          → MONITOR (+ 상승 조건)
```

`rule_level` 은 정밀한 판정이 아니라 **바닥**이고(`final = max(rule, llm)`),
바닥이 틀렸을 때 **위로 틀리게** 설계했다. §4.1 이 과소평가율을 최우선 지표로 삼은 것과
같은 방향이다.

**대가는 과대평가율 상승**이다. 특히 `중증` 과 `치사` 가 같은 배지로 표시되어
문서가 나눈 것을 화면에서 지운다 (D-30 과 상충).

> **그러므로 과대평가율을 과소평가율과 나란히 보고한다.**
> 낮게 나오면 좋은 것이 아니라 **매핑이 의도대로 작동하지 않았다는 신호**다.
> 숨기면 문제이고, 적으면 설계 선택이다.

근거 문장에는 *"치사량"*·*"중증"* 이 그대로 인용되므로 **정보가 아니라 배지만 같아진다.**

#### `치사` 분기는 **검증되지 않았다** — 이유가 자료 부족이 아니다 (D-51)

| `threshold_type` | 행 수 | `dose`+`unit` | `to_mg_per_kg()` 통과 |
|---|---|---|---|
| `치사` | **1** | 1 | **✗ 0** |
| `중증` | 3 | 3 | ✓ 3 |
| `임상징후 발현` | 11 | 11 | ✓ 11 |

치사 행은 **있다** — `F-030-010` 개 · **주목(yew) 2.3 g leaves/kg**.
그런데 단위가 `g leaves/kg` 라 환산표에 없고, **환산하지 않기로 결정했다** (D-51).
잎 무게는 독소 무게가 아니고, 환산하면 **우리가 만든 숫자가 EMERGENCY 를 판정한다.**

> **EMERGENCY 자체는 살아 있다** — `중증` 3행(초콜릿 ×2 · 자일리톨)이 담당한다.
> 다만 *"치사량을 넘겼다"* 는 경로는 **한 번도 실행된 적이 없다.**
> 보고서에 이 상태를 그대로 적는다 — 돌지 않은 분기를 돈 것처럼 세면 그것이 §8이 경계하는 거짓 초록불이다.

---

### 4.3 지연 — **`p95` 를 본다** (2026-08-02 신설)

02 §12.4 로 **SSE 스트리밍을 미채택**했다. 옳은 결정이지만 대가가 있다 —
**체감 지연을 스트리밍으로 가릴 수 없고, 실제 지연이 그대로 침묵으로 나타난다.**
축②(통제가 먼저, 배달이 나중)를 택한 값이므로 지연 예산이 그만큼 빡빡하다.

| 지표 | 정의 |
|---|---|
| `p50` · `p95` (전체) | 골든셋 전 건의 벽시계 지연 |
| **`answered` `p50`·`p95`** | **실제 체감.** ← 판단 기준은 이쪽 |
| 노드별 `p95` | 어디가 느린지. 엔진이 `resp.timings` 를 실으면 자동 수집 |

> **평균을 보지 않는 이유.** 되묻기·거절은 LLM 을 **2번**(분류+슬롯)만 돌고 끝난다.
> 골든셋 52건 중 `clarify` 9 + `none` 9 = **18건(35%)** 이 이 빠른 경로다.
> 섞으면 평균이 낙관적으로 나오고 **긴 답변을 받는 사람의 경험이 가려진다.**

**분모가 0이면 `0.0%` 가 아니라 `—` 다** — 지연도 같다. `answered` 가 0건이면
`--fail-over` 게이트는 **실패**로 처리한다. *"빠른 것"* 과 *"답을 안 한 것"* 은 다르다.

```bash
python eval/harness/run_eval.py --fail-over 15000    # answered p95 15초 상한
```

**목표치는 아직 없다.** 1차 측정 후 확정한다 (§10.1) — 실측 없이 정하면 근거 없는 숫자다.

**어디가 느릴 것인가** — 12노드 중 LLM 호출이 **6번**이고 그것이 지배적이다.
질의 임베딩(**193ms**, §2.5.3 실측)은 전체의 1% 남짓이다.
**캐시를 붙이기 전에 노드별 숫자를 먼저 본다** (D-53).

> **측정 전에 워밍업 1회를 버린다** (D-53). 첫 호출에 임베딩 모델 로딩이 섞이면
> `--only` 로 1건만 돌릴 때 **그 한 건이 로딩 시간으로 찍히고**, `--fail-over` 게이트가
> 로딩 때문에 실패한다. **측정 도구가 자기 측정을 오염시키면 안 된다.**
> 콜드 스타트를 일부러 재려면 `--no-warmup`.

**모델 로딩 시간은 아직 측정된 적이 없다.** `BGEEmbedder` 가 로그로 남기게 해 뒀으니
(`임베딩 모델 로드 완료 — …s`) 처음 도는 사람이 §8 재현성 표에 적는다.

> **하네스는 FastAPI `lifespan` 을 타지 않는다** — 앱을 띄우지 않기 때문이다.
> 그래서 **하네스가 자체 워밍업**을 갖는다. 서버 경로와 평가 경로가 각자 자기 워밍업을 한다.

### 4.2 종별 분리 보고

앵무새 자료는 개·고양이보다 분량이 적을 가능성이 높다.
**전체 평균만 보면 조류 성능 저하가 가려진다.**

---

## 5. 핵심 비교표 — 프로젝트 결론

| 구성 | 태스크 성능 | 통합 성능 | 과소평가율 | VRAM | 지연 | 비용 |
|---|---|---|---|---|---|---|
| A. 베이스 sLLM 프롬프팅 (파인튜닝 없음) | | | | | | |
| B. 태스크별 개별 파인튜닝 (어댑터 N개) | | | | | | |
| **C. 멀티태스크 단일 파인튜닝** ← 본 프로젝트 | | | | | | |
| D. 대형 LLM 프롬프팅 (성능 상한선) | | | | | | |

> **C가 D에 얼마나 근접하면서 비용을 얼마나 줄였는가** — 이것이 프로젝트의 결론 문장이 된다.

---

## 6. 부가 실험

| # | 실험 | 목적 | 관련 문서 |
|---|---|---|---|
| E1 | **청킹 단위 비교** — 항목 단위 vs 500자 고정<br>**+ 템플릿 문장 vs 원문 청크** | 청킹 근거를 실험으로 제시<br>**+ 문장화의 검색 성능 대가를 정량화** (D-38) | 01 §10 · 01 §2.1 |
| ~~E2~~ | ~~cross-lingual 검색 — 다국어 임베딩 vs 사전 번역 인덱싱~~ | **폐기 (2026-08-01)** — 아래 참조 | D-19 후속 |
| E3 | **임베딩 모델 비교** | **한국어 검색 성능.** 다국어 축은 뺐다 — 청크가 전부 한국어라 cross-lingual 능력이 쓰이지 않는다 (02 §8.2) | 01 · 02 §8.2 |
| E4 | **sLLM 노드 적용 전/후** | 각 노드의 기여도 (ablation) | 02·03 |
| E5 | 로컬 LLM vs API 모델 | 성능·비용·지연 비교 | 03 |
| **E6** | **트리아지 하향 금지 게이트 적용 전/후** | **과소평가율 억제 효과 정량화.** 게이트 없이 LLM 판정만 사용한 경우와 비교 | 02 §7.2 · 05 §5 |

> **E2를 폐기한 이유 — 비교할 대상이 사라졌다.**
>
> E2는 *"영문 원문이 벡터DB에 들어간다"* 를 전제로 설계됐다. 그런데 D-37(복제 불가) ·
> D-38(코드가 한국어로 문장화) · D-45(원문 적재 차단)를 거치며 **적재되는 청크가 전부 한국어**가 됐다.
> 번역할 원문이 인덱스에 없으므로 "사전 번역 인덱싱"이라는 비교군이 성립하지 않는다.
>
> **실험을 안 한 것이 아니라 실험 대상이 없어진 것**이고, 그 경위는 D-19 후속에 기록했다.
> 연동돼 있던 **번역 태스크 ⑥도 함께 닫혔다** — 멀티태스크는 5종 확정이다 (D-05).

> **E1의 "원문 청크" 비교군은 살아 있다.** 원문은 `data_work_*.zip` 에 남아 있으므로
> **실험 전용 인덱스를 따로 만들어** 비교할 수 있다. 그 인덱스는 제출물·시연에 쓰지 않는다 —
> D-45가 막는 것은 **적재**이지 내부 측정이 아니다.

> **E6은 본 프로젝트 고유의 안전 설계 실험이다.**
> 게이트 적용으로 과소평가율이 얼마나 줄고 과대평가율이 얼마나 늘었는지를 함께 제시하여,
> **의도된 트레이드오프였음을 숫자로 증명**한다.

> **E4가 특히 중요하다.** "① 분류 노드를 제거하면 Recall@5가 얼마나 떨어지는가"를 보여야
> 각 태스크를 왜 채택했는지 근거가 선다 (03 §1.1).

---

## 7. 실패 분석

보고서에 **실패 사례가 하나도 없으면 오히려 신뢰도가 떨어진다.**

### 7.1 분석 방법

1. 오류를 **검색 실패 / 생성 실패 / 판정 실패**로 분류
2. 각 유형에서 대표 사례 3~5건 선정
3. 원인 → 조치 → 재측정 결과를 함께 기록

### 7.2 중점 관찰 대상

- **과소평가 사례 전수 분석** — 건수가 적더라도 모두 본다
- **종 혼동 사례** — 포유류 기준의 조류 적용
- 되묻기가 필요했는데 추측한 사례
- 거절해야 했는데 답한 사례 (근거 부족 상태에서의 생성)
- 조류 질의에서만 발생하는 실패 패턴
- **문장화 오류로 인한 실패** — 원문은 맞는데 우리 문장이 틀린 경우 (층 0에서 잡히지 않은 것)

### 7.3 개선 사이클

최소 1회의 **개선 전/후 비교**를 보고서에 포함한다.

**이미 한 사이클이 돌았다** — §2.5.3~2.5.6 이 그 기록이다.

```
점검 7건 통과      → 점검 항목이 코퍼스를 대표하지 못함을 발견 (§2.5.3)
  → 16건+음성 4건으로 확장  → 문장화 반전 3종 발견 (§2.5.4)
    → 템플릿 수정 + 회귀 8건 고정 → 재적재
      → 증상 질의가 "틀린 통과"였음을 발견 (§2.5.5) → D-49
        → 검사기 자신의 결함 발견 (§2.5.6) → 별칭 묶음·순위 출력
          → 중복 히트 발견 → dedupe_by_substance ← **재측정 대기**
```

> **이 사이클을 보고서 §8에 그대로 싣는다.** 점검이 통과했다가 확장하니 결함이 나왔고,
> 고쳤더니 검사기가 틀렸던 것이 나왔다 — **개선 전후 비교의 교과서적 형태**다.

**예시로 쓰지 않을 것: 유사도 임계값 조정.** D-46 이 *"임계값으로는 근거 있음/없음을
가를 수 없다"* 를 실측으로 결론냈다. 임계값을 돌려 지표를 올리는 것은
개선이 아니라 **한쪽을 다른 쪽으로 옮기는 것**이다.

남은 후보: 청킹 단위 변경(E1) · 음성 샘플 보강 · 소수 클래스 가중 · **`case_type` 빈 유형 보강**.

---

## 8. 재현성

| 항목 | 조치 | 상태 |
|---|---|---|
| 의존성 | **`pyproject.toml` extras** (D-41) — `.[api,dev]` · `.[db]` · `.[train]`. requirements.txt 는 쓰지 않는다 | ✅ |
| **파라미터** | **`configs/default.yaml` 이 단일 출처** (D-41). 비밀만 `.env`. 프로파일 전환 `PETTRIAGE_PROFILE=train` | ✅ |
| 시드 | `train.seed: 42` — 설정 파일에 고정 | ✅ |
| **측정 장비** | **CPU·GPU·RAM 을 지표와 함께 적는다.** 지연·적재 시간은 장비에 따라 몇 배 달라진다 | ☐ **미기재** |
| **모델 로딩 시간** | `bge-m3` 로드 초. 로그(`임베딩 모델 로드 완료 — …s`)에서 옮겨 적는다 (D-53) | ☐ **미측정** |
| 모델 버전 | **베이스 모델 revision 해시를 박았다** — `Qwen/Qwen3-4B` `1cfa9a7208912126459214e8b04321603b3df60c`.<br>임베딩 `BAAI/bge-m3` · 1024차원 · cosine | ✅ |
| 데이터 스냅샷 | 사실 표 **888행 / 45출처** · 스냅샷 원장 50건 · 삭제 원장 15건 (02 §3.0) | ✅ |
| 실행 절차 | README에 인덱스 재구축·평가 실행 순서 명시. `make verify` · `make serve` | ✅ |
| 추적 | **LangSmith** 트레이스로 노드별 지연·토큰·프롬프트 자동 기록 | ☐ 가명화 미설정 (D-36) |

> **`revision` 을 비워 두면 재현이 깨진다.** HF 의 `main` 은 움직이는 표적이라
> 같은 `base_id` 로도 다른 가중치가 내려온다. 스모크 테스트 시점의 해시를 박아 뒀다.

> LangSmith 트레이스는 그대로 보고서의 근거 자료가 된다 (02 §10).
> 다만 **트레이스가 입력 원문을 저장**하므로 가명화 전에는 실입력을 태우지 않는다 (D-36).

---

## 8.5 물질 동정 — 무엇을 재고 무엇을 못 재나 (D-62)

폐쇄 목록은 **환각을 막지 오분류를 막지 못한다.** 둘은 다른 것이고 측정 가능성도 다르다.

| | 환각 | 오분류 |
|---|---|---|
| 무엇 | 코퍼스에 **없는** 것을 만든다 | 있는 것 중 **틀린** 것을 고른다 |
| 범위 | 무한 | **538종 안** |
| 어떻게 다루나 | 계약이 **막는다** (`contracts.SubstanceName`) | **골든셋으로 잰다** |

그래서 여기서 재는 것은 셋이다.

```
정규화율    표면형이 폐쇄 목록 위로 올라간 비율      직접 · 별칭 · 부분일치
모호율      후보가 둘 이상이라 하나로 못 정한 비율    → 후보를 전부 검색어로 넘긴다
미상율      아무것도 못 찾은 비율                  → 되묻기 (거절이 아니다)
```

⚠️ **아직 재지 않았다.** 노드가 없어 파이프라인을 통과시킬 수 없고,
표면형 목록을 사람이 임의로 고르면 그 숫자는 **표본이 아니라 예시**다.
`G-105`·`G-106`·`G-108` 이 각각 미상·미상·모호 경로를 하나씩 밟는다 —
**세 건은 비율이 아니다.** 노드가 들어온 뒤 골든셋 60건 전량으로 재고 여기에 채운다.

> **"구현했다"와 "효과가 측정됐다"는 다르다** (§2.5.6 이 같은 자리에서 적은 것).
> 지금 적을 수 있는 것은 **경로가 존재한다**까지다.

---

## 9. 보고서 구성안

1. 평가 목표와 정답 정의 (본 문서 §1)
2. 골든 셋 설계 및 통계
3. **층 0 — 데이터 파이프라인 검증** (본 문서 §2.5) — *라이선스 제약에서 나온 고유 검증 층*
4. 층 1 — 태스크별 성능
5. 층 2 — 통합 성능, **과소평가율 중심**
6. 핵심 비교표 (A/B/C/D)
7. 부가 실험 결과 (E1 · E3~E6 — **E2는 폐기 경위를 대신 싣는다**)
8. 실패 사례 분석 및 개선 전후 비교
9. 한계와 향후 과제

### 9.1 층 -1 — 구현물 검토 (`docs/08_코드검토/`)

보고서 8번(*"실패 사례 분석 및 개선 전후 비교"*)의 실질이 여기 있다.
**하네스가 재는 것은 "답이 맞는가"이고, 여기서 재는 것은 "안전 장치가 실제로 도는가"다.**
둘은 다르다 — 2026-08-02 검토가 그것을 실증했다.

| | 문서 | 무엇 |
|---|---|---|
| 1 | [`08/2026-08-02_검토보고`](08_코드검토/2026-08-02_검토보고.md) | 결함 36건 — 치명 5 · 중대 11 · 경미 20. **전 항목 재현 코드 포함** |
| 2 | [`08/2026-08-02_수정설계`](08_코드검토/2026-08-02_수정설계.md) | 수정 4원칙 + 항목별 설계 + 결정 2건 |
| 3 | [`08/2026-08-02_수정완료`](08_코드검토/2026-08-02_수정완료.md) | 재현 28건 재실행 · 남긴 결정 3건 |

**보고서에 실을 때 가장 강한 사례 셋** — 셋 다 *"테스트가 초록인데 안전 장치가 안 돌았다"* 는 형태다.

1. **D-09 게이트를 계약이 덮어쓸 수 있었다.** `apply_gate()` 산술은 옳았고 테스트도 통과했는데,
   `TriageResult(level=1, rule_level=4)` 가 성립했다 → D-54
2. **종 필터가 빈 목록에서 사라졌다.** 그리고 **그 동작을 테스트가 정답으로 단언**하고 있었다 → D-56
3. **문장화가 없는 기준을 만들고 있었다.** `"8.2kg 개가 포도 4-5알"` → `"체중 1kg당 4-5알"`(약 8배).
   이것도 테스트가 고정하고 있었다 → D-57

§8 재현성이 요구하는 *"검사 축소는 드러나야 한다"* 의 반대 사례이기도 하다 —
`check_facts` 의 파일 간 검사는 `if len(paths) > 1:` 때문에 **한 번도 실행되지 않은 채**
`ERROR 0` 을 내고 있었다.

---

## 10. 미해결 사항

### 10.1 남은 것

- ☑ ~~**평가 하네스 구현**~~ — `eval/harness/run_eval.py` · `metrics.py` **완료 (2026-08-02)**.
  게이트는 `--fail-under`(과소평가율) · `--fail-missed`(등급 미판정) · `--min-graded`(최소 분모) ·
  `--fail-over`(지연). **분모가 작으면 통과시키지 않는다** — 08 §17 참조
- ☐ **급여 등급 충돌 5건 정리** — 사과·수박·복숭아·망고 `SAFE(S-064)` vs `CAUTION(S-047)` ·
  우유 `NEVER(S-016)` vs `CAUTION(S-023,S-087)`. `make facts` 가 출처와 함께 지목한다 (08 검토 산물)
- ☐ **`prevent` 유형 2건(G-004·G-021)의 계약 처리** — `answered` 인데 등급이 없다.
  섭취 사건이 아니라 **축 B** 질문이다 (08 §남긴 것)
- ☐ **골든셋 분포** — `VISIT_SOON` 1 · `symptom` 1 · `prevent` 2. `make golden` 이 최소 3건을 요구한다
- ☐ **문장화 표본 대조 건수(N) 확정** → 층 0 §2.5.2
- ☐ LLM-as-judge에 사용할 모델·프롬프트 확정
- ☐ 과소평가율 목표치 설정 (허용 상한) — **중대 과소평가는 이미 0 목표**(§4.1.0)
- ☐ **지연 목표치 설정** (`answered` `p95` 상한) — 1차 측정 후. `--fail-over` 로 CI 에 건다 (§4.3)
- ☐ **§2.5.3 의 171.3초가 어떤 장비였는지 확인** — 재현성 표에 채운다 (§8)
- ☐ **`bge-m3` 로딩 시간 실측** — 문서에 "수십 초" 를 추정으로 적지 않기 위해 (D-53)
- ☐ **`case_type` 소급 분류** — G-001~G-027 (§2.2)
- ☐ **`dedupe_by_substance` 효과 재측정** (§2.5.6)
- ☐ **캐시 적용 여부** — 1차 지연 측정 후 판단 (D-53 · 05 §2 조각 11 🔶)

### 10.2 종결된 것

- ☑ ~~트리아지 등급 체계 확정~~ → **D-39** (§4.1.0). 사용자 노출 배지 표현만 회의 대기
- ☑ ~~골든 셋 100건 작성 분담 확정~~ → **60건 · 둘** 로 확정 (§2.1 · §2.4 · 07 · 04a §7)
- ☑ ~~유사도 임계값 결정~~ → **D-46** `0.50`. 결론은 *"임계값으로는 거절을 만들 수 없다"* 였다
- ☑ ~~cross-lingual 실험 E2~~ → **폐기** (§6). 번역 태스크 ⑥도 함께 닫혔다
- ☑ ~~`치사` 분기 미검증 사유~~ → **D-51** 단위 환산 문제로 규명 (§4.1.x)
- ☐ ⑥ 번역 채택 시 평가 지표 추가 여부
