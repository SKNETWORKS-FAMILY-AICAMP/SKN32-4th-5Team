# 프로젝트 실행 셋업

> ## ★검토 (오한빈 · 2026-08-24) — 아래 세 줄을 먼저 고치고 따라 하세요
>
> 이 메모 덕분에 셋업 경로가 드러났습니다. 고맙습니다. 다만 세 군데가 지금은 다릅니다.
>
> **① `pip install python-dotenv Pillow` — 이제 필요 없습니다.**
> `python-dotenv` 는 핵심 의존성으로 선언했고, `Pillow` 는 이서은 님이 `django` extra 에
> 등록했습니다(`9bfa574`). *"Pillow 는 이미 있었다"* 고 적었던 것은 제가 틀렸습니다 —
> **`ingest` extra 안에** 있어서 Django 만 깐 사람에겐 안 깔렸습니다.
> `pip install -e ".[api,rag,ingest,db,dev,django]" -c constraints.txt` 하나면 됩니다.
>
> **② `INFERENCE_INTERNAL_URL` 은 `8001` 입니다.** `.env` 예시의 8000 은 Django 자기 포트라,
> 그대로 두면 `GET /api/report` 가 자기 자신을 부릅니다. `settings.py` 기본값도 8001 로 고쳤습니다.
>
> **③ 🔴 `DJANGO_DATABASE_URL` 과 `DATABASE_URL` 을 같은 MySQL 로 두면 안 됩니다.**
> `.env.example` 이 *"일부러 다른 파일을 쓴다 — 같은 파일을 공유하면 스키마가 깨진다"* 고
> 적어 둔 그대로이고, §3 의 *"전체 migrate 는 충돌 나므로 4개만"* 이 그 증상입니다.
> **`pets/0001`·`0002`·`diary/0001` 이 아직 한 번도 적용되지 않았습니다.**
> ✅ **2026-08-26 결정됨 (D-104)** — 표의 소유자는 **Django** 이고, 3차 가상 계정·펫·기록은
> 넘기지 않습니다. 다만 FastAPI 라우터 폐기는 7b 단계에 묶여 있어서,
> **그때까지는 `DJANGO_DATABASE_URL` 을 `.env.example` 기본값(별도 sqlite)으로 두십시오.**
> 두 줄을 같은 MySQL 로 가리키면 여전히 `migrate` 가 끝까지 안 돕니다.

---


## 1. 설치할 것

`pyproject.toml`에 빠져있어서 별도로 설치 필요:

```powershell
pip install python-dotenv Pillow
```

- `python-dotenv` — `webapp/settings.py`가 `.env` 로드에 사용
- `Pillow` — `pets` 앱의 `ImageField`에 필요

**Docker Desktop 설치** — nginx 컨테이너 실행용

---

## 2. `.env` 파일 (프로젝트 루트)

```env
DJANGO_SECRET_KEY=<아래 명령어로 발급>
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

DJANGO_DATABASE_URL=mysql+pymysql://root:mysql80@127.0.0.1:3306/pettriage
DATABASE_URL=mysql+pymysql://root:mysql80@127.0.0.1:3306/pettriage
JWT_SECRET_KEY=<위와 동일하게 발급>

INFERENCE_INTERNAL_URL=http://127.0.0.1:8001
```

SECRET 키 발급:
```powershell
python -c "import secrets; print(secrets.token_urlsafe(48))"
```

---

## 3. DB 마이그레이션

```powershell
python manage.py migrate auth
python manage.py migrate sessions
python manage.py migrate contenttypes
python manage.py migrate admin
```

전체 `migrate` 는 pets/diary 스키마 충돌 나므로 위 4개만.

---

## 4. 실행 (3개 터미널)

```powershell
# 터미널 1 — Django
python manage.py runserver 8000

# 터미널 2 — FastAPI
uvicorn src.pettriage.app.main:app --port 8001

# 터미널 3 — nginx (최초 1회)
docker compose up nginx -d
```

접속: `http://localhost/`