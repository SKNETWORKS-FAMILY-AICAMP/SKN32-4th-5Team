# 평가 결과 보고서 — RAG 기반 반려동물 응급 트리아지

작성자: **오한빈(팀장)** · 2026-08-03
대상: SKN 3차 단위 프로젝트 · `save the pet` (이근준 · 권소라 · 이서은 · 오한빈)
설계 근거: `docs/04_테스트-평가계획.md` · `docs/06_설계결정기록.md`

---

## 0. 재현 정보 (04 §8)

```yaml
골든셋:   eval/goldenset/golden_lse.csv · golden_ohb.csv   (60건)
프로파일: PETTRIAGE_PROFILE=eval
엔진:     graph  (SafetyEngine 로 감싼 GraphEngine) · engine_configured=graph
모델:     none(폴백) · api:gpt-4o-mini · langchain:gpt-4o-mini
임베딩:   BAAI/bge-m3
검색:     top_k=5 · score_threshold=0.5
되묻기:   max_clarify_turns=2
시드:     42
측정 시점 HEAD: 529510e8981c67e974bf7282dc6aaee47062d7cf
```

세 실행 모두 `engine == engine_configured == graph` 다 — 다르면 그 결과는 폐기한다
(`eval/reports/README.md`).

> 🔴 **`provenance.dirty = true` 였다. 이 숫자는 커밋되지 않은 변경 위에서 쟀다.**
>
> 위 `529510e` 는 측정 시점의 HEAD 일 뿐, **측정한 코드가 아니다.** 그 상태는 남에게
> 줄 수 없으므로 다음을 지킨다 —
>
> 1. 측정 이후 소스를 **더 고치지 않은 채** 커밋한다.
> 2. `git status --porcelain` 이 비었는지 확인한다.
> 3. 그 커밋 해시를 아래에 적는다. **그것이 이 숫자를 재현하는 유일한 좌표다.**
>
> ```
> 재현 커밋: e6600d949ce23bbd707d1bf0bb566407b81dce51   (2026-08-03, 브랜치 ohb)
> ```
>
> ✅ 측정 이후 소스를 고치지 않은 채 커밋했고 `git status --porcelain` 이 비었음을
> 확인했다. **`529510e`(측정 시점 HEAD)가 아니라 `e6600d9` 로 재현한다.**
>
> ```powershell
> git checkout e6600d949ce23bbd707d1bf0bb566407b81dce51
> pip install -e '.[api,rag,ingest,dev]' -c constraints.txt
> $env:PETTRIAGE_PROFILE="eval"
> python eval/harness/run_eval.py --arm none  --json eval/reports/재현_기준선.json
> python eval/harness/run_eval.py --arm A     --json eval/reports/재현_A.json
> python eval/harness/run_eval.py --arm A-LC  --json eval/reports/재현_A-LC.json
> ```
>
> ⚠️ `--arm A`·`A-LC` 는 `OPENAI_API_KEY` 가 필요하다. **`--arm none` 기준선은 키 없이
> 누구나 재현할 수 있다** — 코드·규칙 부분의 검증은 그것으로 충분하다.
>
> ℹ️ 이 문서에 해시를 적는 행위 자체가 다음 커밋을 만든다(`docs: 재현 커밋 기입`).
> 그 커밋은 **소스를 바꾸지 않으므로** 위 좌표는 그대로 유효하다.
>
> 숨기지 않고 적는 이유는 *"측정하지 않은 것을 0 으로 적지 않는다"* 와 같다 (04 §8).

> ⚠️ **JSON 의 `provenance.model_base_id` · `model_revision` 은 이 실행과 무관하다.**
> `provider=api` 인데 Qwen 기본값(`Qwen/Qwen3-4B`)이 그대로 기록된다. 이 실행을
> 정의하는 값은 JSON 최상위 `"model": "api:gpt-4o-mini"` 이다. 하네스의 결함이며
> 보고 대상이다 (§8) — **안 쓰는 값이 쓰는 값처럼 기록되면 나중에 이 JSON 만 보고
> Qwen 으로 잰 것으로 읽는다.**

---

## 1. 요약

세 축을 같은 골든셋 60건으로 쟀다. **`none` 은 비교군이 아니라 기준선이다** —
*"LLM 이 하는 일이 전부 빠지면 얼마나 되는가"* 를 재는 자리이고, 이 값이 A 와
비슷하면 LLM 을 넣은 의미가 없다는 뜻이므로 반드시 함께 낸다 (04 §3).

| | **none** (코드·규칙만) | **A** (openai SDK) | **A-LC** (LangChain) |
|---|---|---|---|
| 통과 | 23.3% (14/60) | **45.0%** (27/60) | **45.0%** (27/60) |
| 상태 일치 | 58.3% (35/60) | 66.7% (40/60) | 66.7% (40/60) |
| 등급 일치도 | 88.0% *(분모 25)* | **90.0%** *(분모 30)* | **90.0%** *(분모 30)* |
| 인접 허용 | 96.0% | 96.7% | 96.7% |
| 🔴 **과소평가율** | 8.0% (2) | **0.0%** (0) | **0.0%** (0) |
| 과대평가율 | 4.0% (1) | 10.0% (3) | 10.0% (3) |
| 🔴 **중대 과소** | **0** | **0** | **0** |
| 등급 못 낸 긴급 건 | 26.9% (7/26) | **11.5%** (3/26) | **11.5%** (3/26) |
| must_cite (any) | 59.0% | 66.7% | 66.7% |
| must_contain (any) | 37.5% | **66.7%** | **66.7%** |
| must_not_contain *(answered)* | 92.3% | 93.5% | 93.5% |
| 지연 p50 *(answered)* | **155 ms** | 8.38 s | 9.24 s |
| 5태스크 전부 모델 | 0.0% | **100.0%** | **100.0%** |

**세 줄 요약**

1. **LLM 을 넣은 의미가 있다.** 통과 23.3% → 45.0%, 과소평가 8.0% → 0.0%,
   등급을 못 낸 긴급 건 26.9% → 11.5%. 기준선과 A 가 비슷하지 않다.
2. **정량 판정 경로는 사실상 완성됐다.** `dose` 15건이 통과 93.3% · 등급 일치 100% ·
   과대 0%. 하루 전만 해도 이 경로는 **되묻기로 새고 있었다**(§7 이력).
3. **대가는 지연이다.** 155 ms → 8.4 s. 02 §12.4 로 스트리밍을 쓰지 않으므로
   이 값이 그대로 사용자가 느끼는 침묵이다 (§6).

---

## 2. 무엇을 어떻게 쟀나

### 2.1 분모를 섞지 않는다 (04 §1.2)

이 보고서의 비율은 **각자 다른 분모**를 쓴다. 섞으면 *"답을 안 한 것"* 이 만점으로
보고된다 — 실제로 초기 하네스가 그랬다.

| 지표 | 분모 | 왜 |
|---|---|---|
| 통과·상태 일치 | 60 (전건) | |
| 등급 일치·과소·과대 | **양쪽 다 등급이 있는 건**만 (none 25 · A 30) | 등급을 안 낸 건을 "틀렸다"로 세면 거절이 과소평가로 둔갑한다 |
| 등급 못 낸 긴급 건 | 정답이 CALL_NOW 이상인 26건 | 등급 오류가 **아니다.** 분모가 다르므로 과소평가율에 섞지 않는다 |
| must_not_contain | `answered` 만 (별도로 전체도 낸다) | 거절·되묻기는 금지 문구를 쓸 기회가 없어 **거저 통과**한다 |

> ⚠️ **A 의 등급 분모가 25 → 30 으로 늘었다.** 되묻기·거절로 빠지던 5건이 판정으로
> 들어왔다는 뜻이다. **분모가 늘어난 상태에서 일치도가 함께 올랐다**(88.0% → 90.0%)는
> 점이 중요하다 — 쉬운 건만 남겨서 오른 것이 아니다.

### 2.2 비교군 (04 §3 · D-65)

`--arm` 하나로 고르고, 그 이름이 리포트에 그대로 남는다. `arms.py` 가
`PETTRIAGE__MODEL__*` 를 **전부 비우고** 세우므로 앞 실험이 새어 들지 않는다.

| 이름 | 구성 | 상태 |
|---|---|---|
| `none` | LLM 없음 — 5태스크 전부 폴백 | ✅ 기준선 |
| `A` | gpt-4o-mini · openai SDK 직접 | ✅ 측정 |
| `A-LC` | 같은 모델을 **LangChain(`ChatOpenAI`)** 으로 | ✅ 측정 |
| `D` | Qwen3-4B 베이스 (로컬) | ❌ 미측정 (§8) |
| `C` | Qwen3-4B + LoRA 어댑터 | ❌ 미측정 (§8) |

---

## 3. 결과 상세

### 3.1 LLM 이 실제로 불렸는가 (05 §6)

**다른 모든 수치보다 먼저 읽어야 하는 칸이다.** 이것이 없으면 성적이 낮을 때
*모델이 못한 것*인지 *모델이 안 불린 것*인지 구분할 수 없다.

| | none | A · A-LC |
|---|---|---|
| 5태스크 전부 모델 | **0.0%** (0/60) | **100.0%** (60/60) |
| 폴백 태스크 | classify 60 · slot 55 · (raw) 41 · simplify 30 · compress 2 | **없음** |

기준선은 정의상 전부 폴백이고, A·A-LC 는 **60건 전부에서 다섯 태스크가 모두 모델을
탔다.** 아래 수치는 폴백이 섞이지 않은 값이다.

> 이 계기가 없던 동안 ①분류·②슬롯은 폴백을 **기록조차 하지 않았다.** 그래서 이서은
> 팀원이 잡은 D-73(라벨 누락으로 LLM 이 키워드 폴백보다 나빴다)이 오래 보이지 않았다.

### 3.2 트리아지

| | none | A · A-LC |
|---|---|---|
| 분모 | 25 | 30 |
| 일치 | 88.0% | **90.0%** |
| 인접(±1) | 96.0% | 96.7% |
| 🔴 과소 | 8.0% (2) | **0.0%** |
| 과대 | 4.0% (1) | 10.0% (3) |
| 🔴 중대 과소 | 0 | **0** |

기준선의 과소 2건은 **G-011**(백합·cat)·**G-017**(아보카도·bird)이고 둘 다
`rule=3 llm=None` 이었다 — 규칙이 3 에서 천장을 쳤고 올려 줄 LLM 이 없었다.
A 에서 LLM 이 4 로 올려 고쳤다. **하향 금지 게이트(D-09)가 있어야 하는 이유 그 자체다.**

### 3.3 하향 금지 게이트 — 산출물 ④의 물증

```
■ 하향 금지 게이트 (분모 26 — 규칙·LLM 둘 다 등급을 낸 건)   [A · A-LC]
  일치              11
  LLM 이 올렸다       4   (그중 과대로 끝난 것 1)
  🔒 하향 차단        5   ← LLM 이 낮추려 한 것을 게이트가 막았다
  🔒 상승 차단        6   ← 잰 자리라 막았다 (D-80. 그중 여전히 어긋난 것 0)
```

- **하향 차단 5건** — D-09 가 설계대로 작동했다는 직접 증거다. LLM 이 등급을
  낮추려 한 순간이 다섯 번 있었고 전부 막혔다. 그 결과가 **과소평가 0.0%** 다.
- **상승 차단 6건, 그중 어긋난 것 0** — D-80(잰 자리에서는 규칙이 맞다)이
  **한 건도 해를 끼치지 않았다.** 트레이드오프에 적어 둔 우려(*"역치가 한 축만
  덮는다"*)는 60건 범위에서 실현되지 않았다.
- **상승 4건 중 3건이 옳았다** — 전부 *못 잰 자리*(정성 표·양 미상 바닥)다.
  D-09 를 통째로 잠갔다면 이 3건을 잃었을 것이다. **범위를 정한 개정이 옳았다.**

### 3.4 근거·문구

| | none | A · A-LC |
|---|---|---|
| must_cite (any) | 59.0% (23/39) | 66.7% (26/39) |
| must_cite (all) | 35.9% (14/39) | 43.6% (17/39) |
| must_contain (any) | 37.5% (18/48) | **66.7%** (32/48) |
| must_contain (all) | 4.2% (2/48) | 8.3% (4/48) |
| must_not_contain *(answered)* | 92.3% (24/26) | 93.5% (29/31) |
| must_not_contain *(전체)* | 95.6% (43/45) | 95.6% (43/45) |

`must_contain (any)` 가 37.5% → 66.7% 로 가장 크게 올랐다 — 폴백 경로에서는
`draft = context` 라 검색 원문이 그대로 나갔고, LLM 이 붙자 질문에 답하는 문장이
만들어졌다는 뜻이다.

> ⚠️ **`must_contain (all)` 8.3% 는 시스템 성능으로 읽지 않는다.** 한 케이스가 요구하는
> 문구를 *전부* 포함하라는 조건이고, 골든셋의 문구 목록이 지나치게 촘촘한지
> 함께 봐야 한다 (§8 남은 과제).

### 3.5 지연 (02 §12.4)

| | none | A | A-LC |
|---|---|---|---|
| 전체 p50 / p95 | 150 ms / 232 ms | 7.23 s / 10.23 s | 8.34 s / 10.83 s |
| answered p50 / p95 | 155 ms / 215 ms | **8.38 s** / 12.79 s | **9.24 s** / 12.89 s |

**되묻기·거절을 섞지 않고 `answered` 를 따로 냈다.** 거절은 LLM 을 두 번만 돌아 빠르고,
섞으면 평균이 낙관적으로 나와 **긴 답변을 기다리는 사람의 경험이 가려진다.**

02 §12.4 로 스트리밍을 쓰지 않으므로 이 값이 그대로 **침묵**이다. 8.4 초 동안 화면에
아무것도 나오지 않는다. 응급 상황에서 이 침묵은 그 자체로 설계 결함이다 (§8).

### 3.6 종별 (04 §4.2 — 전체 평균은 조류 저하를 가린다)

| 종 | n | 통과 (none → A) | 등급 일치 (none → A) | 과대 (A) |
|---|---|---|---|---|
| dog | 30 | 10.0% → **46.7%** | 100.0% → 94.1% | 5.9% |
| cat | 17 | 35.3% → 41.2% | 83.3% → 87.5% | 12.5% |
| **bird** | 10 | 20.0% → 30.0% | 50.0% → 80.0% | **20.0%** |

**조류가 가장 약하다.** 코퍼스의 조류 정량 역치가 0행이라 정성 표에만 의존하고
(D-09 개정), 그래서 D-78·D-80 의 정량 개선이 조류에는 닿지 않는다. 전체 평균만
보면 이 사실이 가려지므로 종별을 반드시 따로 낸다.

### 3.7 유형별 (04 §2.2)

| 유형 | n | 통과 (none → A) | 등급 일치 (A) | 과대 (A) |
|---|---|---|---|---|
| **dose** | 15 | 33.3% → **93.3%** | **100.0%** | **0.0%** |
| species | 9 | 22.2% → 44.4% | 100.0% | 0.0% |
| symptom | 3 | 0.0% → 66.7% | 100.0% | 0.0% |
| none | 9 | 33.3% → 33.3% | — | — |
| slot | 9 | 22.2% → 22.2% | — | — |
| **fact** | 12 | 8.3% → 16.7% | 66.7% | 33.3% |
| **prevent** | 3 | 33.3% → **0.0%** | — | — |

**`dose` 가 이 프로젝트의 중심 경로이고, 거기서 등급 일치 100% · 과대 0% 다.**
반대로 `none`·`slot` 은 LLM 을 붙여도 **전혀 움직이지 않았다** — 원인이 모델이 아니라
미결 설계에 있다는 신호다 (§5).

---

## 4. A vs A-LC — 추상이 실제로 갈아끼워지는가

**두 실행의 채점 결과가 완전히 같다.** 통과 27/60, 실패 33건의 **case_id 까지 동일**,
게이트 감사 수치도 동일하다. 차이는 지연 하나뿐이다 (p50 7.23 s → 8.34 s).

| | A (openai SDK) | A-LC (LangChain `ChatOpenAI`) |
|---|---|---|
| 통과 | 27/60 | 27/60 |
| 등급 일치 | 90.0% | 90.0% |
| 실패 목록 | 33건 | **같은 33건** |
| 게이트 | 11 / 4 / 5 / 6 | **같음** |
| answered p50 | 8.38 s | 9.24 s |

이것이 뜻하는 것 —

1. **필수 산출물 *"LangChain 을 활용하여 벡터DB와 LLM 연동"* 이 동작으로 증명된다.**
   D-71 로 정한 대로 LangChain 은 **LLM 연동에만** 쓰고 벡터DB는 직접 다룬다.
2. **`LLMClient` 프로토콜 뒤에서 구현을 바꿔도 결과가 같다** (D-21). 결과가 달랐다면
   둘 중 하나가 프롬프트를 바꾸고 있다는 뜻이었을 것이다.
3. 지연 차이 약 0.9 초는 LangChain 계층의 오버헤드로 보이며, 정확도에는 영향이 없다.

---

## 5. 실패 분석 (04 §7)

A·A-LC 의 실패 33건을 원인별로 넷으로 묶는다. **묶음마다 손댈 곳이 다르다.**

### ① `refused → clarify` 6건 — 🔴 설계 미결 (D-46)

`G-020 · G-026 · G-049 · G-050 · G-051 · G-052` (전부 `none` 유형)

*"우리가 다룰 질문이 아니다"* 로 거절해야 하는데 되묻기로 빠진다.
**기준선 7건 → A 6건.** LLM 분류를 붙였는데 **1건밖에 줄지 않았다.**

> 원인은 모델이 아니다. **`intent=general` 이 곧 거절인가에 대한 판정 기준이 없다**
> (D-46 이 ①분류에 넘겼으나 기준을 정하지 않았다 — 06 §8 결정 대기 목록).
> 기준 없이 모델을 바꿔도 이 6건은 움직이지 않는다.

### ② `answered → refused` 8건 — 검색·물질 동정 실패

`G-004 · G-007 · G-012 · G-021 · G-042 · G-046 · G-104 · G-108`
(그중 `G-012 · G-046` 은 **긴급 미판정**)

답해야 하는데 근거를 못 물어 거절했다. `prevent` 3건이 **전부 여기 있다**
(*"먹여도 되나요"* 형 예방 질의 — 통과 0%). 예방 질의는 *이미 먹었다* 를 전제로 만든
검색·판정 경로와 성격이 다르다.

### ③ `clarify → answered` 5건 — 골든셋 정정 후보

`G-003 · G-016 · G-023 · G-047 · G-101`

**기준선과 A 에서 동일하게 나타난다** — 모델과 무관하다는 뜻이다. 이전 검토에서
*G-003 · G-023 · G-047 은 시스템이 맞고 G-016 은 골든셋이 맞다* 로 정리해 둔 건들이다.
마감 전 골든셋 정정 대상 (04a 지침에 따라 근거를 적어 수정한다).

### ④ `근거/문구` 9건 — 채점 기준 재검토 필요

`G-005 · G-008 · G-014 · G-017 · G-019 · G-041 · G-043 · G-044 · G-048`

상태·등급은 맞는데 `must_cite`/`must_contain` 을 못 채웠다. `must_contain (all)` 이
8.3% 인 것과 같은 신호이며, **시스템과 골든셋 양쪽을 봐야 한다.**

### ⑤ 등급 오류 3건 — **전부 정량이 아닌 자리**

| 케이스 | 기대 → 실제 | 게이트 | 원인 |
|---|---|---|---|
| G-018 (fact·dog) | 3 → 4 | `rule=None llm=4` | 규칙이 등급을 못 냈고 LLM 단독 판정 |
| G-027 (fact·cat) | 3 → 4 | `rule=3 llm=4 ↑LLM` | 정성 경로라 D-80 이 막지 않는다 |
| G-103 (fact·bird) | 2 → 4 | `rule=4 llm=4` | **규칙 자체가 4** — 조류 정성 등급 검토 건 |

> **D-80 이 덮을 수 있는 등급 오류는 하나도 남지 않았다.** 정량 계산이 성립한
> 모든 건이 정답과 일치한다. 남은 셋은 정성 표·자료 자체의 문제다.

### ⑥ 거절 이유 불일치 1건

`G-106` — `근거없음` 을 기대했는데 `범위밖` 으로 거절했다. **거절했다는 사실만 보면
통과로 보이는 자리**이고, 하네스가 `expected_refusal_reason` 을 채점하기 시작한 뒤에야
드러났다.

---

## 6. 한계 — 이 숫자를 어떻게 읽지 말아야 하는가

1. **표본 60건.** 유형별 분모가 3~15건이라 `prevent`(3) · `symptom`(3) 의 비율은
   1건 차이로 33%p 씩 움직인다. 유형별 수치는 **방향만** 읽는다.
2. **골든셋과 코퍼스가 같은 자료에서 나왔다.** 초콜릿 4건이 `F-034-020~026` 에
   정확히 떨어지는 것은 검산으로는 의미가 있으나, **일반화 성능의 근거는 아니다.**
3. **`none` 기준선의 ④ 근거 검증은 무의미하다.** 폴백에서는 `draft = context` 라
   판정이 항상 `근거있음` 이고, 근거없음 탐지 재현율은 **0인 채 초록으로 보인다.**
4. **지연 8.4 초는 측정 조건이 최선일 때의 값이다.** 워밍업 1회를 버렸고 API 한도에
   걸리지 않은 실행이다.
5. **비교군 C·D 는 재지 못했다** (§8). 따라서 *"sLLM 파인튜닝이 대형 LLM 을 대체할 수
   있는가"* 에는 아직 답하지 못한다.

---

## 7. 이 결과에 이르기까지 (2026-08-02 ~ 08-03)

같은 골든셋으로 여섯 번 쟀다. **무엇을 고쳤을 때 무엇이 움직였는지**가 이 표에 있다.

| 회차 | 무엇을 고쳤나 | 통과 | 비고 |
|---|---|---|---|
| 1차 | — | 30.0% | ⚠️ 되묻기를 끈 상태. **가짜 초록** |
| 2차 | D-66 되묻기 복구 · 모델 상주 | 16.7% | p50 4710 ms → 153 ms |
| 3차 | D-67 종/물질 분리 · 거절 이유 채점 | 18.3% | |
| 4차 | D-68 물질 미해결 3분기 | 21.7% | |
| 5차 | (재현 확인) | 21.7% | 완전히 동일 |
| **기준선** | D-75~D-79 (끊긴 값 · 폴백 기록 · 유효성분 환산) | **23.3%** | 등급 분모 20 → 25 |
| **A (D-80 전)** | LLM 연결 | 33.3% | 과대 30% |
| **A (최종)** | D-80 정량 상승 차단 | **45.0%** | 과대 10% |

특히 마지막 두 줄이 이 프로젝트의 설계 원칙을 그대로 보여준다 —
**LLM 을 붙이자 답을 하기 시작했지만 등급이 나빠졌고**(과대 30%),
**계산이 성립한 자리에서 LLM 을 막자 둘 다 좋아졌다**(통과 45% · 과대 10%).

관련 결정: D-75(끊긴 값) · D-76(폴백 기록) · D-78(유효성분 환산) ·
D-79(판정에 수치를 준다) · **D-80(D-09 개정 — 잰 자리에서는 규칙이 맞다)**

---

## 8. 남은 과제

### 🔴 마감 전

| 항목 | 근거 |
|---|---|
| **D-46 확정** — `intent=general` 의 거절 기준 | §5 ① 6건이 여기 묶여 있다 |
| **골든셋 정정 5건** (G-003 · G-016 · G-023 · G-047 · G-101) | §5 ③ — 모델과 무관하게 재현된다 |
| **`must_contain` 채점 기준 재검토** | `all` 8.3% 는 시스템이 아니라 채점을 의심할 값이다 |
| **`_provenance()` 가 provider 와 무관한 값을 기록** | §0 — `provider=api` 인데 Qwen `base_id`·`revision` 이 박히고, 정작 `api_model` 은 provenance 에 없다. 재현 좌표가 거짓이 된다 (04 §8) |

### 🟠 그다음

| 항목 | 근거 |
|---|---|
| ~~비교군 **C·D 측정** (Qwen3-4B 베이스/어댑터)~~ | held-out 기준 측정 완료 — **§10** (골든셋 기준은 아직 미측정) |
| 조류 정성 등급 검토 (G-103 · G-017) | §3.6 — 조류가 가장 약하다 |
| `prevent` 유형 경로 (통과 0%) | §5 ② — 예방 질의는 성격이 다르다 |
| **스트리밍** (02 §12.4) | §3.5 — 8.4 초 침묵 |
| 개인정보 필터 (`privacy/` 미구현) | D-36 — 트레이스가 입력 원문을 저장한다 |

---

## 9. 결론

1. **RAG + 규칙 + LLM 의 분업이 수치로 성립한다.** 기준선 대비 통과 23.3% → 45.0%,
   과소평가 8.0% → **0.0%**, 등급을 못 낸 긴급 건 26.9% → 11.5%.
2. **안전 목표를 달성했다.** 🔴 중대 과소 **0건**, 과소평가 **0.0%**. 하향 금지 게이트가
   실제로 5회 작동한 기록이 남아 있다.
3. **수치 판정은 코드가, 문장은 LLM 이** — 이 분업이 `dose` 유형 등급 일치 100% ·
   과대 0% 로 확인된다. 반대로 계산이 없는 자리(`fact`·`bird`)가 가장 약하다.
4. **LangChain 연동이 동일 결과로 증명된다** (A ≡ A-LC).
5. 남은 실패의 다수는 **모델이 아니라 미결 설계와 골든셋**에 있다 (§5 ①③④ = 20건).

---

## 10. WS3 파인튜닝 결과 — 비교군 C·D (Qwen3-4B 베이스/어댑터) — §8 후속

작성: 이서은 · 2026-08-03. §8이 "GPU 미확보"로 미측정 처리한 항목을 채운다.

### 10.0 재현 정보

```yaml
재현 커밋: 71322b1cea325e53fd966199ea9d41d0c13d40df   (2026-08-03, 브랜치 lse)
프로파일:  PETTRIAGE_PROFILE=train-local
베이스:    Qwen/Qwen3-4B (revision 1cfa9a7208912126459214e8b04321603b3df60c)
어댑터:    artifacts/adapters/multitask-v2/checkpoint-484
           QLoRA r=16 · alpha=32 · 2 epoch(설계값 3에폭 중 시간 제약으로 2에폭 채택) · seed=42
학습 데이터: data/train/samples.jsonl (gitignore 대상)
           classify 3027 · slot 2107 · simplify 185 · verify 1221 · compress 199 = 6739건
           train/dev = 9:1, train-dev·골든셋 누수 0건(mixer.check_leakage)
측정 스크립트: scripts/compare_multitask_adapter.py --adapter artifacts/adapters/multitask-v2/checkpoint-484 --per-task 20
측정 표본: samples.jsonl 의 **dev 분할 held-out** 20건씩 — 골든셋 60건(§0~9)과 다른 표본이다
```

> ⚠️ `samples.jsonl`은 gitignore 대상이라 이 커밋 하나로 가중치가 100% 재현되지는 않는다.
> `scripts/generate_*_samples*.py` · `scripts/rebuild_*_samples.py` · `scripts/append_slot_samples.py`를
> 순서대로 재실행하면 같은 파이프라인을 타지만, distillation 단계가 LLM 호출(teacher=api:gpt-4o-mini)이라
> 바이트 단위 재현은 보장하지 않는다. 재현 가능한 것은 **파이프라인과 최종 표본 수**다.
> `git status --porcelain`이 비어 있음을 확인하고 위 해시를 적었다.

### 10.1 결과 — 정답이 명확한 3태스크 (held-out 20건씩, 정확 일치)

| 태스크 | 베이스 (Qwen3-4B) | 어댑터 (2에폭) |
|---|---|---|
| 분류 | 20/20 | 20/20 |
| 슬롯 | 20/20 | 20/20 |
| 🔴 검증 | **10/20 (50%)** | **20/20 (100%)** |

- 분류·슬롯은 **베이스도 이미 만점**이다 — D-73(라벨 명시)·D-86(스키마 명시)로 프롬프트만
  고쳐도 충분했다는 뜻이고, 이 표본으로는 파인튜닝의 추가 이득이 안 보인다(천장 효과 — §10.3 ②).
- 검증은 베이스가 절반을 틀리고 어댑터가 전부 맞춘다. §10.2의 데이터 재작업 + 하이브리드
  결합 로직을 반영한 뒤의 결과다.

### 10.2 이번 학습 직전에 데이터에서 바뀐 것

| 태스크 | 이전 | 이번 | 이유 |
|---|---|---|---|
| ③ 압축 | 407건 — 질의 경로 압축용(이제 존재하지 않는 노드를 향함) | 199건 — 기간 리포트용, `report._digest()`(실제 프로덕션 함수) 그대로 사용 | D-83 |
| ④ 검증 | 1219건 — ③의 LLM 출력을 근거 문서로 재사용(순환 오염) | 1221건 — raw_context(LLM 미개입 원본 검색 결과) | D-83 후속. 위험 방향(근거없음→근거있음) 오판 46→21건 |
| ② 슬롯 | 1907건 — `elapsed_hours` 0건 | 2107건 — 기존 null 채움 1907 + 실값 있는 신규 200 | D-86·D-89 스키마 갭(이서은 발견) |
| `verify_grounding` 판정 로직 | LLM 성공 시 LLM 단독 | LLM·2-gram 하이브리드 — **2-gram이 바닥, LLM은 조이기만 한다** | 한빈·이서은 합의 |

### 10.3 한계 — 이 결과를 어떻게 읽지 말아야 하는가

1. **골든셋 기준 비교군 C·D는 아직 없다.** 여기 표는 학습 분포 안 held-out이라
   "일반화했는가"를 잰다. §0~9의 골든셋 60건은 "실제 서비스 시나리오에서 맞는가"를
   재는 다른 축이고, 서로 대체하지 않는다. **§8 항목이 완전히 닫힌 것은 아니다.**
2. **분류·슬롯의 20/20 대 20/20은 "차이 없음"이 아니라 "이 표본·이 지표로는 안 보임"이다.**
   베이스가 이미 천장이라 어댑터의 이득을 이 측정으로는 주장할 수 없다.
3. **3에폭(설계값)이 아니라 2에폭에서 멈췄다.** 시간 제약으로 3에폭을 중단했고, 2에폭 결과가
   이미 충분히 좋다고 판단해 채택했다 — 3에폭이 더 좋았을지는 모른다.
4. **③압축·⑤평이화는 생성형이라 이 방식(정확 일치)으로 못 잰다.** 육안 확인 수준이다
   (§10.0 표본 20건 생성 중 숫자 환각 의심 1/200 — 확인 결과 오탐).

### 10.4 결론

**이 프로젝트의 핵심으로 명시된 ④근거검증(D-05 — "④가 이 구성의 핵심")에서 가장 크고
명확한 개선이 나왔다** (held-out 50% → 100%). 분류·슬롯은 프롬프트 개선만으로 이미
충분해 보이고, 파인튜닝의 한계 비용 대비 이득은 검증 태스크에 가장 뚜렷하다.
골든셋 기준 측정(§8 잔여 항목)은 다음 라운드 과제로 남긴다.
