# 로컬 실행 구성.
#
#   docker compose up db                    MySQL 만 (팀원 PC 에 설치할 필요 없다)
#   docker compose up                       API + MySQL
#   docker compose --profile train run ...  학습 (GPU 필요)
#
# 🔴 data/ 는 마운트만 하고 이미지에 넣지 않는다 (D-29 · .dockerignore).
#
# **벡터DB 는 여기 없다.** D-44 로 Chroma(파일 기반)를 쓰기로 했으므로
# pgvector 컨테이너를 걷어냈다. 여기 있는 DB 는 **계정·프로필 전용**이다.
#
# 🔴 비밀번호에 기본값을 두지 않는다. `${VAR:?...}` 는 값이 비면 compose 가
#    에러로 죽는다 — Dockerfile 의 ConfigNotFound 와 같은 규칙이다(조용한 폴백 금지).
#    기본값 `changeme` 를 두면 .env 없이도 컨테이너가 조용히 뜨고,
#    그 순간 알려진 비밀번호의 DB 가 열린다.

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      # 🔴 Django 정적·미디어를 nginx 가 직접 낸다 (NFR-17).
      #    `DEBUG=false` 면 Django 가 안 내주므로 관리자 화면이 CSS 없이 뜬다.
      #    비어 있어도 된다 — nginx.conf 의 `try_files` 가 Django 로 되돌린다.
      - ./staticfiles:/srv/staticfiles:ro
      - ./media:/srv/media:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      PETTRIAGE_PROFILE: default
      # 컨테이너에서 DB를 부를 때만 쓰인다. 값은 .env 에서 온다.
      DATABASE_URL: mysql+pymysql://pettriage:${MYSQL_PASSWORD:?MYSQL_PASSWORD 가 없다 — .env 를 확인한다}@db:3306/pettriage
    env_file:
      # 셸에 직접 export 해서 쓰는 경로도 막지 않는다 (required: false 유지).
      # .env 자체의 유무는 위 `:?` 가 이미 걸러낸다.
      - path: .env
        required: false
    volumes:
      # 자료는 이미지에 굽지 않고 실행 시 마운트한다
      - ./data:/app/data:ro
      - ./configs:/app/configs:ro
    depends_on:
      db:
        condition: service_healthy

  db:
    # 계정·반려동물 프로필 (D-48). 벡터는 여기 안 들어온다 — Chroma 가 맡는다 (D-44).
    #
    # 팀원 PC 에 MySQL 을 깔지 않는다. 버전이 제각각이면 "내 PC에선 되는데" 가 시작된다.
    image: mysql:8.4
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_0900_ai_ci
    ports:
      # 🔴 루프백에만 묶는다. "3306:3306" 은 0.0.0.0 에 여는 것이라
      #    강의장·공유 Wi-Fi 에서 같은 망의 누구나 붙을 수 있다.
      #    API 를 컨테이너 밖에서 돌릴 때는 이대로도 127.0.0.1:3306 으로 붙는다.
      - "127.0.0.1:3306:3306"
    environment:
      MYSQL_DATABASE: pettriage
      MYSQL_USER: pettriage
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD 가 없다 — .env 를 확인한다}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD 가 없다 — .env 를 확인한다}
    volumes:
      - mysqldata:/var/lib/mysql
    healthcheck:
      # mysqladmin ping 은 소켓만 확인한다. 실제 인증까지 봐야
      # API 가 "DB는 떴는데 로그인이 안 되는" 상태로 기동하지 않는다.
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u pettriage -p$$MYSQL_PASSWORD"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 30s

  trainer:
    profiles: ["train"]
    build:
      context: .
      dockerfile: docker/train.Dockerfile
    environment:
      PETTRIAGE_PROFILE: train
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data:ro
      - ./artifacts:/app/artifacts
      - hf-cache:/cache/hf
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

volumes:
  mysqldata:
  hf-cache:
